# 数据库实施蓝图（团队分工版）
注意：主项目和ai社会的数据库是两回事，不能搞混！
## 1. 目标与范围
- 目标：在不改动业务目标的前提下，完成数据库从“可运行”到“可交付”的落地。
- 范围：
  - 决策链路（decision）
  - 法庭治理链路（court）
  - 训练与策略链路（governor / marl）
  - 事件审计链路（events）
- 交付标准：可迁移、可追溯、可恢复、可回滚。

## 2. 团队角色与职责边界

### 2.1 后端工程（Backend）
**核心职责**
- 设计并实现 Repository 层，屏蔽业务层直连 SQL/SurrealQL。
- 串联主流程写入：决策 -> 提交 -> 裁决 -> 事件。
- 实现关键事务边界与幂等写入策略。
- 提供按 `trace_id` 的全链路查询接口。

**具体交付物**
- `DecisionTraceRepository`
- `CourtSubmissionRepository`
- `CourtVerdictRepository`
- `EventLogRepository`
- 服务层接口：
  - `createSubmission(...)`
  - `issueVerdict(...)`
  - `queryTrace(traceId)`

**验收点**
- 重放任一 `trace_id` 能还原完整业务路径。
- 关键流程在异常重试场景下不会产生重复脏数据。

### 2.2 数据工程（Data / DBA）
**核心职责**
- 管理迁移版本：`v1_base`、`v2_decision`、`v3_court`、`v4_governor`、`v5_events`。
- 定义主键、索引、状态枚举和约束。
- 维护 `migration_history`，保证迁移幂等与可追踪。
- 建立备份与恢复演练机制。

**具体交付物**
- 迁移脚本目录与执行顺序说明。
- 索引设计文档（按高频查询与回放场景）。
- 数据保留策略（TTL / 冷热分层，可后置）。

**验收点**
- 新环境一键迁移成功。
- 迁移可重复执行且无破坏性副作用。
- 恢复演练在约定时间窗口内完成。

### 2.3 测试工程（QA / Test）
**核心职责**
- 设计数据库相关测试矩阵：功能、回归、并发、容灾。
- 验证迁移前后兼容性与数据完整性。
- 验证“默认拒绝”权限策略与异常路径行为。

**具体交付物**
- 测试用例集：
  - 迁移成功/失败回滚
  - 决策链路落库与回放
  - 裁决流程一致性
  - 并发写入响应配对正确性
- 基线报告：P95 查询时延、写入成功率、错误率。

**验收点**
- 主链路测试通过率达到团队门槛（如 >= 95%）。
- 并发压测下无响应串线、无不可恢复错误。

### 2.4 运维工程（Ops，可由后端兼任）
**核心职责**
- 建立部署与回滚 SOP。
- 建立可观测性：迁移日志、慢查询日志、错误告警。
- 配置环境差异（Windows/Linux）启动兼容策略。

**具体交付物**
- 部署检查清单（预检/执行/回滚）。
- 监控面板指标：
  - 迁移状态
  - 表写入失败率
  - 查询延迟分布

**验收点**
- 故障发生后可按 SOP 在目标时间内恢复。

## 3. 工作分期（建议两周节奏）

### 第 1 周：最小可用闭环
- Backend：完成 Repository 基础层与主链路写入。
- Data：完成 v1~v3 迁移（基础 + 决策 + 法庭）。
- QA：建立冒烟用例与迁移校验用例。
- Ops：打通测试环境部署与日志采集。

**周末里程碑**
- 冷启动后可完成一次完整业务闭环并可回放。

### 第 2 周：稳定性与可交付
- Backend：补齐并发幂等、错误重试、策略快照接口。
- Data：完成 v4~v5 迁移（训练 + 事件）与索引优化。
- QA：完成并发与异常注入测试。
- Ops：完成备份恢复演练与告警阈值调优。

**周末里程碑**
- 达到上线准入：可迁移、可恢复、可追溯、可观测。

## 4. 跨角色协作机制
- 每日 15 分钟站会：只同步阻塞项与依赖项。
- 变更门禁：任何 schema 变更必须附迁移脚本与回滚说明。
- 联调节奏：Backend 与 Data 每日固定联调窗口。
- 缺陷闭环：QA 提单必须包含 `trace_id` 与复现步骤。

## 5. 风险与应对
- 风险：迁移脚本遗漏或空文件。
  - 应对：发布前强制执行迁移完整性检查。
- 风险：并发场景响应错配。
  - 应对：请求-响应显式关联键，不依赖队列顺序。
- 风险：默认放行造成安全口子。
  - 应对：权限策略改为“未匹配即拒绝”。
- 风险：内存持久化导致重启丢数据。
  - 应对：主链路全部切换到持久化存储并做恢复演练。

## 6. 交付清单（最终验收）
- 迁移脚本全集（含执行顺序与回滚说明）
- Repository 与服务接口实现说明
- 测试报告（功能/并发/容灾/性能）
- 运维 SOP（部署、回滚、备份、恢复）
- 上线准入结论（是否满足发布条件）

## 7. 推荐 RACI（可直接套用）
- R（负责）：Backend / Data / QA / Ops 各自模块 owner
- A（签署）：技术负责人
- C（协作）：产品、架构
- I（知会）：项目干系人

---
如需进一步拆分到“人天级任务表（例如每位同学每天做什么）”，可在此文档基础上直接扩展为迭代看板版本。