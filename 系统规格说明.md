不允许抄近道为了实现最小功能，严格按照文档要求，不允许私自更改各个依赖版本和别的方案，只能使用文档要求的方案，同时遇到错误询问我，遇到问题进行多个方案比对，最大稳定性>最大性能>顶级性能，遇到问题先尝试顶级性能，失败后在选择最大性能，过来在尝试最大稳定性，进行文档内容添加修改删除时不允许用批处理命令进行处理，要求严格理解文档要求进行内容的理进行修改文档。

# SoloForge 系统规格说明

***

## 目录（Table of Contents）

### 一、系统总览

- [一、技术栈](#一技术栈)
- [二、系统架构](#二系统架构)
- [三、核心运行时架构](#三核心运行时架构)
- [三-A、AI Runtime 完整运行闭环](#三-aai-runtime-完整运行闭环architecture-master-spec)
- [三-B、AI Runtime V1 技术规格](#三-bai-runtime-v1-技术规格)
  - [三-B-1、Rust Core Trait](#三-b-1rust-core-trait统一运行时协议)
  - [三-B-2、建项目第一天就该创建的文件](#三-b-2建项目第一天就该创建的文件)
- [三-C、插件 + 多JDK + 性能架构](#三-c插件--多jdk--性能架构)
  - [插件补充机制](#插件补充机制)
  - [Runtime 内核补完件](#runtime-内核补完件)

### 二、数据库架构（四）

- **存储层选型**：Dragonfly + SurrealDB + JSONL 三层架构
- **4.1–4.25**：核心表结构、Record Relation、Embedding、Session 快照、事件索引、Memory 版本、模型调用、向量分片、生命周期、一致性、ID 规范、索引策略、归档、Task DAG、输出快照、Workspace 文件、Hash、模型配置、权限缓存、审计、MCP 注册、下载、插件元数据、校验、Outbox
- **4.26–4.59**：命名规范、BaseEntity、软删除、回收站、Chunk 管理、Embedding 任务、Prompt 模板、用户行为、Feature Flag、配置中心、Workspace 快照、Patch、Git 关联、Schema 版本、死信队列、元数据、数据血缘、Context Registry、Prompt Trace、Model Route、Tool 上下文、思考链、Symbol 索引、语义缓存、文件观察、Runtime 快照、资源预算、能力注册、数据来源、影子模式、Feature 实验
- **4.60–4.74**：Agent 注册与 Session、Planner 图、Task 状态机、执行时间轴、Skill 注册、Session 画像与摘要、Memory 权重、Knowledge 热度、Tool 信誉与沙箱、事件总线、模型排行榜、自愈、事故
- **4.75–4.90**：Goal 系统与记忆、Reflection、Failure Pattern、Learning Record、Hypothesis、Observation、三层记忆（Working/Episodic/Semantic）、记忆固化、Cognitive State、Attention、Curiosity、Belief Graph
- **4.91–4.110**：Meta Agent、Agent Network、Message Bus、Delegation、Consensus、Debate、Review、Trust Score、World State、Entity Graph、Workspace KG、Task Simulation、Risk Engine、Policy Engine、Negotiation、Reputation、Shared Memory、Society Rules、Constitution、Evolution
- **4.111–4.130**：Objective Engine、KPI、Optimization、Diagnosis、Bottleneck、Strategy、Adaptation、Environment Model、Resource Planner、Energy Mode、Drift Detector、Memory Decay、Sleep Cycle、Dream Queue、Self Model、Capability Gap、Evolution Planner、Experiment、Safety Memory、Civilization Archive
- **4.131–4.150**：分层边界、Command/CQRS/Projection、Event Schema 与迁移、Runtime Contract、Domain Boundary、Runtime Clock、Replay、Sandbox、Runtime Branch、Merge、Kill Switch、Quarantine、Recovery Plan、Chaos Test、Health Graph、Architecture Constitution
- **4.151–4.159**：Intent Engine、Intent History、Decision Engine、Decision Graph、Uncertainty Engine、Clarification Queue、Assumption Registry、Evidence Registry、Truth Maintenance System
- **4.161–4.171**：Memory GC、Runtime GC、Context Compression、Hierarchical Memory、Governor（资源总调度）、Cognitive Load、Behavior Mode、Personality Kernel、Meta Memory、Runtime DNA
- **4.172–4.182**：Perception Pipeline、Attention Controller、Executive Controller、Interrupt System、Cognitive Stack、Learning Loop、Reward System、Identity Kernel、Narrative Memory、Self Consistency Checker、Cognitive Constitution
- **4.183–4.196**：AI Society Dynamics — Institution、Governance、Role Evolution、Social Memory、Cultural Norm、Social Reputation、Coalition、Market、Economy、Law Engine、Court System、Historical Archive、Civilization Identity、Evolution Constitution

### 三、系统功能（五–二十八）

- [五、UI 布局（三栏主界面）](#五ui布局三栏主界面)
- [六、设置中心](#六设置中心)
- [七、任务调度器](#七任务调度器)
- [八、事件驱动架构](#八事件驱动架构)
- [九、日志系统](#九日志系统)
- [十、安全系统](#十安全系统)
- [十一、技能目录与文件监听](#十一技能目录与文件监听)
- [十二、性能约束](#十二性能约束)
- [十三、明确禁止事项](#十三明确禁止事项)
- [十四、最终交付标准](#十四最终交付标准)
- [十五、错误处理与降级策略](#十五错误处理与降级策略)
- [十六、系统监控与可观测性](#十六系统监控与可观测性)
- [十七、缓存与存储管理](#十七缓存与存储管理)
- [十八、数据备份与迁移](#十八数据备份与迁移)
- [十九、测试策略](#十九测试策略)
- [二十、可访问性规范](#二十可访问性规范)
- [二十一、国际化(i18n)架构](#二十一国际化i18n架构)
- [二十二、系统升级与热更新](#二十二系统升级与热更新)
- [二十三、插件沙箱隔离](#二十三插件沙箱隔离)
- [二十四、架构图与流程图](#二十四架构图与流程图)
- [二十五、API 协议版本管理](#二十五api协议版本管理)
- [二十六、新手引导(ONBOARDING)](#二十六新手引导onboarding)
- [二十七、发布与部署](#二十七发布与部署)
- [二十八、安全漏洞防护](#二十八安全漏洞防护)

### 四、附录

- [附录A：完整目录结构](#附录a完整目录结构)
- [附录B：事件完整列表](#附录b事件完整列表)
- [附录C：错误代码规范](#附录c错误代码规范)

***

## 索引（Index）

> 按关键词快速定位章节。格式：`关键词 → 章节号` 或 `关键词 → 章节名`

### 核心概念

| 关键词                                     | 位置                                                        |
| --------------------------------------- | --------------------------------------------------------- |
| Agent 注册/网络/消息/委派/共识/辩论/审查/信誉           | 4.60, 4.91–4.98                                           |
| Architecture Constitution（架构宪法）         | 4.150                                                     |
| Architecture Master Spec（完整运行闭环）           | 三-A                                                      |
| AI Runtime V1 技术规格                          | 三-B                                                      |
| Attention（注意力）                          | 4.88, 4.173, 三-A②                                          |
| Backup / 备份                             | 十八                                                        |
| BaseEntity                              | 4.27                                                      |
| Behavior Mode（行为模式）                     | 4.168                                                     |
| Belief Graph（信念图）                       | 4.90                                                      |
| Bottleneck（瓶颈检测）                        | 4.115                                                     |
| Governor（资源总调度）                         | 4.165                                                     |
| Cache / 缓存                              | 4.52, 十七                                                  |
| Capability（能力注册/缺口）                     | 4.56, 4.126                                               |
| Chaos Test（混沌测试）                        | 4.148                                                     |
| Chunk / 知识库分块                           | 4.30–4.32                                                 |
| Clarification Queue（澄清队列）               | 4.156                                                     |
| Coalition（联盟机制）                         | 4.189                                                     |
| Cognitive Constitution（认知宪法）            | 4.182                                                     |
| Cognitive Load（认知负载）                    | 4.167                                                     |
| Cognitive Stack（认知栈）                    | 4.176                                                     |
| Cognitive State（认知状态）                   | 4.87                                                      |
| Command 模式 / CQRS                       | 4.132–4.134                                               |
| Configuration / 配置中心                    | 4.37–4.38, 六                                              |
| Consensus（共识机制）                         | 4.95                                                      |
| Constitution（宪法层）                       | 4.109                                                     |
| Context Compression（上下文压缩）              | 4.163                                                     |
| Context Registry（上下文注册）                 | 4.46                                                      |
| Contradiction / 矛盾（见 Truth Maintenance） | 4.159                                                     |
| Court System（裁决系统）                      | 4.193                                                     |
| Cultural Norm（文化规范）                     | 4.187                                                     |
| Curiosity Queue（探索队列）                   | 4.89                                                      |
| Data Lineage（数据血缘）                      | 4.45                                                      |
| Data Provenance（数据来源链）                  | 4.57                                                      |
| Dead Letter Queue（死信队列）                 | 4.43                                                      |
| Debate（辩论系统）                            | 4.96                                                      |
| Decision Engine（决策引擎）                   | 4.153–4.154                                               |
| Delegation（任务委派）                        | 4.94                                                      |
| Diagnosis（运行诊断）                         | 4.114                                                     |
| Domain Boundary（领域边界）                   | 4.138                                                     |
| Dragonfly（热数据层）                         | 四                                                         |
| Dream Queue（联想队列）                       | 4.124                                                     |
| Drift Detector（漂移检测）                    | 4.121                                                     |
| Economy Layer（经济系统）                     | 4.191                                                     |
| Embedding / 向量                          | 4.3, 4.8, 4.32                                            |
| Energy Mode（运行模式）                       | 4.120                                                     |
| Executive Controller（执行控制器）             | 4.174                                                     |
| Entity Graph（实体图谱）                      | 4.100                                                     |
| Environment Model（环境模型）                 | 4.118                                                     |
| Episodic Memory（事件记忆）                   | 4.84                                                      |
| Event / 事件架构                            | 4.43, 4.72, 4.135–4.136, 八, 附录B                           |
| Evidence Registry（证据注册）                 | 4.158                                                     |
| Evolution（进化/规划/宪法）                     | 4.110, 4.127, 4.196                                       |
| Experiment System（实验平台）                 | 4.128                                                     |
| Failure Pattern（失败模式）                   | 4.79                                                      |
| Feature Flag / 实验层                      | 4.36, 4.59                                                |
| GC（垃圾回收）                                | 4.161–4.162                                               |
| Goal System（目标系统）                       | 4.76–4.77                                                 |
| Governance Layer（治理层）                   | 4.184                                                     |
| Health Graph（健康图谱）                      | 4.149                                                     |
| Hierarchical Memory（层级记忆）               | 4.164                                                     |
| Historical Archive（历史编年史）               | 4.194                                                     |
| Hypothesis（假设系统）                        | 4.81                                                      |
| i18n / 国际化                              | 二十一                                                       |
| Identity（身份核心/文明身份）                     | 4.179, 4.195                                              |
| Incident / 事故                           | 4.75                                                      |
| Institution（制度系统）                       | 4.183                                                     |
| Intent Engine（意图引擎）                     | 4.151–4.152                                               |
| Interrupt System（中断系统）                  | 4.175                                                     |
| JSONL（事件真相层）                            | 四                                                         |
| Kill Switch（保命按钮）                       | 4.145                                                     |
| Knowledge Graph（知识图谱）                   | 4.101                                                     |
| Knowledge Heat（热度系统）                    | 4.69                                                      |
| KPI Registry（指标中心）                      | 4.112                                                     |
| Law Engine（法律引擎）                        | 4.192                                                     |
| Learning（学习记录/闭环）                       | 4.80, 4.177                                               |
| Market System（市场机制）                     | 4.190                                                     |
| MCP 工具注册                                | 4.21                                                      |
| Memory（层级/权重/衰减/固化/共享/叙事/安全）            | 4.83–4.86, 4.68, 4.107, 4.122, 4.129, 4.164, 4.170, 4.180 |
| Memory Consolidation（记忆固化）              | 4.86                                                      |
| Merge 策略                                | 4.144                                                     |
| Meta Agent（元 Agent）                     | 4.91                                                      |
| Meta Memory（元记忆）                        | 4.170                                                     |
| Model Route（模型路由）                       | 4.48                                                      |
| Model Benchmark（模型排行榜）                  | 4.73                                                      |
| Narrative Memory（叙事记忆）                  | 4.180                                                     |
| Objective Engine（目标引擎）                  | 4.111                                                     |
| Observation（观察记录）                       | 4.82                                                      |
| Onboarding / 新手引导                       | 二十六                                                       |
| Optimization（优化候选）                      | 4.113                                                     |
| Outbox 模式                               | 4.25                                                      |
| Perception Pipeline（感知流水线）              | 4.172                                                     |
| Personality Kernel（人格内核）                | 4.169                                                     |
| Planner（规划层）                            | 4.62                                                      |
| Plugin / 插件                             | 4.23, 二十二, 二十三                                            |
| Policy Engine（策略引擎）                     | 4.104                                                     |
| Prompt（模板/版本/Trace）                     | 4.33–4.34, 4.47                                           |
| Projection 系统                           | 4.134                                                     |
| Quarantine（隔离区）                         | 4.146                                                     |
| RAG / 向量检索                              | 4.3, 4.30–4.32                                            |
| Recovery Plan（恢复计划）                     | 4.147                                                     |
| Reflection（反思系统）                        | 4.78                                                      |
| Replay（事件回放）                            | 4.141                                                     |
| Reputation（信誉/生态评分）                     | 4.98, 4.106, 4.188                                        |
| Resource Planner（资源规划）                  | 4.119                                                     |
| Reward System（奖励系统）                     | 4.178                                                     |
| Risk Engine（风险引擎）                       | 4.103                                                     |
| Role Evolution（角色进化）                    | 4.185                                                     |
| Runtime Branch（运行时分支）                   | 4.143                                                     |
| Runtime Clock（运行时时钟）                    | 4.140                                                     |
| Runtime Contract                        | 4.137                                                     |
| Runtime DNA                             | 4.171                                                     |
| Safety Memory（安全记忆）                     | 4.129                                                     |
| Sandbox（沙箱）                             | 4.142, 二十二                                                |
| Schema 版本管理                             | 4.42                                                      |
| Security / 安全系统                         | 十, 二十八                                                    |
| Self Consistency Checker（自一致性检查）        | 4.181                                                     |
| Self Heal（自愈）                           | 4.74                                                      |
| Self Model（自我模型）                        | 4.125                                                     |
| Semantic Memory（语义记忆）                   | 4.85                                                      |
| Session（画像/摘要/快照）                       | 4.4, 4.66–4.67                                            |
| Shadow Mode（影子模式）                       | 4.58                                                      |
| Skill Registry（技能注册）                    | 4.65, 十一                                                  |
| Sleep Cycle（休眠周期）                       | 4.123                                                     |
| Social Memory（社会记忆）                     | 4.186                                                     |
| Strategy Registry（策略中心）                 | 4.116                                                     |
| SurrealDB（温数据/AI层）                      | 四                                                         |
| Task（DAG/状态机/模拟）                        | 4.14, 4.63, 4.102, 七                                      |
| Testing / 测试                            | 十九                                                        |
| Tool（信誉/上下文/沙箱）                         | 4.49, 4.70–4.71                                           |
| Trust Score（Agent 信誉）                   | 4.98                                                      |
| Truth Maintenance System（真值维护）          | 4.159                                                     |
| UI 布局                                   | 五                                                         |
| ULID / ID 规范                            | 4.11                                                      |
| Uncertainty Engine（不确定性）                | 4.155                                                     |
| Vector / 向量（见 Embedding）                | <br />                                                    |
| Working Memory（工作记忆）                    | 4.83                                                      |
| Workspace（文件/快照/Symbol/KG）              | 4.16, 4.39, 4.51, 4.101                                   |
| World State（世界状态）                       | 4.99                                                      |

***

## 一、技术栈

### 前端

Electron + React + TypeScript + Zustand + WebSocket + Monaco Editor + Vite + React Router + TanStack Query + React Virtual / react-window + Monaco Editor + Tailwind CSS + Framer Motion + xterm.js + Zod

### 后端

Node.js + TypeScript + WebSocket + 事件驱动架构 + 本地进程管理 + 插件系统 + Rust（高性能模块） + Python（AI工具链/模型生态） + 文件监听系统 + 本地任务调度系统

### 任务分工

- **Node.js / TypeScript**: 主业务编排、WebSocket服务、插件系统、文件管理、会话管理、模型调度orchestration、Electron主进程通信、API层
- **Python**: AI工具链、模型生态接入、向量处理、OCR/embedding/推理辅助、科学计算/agent工具适配
- **Rust**: 高频性能热点、本地索引、日志处理、大文件扫描、搜索加速、diff/patch/parser模块

### 通信结构

- Renderer ↔ Main: IPC
- Main ↔ 内部任务系统: 事件总线
- Main ↔ 前端流送: WebSocket / 内部事件桥接
- Node ↔ Rust: Neon 原生模块
- Node ↔ Python: 受控进程通信

***

## 二、系统架构

### Electron 多进程边界

**主进程（高权限与底层资源操作）**：
文件系统访问、目录授权、日志写入、模型配置读取、Python调用、Rust模块调用、下载任务、环境创建、命令执行、插件调度、安全检查、事件桥接、会话目录管理、技能同步

**渲染进程（只负责）**：
页面渲染、用户输入、状态展示、任务流送展示、预览展示、编辑器交互、受控动作发起

**渲染进程禁止**：
任意文件写入、任意shell命令、任意目录扫描、直接数据库访问、直接加载Rust模块、直接操作Python解释器、直接访问敏感系统路径

### 跨语言通信

- Node ↔ Rust: Neon（本地.node插件），编译生成原生.node文件，直接require()调用
- Node ↔ Python: 受控进程通信
- Python运行时不依赖用户本机Python，使用python-build-standalone分配独立运行时，resources/python/目录

### 构建工具要求

- Rust: Neon构建.node，按平台分发预编译资源
- Python: 独立运行时放入resources/python/，不依赖用户本地Python，必须使用绝对路径调用

### 版本要求（严格锁定，不允许私自更改）

| 组件               | 版本            | 说明              |
| ---------------- | ------------- | --------------- |
| Electron         | 37.x          | 主框架             |
| Node.js          | 22 LTS        | 后端运行时           |
| Python           | 3.12.x        | AI工具链           |
| Rust             | stable（最新稳定版） | 高性能模块           |
| Dragonfly        | 1.34.x+       | 热数据层（替代Redis）   |
| SurrealDB        | 3.0.x         | AI/向量/温数据层      |
| SurrealDB JS SDK | 2.x           | 对应SurrealDB 3.0 |

**禁止使用**：Node 24、Python 3.13、Rust nightly

### 兼容性注意事项

- Electron原生模块同步慢（注意编译策略）
- Neon/NAPI边缘兼容问题
- Torch生态未完全跟进
- AI embedding库存在兼容问题

***

## 三、核心运行时架构

系统采用**运行时优先（Runtime First）架构**，核心由五个关键对象构成：

1. **Session**: 持续交互的顶层执行容器，负责工作区、流送、日志与生命周期隔离
2. **ExecutionContext**: 某一轮任务执行时的统一上下文快照，负责定义本轮可见信息与执行条件
3. **Task**: 系统内所有可调度、可追踪、可中断、可重试、可产生副作用的执行单元
4. **Event**: 运行时状态变化的统一表达，用于流送、日志、回放、审计与指标统计
5. **Orchestrator Runtime**: 核心调度内核，负责创建上下文、生成任务树、推进状态机并协调执行器

ModelRegistry、LoadBalancer、PermissionGuard、PluginManager、StreamBus、LogManager作为运行时服务适配器（Runtime Service Adapters）。

### 核心原则

- 系统主线从"聊天消息流"升级为"任务执行流"
- 所有状态变化统一沉淀为Event
- 所有副作用统一经过Task + PermissionGate控制
- UI、日志、审计、回放围绕同一份运行时事实展开

---

## 三-A、AI Runtime 完整运行闭环（Architecture Master Spec）

这不是数据库设计。这是 **AI Runtime 如何真正跑起来**的完整生命周期——从输入到进化，20 层闭环。

### 整体运行图

```
Input（用户/文件/工具/系统）
  ↓
① Perception Engine（感知系统）—— 统一所有输入
  ↓
② Attention Engine（注意力引擎）—— 决定系统看什么
  ↓
③ Intent Engine（意图引擎）—— 用户话 ≠ 任务
  ↓
④ Goal System（目标系统）—— 区分任务 vs 长期目标
  ↓
⑤ Planning Engine（规划引擎）—— Goal → 可执行步骤
  ↓
⑥ Decision Engine（决策引擎）—— 统一"为什么这么做"
  ↓
⑦ Executive Controller（执行控制器）—— 暂停/恢复/抢占/取消
  ↓
⑧ Action Layer（动作执行）—— 调模型/调工具/调Agent
  ↓
⑨ Observation Layer（观察收集）—— 收集执行结果
  ↓
⑩ Truth Maintenance System（真值维护）—— Evidence → Truth
  ↓
⑪ Hierarchical Memory（层级记忆）—— Working → Episode → Semantic → Archive
  ↓
⑫ Memory Consolidation（记忆固化）—— 防止记忆无限增长
  ↓
⑬ Reflection Engine（反思引擎）—— 复盘：哪里错、哪里浪费
  ↓
⑭ Learning Loop（学习闭环）—— Action → Result → Lesson → Strategy
  ↓
⑮ Governor（资源总调度）—— 系统不要拖死自己
  ↓
⑯ Meta Memory（元记忆）—— 系统记住"自己怎么学习"
  ↓
⑰ Identity Kernel（身份核心）—— 防人格漂移
  ↓
⑱ Sleep Cycle（休眠周期）—— 后台整理/GC/Embedding
  ↓
⑲ Evolution Engine（进化引擎）—— 系统自己升级
  ↓
⑳ Constitution（系统宪法）—— 最高层约束
```

---

### ① Perception Engine（感知系统）

**目标**：统一处理来自用户、文件、网页、工具、Agent、系统事件的所有输入。否则文本一套、文件一套、工具一套，后期必炸。

**运行流程**：
```
Input
  ↓
Normalize（格式化统一）
  ↓
Entity Extract（实体提取）
  ↓
Signal Detect（信号检测：bug/urgent/risk/question/goal/emotion）
  ↓
Context Enrich（上下文补充）
  ↓
Observation（输出统一观测对象）
```

**核心数据结构**：
```typescript
// table: observation
{
  id: record<observation>,
  sourceType: 'user' | 'file' | 'web' | 'tool' | 'agent' | 'system',
  sourceId: string,
  content: string,
  entities: string[],            // 提取的实体
  signals: string[],             // 检测到的信号
  importance: number,            // 重要性 0-1
  confidence: number,            // 置信度 0-1
  timestamp: datetime
}
```

**实现方案**：
- 第一版：Regex + LLM 分类
- 第二版：NER + Embedding + Classifier
- 第三版：Perception Agent（专职感知的 Agent）

**风险**：输入风暴。缓解：rate-limit + dedup + batch。

---

### ② Attention Engine（注意力引擎）

**目标**：解决"系统看什么"。信息太多时必须过滤。

**评分公式**：
```
attention = importance × 0.3 + recency × 0.2 + goal × 0.3 + risk × 0.2
```

**运行流程**：
```
Observation
  ↓
Scoring（四维评分）
  ↓
Priority Queue（O(log n) 排序）
  ↓
Top-K（取最高分的 N 个）
  ↓
Runtime（送入下层）
```

**核心数据结构**：
```typescript
// table: attention
{
  sourceId: string,
  score: number,
  dimensions: { importance, recency, goalRelevance, risk },
  reason: string,
  expireAt: datetime
}
```

**实现方案**：
- 第一版：规则评分
- 第二版：Embedding 相似度
- 第三版：Transformer Rerank

**风险**：热点信息霸占注意力。缓解：attention_decay + 多样性策略。

---

### ③ Intent Engine（意图引擎）

**目标**：用户话 ≠ 任务。"帮我优化项目"实际可能是分析 + 重构 + 文档 + 测试四个意图。

**运行流程**：
```
Input
  ↓
Intent Parse（类型分类）
  ↓
Expand（拆分子意图）
  ↓
Intent Graph（建立依赖图）
```

**核心数据结构**：
```typescript
// table: intent
{
  id: record<intent>,
  rawInput: string,
  type: 'refactor' | 'feature' | 'bugfix' | 'research' | 'config' | 'question',
  confidence: number,
  entities: object,
  parentIntent?: record<intent>,
  state: 'created' | 'parsed' | 'expanded' | 'planned' | 'running' | 'completed'
}

// table: intent_edge
{
  from: record<intent>,
  to: record<intent>,
  relation: 'depend' | 'parallel' | 'optional'
}
```

**实现方案**：第一版 Prompt 分类 → 第二版 Embedding 召回 → 第三版专用 Intent 模型。

---

### ④ Goal System（目标系统）

**目标**：区分"任务"和"长期目标"。Task 做完了，Goal 还在。

**核心数据结构**：
```typescript
// table: goal
{
  id: record<goal>,
  title: string,
  priority: 'low' | 'medium' | 'high' | 'critical',
  progress: number,            // 0-100
  confidence: number,          // 目标置信度
  status: 'active' | 'paused' | 'completed' | 'abandoned'
}
```

**结构**：
```
Goal（长期目标）
  ↓
Plan（规划）
  ↓
Task（可执行单元）
```

**特性**：支持暂停、恢复、回滚、优先级调整。

---

### ⑤ Planning Engine（规划引擎）

**目标**：把 Goal 拆成可执行步骤，检测依赖关系。

**运行流程**：
```
Goal
  ↓
Task Decomposition（任务拆分）
  ↓
Dependency Detection（依赖检测）
  ↓
Task Graph（DAG 有向无环图）
```

**核心数据结构**：
```typescript
// table: plan_task
{
  id: record<plan_task>,
  goalId: record<goal>,
  title: string,
  priority: number,
  dependencies: string[],      // 依赖的 taskId 列表
  state: 'pending' | 'ready' | 'running' | 'completed'
}
```

**约束**：最大深度 < 4（防止无限拆分）。

---

### ⑥ Decision Engine（决策引擎）

**目标**：所有"为什么这么做"统一管理。替代散落的 if/else。

**运行流程**：
```
Task
  ↓
Generate Candidates（生成候选方案）
  ↓
Scoring（多维评分）
  ↓
Select（选中最优）
```

**核心数据结构**：
```typescript
// table: decision
{
  id: record<decision>,
  type: 'model_select' | 'tool_select' | 'agent_select' | 'strategy_select',
  candidates: string[],
  selected: string,
  confidence: number,
  reason: string
}
```

**评分公式**：`score = quality + latency + cost + history`（可配置权重）

**实现**：规则 → 统计学习 → 强化学习。支持 Decision Replay。

---

### ⑦ Executive Controller（执行控制器）

**目标**：系统大脑。负责暂停、恢复、抢占、取消、优先级调度。

**运行流程**：
```
Task Queue
  ↓
Priority Queue
  ↓
Scheduler（FIFO / RoundRobin / Weighted）
  ↓
Runtime
```

**核心数据结构**：
```typescript
// table: executive
{
  id: record<executive>,
  taskId: record<task>,
  priority: 'low' | 'medium' | 'high' | 'critical',
  state: 'queued' | 'running' | 'waiting' | 'paused' | 'completed',
  constraints: object
}
```

**调度策略**：FIFO、Round Robin、Weighted。支持 Aging 防饥饿。

---

### ⑧ Interrupt System（中断系统）

**目标**：处理中断——任务运行中用户说"先修这个 Bug"。

**核心数据结构**：
```typescript
// table: interrupt
{
  id: record<interrupt>,
  targetTaskId: record<task>,
  reason: string,
  priority: number,
  action: 'pause' | 'resume' | 'cancel' | 'preempt'
}
```

**流程**：Interrupt → Executive → Task Snapshot → 暂停/切换 → 恢复。

---

### ⑨ Cognitive Stack（认知栈）

**目标**：类似程序调用栈——跟踪当前执行栈，失败时自动回退。

```typescript
// table: cognitive_stack
{
  frameId: record<cognitive_stack>,
  taskId: record<task>,
  parent: record<cognitive_stack>,
  state: 'running' | 'suspended' | 'completed' | 'failed'
}
```

**示例**：重构 → 分析 → 扫描 → 工具调用。任一失败 → 回退到上一帧。

---

### ⑩ Truth Maintenance System（真值维护系统）

**核心**。所有事实通过 Evidence → Truth 链路管理，支撑证据失效则 Truth 自动重算。

**流程**：
```
Observation
  ↓
Evidence（提取证据）
  ↓
Assumption（生成假设）
  ↓
Verification（验证）
  ↓
Truth（写入真值库）
  ↓
Memory（存入记忆）
```

**核心数据结构**：
```typescript
// table: truth
{
  id: record<truth>,
  statement: string,
  confidence: number,
  evidenceIds: string[],
  dependencies: string[],
  status: 'active' | 'weak' | 'conflict' | 'invalid'
}
```

**冲突处理**：React vs Vue 同时出现 → 进入 ConflictQueue → 按证据权重裁决。

---

### ⑪ Hierarchical Memory（层级记忆）

不要 Memory[]。要分层结构。

```
Working Memory（当前思考，分钟级）
  ↓ 总结
Episodic Memory（经历，周级）
  ↓ 提炼
Semantic Memory（事实，月/年）
  ↓ 归档
Archive（历史，永久）
```

详细定义见 4.83–4.86、4.164。

---

### ⑫ Memory Consolidation（记忆固化）

**目标**：防止记忆无限增长。空闲时将 Working → Episodic → Semantic 逐级提炼。

```typescript
// table: consolidation
{
  id: record<consolidation>,
  source: string[],
  target: string,
  strategy: 'summarize' | 'extract' | 'deduplicate',
  compressionRatio: number,
  status: 'pending' | 'running' | 'completed'
}
```

**后台执行**：Sleep Cycle 自动触发。

---

### ⑬ Reflection Engine（反思引擎）

**目标**：让系统复盘——哪里做错、哪里浪费、哪里能优化。

**流程**：
```
Task 完成
  ↓
收集 Result
  ↓
LLM Review（Prompt: 哪里错、哪里浪费、哪里能优化）
  ↓
生成 Reflection
```

```typescript
// table: reflection
{
  id: record<reflection>,
  taskId: record<task>,
  problem: string,
  rootCause: string,
  improvement: string,
  confidence: number
}
```

---

### ⑭ Learning Loop（学习闭环）

**流程**：Action → Result → Reflection → Lesson → Strategy Update。

```typescript
// table: lesson
{
  id: record<lesson>,
  issue: string,
  solution: string,
  score: number,        // 低分自动淘汰
  applied: boolean,
  effectiveness: number
}
```

---

### ⑮ Governor（资源总调度）

**目标**：系统不要拖死自己。

**监控范围**：CPU、内存、Token、工具数、活跃 Agent 数。

```typescript
// table: governor
{
  id: record<governor>,
  cpu: number,
  memory: number,
  token: number,
  latency: number,
  mode: 'performance' | 'balanced' | 'economy' | 'emergency',
  loadLevel: 'normal' | 'warning' | 'critical' | 'overload'
}
```

**调控规则**：
```
CPU > 80%     → 暂停 Embedding
Token 超预算  → 停副 Agent
延迟 > 5s     → 切快速模型
内存 > 85%    → 触发 Runtime GC
```

**状态**：normal → warning → critical → overload，逐级恢复。

---

### ⑯ Meta Memory（元记忆）

**目标**：系统记住"自己怎么学习"的——记忆策略效果评估。

```typescript
// table: meta_memory
{
  id: record<meta_memory>,
  strategy: 'semantic_search' | 'episodic' | 'hybrid',
  effectiveness: number,    // 0-1
  hitRate: number
}
```

**自优化**：effectiveness < 0.6 → 自动切换检索策略。

---

### ⑰ Identity Kernel（身份核心）

**目标**：防止人格漂移。不是 Prompt——是系统身份。

```typescript
// table: identity
{
  id: record<identity>,
  principle: string,         // 如 "安全优先"
  weight: number,            // 权重
  immutable: boolean         // 是否不可变
}
```

**预置身份原则**：
- 安全优先
- 透明优先
- 可恢复优先
- 证据优先
- 长期目标优先

---

### ⑱ Sleep Cycle（休眠周期）

**目标**：系统空闲时执行后台任务。

**执行内容**：
- Memory Consolidation（记忆整理）
- Embedding 更新
- GC 垃圾回收
- Context 压缩
- 日志归档
- 模型评估

**调度**：
| 周期 | 任务 |
|------|------|
| 5 分钟 | 轻量 GC、Memory 衰减 |
| 30 分钟 | Memory Consolidation、Embedding 增量 |
| 夜间 | 深度 Consolidation、模型评估、日志归档 |

```typescript
// table: sleep_job
{
  id: record<sleep_job>,
  type: 'gc' | 'consolidation' | 'embedding' | 'archive',
  status: 'pending' | 'running' | 'completed',
  lastRun: datetime,
  interval: number
}
```

---

### ⑲ Evolution Engine（进化引擎）

**目标**：系统自己升级——从问题到实验到上线全流程。

**流程**：
```
问题识别
  ↓
候选优化方案
  ↓
实验设计（A/B Test）
  ↓
评估
  ↓
上线 / 回滚
```

```typescript
// table: experiment
{
  id: record<experiment>,
  hypothesis: string,
  control: string,        // 对照组
  treatment: string,      // 实验组
  result: object,         // 实验结果
  status: 'running' | 'completed' | 'rolled_back'
}
```

---

### ⑳ Constitution（系统宪法）

**最高层约束**。所有 Agent、Tool、Runtime、Memory 必须遵守。

| # | 原则 | 说明 | 不可变更 |
|---|------|------|---------|
| 1 | 不确定先提问 | score < 0.5 时暂停，向用户澄清 | 是 |
| 2 | 默认可恢复 | 任何操作必须可回滚 | 是 |
| 3 | 风险优先 | 不确定性高时采用最保守策略 | 是 |
| 4 | 证据优先 | 没有证据的假设不能当事实 | 是 |
| 5 | 所有动作可回放 | 每个决策必须可追溯 | 是 |
| 6 | 不允许绕过 Review | 高风险任务必须先审查 | 是 |
| 7 | 高风险必须确认 | 涉及文件删除/修改必须用户确认 | 是 |
| 8 | 长期目标优先 | 不因短期任务牺牲长期方向 | 否 |

---

### 附录：章节映射

本架构各层与主规格数据库章节的映射关系：

| 架构层 | 对应章节 |
|--------|---------|
| ① Perception Engine | 4.172 Perception Pipeline |
| ② Attention Engine | 4.173 Attention Controller |
| ③ Intent Engine | 4.151–4.152 |
| ④ Goal System | 4.76–4.77 |
| ⑤ Planning Engine | 4.62 Planner |
| ⑥ Decision Engine | 4.153–4.154 |
| ⑦ Executive Controller | 4.174 |
| ⑧ Interrupt System | 4.175 |
| ⑨ Cognitive Stack | 4.176 |
| ⑩ Truth Maintenance System | 4.159 |
| ⑪ Hierarchical Memory | 4.83–4.86, 4.164 |
| ⑫ Memory Consolidation | 4.86 |
| ⑬ Reflection Engine | 4.78 |
| ⑭ Learning Loop | 4.177 |
| ⑮ Governor | 4.165 |
| ⑯ Meta Memory | 4.170 |
| ⑰ Identity Kernel | 4.179 |
| ⑱ Sleep Cycle | 4.123 |
| ⑲ Evolution Engine | 4.110, 4.127–4.128 |
| ⑳ Constitution | 4.109, 4.182, 4.196 |

---

## 三-B、AI Runtime V1 技术规格

### 目标

不是"未来数字生命"。是 **3~8 周能跑起来、可持续扩展、以后不推翻**的可实现版本。

### 技术栈

| 层 | 选型 |
|------|------|
| Frontend | Electron |
| Runtime | Node.js + TypeScript |
| Core Engine | Rust |
| DB | SurrealDB |
| Vector | SurrealDB Vector |
| Event | JSONL + EventBus |
| LLM | Provider Router（多 Provider） |
| Embedding | 独立服务 |

### 核心原则

1. **JSONL 是真相源（Event Source）**——所有数据以事件形式持久化
2. **SurrealDB 是查询层**——数据库只是缓存，真相在事件日志
3. **所有写操作事件化**——没有直接写 DB 的逻辑
4. **默认可恢复**——每个操作都能回滚
5. **所有状态可回放**——从事件日志重建任意时刻状态

---

### 一、目录结构（直接可建）

```
/apps
    /desktop          # Electron UI
    /runtime-api      # Node API
    /gateway          # Provider Gateway

/core
    /runtime          # Runtime Kernel
    /agent            # Agent 系统
    /memory           # 记忆系统
    /decision         # 决策引擎
    /intent           # 意图引擎
    /planner          # 规划引擎
    /governor         # 资源调度
    /attention        # 注意力
    /reflection       # 反思
    /learning         # 学习

/rust
    /scheduler        # 任务调度器
    /graph            # DAG 图
    /queue            # Priority Queue
    /reranker         # Rerank 模型
    /tokenizer        # Tokenizer

/storage
    /eventlog         # JSONL 事件日志
    /snapshot         # 快照
    /projection       # 投影

/plugins
/providers
/tests
```

**设计原则**：Runtime 和 UI 彻底分离。以后桌面、Web、Server、CLI 全复用。

---

### 二、Runtime 核接口

所有模块只认 **Runtime Contract**，禁止 agent.db.write()、agent.call() 等直接互调。

```typescript
interface Runtime {
  execute(input: Input): Promise<ExecutionResult>;
  observe(): Observation;
  delegate(task: Task, target: string): Promise<Result>;
  reflect(taskId: string): Reflection;
  snapshot(): RuntimeSnapshot;
  replay(from: string, to?: string): Promise<void>;
}
```

---

### 三、Event Source（事件真相源）

**目录结构**：
```
/storage/eventlog/
    2026/
        session_001.jsonl
        session_002.jsonl
```

**事件格式**：
```json
{
  "id": "evt_001",
  "type": "IntentCreated",
  "sessionId": "sess_x",
  "payload": {},
  "timestamp": 1716000000000
}
```

**约束**：
- 只追加（append-only）
- 禁止 update / delete
- 事件是真相，数据库只是缓存

---

### 四、事件 Schema

定义在 `/core/schema/event.ts`：

```typescript
type RuntimeEvent =
  | 'IntentCreated'
  | 'IntentExpanded'
  | 'GoalCreated'
  | 'GoalUpdated'
  | 'PlanCreated'
  | 'TaskCreated'
  | 'TaskStarted'
  | 'TaskCompleted'
  | 'TaskFailed'
  | 'DecisionMade'
  | 'MemoryAdded'
  | 'MemoryPromoted'
  | 'MemoryArchived'
  | 'ToolCalled'
  | 'ToolResultReceived'
  | 'ObservationAdded'
  | 'ReflectionCreated'
  | 'LessonStored'
  | 'SnapshotTaken'
  | 'InterruptTriggered'
  | 'ResourceWarning'
  | 'ErrorOccurred';
```

所有模块只收发事件，不直接调用其他模块。

---

### 五、Event Bus

```typescript
class EventBus {
  emit(event: RuntimeEvent, payload: object): void;
  subscribe(event: RuntimeEvent, handler: Handler): Subscription;
  unsubscribe(sub: Subscription): void;
  replay(sessionId: string): AsyncIterable<StoredEvent>;
}
```

**实现路线**：
- V1：Node.js EventEmitter（进程内）
- V2：Redis Pub/Sub（跨进程）
- V3：NATS（分布式）

**禁止**：`planner.call(memory)`。统一用 `Planner → Event → Memory`。

---

### 六、数据库 DDL（第一批）

不要一次 150 张表。第一阶段只建：

```sql
-- Session
DEFINE TABLE session SCHEMAFULL;
DEFINE FIELD sessionId ON session TYPE string;
DEFINE FIELD status ON session TYPE string;
DEFINE FIELD createdAt ON session TYPE datetime;

-- Intent
DEFINE TABLE intent SCHEMAFULL;
DEFINE FIELD sessionId ON intent TYPE string;
DEFINE FIELD rawInput ON intent TYPE string;
DEFINE FIELD type ON intent TYPE string;
DEFINE FIELD confidence ON intent TYPE float;
DEFINE FIELD entities ON intent TYPE object;
DEFINE FIELD state ON intent TYPE string;
DEFINE FIELD parentIntent ON intent TYPE string;

-- Goal
DEFINE TABLE goal SCHEMAFULL;
DEFINE FIELD sessionId ON goal TYPE string;
DEFINE FIELD title ON goal TYPE string;
DEFINE FIELD priority ON goal TYPE int;
DEFINE FIELD progress ON goal TYPE float;
DEFINE FIELD status ON goal TYPE string;

-- Task
DEFINE TABLE task SCHEMAFULL;
DEFINE FIELD title ON task TYPE string;
DEFINE FIELD state ON task TYPE string;
DEFINE FIELD priority ON task TYPE int;
DEFINE FIELD goalId ON task TYPE string;
DEFINE FIELD owner ON task TYPE string;
DEFINE FIELD constraints ON task TYPE object;

-- Task 依赖关系（图结构）
RELATE task:A->depends_on->task:B;

-- Decision
DEFINE TABLE decision SCHEMAFULL;
DEFINE FIELD taskId ON decision TYPE string;
DEFINE FIELD type ON decision TYPE string;
DEFINE FIELD candidates ON decision TYPE array;
DEFINE FIELD selected ON decision TYPE string;
DEFINE FIELD confidence ON decision TYPE float;
DEFINE FIELD reason ON decision TYPE string;

-- Memory
DEFINE TABLE memory SCHEMAFULL;
DEFINE FIELD sessionId ON memory TYPE string;
DEFINE FIELD layer ON memory TYPE string;
DEFINE FIELD content ON memory TYPE string;
DEFINE FIELD entities ON memory TYPE array;
DEFINE FIELD confidence ON memory TYPE float;
DEFINE FIELD status ON memory TYPE string;
DEFINE FIELD vector ON memory TYPE vector<float>;

-- Observation
DEFINE TABLE observation SCHEMAFULL;
DEFINE FIELD sourceType ON observation TYPE string;
DEFINE FIELD content ON observation TYPE string;
DEFINE FIELD entities ON observation TYPE array;
DEFINE FIELD signals ON observation TYPE array;
DEFINE FIELD importance ON observation TYPE float;

-- Reflection
DEFINE TABLE reflection SCHEMAFULL;
DEFINE FIELD taskId ON reflection TYPE string;
DEFINE FIELD problem ON reflection TYPE string;
DEFINE FIELD improvement ON reflection TYPE string;
DEFINE FIELD confidence ON reflection TYPE float;

-- Snapshot
DEFINE TABLE snapshot SCHEMAFULL;
DEFINE FIELD sessionId ON snapshot TYPE string;
DEFINE FIELD state ON snapshot TYPE object;
DEFINE FIELD checksum ON snapshot TYPE string;
```

---

### 七、Intent 模块

**目录**：`/core/intent/`

**文件**：
```
parser.ts      # 意图解析
expander.ts    # 子意图展开
graph.ts       # 意图图构建
service.ts     # 对外接口
```

**接口**：
```typescript
interface IntentService {
  parse(input: string): Intent;
  expand(intent: Intent): Intent[];
  buildGraph(intents: Intent[]): IntentGraph;
}

interface Intent {
  id: string;
  type: 'refactor' | 'feature' | 'bugfix' | 'research' | 'config' | 'question';
  entities: Record<string, string>;
  confidence: number;
  children?: Intent[];
}
```

**V1 实现**：Prompt 解析。

**Prompt 模板**：
```
识别以下输入中的：
1. 目标（用户想做什么）
2. 实体（项目名、文件名、技术栈）
3. 动作（分析/修改/创建/删除）
4. 风险级别（低/中/高/严重）
5. 子任务列表

输入：{input}
```

---

### 八、Planning 模块

**目录**：`/core/planner/`

**接口**：
```typescript
interface Planner {
  plan(goal: Goal): TaskGraph;
  replan(taskGraph: TaskGraph, failedTask: string): TaskGraph;
}

interface TaskGraph {
  tasks: Task[];
  edges: { from: string; to: string; relation: 'depends_on' | 'optional' }[];
}
```

**输入**：Goal → **输出**：Task Graph（DAG）

```
A
↓
B → C → D
```

**存储**：SurrealDB RELATE 关系。

---

### 九、Decision 模块

**目录**：`/core/decision/`

**接口**：
```typescript
interface DecisionEngine {
  evaluate(context: DecisionContext): Candidate[];
  select(candidates: Candidate[]): Decision;
}

interface Candidate {
  name: string;
  scores: {
    quality: number;    // 0-10
    cost: number;       // 0-10
    latency: number;    // 0-10
    history: number;    // 0-10
  };
  totalScore: number;
}
```

**评分公式**：
```
totalScore = quality × 0.4 + cost × 0.2 + latency × 0.2 + history × 0.2
```

**配置（热更新）**：
```yaml
decision:
  weights:
    quality: 0.4
    cost: 0.2
    latency: 0.2
    history: 0.2
```

---

### 十、Memory 系统

**目录**：`/core/memory/`

```
working/        # 工作记忆
episodic/       # 事件记忆
semantic/       # 语义记忆
archive/        # 归档
```

**接口**：
```typescript
interface MemoryService {
  add(layer: MemoryLayer, entry: MemoryEntry): void;
  retrieve(layer: MemoryLayer, query: MemoryQuery): MemoryEntry[];
  promote(from: MemoryLayer, to: MemoryLayer, id: string): void;
  archive(before: Date): void;
}
```

**V1 存储策略**：
| 层 | 存储 | 容量 |
|-----|------|------|
| Working | 内存 | 当前会话 |
| Episodic | SurrealDB | 最近 90 天 |
| Semantic | SurrealDB + Vector | 按置信度保留 |
| Archive | JSONL 压缩 | 永久 |

---

### 十一、Memory Consolidation（记忆固化）

**后台 Job**：

```typescript
interface ConsolidationJob {
  memoryId: string;
  strategy: 'summary' | 'extract' | 'merge';
  status: 'pending' | 'running' | 'completed';
}
```

**流程**：
```
Task 完成
  ↓
生成 Summary
  ↓
提取实体
  ↓
写入 Semantic Memory
  ↓
清理 Working Memory
```

**触发**：空闲时 / 定时（每 30 分钟）。

---

### 十二、Attention 模块

**目录**：`/core/attention/`

**实现**：Rust PriorityQueue（BinaryHeap）

```typescript
interface AttentionItem {
  sourceId: string;
  score: number;
  dimensions: {
    importance: number;
    goalRelevance: number;
    risk: number;
    recency: number;
  };
  expireAt: number;
}
```

**公式**：
```
attention = importance × 0.3 + goalRelevance × 0.3 + risk × 0.2 + recency × 0.2
```

**Top-K**：每次取最高分 50 条送 LLM。

---

### 十三、Governor（资源调度）

**目录**：`/core/governor/`

```typescript
interface GovernorState {
  cpu: number;
  memory: number;
  tokens: number;
  activeAgents: number;
  mode: 'normal' | 'warning' | 'critical';
}
```

**策略（热更新）**：
```yaml
rules:
  - condition: cpu > 80
    action: pauseEmbedding
  - condition: memory > 85
    action: triggerGC
  - condition: tokens > dailyBudget * 0.8
    action: switchToLowCost
```

---

### 十四、Interrupt 系统

**目录**：`/core/executive/`

```typescript
interface Interrupt {
  taskId: string;
  action: 'pause' | 'resume' | 'cancel' | 'preempt';
  reason: string;
  priority: number;
}
```

**流程**：
```
Interrupt 触发
  ↓
Task Snapshot（保存当前状态）
  ↓
Executive 处理（暂停/切换）
  ↓
完成后 Resume
```

**快照**：`/storage/snapshot/task_{id}.json`

---

### 十五、Snapshot（快照）

**目录**：`/storage/snapshot/`

```json
{
  "sessionId": "sess_x",
  "taskStack": [/* 当前执行栈 */],
  "workingMemory": [/* 当前工作记忆 */],
  "state": { /* 运行时状态 */ },
  "timestamp": 1716000000000
}
```

**触发时机**：
- 任务切换时
- 退出前
- 崩溃前（Signal 捕获）

---

### 十六、Replay（回放）

**启动流程**：
```
1. Load Snapshot（加载最近快照）
2. Replay Events（回放后续事件）
3. Restore Runtime（重建运行时）
4. Verify Consistency（校验一致性）
```

**用途**：
- 事故恢复
- 调试
- 时间旅行（回退到任意时刻）

---

### 十七、Provider Router

**目录**：`/providers/`

```typescript
interface Provider {
  chat(messages: Message[]): Promise<Response>;
  embed(text: string): Promise<number[]>;
  rerank(query: string, docs: string[]): Promise<number[]>;
}
```

**路由策略**：
| 任务类型 | 推荐 Provider | 原因 |
|---------|--------------|------|
| 代码 | Claude | 代码能力强 |
| 搜索 | GPT | 搜索工具链成熟 |
| 本地 | Qwen / Ollama | 免费 + 本地运行 |
| Embedding | 独立 Embedding 服务 | 专用模型 |

---

### 十八、Rust 职责（别全 Node）

**Rust 负责**（性能敏感）：
- Graph 构建与拓扑排序
- Scheduler Priority Queue
- Embedding 计算
- Rerank 推理
- Tokenizer
- 大文件处理 / Heavy Memory 操作

**Node 负责**（业务编排）：
- LLM Prompt 编排
- 业务流程
- Event Bus
- HTTP/WebSocket

**Electron 负责**（UI）：
- 渲染
- 用户交互

**否则 Node 后面必卡。**

---

### 十九、V1 开发顺序（非常重要）

不要一起开。分阶段：

| 阶段 | 周期 | 内容 | 交付物 |
|------|------|------|--------|
| **Phase 1** | 1-2 周 | 事件系统 + Session + Intent + Task + Snapshot | 可跑通"输入→意图→任务" |
| **Phase 2** | 2-3 周 | Planning + Decision + Memory + Replay | 可执行完整任务流 |
| **Phase 3** | 2-3 周 | Attention + Governor + Reflection | 系统自调节 |
| **Phase 4** | 1-2 周 | Learning + Meta + Evolution | 系统自成长 |

**Phase 1 红线**：Session 创建 → Intent 解析 → Task 创建 → 执行 → Snapshot。其他全部锁死不改。

---

### 二十、V1 先删掉这些（现在全是负债）

先不要做：
- ✗ Belief Graph
- ✗ Court System
- ✗ Economy Layer
- ✗ Civilization
- ✗ Dream Queue
- ✗ Emotion System
- ✗ Society Rules
- ✗ Constitution Engine（保留原则，不做引擎）
- ✗ Multi-Agent Market
- ✗ Agent Network（保留 Agent Registry，不做网络）

**V1 只保留**：
- ✅ Intent
- ✅ Task
- ✅ Memory
- ✅ Decision
- ✅ Replay
- ✅ Governor
- ✅ Planning
- ✅ Attention
- ✅ Reflection

---

### 三-B-1、Rust Core Trait（统一运行时协议）

先解决最大问题：模块禁止互相直连。不出现 `planner.memory()`、`decision.agent()`、`runtime.db()`。

#### 统一 RuntimeModule Trait

所有模块（Intent、Memory、Scheduler、Governor、Learning、Attention）实现同一接口：

```rust
pub trait RuntimeModule {
    fn name(&self) -> String;
    fn initialize(&mut self) -> Result<()>;
    fn handle_event(&mut self, event: RuntimeEvent) -> Result<Vec<RuntimeEvent>>;
    fn snapshot(&self) -> Result<StateSnapshot>;
    fn restore(&mut self, snapshot: StateSnapshot) -> Result<()>;
}
```

Runtime 只需要：
```rust
for module in modules {
    module.handle_event(event)?;
}
```

#### Runtime Trait

```rust
pub trait Runtime {
    fn execute(&mut self, input: Input);
    fn emit(&mut self, event: RuntimeEvent);
    fn replay(&mut self);
    fn snapshot(&mut self);
}

pub struct RuntimeCore {
    modules: Vec<Box<dyn RuntimeModule>>,
    bus: Bus,
    state: RuntimeState,
}
```

#### Task DAG

不要 `Vec<Task>`。必须图结构。

```rust
pub struct TaskNode {
    pub id: String,
    pub title: String,
    pub state: TaskState,
    pub priority: u32,
    pub deps: Vec<String>,
}

pub struct TaskGraph {
    nodes: HashMap<String, TaskNode>,
}
```

**示例**：
```
分析项目
  ↓
重构
  ↓
生成文档
```

**调度**：
```rust
pub fn ready_tasks(&self) -> Vec<TaskNode> {
    // 过滤：所有依赖已完成的任务
}
```

#### Scheduler（调度器）

目录：`rust/scheduler`

```rust
pub struct Scheduler {
    queue: BinaryHeap<TaskItem>,
}

pub struct TaskItem {
    priority: u32,
    task_id: String,
    aging: u32,
    deadline: u64,
}
```

**排序**：实现 `Ord for TaskItem`，支持 priority + aging + deadline。

**公式**：`score = priority + aging + urgency`

**状态**：queued → running → waiting → paused → completed

**执行**：`pub fn next() { queue.pop() }`

#### Interrupt（中断）

```rust
pub enum InterruptAction { Pause, Resume, Cancel, Preempt }

pub struct Interrupt {
    task_id: String,
    action: InterruptAction,
}
```

**处理**：`scheduler.handle_interrupt(interrupt)`

**抢占流程**：高优任务到达 → 保存当前快照 → 切换执行

#### Snapshot Trait

```rust
pub trait Snapshotable {
    fn save() -> StateSnapshot;
    fn restore(snapshot: StateSnapshot);
}
```

**Task 快照内容**：{ queue, stack, memory }
**写入**：`storage/snapshot/`

#### Event 定义（禁止字符串事件）

```rust
pub enum RuntimeEvent {
    IntentCreated,
    GoalCreated,
    TaskCreated,
    TaskStarted,
    TaskFinished,
    MemoryAdded,
    ReflectionCreated,
    DecisionMade,
}
```

**禁止字符串事件**——否则后期地狱。

#### Embedding Pipeline

目录：`rust/embedding`

**流程**：Memory → Chunk → Embedding → Store → Vector Index

```rust
pub struct Chunk { id: String, content: String }

pub trait Embedder {
    fn embed(text: String) -> Vec<f32>;
}
```

**实现**：OpenAIEmbedder、OllamaEmbedder

**参数**：chunk_size = 512, overlap = 64

#### Retrieval（检索）

```rust
pub trait Retriever {
    fn search(query: String) -> Vec<Memory>;
}
```

**流程**：向量召回 → Rerank → TopK（默认 Top=10）

#### Reranker

目录：`rust/reranker`

```rust
pub trait Reranker {
    fn rerank(items: Vec<Item>);
}
```

- V1：BM25 + Embedding 混合
- V2：Cross-Encoder

#### Plugin 协议（MCP 风格）

插件禁止直接 import。统一描述：

```json
{
  "name": "filesystem",
  "version": "1.0",
  "tools": [
    { "name": "read", "description": "读取文件" },
    { "name": "write", "description": "写入文件" }
  ]
}
```

```typescript
interface Plugin {
  init(): void;
  tools(): ToolDef[];
  execute(tool: string, args: object): Promise<Result>;
}
```

#### Plugin Runtime

```rust
pub trait Tool {
    fn execute(args: String) -> ToolResult;
}
```

**注册**：`registry.add(tool)`
**调用**：`registry.call(tool_name, args)`

#### 安全层

插件默认无权限。声明式权限：

```json
{
  "permissions": ["filesystem.read", "browser"]
}
```

**运行约束**：Sandbox 隔离、限制 CPU/内存/timeout。

#### Provider Trait

```rust
pub trait Provider {
    fn chat(messages: Vec<Message>) -> Result<Response>;
    fn embedding(text: String) -> Vec<f32>;
    fn rerank(query: String, docs: Vec<String>) -> Vec<f32>;
}
```

**实现**：GPT、Claude、Gemini、Qwen、Ollama
**路由**：`route(task)` 按任务类型分发

#### 第一阶段启动链路

```
启动:
  load snapshot → load modules → subscribe events → replay → runtime ready

执行:
  input → intent → goal → task → scheduler → eventlog → snapshot
```

---

### 三-B-2、建项目第一天就该创建的文件

目标：`pnpm install → docker compose up → pnpm dev → Runtime Ready`

#### pnpm-workspace.yaml

```yaml
packages:
  - apps/*
  - packages/*
  - core/*
```

#### 根 package.json

```json
{
  "name": "ai-runtime",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "latest"
  }
}
```

#### turbo.json

```json
{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false
    }
  }
}
```

#### tsconfig.base.json

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "CommonJS",
    "strict": true,
    "baseUrl": ".",
    "paths": {
      "@types/*": ["packages/types/*"],
      "@events/*": ["packages/events/*"],
      "@core/*": ["core/*"]
    }
  }
}
```

#### Docker Compose

目录：`infra/docker-compose.yml`

```yaml
version: "3"
services:
  surreal:
    image: surrealdb/surrealdb:latest
    command: start --user root --pass root file:/db/db
    ports: ["8000:8000"]
    volumes: ["./data/surreal:/db"]

  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]

  runtime:
    build: ..
    ports: ["3000:3000"]
    depends_on: [surreal, redis]
    env_file: ../.env
```

**启动**：`docker compose up -d`

#### 环境变量（.env）

```
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
QWEN_KEY=
SURREAL_URL=http://localhost:8000
SURREAL_USER=root
SURREAL_PASS=root
REDIS_URL=redis://localhost:6379
MODEL_ROUTER=gpt
```

**读取**：`import "dotenv/config"`，统一 `process.env`

#### Runtime API 入口（apps/runtime-api）

```typescript
import Fastify from "fastify";
const app = Fastify();

app.post("/runtime/execute", async (req) => {
  return runtime.execute(req.body);
});

app.listen({ port: 3000 });
```

#### Electron 结构（apps/desktop）

```
src/
  main/       # Electron 主进程
  preload/    # preload 桥接
  renderer/   # 渲染进程
```

**通信路径**：`Electron → IPC → Runtime API`

**preload**：
```typescript
contextBridge.exposeInMainWorld("runtime", {
  execute: (input) => ipcRenderer.invoke("runtime", input),
});
```

**禁止**：UI 直接碰 DB。

#### Provider 实现（providers）

统一接口：
```typescript
export interface Provider {
  chat(messages: Message[]): Promise<Response>;
  embed(text: string): Promise<number[]>;
  rerank(query: string, docs: string[]): Promise<number[]>;
}
```

**OpenAI**：class GPTProvider implements Provider
**Claude**：class ClaudeProvider implements Provider
**Router**：根据 task.type 路由（code → Claude，其他 → GPT）

#### Intent Prompt（V1）

文件：`core/intent/prompt.txt`

```
分析用户输入。
输出JSON：
{
  intentType,
  entities: [],
  subtasks: [],
  riskLevel,
  confidence
}
仅输出JSON
```

**Node 端**：
```typescript
const result = await provider.chat(prompt);
const parsed = IntentSchema.parse(JSON.parse(result.content));
```

**校验**：使用 Zod Schema，不信任模型输出。

```typescript
const IntentSchema = z.object({
  intentType: z.string(),
  entities: z.array(z.string()),
});
```

#### Memory 实现

```typescript
class WorkingMemory {
  map = new Map<string, MemoryEntry>();
}

class SemanticMemory {
  retrieve(query: Query): MemoryEntry[];
  store(entry: MemoryEntry): void;
}
```

**Archive**：`storage/archive/` 按天归档 `day-1.json`
```
{ summary: "", entities: [] }
```

#### Runtime 启动文件

```typescript
class Runtime {
  constructor() {
    this.bus = new RuntimeBus();
    this.memory = new Memory();
    this.intent = new Intent();
    this.scheduler = new Scheduler();
  }
}

// 启动
runtime.init();
// 内部: subscribe() → restore() → ready
```

#### 事件订阅（事件驱动）

```typescript
bus.on("IntentCreated", async (event) => { goal.create(); });
bus.on("GoalCreated", () => { planner.plan(); });
bus.on("TaskCreated", () => { scheduler.enqueue(); });
```

**禁止**：`intent → goal → planner` 硬调。

#### SurrealDB 连接

```typescript
import Surreal from "surrealdb";
const db = new Surreal();
await db.connect(process.env.SURREAL_URL);
await db.signin({
  user: process.env.SURREAL_USER,
  pass: process.env.SURREAL_PASS,
});
```

#### 初始化 DDL 脚本

目录：`infra/`

```
surreal import --conn http://localhost:8000 init.surql
```

**版本管理**：`v1.sql` → `v2.sql`
**禁止**：手改数据库。

#### 事件回放实现

```typescript
const lines = fs.readFileSync(path, "utf-8").split("\n");
for (const line of lines) {
  runtime.apply(JSON.parse(line));
}
```

**启动**：`snapshot → eventlog → restore`

**恢复**：Task 状态、Memory、Queue。

#### Dockerfile（根）

```dockerfile
FROM node:22
WORKDIR /app
COPY . .
RUN corepack enable && pnpm install && pnpm build
EXPOSE 3000
CMD ["pnpm", "dev"]
```

#### 第一批依赖

```bash
# 生产
pnpm add zod dotenv uuid fastify eventemitter3 surrealdb openai

# 开发
pnpm add -D typescript tsx turbo
```

#### 第一批测试

目录：`tests/`

**测试场景**：
```
输入: "帮我优化博客系统"
预期事件: IntentCreated → GoalCreated → TaskCreated
重启后: Replay 成功
断言: Task 数量 = 3
```

#### 现在不要做（V1 删减清单）

```
删掉（现在全是负债）：
  ✗ Agent Market
  ✗ Economy
  ✗ Society
  ✗ Civilization
  ✗ Emotion
  ✗ Dream
  ✗ Belief Graph
  ✗ Court System

继续只保留：
  ✅ Runtime
  ✅ Intent
  ✅ Task
  ✅ Memory
  ✅ Replay
  ✅ Scheduler
  ✅ Event Bus
```

---

## 三-C、插件 + 多JDK + 性能架构

### 总目标

| 维度 | 目标 |
|------|------|
| 主工程 | Java 26 |
| 插件 | Java 17/21/26 + Python + Rust + Node，互不影响 |
| 性能 | 50 插件安装，实际仅 5-6 常驻，用户感觉接近秒开 |

### 核心思想

```
工具链隔离 + Worker 池 + 冷热插件 + 预热 + 预测启动
```

### 架构总览

```
User
  ↓
UI(Electron)
  ↓
Intent
  ↓
Planner
  ↓
Task DAG
  ↓
Scheduler
  ↓
Governor
  ↓
Plugin Manager
  ↓
Worker Pool
  ↓
Runtime Manager
  ↓
Plugin
  ↓
Event Store
  ↓
Projection
  ↓
UI
```

#### 模块职责

**UI 层**（Electron + React）
- 输入 / 状态显示 / 日志 / 插件市场

**Intent**
- 输入：`帮我重构项目并打安卓包`
- 输出：`{ intent: "project", entities: ["android"], subtasks: ["analyze", "build"] }`
- 作用：理解需求

**Planner**
- 职责：任务拆解
- 输出：分析 → 构建 → 部署

**Task DAG**（TaskGraph）
- 结构：
  ```
  分析项目
  ├─ 扫描
  ├─ 读取依赖
  └─ 结构分析
    ↓
  构建
  ```
- 职责：依赖管理、并行

**Scheduler**
- 职责：调度任务
- 算法：`priority + aging + urgency`
- 避免：任务饿死

**Governor**
- 职责：资源管理
- 控制：CPU / 内存 / GPU / 线程
- 防：所有插件抢资源

**Plugin Manager**
- 职责：插件生命周期
- 维护：启动 / 销毁 / 休眠 / 缓存

**Runtime Manager**
- 职责：多工具链管理
- 目录：`runtime/jdks/{16,21,26}/` + `python/` + `node/`
- 插件声明 `"runtime": "java26"` 自动路由

**Worker Pool**
- 原则：不要 1 插件 1 JVM
- 按版本共享池：`Java16Pool`、`Java26Pool`
- 共享：JVM、GC、线程
- 隔离：ClassLoader、Plugin 协议

**Plugin**
- 目录：`plugin/plugin.json` + `app.jar`
- 配置：`{ name, runtime, permissions: ["filesystem.read"] }`

**通信**
- 统一 RPC
- 请求：`{ id, tool: "build" }` → 返回：`{ success: true }`
- 演进：V1 stdio → V2 socket / gRPC

**Sandbox**
- 插件默认零权限
- 声明制：`network`、`filesystem`、`camera`，否则拒绝

**HealthCheck**
- 后台 `ping` → `pong`
- 超时自动重启

**Circuit Breaker**
- 失败 3 次 → 熔断暂停调用 → 避免死循环

**Event Sourcing**
- 不直接改数据库
- 写事件：`TaskCreated → TaskStarted → TaskCompleted`
- 存：`events.jsonl`

**Projection**
- 事件 = 历史，Projection = 当前状态
- 存 SQLite，UI 直接读

**Snapshot**
- 保存：`{ task: "123", progress: 80 }`
- 作用：崩溃恢复

**Metrics**
- 记录：CPU、内存、启动时间、调用次数、错误率
- 用于：热度、自动优化

**热度系统**
- 公式：`score = recent×0.5 + frequency×0.3 + priority×0.2`
- 决定：常驻 / 休眠 / 卸载

**预测器**
- 用户：`帮我打安卓包`
- 后台预启动：`android-builder`

**插件启动层级**
- **热**（filesystem、memory）：常驻
- **温**（git、android）：保活 10 分钟
- **冷**（pdf、deploy）：按需启动

**最终运行**
- 安装 50 插件，实际在线 5、运行 3
- 内存：500MB ~ 1GB（不是 50×JVM）

---

### 第一层：RuntimeManager（多 JDK 管理）

**职责**：管理所有运行时环境。

**目录结构**：
```
runtime/
  ├── jdks/
  │   ├── 17/
  │   ├── 21/
  │   └── 26/
  ├── python/
  └── node/
```

**维护映射表**：
```typescript
const runtimeMap = {
  java17:  { path: 'runtime/jdks/17' },
  java21:  { path: 'runtime/jdks/21' },
  java26:  { path: 'runtime/jdks/26' },
  python3: { path: 'runtime/python' },
  node22:  { path: 'runtime/node' },
};
```

**功能**：根据插件需求返回正确运行环境。

```typescript
// plugin.runtime = 'java26'
// → 返回 runtime/jdks/26/bin/java
class RuntimeManager {
  resolve(plugin: Plugin): Runtime {
    return this.runtimes.get(plugin.runtime);
  }
}
```

**原则**：插件自己决定版本，而不是系统统一。

---

### 第二层：PluginManager

**职责**：插件生命周期管理。

```typescript
interface PluginManager {
  load(pluginId: string): Worker;
  start(pluginId: string): Worker;
  stop(pluginId: string): void;
  destroy(pluginId: string): void;
  get(pluginId: string): Worker | null;
}
```

**内部结构**：`Map<pluginId, { process, lastAccess, state }>`

**调用流程**：
```
请求插件
  ↓
检查是否已运行
  ↓
  已运行 → 直接返回
  未运行 → 创建 Worker
```

---

### 第三层：插件协议

**插件目录结构**：
```
plugins/
  └── android/
      ├── plugin.json
      └── app.jar
```

**plugin.json**：
```json
{
  "name": "android",
  "runtime": "java26",
  "entry": "app.jar",
  "args": ["-Xmx256m"],
  "idleTimeout": 600,
  "mode": "worker",
  "permissions": ["filesystem.read", "process.spawn"]
}
```

| 字段 | 含义 |
|------|------|
| runtime | 所需运行环境（java17/java21/java26/python/node） |
| idleTimeout | 空闲多久后休眠（秒） |
| mode | 运行模式（worker / oneshot） |
| permissions | 权限声明 |

---

### 第四层：通信协议

插件禁止直接 import 宿主。统一 RPC。

**请求格式**：
```json
{
  "id": "req_001",
  "tool": "build",
  "args": { "path": "app" }
}
```

**响应格式**：
```json
{
  "id": "req_001",
  "success": true,
  "data": {},
  "error": null
}
```

**传输路线**：
```
V1: stdin/stdout（stdio）
V2: Socket（TCP）
V3: gRPC
```

**语言无关**：Java、Python、Rust、Node 都能接。

**Rust 接口定义**：
```rust
pub trait PluginTransport {
    fn send(request: Request) -> Result<Response>;
    fn receive() -> Result<Response>;
}
```

---

### 第五层：Worker 模式

不要每次启动插件。改插件启动后保留。

```typescript
interface Worker {
  process: ChildProcess;
  lastAccess: number;
  state: 'idle' | 'busy' | 'stopped';
  capabilities: string[];
}
```

**效果对比**：

| 方案 | 首次 | 后续 |
|------|------|------|
| 每次启动新 JVM | 300ms~2s | 300ms~2s（每次都慢） |
| Worker 复用 | 300ms~2s | 几十 ms（秒开） |

---

### 第六层：自动休眠

**后台**：每分钟扫描一次。

```typescript
class IdleScanner {
  scan(): void {
    for (const [id, worker] of this.workers) {
      const idle = Date.now() - worker.lastAccess;
      const timeout = this.configs.get(id).idleTimeout * 1000;
      if (idle > timeout) {
        worker.process.kill();
        this.workers.delete(id);
      }
    }
  }
}
```

**示例**：10 分钟没用 → kill worker。

**目的**：避免几十个插件常驻内存。

---

### 第七层：Worker 池

再升级。不要 10 个插件 = 10 个 JVM。

```typescript
// 按 Runtime 分组
const pools = {
  java17: [Worker1],
  java26: [Worker1, Worker2],
  python: [Worker1],
};
```

**隔离机制**：ClassLoader（Java）或 独立进程。

**效果**：
```
10 个插件 → 2 个 JVM
50 个插件 → 5-6 个 Worker

内存对比：
  10 JVM × 150MB = 1500MB
  2 JVM   × 150MB = 300MB
```

---

### 第八层：插件冷热分层

```
热（永久在线）：
  filesystem, terminal, process, security, model-router, task-scheduler, event-bus, memory
  → 进程常驻

温（10 分钟保活）：
  code-editor, preview, browser, git, debugger, knowledge-base, skill-registry, rule-engine
  → idleTimeout = 600

冷（按需启动）：
  node-manager, python-manager, java-manager, android-builder, docker, database, http-client, websocket
  → 用完即关，idleTimeout = 60

工具层（按需启动）：
  pdf, ocr, image, archive, downloader, notifier
  → 用完即关，idleTimeout = 60
```

**效果**：50 个插件安装 → 仅 5-6 个实际运行。

---

### 第九层：预热

**启动软件后后台自动预热**：
```typescript
class Prewarmer {
  async warmup(): Promise<void> {
    // 后台提前启动常用 Worker
    await this.pools.java26.start();
    await this.pools.java17.start();
    await this.pools.python.start();
  }
}
```

**效果**：用户第一次点击也接近秒开（Worker 已经跑起来了）。

---

### 第十层：预测启动

**原理**：Intent Engine 分析输入时，预先启动即将需要的插件。

```typescript
class PredictiveStarter {
  onIntentParsed(intent: Intent): void {
    const needed = this.predictPlugins(intent);
    for (const plugin of needed) {
      if (!this.manager.isRunning(plugin)) {
        this.manager.start(plugin); // 提前启动
      }
    }
  }

  private predictPlugins(intent: Intent): string[] {
    if (intent.type === 'android') return ['android', 'filesystem', 'gradle'];
    if (intent.type === 'python') return ['python', 'terminal'];
    return ['filesystem'];
  }
}
```

**流程**：
```
用户输入 "帮我打安卓包"
  ↓
Intent 解析出 android
  ↓
预测启动 android-builder
  ↓
用户还在输入 "并部署到测试机"...
  ↓
插件已经启动好了
  ↓
Intent 执行时 → 秒开
```

---

### 第十一层：性能真实结果

| 项目 | 数值 |
|------|------|
| 安装插件数 | 50 |
| 实际常驻 | 5-6 |
| Java26 Worker | ~150MB |
| Java17 Worker | ~100MB |
| Python Worker | ~80MB |
| 主程序 | ~300MB |
| **总计** | **~500-800MB** |

**对比**：50 × JVM = 5GB+ → 直接爆内存。

---

### 实施路线图

| 阶段 | 内容 | 交付物 |
|------|------|--------|
| **第一周** | RuntimeManager + PluginManager + RPC协议 + Worker + Idle关闭 | 单个插件可独立运行，空闲自动回收 |
| **第二周** | 预热池 + 预测启动 + 冷热分层 | 首次启动秒开，50 插件仅 5 常驻 |
| **第三周** | 共享 JVM 池 + ClassLoader 隔离 + 权限沙箱 | 10 插件 2 JVM，插件安全隔离 |
| **第四周** | MCP 协议 + 插件市场 | 第三方插件可安装、发现、升级 |

---

### 插件补充机制

以下机制按优先级排列，从第一优先到第三优先。

#### 第一优先

##### 1. Capability Graph（插件能力图）

现在插件用名字识别：`android-builder`、`filesystem`、`git`。以后插件市场一来就炸——必须按**能力**路由，不按名字。

```json
{
  "name": "android-builder",
  "capabilities": [
    "android.build",
    "gradle.build",
    "apk.sign"
  ]
}
```

**Runtime 路由**：
```
需求: android.build
  ↓
Capability Graph 查找
  ↓
返回: android-builder（v2）、gradle-wrapper（v1）
  ↓
选最优版本
```

**禁止写死插件名**。Runtime 只认 capability。

```typescript
class CapabilityRegistry {
  private graph: Map<string, PluginInfo[]> = new Map();

  register(plugin: Plugin): void {
    for (const cap of plugin.capabilities) {
      if (!this.graph.has(cap)) this.graph.set(cap, []);
      this.graph.get(cap)!.push(plugin);
    }
  }

  resolve(capability: string): PluginInfo | null {
    const candidates = this.graph.get(capability);
    if (!candidates || candidates.length === 0) return null;
    return candidates.sort((a, b) => b.apiVersion - a.apiVersion)[0];
  }
}
```

---

##### 2. Sandbox 权限系统

插件默认**零权限**。权限必须声明，运行时检查。

```json
{
  "name": "android-builder",
  "permissions": [
    "filesystem.read",
    "filesystem.write",
    "process.spawn",
    "network"
  ]
}
```

**运行时检查**：
```
Plugin Manager
  ↓
Permission Guard（检查声明）
  ↓
是否允许？
  ↓
  YES → 执行
  NO  → 拒绝 + 报错
```

**扩展能力**：
- CPU 限制（cgroup / task set）
- 内存上限（JVM -Xmx 参数注入）
- 执行超时（kill 超时进程）
- 文件系统沙箱（chroot / 容器）
- 网络限制（只允许特定端口）

**原则**：一个恶意插件 `rm -rf /` 不会影响系统。

---

##### 3. 健康检查（HealthCheck）

插件可能没死但已经卡死。心跳机制检测。

**协议**：
```json
// 宿主 → 插件
{ "type": "ping" }

// 插件 → 宿主
{ "type": "pong", "timestamp": 1716000000000 }
```

**策略**：
```typescript
class HealthChecker {
  private timeouts: Map<string, number> = new Map();

  ping(workerId: string): void {
    const now = Date.now();
    if (this.timeouts.has(workerId)) {
      const last = this.timeouts.get(workerId)!;
      if (now - last > 10_000) { // 10s 无响应
        this.restart(workerId);
      }
    }
    this.timeouts.set(workerId, now);
  }

  private restart(workerId: string): void {
    // kill → 重新创建 Worker → 恢复任务
  }
}
```

**效果**：UI 不会一直转圈，用户不会觉得软件坏了。

---

#### 第二优先

##### 4. 熔断器（Circuit Breaker）

插件会挂、AI 会挂、网络更会挂。熔断器防止雪崩。

```typescript
class CircuitBreaker {
  state: 'closed' | 'open' | 'half-open' = 'closed';
  failures: number = 0;
  threshold: number = 3;

  async call(fn: () => Promise<Result>): Promise<Result> {
    if (this.state === 'open') {
      throw new Error('Circuit open: plugin unavailable');
    }
    try {
      const result = await fn();
      this.failures = 0;
      return result;
    } catch (e) {
      this.failures++;
      if (this.failures >= this.threshold) {
        this.state = 'open';
        setTimeout(() => { this.state = 'half-open'; }, 30_000);
      }
      throw e;
    }
  }
}
```

**状态**：
```
closed（正常）
  ↓ 连续 3 次失败
open（熔断）
  ↓ 30 秒后
half-open（半开，尝试恢复）
  ↓ 成功 → closed
  ↓ 失败 → open
```

**效果**：不会疯狂重启插件导致 CPU 起飞。

---

##### 5. 插件观测系统（Metrics）

以后你一定会问"为什么卡"。所以每插件记录指标。

```json
{
  "plugin": "android",
  "latency": 212,
  "memory": 95,
  "cpu": 12,
  "calls": 51,
  "errors": 2,
  "errorRate": 0.039,
  "lastActivity": 1716000000000
}
```

```typescript
interface PluginMetrics {
  pluginId: string;
  latency: number;       // 平均耗时（ms）
  memory: number;        // 内存占用（MB）
  cpu: number;           // CPU 使用率（%）
  calls: number;         // 调用次数
  errors: number;        // 错误次数
  errorRate: number;     // 错误率
  lastActivity: number;  // 最后活跃时间
}
```

**UI 插件监控页**：
```
┌──────────┬────────┬──────┬──────┬──────┬──────┐
│ Plugin   │ Calls  │ Err% │ Mem  │ CPU  │ Lat  │
├──────────┼────────┼──────┼──────┼──────┼──────┤
│ android  │ 51     │ 3.9% │ 95MB │ 12%  │ 212  │
│ filesys  │ 1024   │ 0.1% │ 32MB │ 2%   │ 4    │
│ git      │ 237    │ 1.2% │ 48MB │ 5%   │ 87   │
└──────────┴────────┴──────┴──────┴──────┴──────┘
```

---

#### 第三优先

##### 6. 崩溃恢复队列（Crash Recovery）

最容易漏的。插件执行到一半软件崩了——任务不能丢。

```typescript
interface TaskSnapshot {
  taskId: string;
  pluginId: string;
  step: string;          // 执行到哪一步
  args: object;          // 执行参数
  state: object;         // 中间状态
  timestamp: number;
}
```

**目录**：`storage/crash_recovery/`

**文件**：`task_{taskId}.json`

**启动恢复流程**：
```
Runtime 启动
  ↓
扫描 crash_recovery 目录
  ↓
找到未完成的任务
  ↓
检查插件是否可用
  ↓
  可用 → 继续执行
  不可用 → 暂存，等待插件恢复
```

**防止大型任务全废**。

---

##### 7. 插件版本协商（Version Negotiation）

以后 android-builder v2、filesystem v7 协议变化时工具参数不同。

```json
{
  "name": "filesystem",
  "apiVersion": "2",
  "compatible": ["1", "2"]
}
```

**握手流程**：
```
插件启动
  ↓
宿主发送支持的 API 版本列表
  ↓
插件回复使用的版本
  ↓
协商完成 → 开始通信
```

**避免**：升级一个插件，全系统挂。

---

#### 后续高级层（先不碰）

```
✗ 插件市场
✗ 插件签名
✗ 远程插件
✗ 多机 Worker
✗ 云插件
✗ WASM 插件
```

---

### Runtime 内核补完件

以下 10 项属于"你现在不做，以后架构会变硬、很难插进去"的层级。

---

#### 8. 任务状态机（Task State Machine）

现在的 `Queued → Running → Completed` 不够。

**完整状态**：
```
Created → Queued → Preparing → Running → Completed
                                      ↓
                                   Paused
                                      ↓
                                   Retrying
                                      ↓
                                   Failed
                                      ↓
                                Cancelled
```

**状态流转图**：
```
Created
  ↓
Queued
  ↓
Preparing
  ↓
Running ──→ Completed
  │  ↑         ↑
  │  │         │
  ├──Paused────┘
  │
  └──Retrying ──→ Failed
                    ↓
               Cancelled
```

```typescript
type TaskState = 
  | 'created'
  | 'queued'
  | 'preparing'
  | 'running'
  | 'paused'
  | 'retrying'
  | 'failed'
  | 'cancelled'
  | 'completed';
```

**意义**：暂停任务、断点恢复、优先级抢占全靠它。否则未来出现"任务到底卡哪了？没人知道"。

**实现**：每个 Task 带 `stateHistory: { from, to, timestamp, reason }[]`，完整可追溯。

---

#### 9. 任务 DAG 真正落地——并行执行策略

前面只有图结构，现在补执行策略。

**结构**：
```
分析项目
  ├── 扫描文件
  ├── 读取依赖
  └── 分析结构
      ↓
    重构（依赖以上全部完成）
```

**调度逻辑**：
```typescript
class DAGScheduler {
  readyTasks(): Task[] {
    return this.tasks.filter(t =>
      t.state === 'queued' &&
      t.dependencies.every(depId =>
        this.tasks.get(depId)?.state === 'completed'
      )
    );
  }
}
```

**效果**：4 核 CPU → 4 个无依赖任务同时跑。速度差距很大。

**执行**：
```
DAG Scheduler
  ↓
readyTasks() → 返回所有依赖已满足的任务
  ↓
Scheduler 分发到 Worker Pool
  ↓
多任务并行执行
```

---

#### 10. Governor（资源总管）

否则插件要内存、AI 要显存、数据库要线程——全抢。

**监控范围**：
```
CPU
Memory
GPU
Threads
Network
Disk I/O
```

**申请流程**：
```
Task 启动
  ↓
申请资源（CPU 2核 / 内存 512MB）
  ↓
Governor
  ↓
  资源足够 → 批准，扣除配额
  资源不足 → 排队等待
  ↓
Task 完成 → 释放资源
```

```typescript
interface Governor {
  resources: {
    cpu: { total: number; used: number; available: number };
    memory: { total: number; used: number; available: number };
    gpu: { total: number; used: number; available: number };
  };

  acquire(request: ResourceRequest): Promise<boolean>;
  release(resourceId: string): void;
  status(): ResourceStatus;
}
```

**像操作系统里的调度器**。

---

#### 11. Priority + Aging（防饿死）

高优先级任务一直插队，低优先级永远执行不到。

**公式**：
```
score = priority + waitingTime × agingFactor
```

```typescript
class PriorityQueue {
  private tasks: ScheduledTask[] = [];

  next(): ScheduledTask {
    const now = Date.now();
    // 每等待 1 秒，score 增加 agingFactor
    return this.tasks.sort((a, b) => {
      const aScore = a.priority + (now - a.enqueuedAt) * 0.001;
      const bScore = b.priority + (now - b.enqueuedAt) * 0.001;
      return bScore - aScore;
    })[0];
  }
}
```

**效果**：等待越久权重越高，确保所有任务最终都能执行到。

---

#### 12. 快照（Snapshot）——断点恢复

任务执行到 80% 突然崩了/断电/升级。

**保存**：
```json
{
  "taskId": "task_123",
  "step": "build",
  "progress": 80,
  "state": { /* 中间状态 */ },
  "timestamp": 1716000000000
}
```

**目录**：`storage/snapshot/tasks/`

**启动恢复**：
```
Runtime 启动
  ↓
扫描未完成的任务快照
  ↓
  有 progress → 断点续传
  无快照 → 重新执行
```

**否则大型任务重新来会很痛**。

---

#### 13. Event Sourcing（事件溯源）

不要直接改数据库。改写事件。

```typescript
// 不要：db.updateTaskState(taskId, 'running')
// 要：eventBus.emit('TaskStarted', { taskId })

// 示例事件流
events.jsonl:
  { "type": "IntentCreated", "payload": { ... } }
  { "type": "GoalCreated",   "payload": { ... } }
  { "type": "TaskCreated",   "payload": { ... } }
  { "type": "TaskStarted",   "payload": { ... } }
  { "type": "TaskCompleted", "payload": { ... } }
```

**好处**：
- 回放：重现任何时刻的状态
- 调试：精确看到每一步
- AI 分析：事件日志本身就是训练数据
- 恢复：从 eventlog 重建

---

#### 14. Projection 层（投影）

事件只是历史。UI 要的是**当前状态**。

```
EventLog（JSONL 真相源）
  ↓
Projection（事件 → 当前状态）
  ↓
SQLite / SurrealDB（查询层）
  ↓
UI
```

**原因**：别每次扫所有事件——会慢死。

```typescript
class Projection {
  private db: Database;

  apply(event: RuntimeEvent): void {
    switch (event.type) {
      case 'TaskCreated':
        this.db.insert('tasks', event.payload.task);
        break;
      case 'TaskStarted':
        this.db.update('tasks', event.payload.taskId, { state: 'running' });
        break;
      case 'TaskCompleted':
        this.db.update('tasks', event.payload.taskId, { state: 'completed' });
        break;
    }
  }
}
```

**原则**：Event Log = 真相源，Projection DB = 只读缓存。Projection 可随时从 Event Log 重建。

---

#### 15. 插件签名

以后插件市场一开，下载插件危险。

```
plugin-v1.0.zip
plugin-v1.0.sig（签名文件）
```

**验证流程**：
```
下载插件
  ↓
验证签名（公钥）
  ↓
  有效 → 允许安装
  无效 → 拒绝
```

**防止恶意插件混进去**。

---

#### 16. 沙箱文件系统

插件不要直接访问 `C:\` 或 `/root`。

**映射**：
```
插件 A 视角：
  / → workspace/plugins/pluginA/sandbox/

实际磁盘：
  workspace/plugins/pluginA/sandbox/
```

```typescript
class SandboxFS {
  private root: string;

  resolve(path: string): string {
    const real = path.join(this.root, path);
    // 防止跳出沙箱
    if (!real.startsWith(this.root)) {
      throw new Error('Path escape detected');
    }
    return real;
  }
}
```

**插件互相隔离**。

---

#### 17. AI 上下文总线（ContextBus）

Planner、Memory、Browser、CodeAnalyzer 都需要上下文。不要互相传对象。

```typescript
class ContextBus {
  private store: Map<string, ContextEntry> = new Map();

  set(key: string, value: unknown, ttl?: number): void {
    this.store.set(key, { value, timestamp: Date.now(), ttl });
  }

  get(key: string): unknown | null {
    const entry = this.store.get(key);
    if (!entry) return null;
    if (entry.ttl && Date.now() - entry.timestamp > entry.ttl) {
      this.store.delete(key);
      return null;
    }
    return entry.value;
  }

  // 上下文快照：保存当前所有上下文
  snapshot(): ContextSnapshot {
    return { entries: Array.from(this.store.entries()), timestamp: Date.now() };
  }
}
```

**使用**：
```
Planner → ContextBus.set('goal', '重构项目')
Memory  → ContextBus.get('goal')
Browser → ContextBus.get('currentTask')
```

**避免依赖地狱**。

---

#### 最后一层（未来再做）

```
✗ 远程 Worker
✗ 云插件
✗ WASM 插件
✗ 多机集群
✗ GPU 调度
```

**当前已覆盖**：Runtime 内核 + 插件系统 + 调度系统 + 恢复系统 + AI 执行系统。

---

### 插件清单总结

#### 一、插件分层体系

SoloForge 插件分为四层：核心层、能力层、场景层、工具层。

| 层级 | 插件数量 | 说明 |
|------|---------|------|
| 核心层 | 8 个 | 系统运行必须 |
| 能力层 | 8 个 | 差异化竞争力 |
| 场景层 | 8 个 | 按需启用 |
| 工具层 | 6 个 | 辅助功能 |
| **总计** | **30 个** | 覆盖完整开发生态 |

#### 二、核心层插件（必须内置）

| # | 插件名称 | Java 版本 | 核心功能 |
|---|---------|-----------|---------|
| 1 | **filesystem** | Java 21 | 文件读写、目录操作、路径解析 |
| 2 | **terminal** | Java 21 | 命令行执行（bash/cmd/powershell） |
| 3 | **process** | Java 21 | 进程管理、进程间通信 |
| 4 | **security** | Java 26 | 权限管理、安全策略执行 |
| 5 | **model-router** | Java 26 | 模型调度、Provider 管理 |
| 6 | **task-scheduler** | Java 26 | 任务队列、调度器、状态机 |
| 7 | **event-bus** | Java 21 | 事件发布/订阅、事件溯源 |
| 8 | **memory** | Java 21 | Working/Episodic/Semantic Memory |

#### 三、能力层插件（核心功能）

| # | 插件名称 | Java 版本 | 核心功能 | 推荐理由 |
|---|---------|-----------|---------|---------|
| 9 | **code-editor** | Java 21 | Monaco/编辑器集成 | 核心交互界面 |
| 10 | **preview** | Java 21 | 实时预览、隔离渲染 | 效果实时展示 |
| 11 | **browser** | Java 21 | WebView/浏览器控制 | 预览 HTML/React 等 |
| 12 | **git** | Java 21 | Git 操作、Diff、Merge | 开发者必备 |
| 13 | **debugger** | Java 21 | 代码调试、断点管理 | 代码质量保障 |
| 14 | **knowledge-base** | Java 21 | RAG 知识库、向量检索 | AI 能力增强 |
| 15 | **skill-registry** | Java 21 | 技能管理、SKILL.md 加载 | 插件化能力扩展 |
| 16 | **rule-engine** | Java 21 | 规则管理、执行引擎 | 安全与权限 |

#### 四、场景层插件（按需启用）

| # | 插件名称 | Java 版本 | 适用场景 |
|---|---------|-----------|---------|
| 17 | **node-manager** | Java 21 | Node.js 环境检测、版本管理 |
| 18 | **python-manager** | Java 21 | Python 环境、虚拟环境管理 |
| 19 | **java-manager** | Java 21 | JDK 检测、多版本切换 |
| 20 | **android-builder** | Java 26 | Gradle 构建、APK 打包 |
| 21 | **docker** | Java 21 | 容器管理、镜像操作 |
| 22 | **database** | Java 21 | 数据库连接、SQL 执行 |
| 23 | **http-client** | Java 21 | API 测试、HTTP 请求 |
| 24 | **websocket** | Java 21 | WebSocket 客户端/服务端 |

#### 五、工具层插件（辅助功能）

| # | 插件名称 | Java 版本 | 核心功能 |
|---|---------|-----------|---------|
| 25 | **pdf** | Java 17 | PDF 生成、解析 |
| 26 | **ocr** | Java 17 | 文字识别 |
| 27 | **image** | Java 17 | 图片处理、缩略图 |
| 28 | **archive** | Java 17 | 压缩/解压 ZIP/TAR/GZ |
| 29 | **downloader** | Java 17 | 下载管理（aria2 集成） |
| 30 | **notifier** | Java 17 | 系统通知、桌面提醒 |

#### 六、Java 版本分布

| Java 版本 | 插件数量 | 说明 |
|-----------|---------|------|
| **Java 26** | 3 个 | model-router、task-scheduler、security（核心调度，必须最新） |
| **Java 21** | 12 个 | 主流 LTS 版本，大部分插件 |
| **Java 17** | 5 个 | 工具类插件，不需要最新特性 |

#### 七、插件依赖关系图

```
┌─────────────────────────────────────────────────────────┐
│                    核心层（必须）                           │
│  filesystem ─ terminal ─ process ─ security ─ event-bus │
│       │                                         │          │
│       └────────── memory ─ model-router ─ task-scheduler ─┘
└─────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────┐
│                   能力层（核心差异）                        │
│  code-editor ─ preview ─ browser ─ git ─ debugger ──  │
│  knowledge-base ─ skill-registry ─ rule-engine          │
└─────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────┐
│                   场景层（按需启用）                        │
│  node-manager ─ python-manager ─ java-manager ─ android │
│  docker ─ database ─ http-client ─ websocket            │
└─────────────────────────────────────────────────────────┘
```

#### 八、第一批开发优先级

| 优先级 | 插件 | 理由 |
|--------|------|------|
| P0 | filesystem | 一切的基础 |
| P0 | terminal | 必须能执行命令 |
| P0 | event-bus | 事件驱动架构核心 |
| P0 | task-scheduler | 任务系统核心 |
| P1 | security | 安全边界必须早期建立 |
| P1 | memory | 记忆系统核心 |
| P1 | model-router | AI 能力核心 |
| P1 | code-editor | UI 交互核心 |
| P2 | git | 开发者必备 |
| P2 | preview | 效果展示核心 |
| P2 | browser | 预览能力 |
| P2 | knowledge-base | RAG 增强 |
| P2 | skill-registry | 技能系统 |
| P2 | debugger | 代码质量 |
| P3 | node/python/java-manager | 环境管理 |
| P3 | android-builder | 移动开发 |

---

## 四、数据库架构

### 最终架构：Dragonfly + SurrealDB + JSONL

```
Electron
    ↓
Runtime / EventBus / Orchestrator
    ↓
🔥 Dragonfly（热数据）
    ↓
🌡 SurrealDB（AI + 向量 + 温数据）
    ↓
❄ JSONL / FileSystem（事件真相层）
```

### 三层角色

| 层       | 技术        | 角色     |
| ------- | --------- | ------ |
| 🔥 热数据  | Dragonfly | 高并发运行层 |
| 🌡 温数据  | SurrealDB | AI理解层  |
| ❄ 事实与审计 | JSONL     | 事实与审计层 |

### Dragonfly（热数据层 — 替代Redis）

存储内容：TaskQueue、TaskState、SessionContext、EventStream、Lock、Counter、Cache、WebSocket状态

选型理由：支持Redis协议、真多线程、Stream、PubSub、Lua、大对象更抗压

### SurrealDB（温数据/AI层）

存储内容：长期记忆、知识库、Embedding、规则、技能、任务关系、MCP、会话摘要

选型理由：支持图 + 文档 + 向量 + 全文搜索 + Record关系 + AI Agent Memory。SurrealDB 3.0 针对AI Agent、记忆和向量能力做了大量增强

### JSONL / FileSystem（事件真相层）

存储内容：session.events.jsonl、session.audit.jsonl、session.log、session.error.log

### 数据流

```
Task
 ↓
Event
 ↓
session.events.jsonl（先写事件流，作为真相源）
 ↓
Dragonfly状态（同步热数据）
 ↓
SurrealDB索引（写入温数据/向量）
 ↓
UI
```

优势：崩溃恢复、事件回放、审计、Agent追踪、长期记忆

### 设计原则

- JSONL作为"真相源"（source of truth），所有事件先落JSONL
- Dragonfly作为运行态缓存和队列，不存持久数据
- SurrealDB负责AI需要理解和查询的结构化/向量数据
- UI只从Dragonfly和SurrealDB读取，JSONL用于回放和审计

***

### 4.1 SurrealDB 核心表结构

所有表遵循统一命名规范，禁止各模块随意定义字段。

#### 4.1.1 session（会话）

```typescript
{
  id: record<session>,
  title: string,
  createdAt: datetime,
  updatedAt: datetime,
  status: 'active' | 'archived' | 'deleted',
  workspacePath: string,
  currentRootTaskId: option<string>,
  metadata: {
    model: string,
    mode: string,
    tags: string[]
  }
}
```

#### 4.1.2 task（任务）

```typescript
{
  id: record<task>,
  sessionId: record<session>,
  parentTaskId?: record<task>,
  type: string,
  status: 'pending' | 'running' | 'waiting_confirm' | 'succeeded' | 'failed',
  retryCount: number,
  startedAt: datetime,
  finishedAt?: datetime,
  payload: object,
  result?: object
}
```

#### 4.1.3 event（事件）

```typescript
{
  id: record<event>,
  sessionId: record<session>,
  taskId: record<task>,
  type: string,
  timestamp: datetime,
  source: string,
  level: string,
  title: string,
  message?: string,
  payload?: object,
  jsonlOffset?: number
}
```

#### 4.1.4 memory（记忆）

```typescript
{
  id: record<memory>,
  content: string,
  version: number,
  previousVersion?: record<memory>,
  source: 'user' | 'extracted' | 'imported',
  createdAt: datetime,
  updatedAt: datetime,
  tags: string[]
}
```

#### 4.1.5 knowledge（知识库）

```typescript
{
  id: record<knowledge>,
  title: string,
  type: 'file' | 'text' | 'url',
  content?: string,
  filePath?: string,
  sourceUrl?: string,
  createdAt: datetime,
  updatedAt: datetime,
  metadata: object
}
```

#### 4.1.6 embedding（向量嵌入）

```typescript
{
  id: record<embedding>,
  sourceType: 'memory' | 'knowledge' | 'session' | 'file',
  sourceId: string,
  model: string,
  dimension: number,
  vector: array<number>,
  content: string,
  createdAt: datetime
}
```

支持模型切换：bge、jina、e5、qwen embedding 等

#### 4.1.7 model\_config（模型配置）

```typescript
{
  id: record<model_config>,
  name: string,
  provider: string,
  apiEndpoint: string,
  apiKey?: string,
  capabilities: string[],
  isLocal: boolean,
  createdAt: datetime,
  updatedAt: datetime
}
```

#### 4.1.8 skill（技能）

```typescript
{
  id: record<skill>,
  name: string,
  description: string,
  filePath: string,
  version: string,
  permissions: string[],
  installedAt: datetime,
  updatedAt: datetime
}
```

#### 4.1.9 rule（规则）

```typescript
{
  id: record<rule>,
  name: string,
  content: string,
  type: string,
  enabled: boolean,
  createdAt: datetime,
  updatedAt: datetime
}
```

#### 4.1.10 plugin（插件）

```typescript
{
  id: record<plugin>,
  name: string,
  version: string,
  description: string,
  permissions: string[],
  enabled: boolean,
  installedAt: datetime
}
```

#### 4.1.11 download（下载）

```typescript
{
  id: record<download>,
  url: string,
  fileName: string,
  filePath: string,
  status: 'downloading' | 'paused' | 'completed' | 'failed',
  progress: number,
  totalBytes: number,
  downloadedBytes: number,
  createdAt: datetime,
  completedAt?: datetime
}
```

#### 4.1.12 audit（审计）

```typescript
{
  id: record<audit>,
  sessionId: record<session>,
  taskId: record<task>,
  action: string,
  actor: string,
  target: object,
  decision: string,
  riskLevel: string,
  userConfirmed: boolean,
  timestamp: datetime,
  message: string
}
```

#### 4.1.13 workspace（工作区）

```typescript
{
  id: record<workspace>,
  name: string,
  path: string,
  description?: string,
  createdAt: datetime,
  updatedAt: datetime,
  settings: object
}
```

#### 4.1.14 channel（频道）

```typescript
{
  id: record<channel>,
  platform: string,
  name: string,
  config: object,
  enabled: boolean,
  authorized: boolean,
  createdAt: datetime
}
```

#### 4.1.15 environment（环境）

```typescript
{
  id: record<environment>,
  name: string,
  type: string,
  path: string,
  version?: string,
  status: 'installing' | 'ready' | 'error',
  createdAt: datetime,
  updatedAt: datetime
}
```

***

### 4.2 SurrealDB Record Relation 图关系

利用 SurrealDB 的图能力，定义 Record 之间的关联关系。

#### AI 关系图

```
session
 ├─contains→ task
 ├─use→ skill
 ├─remember→ memory
 └─reference→ knowledge

task
 ├─spawn→ task
 └─call→ model_config
```

#### 关系定义

**session → task（会话包含任务）**

```sql
RELATE session:{id}->contains->task:{id}
  CONTENT {
    relationType: 'contains',
    createdAt: time::now()
  };
```

**task → task（任务派生子任务）**

```sql
RELATE task:{id}->spawn->task:{id}
  CONTENT {
    relationType: 'spawn',
    parentStatus: 'running',
    createdAt: time::now()
  };
```

**session → skill（会话使用技能）**

```sql
RELATE session:{id}->use->skill:{id}
  CONTENT {
    relationType: 'use',
    activatedAt: time::now()
  };
```

**session → memory（会话记住记忆）**

```sql
RELATE session:{id}->remember->memory:{id}
  CONTENT {
    relationType: 'remember',
    context: 'session',
    createdAt: time::now()
  };
```

**session → knowledge（会话引用知识）**

```sql
RELATE session:{id}->reference->knowledge:{id}
  CONTENT {
    relationType: 'reference',
    referencedAt: time::now()
  };
```

**knowledge → embedding（知识拥有向量）**

```sql
RELATE knowledge:{id}->embedding->embedding:{id}
  CONTENT {
    relationType: 'vector',
    model: 'bge-m3',
    createdAt: time::now()
  };
```

***

### 4.3 Embedding 数据结构

```typescript
// table: embedding
{
  id: record<embedding>,

  // 来源类型：记忆/知识库/会话/文件
  sourceType: 'memory' | 'knowledge' | 'session' | 'file',

  // 来源记录ID
  sourceId: string,

  // 使用的向量模型名称
  model: string,

  // 向量维度
  dimension: number,

  // 向量数据
  vector: array<number>,

  // 原始文本内容（用于验证和重建）
  content: string,

  // 创建时间
  createdAt: datetime
}
```

**设计要点**：

- `model` 字段记录具体向量模型名，支持后续切换（bge / jina / e5 / qwen embedding 等）
- `dimension` 记录维度，不同模型维度不同（如 bge-m3=1024, e5=768）
- `content` 保留原文，用于向量模型切换时重建
- 通过 `sourceType + sourceId` 反向关联业务记录

***

### 4.4 Session 快照表（execution\_snapshot）

用于任务恢复、崩溃恢复、事件回放。

```typescript
// table: execution_snapshot
{
  id: record<execution_snapshot>,

  // 所属会话
  sessionId: record<session>,

  // 轮次ID
  turnId: string,

  // 根任务ID
  rootTaskId: string,

  // 当时的模型配置快照
  modelConfig: {
    primary: string,
    secondary: string[]
  },

  // 记忆引用列表
  memoryRefs: string[],

  // 工作区快照（打开文件、光标位置等）
  workspaceSnapshot: object,

  // 本轮有效权限
  permissions: string[],

  // 创建时间
  createdAt: datetime
}
```

触发时机：

- 每轮对话开始前创建
- 关键状态变更时更新
- 任务完成后标记

***

### 4.5 事件索引表（event\_index）

JSONL 是真相源，但直接回放几百万行会卡死。通过事件索引实现快速定位。

```typescript
// table: event_index
{
  id: record<event_index>,

  // 事件ID（与JSONL中一致）
  eventId: string,

  // 所属会话
  sessionId: record<session>,

  // 所属任务
  taskId: record<task>,

  // 事件类型
  type: string,

  // 事件时间
  timestamp: datetime,

  // 在JSONL文件中的偏移量（字节位置，用于快速跳转）
  jsonlOffset: number,

  // 事件级别（用于过滤）
  level?: string
}
```

**查询场景**：

- 某 task 的所有事件 → `SELECT * FROM event_index WHERE taskId = $taskId ORDER BY timestamp`
- 某 session 的错误事件 → `SELECT * FROM event_index WHERE sessionId = $sessionId AND level = 'error'`
- 某 trace 完整链路 → `SELECT * FROM event_index WHERE eventId IN $traceIds`

***

### 4.6 Memory 版本机制

防止 AI 改坏长期记忆后无法恢复。

```typescript
// table: memory
{
  id: record<memory>,

  // 记忆内容
  content: string,

  // 版本号（递增）
  version: number,

  // 上一版本引用（形成版本链）
  previousVersion?: record<memory>,

  // 来源
  source: 'user' | 'extracted' | 'imported',

  // 时间
  createdAt: datetime,
  updatedAt: datetime,

  // 标签
  tags: string[]
}
```

**版本链规则**：

- 每次修改时 version+1，previousVersion 指向上一个版本
- 查询默认返回最新版本（WHERE version = (SELECT max(version) FROM memory WHERE ...)）
- 支持回滚到指定版本
- 保留最近 N 个版本（N 可配置，默认 10）

***

### 4.7 模型调用历史表（model\_call）

用于 token 统计、成本统计、调优、模型对比。

```typescript
// table: model_call
{
  id: record<model_call>,

  // 所属任务
  taskId: record<task>,

  // 模型名称
  model: string,

  // 提供商
  provider: string,

  // Token 统计
  tokensIn: number,
  tokensOut: number,

  // 延迟（毫秒）
  latency: number,

  // 成本（美元，精确到小数点后6位）
  cost?: number,

  // 是否成功
  success: boolean,

  // 错误信息
  error?: string,

  // 工具调用次数
  toolCalls: number,

  // 调用时间
  createdAt: datetime
}
```

**索引建议**：

- `model + createdAt`：模型使用趋势
- `taskId`：按任务聚合
- `createdAt`：时间范围查询

***

### 4.8 向量分片策略

知识库体积增大后（100万+ embedding），需分片管理。

```typescript
// table: knowledge_partition
{
  id: record<knowledge_partition>,

  // 所属知识库
  knowledgeId: record<knowledge>,

  // 当前分片中的向量数量
  vectorCount: number,

  // 文件数量
  fileCount: number,

  // 数据大小（字节）
  size: number,

  // 分片序号
  shardIndex: number,

  // 创建时间
  createdAt: datetime
}
```

**分片规则**：

- 单分片超过 10 万 embedding 时自动创建新分片
- 查询时按 knowledgeId 聚合所有分片
- 支持按 shardIndex 并行检索
- 合并结果时按相似度排序取 Top-K

***

### 4.9 数据生命周期

定义各层数据的保留时间与清理策略，避免无限堆积。

```
事件
 ↓
🔥 Dragonfly（热数据）── 实时 ↔ 5分钟内活跃
 ↓
🌡 SurrealDB（温数据）── 5分钟 ↔ 30天
 ↓
❄ Archive（归档）    ── 30天 ↔ 90天
 ↓
🗑 删除              ── 超过90天
```

#### 各层策略

| 数据层                    | 保留时长        | 清理条件               | 清理动作       |
| ---------------------- | ----------- | ------------------ | ---------- |
| Dragonfly              | 任务结束后 + 5分钟 | 任务完成且5分钟内无访问       | 自动过期       |
| SurrealDB session      | 30天         | lastAccessed > 30天 | 标记archived |
| SurrealDB event\_index | 90天         | createdAt > 90天    | 删除索引       |
| SurrealDB memory       | 永久（含版本链）    | 用户手动删除             | 软删除        |
| SurrealDB knowledge    | 永久          | 用户手动删除             | 硬删除        |
| JSONL 事件日志             | 90天         | createdAt > 90天    | 压缩归档       |
| JSONL 审计日志             | 365天        | createdAt > 365天   | 压缩归档       |
| session.log            | 30天         | createdAt > 30天    | 自动清理       |
| session.error.log      | 90天         | createdAt > 90天    | 自动清理       |
| 预览缓存                   | 会话结束        | 会话关闭               | 立即清理       |
| 下载文件                   | 永久          | 用户手动删除             | 硬删除        |

#### 回温策略

- 用户打开历史会话 → 从 SurrealDB 加载到 Dragonfly
- 搜索命中归档数据 → 从 Archive 解压加载到 SurrealDB
- 模型引用旧记忆 → 从版本链加载最新版本

***

### 4.10 数据一致性策略

写入三条链路（JSONL → Dragonfly → SurrealDB）必须有明确的失败处理。

```typescript
// 写入结果记录
interface PersistenceResult {
  jsonl: boolean,    // JSONL 写入结果
  dragonfly: boolean, // Dragonfly 写入结果
  surreal: boolean    // SurrealDB 写入结果
}
```

#### 分级策略

| 场景                                     | 动作                 | 后果                     |
| -------------------------------------- | ------------------ | ---------------------- |
| JSONL 成功 + Dragonfly 成功 + SurrealDB 成功 | ✅ 正常返回             | 全部一致                   |
| JSONL 成功 + Dragonfly 失败                | ⏳ 写入补偿队列，下次事件触发时重试 | Dragonfly 短暂不一致，可容忍    |
| JSONL 成功 + SurrealDB 失败                | 🔄 标记索引待重建，后台定时重建  | SurrealDB 索引缺失，查询可能不完整 |
| JSONL 失败                               | ❌ 整体失败，不执行后续写入     | 原子性保证，不产生脏数据           |

#### 补偿队列（Dragonfly 补偿）

```
补偿队列（存储在 Dragonfly 中）
  ↓
定时扫描未同步项（每5秒）
  ↓
重试写入 Dragonfly
  ↓
成功后移除补偿项
  ↓
超过最大重试次数（10次）→ 写入 error 日志，标记为失败
```

#### SurrealDB 后台重建

```
检测到 SurrealDB 索引缺失
  ↓
从 JSONL 回放相关事件
  ↓
重建 SurrealDB 记录
  ↓
验证一致性
  ↓
清除重建标记
```

#### 核心原则

- **JSONL 是唯一真相源**：任何情况下 JSONL 写失败 = 整个操作失败
- **Dragonfly 允许最终一致**：短暂不一致不影响系统正确性
- **SurrealDB 可重建**：基于 JSONL 回放可完全重建 SurrealDB 数据
- **禁止出现**：UI 显示成功但数据库无数据的场景

***

### 4.11 统一 ID 规范

所有 ID 使用统一生成规则，禁止各模块各自为政。

#### ID 生成策略

| ID 类型     | 前缀       | 格式                           |
| --------- | -------- | ---------------------------- |
| sessionId | `sess_`  | sess\_{timestamp}\_{random}  |
| taskId    | `task_`  | task\_{timestamp}\_{random}  |
| turnId    | `turn_`  | turn\_{timestamp}\_{random}  |
| traceId   | `trace_` | trace\_{timestamp}\_{random} |
| eventId   | `evt_`   | evt\_{timestamp}\_{random}   |

**示例**：

- `sess_20260522_abcd123`
- `task_20260522_qwe456`
- `trace_20260522_xkz789`

**推荐采用 ULID**（优于 UUID 的理由）：

- 时间有序 → SurrealDB 索引友好
- 可排序 → 日志回放按 ID 即可排序，无需额外时间字段
- 查询性能好 → 范围查询、分页、跳转效率高
- 三端统一（JSONL / Dragonfly / SurrealDB）不出格式冲突

**实现**：

```typescript
interface IdStrategy {
  generate(type: 'session' | 'task' | 'turn' | 'trace' | 'event'): string
  // 返回格式: prefix + timestamp + random
  // 或使用 ULID 库直接生成
}
```

***

### 4.12 SurrealDB 索引策略

所有表必须定义索引，否则几十万条后查询性能崩溃。

#### 标准索引

```sql
-- Session 表：按更新时间查询
DEFINE INDEX idx_session_updated ON session FIELDS updatedAt;

-- Task 表：按会话 + 状态联合查询（最常用）
DEFINE INDEX idx_task_session_status ON task FIELDS sessionId, status;

-- 事件索引表：按任务 + 时间查询
DEFINE INDEX idx_event_task_time ON event_index FIELDS taskId, timestamp;

-- 事件索引表：按会话 + 级别查询（错误排查）
DEFINE INDEX idx_event_session_level ON event_index FIELDS sessionId, level;

-- 记忆表：按标签查询
DEFINE INDEX idx_memory_tags ON memory FIELDS tags;

-- 模型调用历史：按模型名查询
DEFINE INDEX idx_model_call_model ON model_call FIELDS model;

-- 模型调用历史：按时间范围查询
DEFINE INDEX idx_model_call_created ON model_call FIELDS createdAt;

-- 工作区文件：按会话 + 扩展名查询
DEFINE INDEX idx_workspace_file_session ON workspace_file FIELDS sessionId, extension;

-- 权限缓存：按过期时间清理
DEFINE INDEX idx_permission_cache_expire ON permission_cache FIELDS expireAt;

-- 审计记录：按时间查询
DEFINE INDEX idx_audit_record_time ON audit_record FIELDS timestamp;

-- 下载任务：按状态查询
DEFINE INDEX idx_download_status ON download_task FIELDS status;
```

#### 向量索引

```sql
-- Embedding 向量索引（HNSW 算法）
DEFINE INDEX hnsw_embedding ON embedding FIELDS vector
  HNSW DIMENSION 1024
  DISTANCE COSINE;

-- 分区向量索引：按 sourceType 过滤后检索
DEFINE INDEX hnsw_embedding_memory ON embedding FIELDS vector
  HNSW DIMENSION 1024
  DISTANCE COSINE
  WHERE sourceType = 'memory';
```

#### 索引设计原则

- 所有外键字段（sessionId、taskId 等）必须建索引
- 所有 WHERE 查询条件字段必须建索引
- 所有 ORDER BY / SORT 字段必须建索引
- 向量索引使用 HNSW，距离算法统一使用 COSINE
- 索引数量控制在每表 5 个以内，避免写入放大

***

### 4.13 Session 归档表

会话超过 30 天未访问时归档到此表。

```typescript
// table: session_archive
{
  // 原会话 ID
  sessionId: string,

  // 是否压缩
  compressed: boolean,

  // 归档时间
  archivedAt: datetime,

  // 归档前原始路径
  originalPath: string,

  // 归档文件大小
  size: number,

  // 归档存储路径
  archivePath: string,

  // 预计删除时间
  expiresAt: datetime
}
```

**归档目录结构**：

```
archive/
  2026/
    05/
      session_sess_20260522_abcd123.zip
      session_sess_20260522_qwe456.zip
    06/
      ...
```

**归档内容**（zip）：

- session.events.jsonl
- session.audit.jsonl
- session.log
- session.error.log
- 会话相关 SurrealDB 数据导出

***

### 4.14 Task DAG（任务依赖表）

任务之间不只有父子层级，还有多依赖关系（DAG）。

```typescript
// table: task_dependency
{
  // 当前任务
  taskId: record<task>,

  // 依赖的任务
  dependsOnTaskId: record<task>,

  // 依赖类型
  dependencyType: 'hard' | 'soft',

  // 创建时间
  createdAt: datetime
}
```

**依赖类型说明**：

- `hard`：前置任务必须成功，当前任务才能执行（如知识搜索 → 模型推理）
- `soft`：前置任务建议完成，但当前任务可以先行（如副模型协作，结果合并前等待即可）

**典型 DAG 示例**：

```
根任务
 ├─hard→ 知识搜索
 ├─hard→ 工具执行
 ├─soft→ 副模型1
 ├─soft→ 副模型2
 └─soft→ 副模型3
       ↓
   结果合并（依赖以上所有任务完成）
```

***

### 4.15 Task 输出快照表

任务完成后，结果必须有结构化的存储位置。

```typescript
// table: task_result
{
  // 任务 ID
  taskId: record<task>,

  // 执行摘要（用户可读）
  summary: string,

  // 产物列表
  artifacts: [
    {
      type: 'file' | 'image' | 'patch' | 'log',
      path: string,
      description?: string
    }
  ],

  // 模型原始输出
  modelOutput: string,

  // 调用统计
  stats?: {
    tokensIn: number,
    tokensOut: number,
    latency: number,
    toolCalls: number
  },

  // 创建时间
  createdAt: datetime
}
```

**设计要点**：

- `summary` 由执行摘要器生成，供用户可读
- `artifacts` 记录所有产物的路径
- `modelOutput` 保留模型原始输出，用于排查
- 无需从 JSONL 回放扒结果

***

### 4.16 Workspace 文件索引表

代码区资源管理器、Monaco 编辑器、文件树搜索依赖此索引。

```typescript
// table: workspace_file
{
  // 所属会话
  sessionId: record<session>,

  // 完整路径
  path: string,

  // 文件名
  filename: string,

  // 扩展名
  extension: string,

  // 文件大小
  size: number,

  // 文件哈希（用于变化检测）
  hash: string,

  // 修改时间
  modifiedAt: datetime,

  // 语言类型（根据扩展名映射）
  language: string,

  // 是否已索引
  indexed: boolean
}
```

**分工**：

- **Rust** 负责扫描文件系统、计算 hash、检测变化
- **SurrealDB** 负责存储索引、提供查询
- **前端** 通过 SurrealDB 查询获取文件列表

***

### 4.17 文件 Hash 表

用于增量扫描、重复文件检测、知识库变化检测、缓存命中。

```typescript
// table: file_hash
{
  // 文件路径
  path: string,

  // SHA256 哈希
  sha256: string,

  // 更新时间
  updatedAt: datetime
}
```

**应用场景**：

- 增量扫描：hash 没变跳过，避免 chokidar 疯狂扫描
- 重复文件检测：知识库添加文件时去重
- 文件变化检测：工作区文件变更时触发对应操作
- 缓存命中：内容不变直接复用缓存结果

***

### 4.18 模型配置拆分表

模型配置拆为三张表，避免所有配置混在一起。

```typescript
// table: model_provider
{
  id: record<model_provider>,
  name: string,
  endpoint: string,
  apiType: string,
  isLocal: boolean,
  createdAt: datetime
}

// table: model_instance
{
  id: record<model_instance>,
  providerId: record<model_provider>,
  modelName: string,
  displayName?: string,
  apiKey?: string,
  config?: object,
  enabled: boolean,
  createdAt: datetime,
  updatedAt: datetime
}

// table: model_capability
{
  id: record<model_capability>,
  instanceId: record<model_instance>,
  reasoning: boolean,
  tool: boolean,
  image: boolean,
  voice: boolean,
  embedding: boolean,
  maxTokens: number,
  createdAt: datetime
}
```

**支持的服务商**：OpenAI、Claude、Gemini、Qwen、DeepSeek、LMStudio、Ollama 等

***

### 4.19 权限缓存表

安全引擎频繁查询"是否允许"，避免用户被连续弹框。

```typescript
// table: permission_cache
{
  // 执行者（模型、插件等）
  actor: string,

  // 目标路径/操作
  target: string,

  // 决策结果
  decision: 'allow' | 'allow_with_warning' | 'deny',

  // 过期时间
  expireAt: datetime
}
```

**缓存范围**：

| 范围    | 有效期  | 说明                  |
| ----- | ---- | ------------------- |
| 本次动作  | 一次性  | 仅本次                 |
| 本轮任务  | 任务结束 | task 范围             |
| 当前会话  | 会话结束 | session 范围          |
| 当前工作区 | 永久   | workspace 范围（仅低中风险） |

**原则**：

- destructive 操作禁止永久缓存
- 系统敏感目录禁止缓存
- 用户可随时清除所有权限缓存

***

### 4.20 审计实体表

JSONL 是审计真相源，但查询审计需要 SurrealDB 索引。

```typescript
// table: audit_record
{
  id: record<audit_record>,
  taskId: record<task>,
  action: string,
  actor: string,
  decision: 'allow' | 'allow_with_warning' | 'require_user_confirm' | 'deny',
  target: object,
  risk: 'low' | 'medium' | 'high' | 'critical',
  reasonCodes: string[],
  userConfirmed?: boolean,
  sessionId: record<session>,
  traceId: string,
  timestamp: datetime
}
```

**查询场景**：

```sql
-- 最近7天所有高危命令
SELECT * FROM audit_record
WHERE risk = 'high'
  AND timestamp > time::now() - 7d
  AND action LIKE 'command%'
ORDER BY timestamp DESC;

-- 某用户的所有确认/拒绝动作
SELECT * FROM audit_record
WHERE actor = 'user'
  AND decision IN ['require_user_confirm', 'deny'];
```

***

### 4.21 MCP 工具注册表

MCP 工具必须有数据库记录，否则工具越来越多管理困难。

```typescript
// table: mcp_registry
{
  id: record<mcp_registry>,
  name: string,
  description: string,
  endpoint: string,
  authType: 'none' | 'api_key' | 'oauth' | 'token',
  permissions: string[],
  status: 'active' | 'inactive' | 'error',
  version: string,
  installedAt: datetime,
  updatedAt: datetime
}

// table: mcp_call_history
{
  id: record<mcp_call_history>,
  mcpId: record<mcp_registry>,
  taskId: record<task>,
  request: object,
  response?: object,
  status: 'success' | 'failed',
  latency: number,
  createdAt: datetime
}
```

***

### 4.22 下载任务表

aria2 管理传输，数据库管理状态。

```typescript
// table: download_task
{
  id: record<download_task>,
  url: string,
  fileName: string,
  savePath: string,
  progress: number,
  speed: number,
  totalBytes: number,
  downloadedBytes: number,
  status: 'downloading' | 'paused' | 'completed' | 'failed' | 'cancelled',
  retries: number,
  maxRetries: number,
  error?: string,
  createdAt: datetime,
  completedAt?: datetime
}
```

***

### 4.23 插件元数据表

插件沙箱和安全系统依赖此表验证。

```typescript
// table: plugin
{
  id: record<plugin>,
  name: string,
  version: string,
  description: string,
  permissions: string[],
  checksum: string,         // 完整性校验
  source: string,           // 来源（marketplace / local / url）
  installPath: string,
  installTime: datetime,
  enabled: boolean,
  lastUpdated: datetime
}
```

**checksum 作用**：

- 安装时校验文件完整性
- 运行时检测是否被篡改
- 更新时判断是否需要重新安装

***

### 4.24 数据完整性校验表

用于导出导入、崩溃恢复、迁移时的数据验证。

```typescript
// table: data_checksum
{
  // 表名
  tableName: string,

  // 记录 ID
  recordId: string,

  // SHA256 校验和
  checksum: string,

  // 更新时间
  updatedAt: datetime
}
```

**校验策略**：

| 场景    | 动作                              |
| ----- | ------------------------------- |
| 写入时   | 计算 checksum，写入 data\_checksum 表 |
| 导出时   | 校验所有记录 checksum                 |
| 导入时   | 重新计算并与源 checksum 比对             |
| 崩溃恢复时 | 扫描 data\_checksum 表，发现不一致的记录    |
| 定时巡检  | 后台任务每天扫描 1% 记录随机校验              |

***

### 4.25 多数据库事务与最终一致性（Outbox 模式）

JSONL → Dragonfly → SurrealDB 是三个存储系统，没有跨系统事务。

**问题场景**：Task 成功 → Dragonfly 成功 → SurrealDB 失败 → UI 显示成功但历史记录没有

#### Outbox 事件表

```typescript
// table: outbox_event
{
  id: record<outbox_event>,

  // 事件类型
  event: string,

  // 目标存储
  target: 'dragonfly' | 'surreal_db',

  // 完整负载（JSON）
  payload: string,

  // 重试次数
  retryCount: number,

  // 最大重试次数
  maxRetries: number,

  // 状态
  status: 'pending' | 'processing' | 'completed' | 'failed',

  // 错误信息
  lastError?: string,

  // 创建时间
  createdAt: datetime,

  // 最后重试时间
  lastRetryAt?: datetime
}
```

#### 写入流程

```
主操作
  ↓
写入 JSONL（真相源，必须成功）
  ↓
主操作标记完成
  ↓
写入 Outbox 表（Dragonfly 任务、SurrealDB 任务各一条记录）
  ↓
返回 UI 成功
```

#### 消费流程

```
调度器定时轮询 Outbox 表（每 1 秒）
  ↓
取出 status=pending 的记录
  ↓
标记 processing（防止重复消费）
  ↓
写入 Dragonfly 或 SurrealDB
  ↓
成功 → 标记 completed
失败 → retryCount++，写 lastError
  ↓
超过 maxRetries → 标记 failed，发告警
```

#### 补偿队列

```
补偿队列（Dragonfly 中存储）
  ↓
扫描 outbox_event WHERE status='failed'
  ↓
每 30 秒批量重试
  ↓
成功 → 标记 completed
  ↓
持续失败 → 写入错误日志，通知用户
```

#### 最终一致性保证

| 写入路径      | 保证级别 | 说明                                |
| --------- | ---- | --------------------------------- |
| JSONL     | 强一致  | 同步写入，失败则整体回滚                      |
| Dragonfly | 最终一致 | 通过 Outbox + 补偿队列达到最终一致            |
| SurrealDB | 最终一致 | 通过 Outbox + 后台重建达到最终一致            |
| UI 查询     | 最终一致 | 可能读到短暂不一致的 Dragonfly/SurrealDB 数据 |

***

### 4.26 数据库命名规范

所有表名、字段名必须统一，禁止各模块自定义命名风格。

#### 表名规范

- **全小写 + 下划线**，禁止驼峰/大小写混用
- 禁止：`knowledgeBase`、`Knowledge`、`knowledge_base`（虽然这个可以，但统一用 knowledge\_chunk 风格）
- 统一表名清单：

| 正确                | 错误                                |
| ----------------- | --------------------------------- |
| `session`         | `Session`、`sessions`              |
| `task`            | `Task`、`tasks`                    |
| `task_result`     | `taskResult`、`TaskResult`         |
| `task_dependency` | `taskDependency`                  |
| `model_call`      | `modelCall`、`ModelCall`           |
| `knowledge`       | `Knowledge`                       |
| `knowledge_chunk` | `knowledgeChunk`、`KnowledgeChunk` |
| `embedding`       | `Embedding`                       |
| `memory`          | `Memory`                          |
| `plugin`          | `Plugin`                          |
| `download_task`   | `downloadTask`                    |
| `audit_record`    | `auditRecord`                     |
| `workspace_file`  | `workspaceFile`                   |
| `prompt_template` | `promptTemplate`                  |

#### 字段名规范

- **时间字段统一**：`createdAt`、`updatedAt`、`deletedAt`
- 禁止：`createTime`、`created_time`、`time`、`updateAt`
- **用户字段统一**：`createdBy`、`updatedBy`
- **ID 字段统一**：`id`（所有表主键统一为 `id`）

***

### 4.27 全局 BaseEntity

所有 SurrealDB 表继承此基础结构，避免每个表重复定义公共字段。

```typescript
// BaseEntity — 所有表的基础结构
interface BaseEntity {
  // 主键
  id: string,

  // 创建时间（Unix 毫秒时间戳）
  createdAt: number,

  // 更新时间
  updatedAt: number,

  // 乐观锁版本号（递增，用于并发控制）
  version: number,

  // 删除时间（软删除标记）
  deletedAt?: number,

  // 创建者
  createdBy?: string,

  // 最后更新者
  updatedBy?: string
}
```

**示例继承**：

```typescript
// session extends BaseEntity
{
  id: record<session>,
  createdAt: number,
  updatedAt: number,
  version: number,
  deletedAt?: number,
  createdBy?: string,
  updatedBy?: string,
  title: string,
  status: 'active' | 'archived' | 'deleted',
  workspacePath: string,
  // ...其他业务字段
}
```

**不继承 BaseEntity 的表**：

- `event_index`（纯查询加速，不关心时间）
- `data_checksum`（纯校验）
- `permission_cache`（临时缓存，过期即删）

***

### 4.28 软删除机制

AI 系统误删概率高，所有核心数据必须支持软删除。

#### 软删除表

所有核心表增加以下字段：

```typescript
{
  deletedAt?: number,   // 删除时间戳
  isDeleted?: boolean   // 是否已删除
}
```

#### 适用范围

| 表               | 软删除 | 回收站停留  | 永久删除   |
| --------------- | --- | ------ | ------ |
| session         | ✅   | 7天     | 到期自动清理 |
| task            | ✅   | 7天     | 到期自动清理 |
| memory          | ✅   | 30天    | 到期自动清理 |
| knowledge       | ✅   | 7天     | 到期自动清理 |
| skill           | ✅   | 7天     | 到期自动清理 |
| rule            | ✅   | 7天     | 到期自动清理 |
| plugin          | ✅   | 7天     | 到期自动清理 |
| workspace\_file | ✅   | 不经过回收站 | 立即软删除  |
| 临时数据            | ❌   | 不经过回收站 | 直接硬删除  |

#### 查询规则

```sql
-- 所有查询默认过滤已删除记录
SELECT * FROM session WHERE isDeleted != true OR isDeleted IS NONE;

-- 管理员可查看已删除
SELECT * FROM session WHERE isDeleted = true;

-- 自动清理过期软删除记录（定时任务，每天执行一次）
DELETE FROM session WHERE deletedAt < time::now() - 7d AND isDeleted = true;
```

***

### 4.29 回收站表

用户删除的数据先进入回收站，可恢复。

```typescript
// table: trash
{
  id: record<trash>,

  // 实体类型
  entityType: 'session' | 'task' | 'memory' | 'knowledge' | 'skill' | 'rule' | 'plugin',

  // 被删除实体的 ID
  entityId: string,

  // 原位置路径（用于恢复）
  originalPath?: string,

  // 被删除时的元数据快照
  metadata?: object,

  // 删除时间
  deletedAt: datetime,

  // 过期时间（超过此时间自动永久删除）
  expireAt: datetime
}
```

**操作流程**：

```
用户删除
  ↓
标记软删除（isDeleted=true, deletedAt=now）
  ↓
写入回收站（trash 表）
  ↓
7天内可恢复
  ↓
到期自动永久删除
```

**恢复流程**：

```
用户从回收站选择恢复
  ↓
查询 trash 记录
  ↓
清除实体 deletedAt/isDeleted 标记
  ↓
从 trash 表移除记录
```

***

### 4.30 知识库 Chunk 表

知识库文件必须切块后再做 embedding，否则 RAG 检索太粗。

```typescript
// table: knowledge_chunk
{
  id: record<knowledge_chunk>,

  // 所属知识库记录
  knowledgeId: record<knowledge>,

  // 分块序号（用于排序还原）
  chunkIndex: number,

  // 分块文本内容
  content: string,

  // Token 数量
  tokenCount: number,

  // 在源文件中的起始偏移
  startOffset: number,

  // 在源文件中的结束偏移
  endOffset: number,

  // 元数据（来源页码、标题等）
  metadata: object,

  // 版本号（用于 Chunk 版本管理）
  version: number,

  // 创建时间
  createdAt: datetime
}
```

**数据链路**：

```
knowledge
  ↓
knowledge_chunk（分块）
  ↓
embedding（向量化）
```

**分块策略**：

- 默认每块 512 tokens（可配置）
- 块间重叠 64 tokens（保持上下文连贯）
- 支持按段落/标题/代码函数边界分块

***

### 4.31 Chunk 版本管理

知识库文件修改后，旧 chunk 和旧 embedding 必须清理。

```typescript
// table: knowledge_chunk_version
{
  id: record<knowledge_chunk_version>,

  // 所属知识库
  knowledgeId: record<knowledge>,

  // 版本号
  version: number,

  // 总 chunk 数量
  chunkCount: number,

  // 总 token 数
  totalTokens: number,

  // 状态
  status: 'active' | 'deprecated',

  // 创建时间
  createdAt: datetime
}
```

**版本更新流程**：

```
知识库文件被修改
  ↓
旧版本标记为 deprecated
  ↓
创建新版本（version+1）
  ↓
重新切 chunk（新版本ID）
  ↓
重新生成 embedding（新版本ID关联）
  ↓
清理旧 chunk 和旧 embedding（后台延迟清理）
```

***

### 4.32 Embedding 任务队列

大文件导入时不能直接阻塞 UI，必须通过异步任务队列。

```typescript
// table: embedding_job
{
  id: record<embedding_job>,

  // 来源类型
  sourceType: 'knowledge' | 'memory' | 'session',

  // 来源 ID
  sourceId: string,

  // 状态
  status: 'pending' | 'processing' | 'completed' | 'failed',

  // 重试次数
  retryCount: number,

  // 使用的向量模型
  model: string,

  // 进度
  progress: number,

  // 总任务项数
  total: number,

  // 已完成项数
  completed: number,

  // 错误信息
  error?: string,

  // 创建时间
  createdAt: datetime,

  // 完成时间
  completedAt?: datetime
}
```

**处理流程**：

```
上传知识库文件
  ↓
切 chunk
  ↓
创建 embedding_job（status=pending）
  ↓
后台 Worker 轮询
  ↓
逐个生成 embedding
  ↓
写入 SurrealDB
  ↓
标记 completed
  ↓
UI 通过 WebSocket 实时接收进度
```

**并发控制**：

- 默认最大 2 个 embedding 任务并行
- 大文件（>1000 chunk）自动降级为单线程
- 失败任务自动重试 3 次，超过后标记 failed

***

### 4.33 Prompt 模板表

所有系统 Prompt 集中管理，禁止散落各处。

```typescript
// table: prompt_template
{
  id: record<prompt_template>,

  // 模板名称
  name: string,

  // 描述
  description: string,

  // Prompt 内容（支持变量占位符 {{变量名}}）
  content: string,

  // 版本号
  version: number,

  // 标签（用于分类和搜索）
  tags: string[],

  // 是否启用
  enabled: boolean,

  // 创建时间
  createdAt: datetime,

  // 更新时间
  updatedAt: datetime
}
```

**预设模板清单**：

| 名称                      | 用途           |
| ----------------------- | ------------ |
| `system.planning`       | 任务规划 Prompt  |
| `system.tool_call`      | 工具调用 Prompt  |
| `system.summary`        | 对话摘要 Prompt  |
| `system.code_gen`       | 代码生成 Prompt  |
| `system.review`         | 代码审查 Prompt  |
| `system.debug`          | 调试分析 Prompt  |
| `system.memory_extract` | 记忆提取 Prompt  |
| `system.rag_retrieval`  | 知识库检索 Prompt |
| `user.custom_*`         | 用户自定义模板      |

***

### 4.34 Prompt 版本历史

AI 系统的 Prompt 一定会反复调整，必须有版本管理。

```typescript
// table: prompt_version
{
  id: record<prompt_version>,

  // 所属模板
  templateId: record<prompt_template>,

  // 版本号
  version: number,

  // 当时的 Prompt 内容
  content: string,

  // 变更说明
  changeLog?: string,

  // 创建时间
  createdAt: datetime
}
```

**版本规则**：

- 每次修改 prompt\_template 时自动创建 prompt\_version
- 保留全部历史版本（不删除）
- 支持回滚到任意历史版本
- 上线前可对比两个版本的差异

***

### 4.35 用户行为表

本地产品也需要数据驱动优化。

```typescript
// table: user_behavior
{
  id: record<user_behavior>,

  // 行为类型
  action: string,

  // 所在页面/模块
  page: string,

  // 停留时长（毫秒）
  duration: number,

  // 附加数据
  metadata?: object,

  // 发生时间
  timestamp: datetime
}
```

**分析场景**：

- 最常用模型排名 → 优化默认模型选择
- 最常用技能排名 → 优化技能推荐
- 热点页面/区域 → 优化 UI 布局
- 冷门功能 → 考虑引导或隐藏
- 高频报错页面 → 优先修复

***

### 4.36 Feature Flag 表

实验功能、灰度发布、A/B 测试通过 Feature Flag 控制。

```typescript
// table: feature_flag
{
  id: record<feature_flag>,

  // 功能名称
  name: string,

  // 是否启用
  enabled: boolean,

  // 灰度比例（0-100）
  percentage: number,

  // 启用条件
  condition?: {
    platform?: string[],
    version?: string,
    userId?: string[]
  },

  // 描述
  description: string,

  // 创建时间
  createdAt: datetime,

  // 更新时间
  updatedAt: datetime
}
```

**典型 Flag 示例**：

| Flag 名称                 | 用途           |
| ----------------------- | ------------ |
| `ui.streaming_v2`       | 新流送 UI       |
| `scheduler.neo_routing` | 新模型路由        |
| `scheduler.v2`          | 新调度器         |
| `editor.monaco_v2`      | Monaco 编辑器升级 |
| `rag.hybrid_search`     | 混合搜索实验       |
| `agent.parallel_tool`   | 并行工具调用       |

***

### 4.37 配置中心拆分

配置按层级拆分，避免单个配置文件膨胀到几千行。

```typescript
// table: setting_user
{
  id: record<setting_user>,
  key: string,
  value: object,
  updatedAt: datetime
}

// table: setting_runtime
{
  id: record<setting_runtime>,
  key: string,
  value: object,
  updatedAt: datetime
}

// table: setting_system
{
  id: record<setting_system>,
  key: string,
  value: object,
  updatedAt: datetime
}

// table: setting_experimental
{
  id: record<setting_experimental>,
  key: string,
  value: object,
  featureFlag?: string,
  updatedAt: datetime
}
```

**层级说明**：

| 层级                    | 优先级 | 覆盖范围               | 示例          |
| --------------------- | --- | ------------------ | ----------- |
| setting\_user         | 最高  | 当前用户               | 主题、语言、编辑器配置 |
| setting\_runtime      | 中   | 当前运行实例             | 模型选择、工作区路径  |
| setting\_system       | 低   | 全局默认               | 日志级别、端口配置   |
| setting\_experimental | 实验  | 受 feature\_flag 控制 | 新功能开关       |

**合并规则**：user > runtime > system > 默认值

***

### 4.38 配置版本

配置升级时自动迁移，防止升级后配置不兼容。

```typescript
// table: setting_version
{
  id: record<setting_version>,

  // 配置层级
  level: 'user' | 'runtime' | 'system',

  // schema 版本号
  schemaVersion: number,

  // 配置数据版本
  dataVersion: number,

  // 更新时间
  updatedAt: datetime
}
```

**迁移策略**：

```
检测当前 schemaVersion
  ↓
与代码中预期版本比较
  ↓
落后 → 执行逐版本迁移（v1→v2→v3）
  ↓
更新 schemaVersion
  ↓
标记迁移完成
```

***

### 4.39 Workspace 快照

用于崩溃恢复、回滚、预览恢复。

```typescript
// table: workspace_snapshot
{
  id: record<workspace_snapshot>,

  // 所属会话
  sessionId: record<session>,

  // 工作区路径
  path: string,

  // 文件列表快照
  files: [
    {
      path: string,
      hash: string,
      size: number
    }
  ],

  // 整体哈希（用于快速判断是否变化）
  hash: string,

  // 创建时间
  createdAt: datetime
}
```

***

### 4.40 Patch 历史

AI 修改代码必须生成 diff，支持用户回滚。

```typescript
// table: file_patch
{
  id: record<file_patch>,

  // 所属任务
  taskId: record<task>,

  // 文件路径
  path: string,

  // diff 内容（unified format）
  diff: string,

  // 操作类型
  operation: 'create' | 'modify' | 'delete',

  // 原始文件快照（用于完全恢复）
  originalContent?: string,

  // 创建时间
  createdAt: datetime
}
```

**应用流程**：

```
AI 生成代码变更
  ↓
生成 diff
  ↓
写入 file_patch（但尚未应用到文件）
  ↓
用户确认（或自动模式直接应用）
  ↓
应用 patch 到文件系统
  ↓
标记 task_result.artifacts
```

***

### 4.41 Git 关联表

AI 需要感知当前 Git 上下文。

```typescript
// table: git_context
{
  id: record<git_context>,

  // 所属会话
  sessionId: record<session>,

  // 当前分支
  branch: string,

  // 当前 commit
  commit: string,

  // 变更文件列表
  changedFiles: [
    {
      path: string,
      status: 'modified' | 'added' | 'deleted' | 'untracked'
    }
  ],

  // 工作区是否干净
  isClean: boolean,

  // 创建时间
  createdAt: datetime
}
```

***

### 4.42 Schema 版本管理

数据库迁移必须有版本记录，否则迁移状态容易混乱。

```typescript
// table: schema_version
{
  id: record<schema_version>,

  // 版本号（递增）
  version: number,

  // 迁移描述
  description: string,

  // 迁移文件路径
  migrationFile: string,

  // 校验和
  checksum: string,

  // 是否成功
  success: boolean,

  // 执行时间
  appliedAt: datetime,

  // 执行耗时（毫秒）
  duration: number
}
```

**迁移规则**：

- 按版本号顺序执行（1 → 2 → 3 ...）
- 每个版本只执行一次
- 失败时记录错误，支持手动修复后重试
- 生产环境禁止跳过版本

***

### 4.43 Outbox + 死信队列

Outbox 写入失败的事件进入死信队列，防止静默丢失。

```typescript
// table: dead_letter_queue
{
  id: record<dead_letter_queue>,

  // 原始 outbox 事件
  event: string,

  // 目标存储
  target: 'dragonfly' | 'surreal_db',

  // 完整负载
  payload: string,

  // 失败原因
  reason: string,

  // 已重试次数
  retryCount: number,

  // 最后错误时间
  lastErrorAt: datetime,

  // 创建时间
  createdAt: datetime
}
```

**处理流程**：

```
Outbox 写入失败
  ↓
重试（指数退避，最多3次）
  ↓
全部失败 → 移入 dead_letter_queue
  ↓
告警通知
  ↓
人工介入恢复
  ↓
恢复成功 → 从 DLQ 移除
```

***

### 4.44 统一元数据字段

所有表的 `payload` / `metadata` 字段使用统一结构。

```typescript
// 统一 metadata 结构
interface UnifiedMetadata {
  source: string,       // 来源（system/user/model/plugin）
  traceId: string,      // 追踪 ID
  tags: string[],       // 标签
  extra?: object        // 扩展字段（尽量避免使用）
}
```

**禁止**：每表各自定义 `extra`、`data`、`payload`、`extend`。
**统一**：全部使用 `metadata: UnifiedMetadata`。

***

### 4.45 数据血缘

AI 系统的结论来源需要可追溯。

```typescript
// table: data_lineage
{
  id: record<data_lineage>,

  // 来源
  sourceId: string,
  sourceType: 'knowledge' | 'memory' | 'tool_result' | 'model_output',

  // 目标
  targetId: string,
  targetType: 'memory' | 'task_result' | 'session_summary',

  // 关系描述
  relation: 'derived_from' | 'references' | 'generated_by',

  // 权重（贡献度 0-1）
  weight?: number,

  // 创建时间
  createdAt: datetime
}
```

**典型链路**：

```
知识库文档
  ↓ derived_from
知识库 Chunk
  ↓ derived_from
Embedding
  ↓ references
RAG 检索结果
  ↓ generated_by
模型回答
  ↓ derived_from
Session 摘要
  ↓ derived_from
长期记忆
```

***

### 4.46 Context Registry（上下文注册中心）

记录本轮模型调用时到底吃了哪些上下文，解决"为什么 AI 会这样回答"的问题。

```typescript
// table: context_registry
{
  id: record<context_registry>,

  // 所属会话
  sessionId: record<session>,

  // 所属任务
  taskId: record<task>,

  // 上下文来源类型
  sourceType: 'memory' | 'knowledge' | 'skill' | 'workspace' | 'prompt' | 'file' | 'rule',

  // 来源记录 ID
  sourceId: string,

  // 消耗的 Token 数
  tokenCount: number,

  // 相关性评分（0-1）
  score: number,

  // 是否最终被选中注入
  selected: boolean,

  // 创建时间
  createdAt: datetime
}
```

**流程**：

```
上下文组装
  ↓
记录引用（context_registry 写入每条引用）
  ↓
模型调用
  ↓
后续可查：为什么 AI 这样回答 → 因为注入了哪些记忆/知识/规则
```

***

### 4.47 Prompt Trace（Prompt 追踪）

AI 项目最大黑洞是 Prompt 漂移：不同模型可能用了不同 Prompt。

```typescript
// table: prompt_trace
{
  id: record<prompt_trace>,

  // 所属任务
  taskId: record<task>,

  // Prompt 类型
  promptType: 'system' | 'planning' | 'tool_call' | 'summary' | 'code_gen' | 'review',

  // 使用的模板 ID
  templateId: record<prompt_template>,

  // 最终发送给模型的完整 Prompt
  finalPrompt: string,

  // Token 数
  tokenCount: number,

  // 模型名称
  model: string,

  // 创建时间
  createdAt: datetime
}
```

**作用**：

- 排查"为什么模型 A 好、模型 B 差" — 可能 Prompt 不一样
- Prompt 版本回滚时对比 finalPrompt 差异
- 审计模型实际收到的指令

***

### 4.48 Model Route（模型路由决策记录）

记录为什么选择了某个模型，用于模型调优。

```typescript
// table: model_route
{
  id: record<model_route>,

  // 所属任务
  taskId: record<task>,

  // 候选模型列表
  candidateModels: string[],

  // 最终选中的模型
  selectedModel: string,

  // 选中原因
  reason: string,

  // 置信度
  confidence: number,

  // 实际延迟
  latency: number,

  // 预估成本
  costEstimate: number,

  // 创建时间
  createdAt: datetime
}
```

**典型路由示例**：

```
任务类型：代码生成
候选：[GPT-4o, QwenCoder, DeepSeekCoder]
选中：QwenCoder
原因：工具能力强 + 本地部署低延迟
```

***

### 4.49 Tool 执行上下文

每个工具调用的详细记录，用于分析性能和稳定性。

```typescript
// table: tool_context
{
  id: record<tool_context>,

  // 所属任务
  taskId: record<task>,

  // 工具名称
  toolName: string,

  // 输入参数
  input: object,

  // 输出结果
  output?: object,

  // 执行耗时（毫秒）
  duration: number,

  // 是否成功
  success: boolean,

  // 错误信息
  error?: string,

  // 创建时间
  createdAt: datetime
}
```

**分析场景**：

- 哪个工具最慢 → `ORDER BY duration DESC`
- 哪个工具最容易失败 → `WHERE success = false GROUP BY toolName`
- 工具调用频率排名 → `GROUP BY toolName ORDER BY count DESC`

***

### 4.50 Agent 思考链摘要（ReasoningSummary）

不是存完整 Chain of Thought（太占空间），只存摘要用于流送时间轴。

```typescript
// table: reasoning_summary
{
  id: record<reasoning_summary>,

  // 所属任务
  taskId: record<task>,

  // 思考步骤摘要列表
  steps: [
    "分析用户任务需求",
    "检索相关记忆",
    "检索知识库",
    "调用工具查询信息",
    "合成最终回答"
  ],

  // 总耗时
  duration: number,

  // 创建时间
  createdAt: datetime
}
```

**不存完整 CoT，只存步骤摘要**，用于：

- 流送时间轴展示
- 日志可读性
- 回放精度

***

### 4.51 Workspace Symbol 索引

Monaco 编辑器的跳转定义、引用搜索依赖此索引。

```typescript
// table: workspace_symbol
{
  id: record<workspace_symbol>,

  // 所属会话
  sessionId: record<session>,

  // 文件路径
  file: string,

  // 符号名称
  symbol: string,

  // 符号类型
  type: 'function' | 'class' | 'variable' | 'interface' | 'type' | 'method' | 'module',

  // 行号
  line: number,

  // 列号
  column: number,

  // 语言
  language: string,

  // 创建时间
  createdAt: datetime
}
```

**扫描分工**：

- **Rust** 负责扫描解析 ts/tsx/py/rs 等文件，提取符号
- **SurrealDB** 存储索引并提供查询

***

### 4.52 Semantic Search Cache（语义搜索缓存）

相同或相似的知识库查询避免重复 embedding。

```typescript
// table: semantic_cache
{
  id: record<semantic_cache>,

  // 查询文本的哈希值
  queryHash: string,

  // 原始查询文本
  queryText: string,

  // 命中的 Embedding ID 列表
  embeddingIds: string[],

  // 检索结果
  result: object,

  // 过期时间
  expireAt: datetime,

  // 命中次数
  hitCount: number,

  // 创建时间
  createdAt: datetime
}
```

**缓存策略**：

- 相同查询直接命中 → 返回缓存结果
- 相似查询（余弦相似度 > 0.95）→ 返回缓存结果
- 超过 expireAt → 删除缓存
- 最大缓存条目：1000 条（可配置）

***

### 4.53 文件观察去抖队列

chokidar 在编辑器连续保存时会触发大量事件，需要去抖。

```typescript
// table: file_watch_queue
{
  id: record<file_watch_queue>,

  // 文件路径
  path: string,

  // 操作类型
  operation: 'created' | 'modified' | 'deleted',

  // 事件时间
  timestamp: datetime,

  // 是否已处理
  processed: boolean
}
```

**流程**：

```
文件变化
  ↓
入队 file_watch_queue
  ↓
500ms 去抖窗口（相同路径的操作合并）
  ↓
触发事件
  ↓
标记 processed
```

***

### 4.54 Runtime 状态快照

记录系统崩溃前一刻的运行状态。

```typescript
// table: runtime_snapshot
{
  id: record<runtime_snapshot>,

  // 活跃会话数
  activeSessions: number,

  // 活跃任务数
  activeTasks: number,

  // 队列大小
  queueSize: number,

  // 内存使用（MB）
  memoryUsage: number,

  // WebSocket 连接数
  websocketCount: number,

  // 创建时间
  createdAt: datetime
}
```

**采集策略**：

- 每 60 秒自动采集一次
- 检测到异常时（内存飙升、任务堆积）立即采集
- 保留最近 24 小时的快照

***

### 4.55 AI 资源预算系统

多模型混合执行必须有资源预算控制，否则 Token/费用可能失控。

```typescript
// table: budget
{
  id: record<budget>,

  // 所属会话
  sessionId: record<session>,

  // Token 预算上限
  tokenBudget: number,

  // 已使用 Token
  tokenUsed: number,

  // 模型调用费用（美元）
  modelCost: number,

  // 工具调用费用
  toolCost: number,

  // 预算周期
  period: 'session' | 'daily' | 'monthly',

  // 预算状态
  status: 'normal' | 'warning' | 'exceeded',

  // 创建时间
  createdAt: datetime,

  // 更新时间
  updatedAt: datetime
}
```

**超预算策略**：

| 状态                | 动作                 |
| ----------------- | ------------------ |
| normal（< 70%）     | 正常执行               |
| warning（70%-100%） | 提示用户，降级为低成本模型      |
| exceeded（> 100%）  | 暂停副模型调用，仅允许主模型轻量执行 |

***

### 4.56 Capability Registry（能力注册中心）

工具、技能、MCP、插件统一注册，AI 可自动发现和调用。

```typescript
// table: capability_registry
{
  id: record<capability_registry>,

  // 能力类型
  type: 'tool' | 'skill' | 'mcp' | 'plugin',

  // 能力名称
  name: string,

  // 描述
  description: string,

  // 所需权限列表
  permissions: string[],

  // 调用 schema（JSON Schema 格式）
  schema: object,

  // 是否启用
  enabled: boolean,

  // 创建时间
  createdAt: datetime,

  // 更新时间
  updatedAt: datetime
}
```

**核心价值**：

- AI 通过查询 capability\_registry 发现可用能力
- 新能力注册后自动上线，无需改代码
- 权限声明集中在 registry 中，安全引擎直接读取

***

### 4.57 Data Provenance（数据来源链）

AI 产品的核心信任能力：回答可追溯来源。

```typescript
// table: data_provenance
{
  id: record<data_provenance>,

  // 目标（最终输出）
  targetId: string,
  targetType: 'answer' | 'summary' | 'code' | 'suggestion',

  // 来源列表
  sourceIds: string[],
  sourceTypes: string[],

  // 关系描述
  relation: 'synthesized_from' | 'direct_copy' | 'transformed',

  // 创建时间
  createdAt: datetime
}
```

**追溯链路示例**：

```
回答
  ↓ synthesized_from
知识库文档A、工具结果B、记忆C
  ↓
用户可看到：这份回答参考了哪些资料
```

***

### 4.58 Shadow Mode（影子模式）

新模型上线时，先跑影子模式对比再切换，不影响用户。

```typescript
// table: shadow_run
{
  id: record<shadow_run>,

  // 原始任务
  taskId: record<task>,

  // 主模型输出
  primaryOutput: string,

  // 影子模型输出
  shadowOutput: string,

  // 对比评分（AI 自动评分或人工评分）
  score: number,

  // 影子模型名称
  shadowModel: string,

  // 创建时间
  createdAt: datetime
}
```

**流程**：

```
主模型正常响应
  ↓
后台偷偷用新模型跑同一任务（shadow mode）
  ↓
记录 shadow_run
  ↓
定期对比 primaryOutput vs shadowOutput
  ↓
评分达到阈值 → 切换为新模型
```

***

### 4.59 Runtime Feature 实验层

运行时特性控制，支持灰度发布和实验。

```typescript
// table: feature_runtime
{
  id: record<feature_runtime>,

  // 功能名称
  name: string,

  // 生效范围
  scope: {
    percentage?: number,   // 灰度比例
    sessions?: string[],   // 指定会话
    users?: string[],      // 指定用户
    mode?: 'dev'           // 开发模式
  },

  // 配置值
  config: object,

  // 是否启用
  enabled: boolean,

  // 创建时间
  createdAt: datetime,

  // 更新时间
  updatedAt: datetime
}
```

**避免**：代码中散落 `if (isDev)` / `if (process.env.FLAG_X)`。
**采用**：统一查询 `feature_runtime` 表决定功能行为。

***

### 4.60 Agent Registry（Agent 注册中心）

Agent 统一注册，避免散落在代码中写死。

```typescript
// table: agent_registry
{
  id: record<agent_registry>,

  // Agent 名称
  name: string,

  // 描述
  description: string,

  // 系统 Prompt
  systemPrompt: string,

  // 能力列表
  capabilities: string[],

  // 可用工具列表
  tools: string[],

  // 支持的模型列表
  supportedModels: string[],

  // 是否启用
  enabled: boolean,

  // 版本号
  version: number,

  // 创建时间
  createdAt: datetime,

  // 更新时间
  updatedAt: datetime
}
```

**典型 Agent 清单**：

| Agent         | 职责      | 工具                                   |
| ------------- | ------- | ------------------------------------ |
| CodingAgent   | 代码生成与修改 | file\_read, file\_write, git, search |
| ResearchAgent | 知识检索与归纳 | knowledge\_search, web\_search       |
| PlanningAgent | 任务规划与拆解 | planner                              |
| MemoryAgent   | 记忆提取与管理 | memory\_read, memory\_write          |
| BrowserAgent  | 网页操作    | browser\_navigate, browser\_click    |

**关系链**：

```
agent_registry
  ↓ (has)
capability
  ↓ (uses)
tool (关联 capability_registry)
```

***

### 4.61 Agent Session（Agent 状态）

Agent 不是无状态的，任务中断后必须能恢复现场。

```typescript
// table: agent_session
{
  id: record<agent_session>,

  // 所属会话
  sessionId: record<session>,

  // 关联的 Agent
  agentId: record<agent_registry>,

  // 当前状态
  state: {
    goal: string,           // 当前目标
    plan: string[],         // 当前计划步骤
    currentStep: number,    // 执行到第几步
    confidence: number,     // 当前置信度
    context: object         // 当前上下文快照
  },

  // 创建时间
  createdAt: datetime,

  // 更新时间
  updatedAt: datetime
}
```

***

### 4.62 Planner 图（规划层）

目标拆解为可执行计划，支持依赖关系和执行树。

```typescript
// table: plan
{
  id: record<plan>,

  // 所属任务
  taskId: record<task>,

  // 目标描述
  goal: string,

  // 计划状态
  status: 'created' | 'executing' | 'completed' | 'failed',

  // 创建时间
  createdAt: datetime
}

// table: plan_step
{
  id: record<plan_step>,

  // 所属计划
  planId: record<plan>,

  // 步骤序号
  stepIndex: number,

  // 步骤描述
  description: string,

  // 依赖的步骤索引列表
  dependsOn: number[],

  // 执行状态
  status: 'pending' | 'running' | 'completed' | 'failed',

  // 执行结果
  result?: string,

  // 执行耗时（毫秒）
  duration?: number,

  // 创建时间
  createdAt: datetime
}
```

**关系**：

```
plan
  ↓
plan_step（可拖动重排）
  ↓
执行引擎
```

***

### 4.63 Task 状态机（task\_state\_history）

当前只有任务状态不够，需要完整状态变迁历史。

```typescript
// 完整状态定义
type TaskStatus =
  | 'created'
  | 'queued'
  | 'planning'
  | 'executing'
  | 'waiting_tool'
  | 'waiting_user'
  | 'retrying'
  | 'completed'
  | 'failed'
  | 'cancelled';

// table: task_state_history
{
  id: record<task_state_history>,

  // 任务
  taskId: record<task>,

  // 上一个状态
  from: TaskStatus,

  // 当前状态
  to: TaskStatus,

  // 状态变更原因
  reason: string,

  // 变更时间
  timestamp: datetime
}
```

**查询场景**：

```sql
-- 为什么卡住了？
SELECT * FROM task_state_history
WHERE taskId = $taskId
  AND to = 'waiting_tool'
ORDER BY timestamp;

-- 为什么失败？
SELECT * FROM task_state_history
WHERE taskId = $taskId
  AND to = 'failed'
ORDER BY timestamp DESC
LIMIT 1;
```

***

### 4.64 Execution Timeline（执行时间轴）

直接驱动 UI 右侧时间轴的事件源。

```typescript
// table: timeline_event
{
  id: record<timeline_event>,

  // 所属会话
  sessionId: record<session>,

  // 所属任务
  taskId: record<task>,

  // 事件类型
  type: 'task' | 'model' | 'tool' | 'skill' | 'file' | 'preview' | 'error',

  // 标题
  title: string,

  // 摘要描述
  summary: string,

  // payload
  payload?: object,

  // 事件时间
  timestamp: datetime
}
```

**时间轴示例**：

```
⏱ 09:00:01 开始任务
⏱ 09:00:03 检索知识库（命中3条记录）
⏱ 09:00:05 调用工具：search_code
⏱ 09:00:08 模型生成代码变更
⏱ 09:00:10 写文件：src/App.tsx
⏱ 09:00:11 更新预览
⏱ 09:00:12 任务完成
```

***

### 4.65 Skill Registry（技能注册中心）

技能统一管理，支撑未来的技能商店和技能分享。

```typescript
// table: skill_registry
{
  id: record<skill_registry>,

  // 技能名称
  name: string,

  // 描述
  description: string,

  // 输入 schema（JSON Schema）
  inputSchema: object,

  // 输出 schema（JSON Schema）
  outputSchema: object,

  // 版本号
  version: string,

  // 所需权限
  permissions: string[],

  // 是否启用
  enabled: boolean,

  // 安装来源
  source: 'builtin' | 'marketplace' | 'local',

  // 创建时间
  createdAt: datetime,

  // 更新时间
  updatedAt: datetime
}
```

**关系**：

```
skill_registry
  ↓ (调用)
tool
  ↓ (声明)
permission
```

***

### 4.66 Session 画像

根据用户使用习惯自动构建画像，用于个性化服务。

```typescript
// table: session_profile
{
  id: record<session_profile>,

  // 会话
  sessionId: record<session>,

  // 偏好的模型
  preferredModel: string,

  // 常见任务类型
  commonTasks: string[],

  // 常用编程语言
  codingLanguage: string[],

  // 高频使用的工具
  frequentTools: string[],

  // 标签
  tags: string[],

  // 更新时间
  updatedAt: datetime
}
```

**作用**：

- 新会话自动推断用户偏好
- 默认模型选择更精准
- 工具推荐更智能

***

### 4.67 Session 摘要层

长对话必须分段摘要，否则上下文窗口会爆炸。

```typescript
// table: session_summary
{
  id: record<session_summary>,

  // 会话
  sessionId: record<session>,

  // 轮次范围（如 1-50, 51-100）
  turnRange: {
    start: number,
    end: number
  },

  // 摘要内容
  content: string,

  // Token 计数
  tokenCount: number,

  // 创建时间
  createdAt: datetime
}
```

**压缩策略**：

```
轮次 1-50 → session_summary（50→5 tokens，压缩 90%）
轮次 51-100 → session_summary
...
加载时：取最后 N 轮完整 + 之前的所有摘要
```

***

### 4.68 Memory 权重系统

长期记忆不能平铺，需要权重排序。

```typescript
// table: memory_score
{
  id: record<memory_score>,

  // 记忆
  memoryId: record<memory>,

  // 重要度（0-1，AI 自动评估 + 用户明确标记）
  importance: number,

  // 访问次数
  accessCount: number,

  // 最后访问时间
  lastAccessed: datetime,

  // 置信度（0-1）
  confidence: number,

  // 更新时间
  updatedAt: datetime
}
```

**权重提升规则**：

- 经常访问的 → importance++
- 用户明确说"记住这个" → importance 设为 1.0
- 长期有效的信息 → 不衰减
- 短期上下文 → 随时间衰减

**检索排序**：`ORDER BY importance DESC, accessCount DESC，lastAccessed DESC`

***

### 4.69 Knowledge 热度系统

知识库变大后，热门知识优先展示，冷知识自动归档。

```typescript
// table: knowledge_heat
{
  id: record<knowledge_heat>,

  // 知识条目
  knowledgeId: record<knowledge>,

  // 命中次数
  hitCount: number,

  // 有效命中的次数（用户实际使用了检索结果）
  successCount: number,

  // 最后命中时间
  lastHit: datetime,

  // 更新时间
  updatedAt: datetime
}
```

**热度策略**：

| 条件                     | 状态 | 处理         |
| ---------------------- | -- | ---------- |
| hitCount > 100         | 热门 | 优先索引，常驻内存  |
| hitCount 10-100        | 温  | 正常索引       |
| hitCount < 10 且 30天未命中 | 冷  | 降级索引，归档到磁盘 |
| 90天未命中                 | 冰  | 移出索引，仅存元数据 |

***

### 4.70 Tool 信誉系统

不同工具成功率和速度不同，自动路由到高信誉工具。

```typescript
// table: tool_score
{
  id: record<tool_score>,

  // 工具名称
  tool: string,

  // 成功率（0-1）
  successRate: number,

  // 平均延迟（毫秒）
  averageLatency: number,

  // 失败次数
  failures: number,

  // 最后调用时间
  lastCalled: datetime,

  // 更新时间
  updatedAt: datetime
}
```

**调度策略**：

- 优先选择 successRate > 0.95 的工具
- 同等成功率选延迟最低的
- 连续失败 3 次 → 临时降权，尝试备用工具

***

### 4.71 Tool 沙箱快照

工具执行时的环境快照，用于事故复现。

```typescript
// table: tool_snapshot
{
  id: record<tool_snapshot>,

  // 所属任务
  taskId: record<task>,

  // 工作目录
  cwd: string,

  // 执行时的文件列表快照
  files: string[],

  // 环境变量哈希
  envHash: string,

  // 创建时间
  createdAt: datetime
}
```

***

### 4.72 Runtime 事件总线记录

跟踪事件总线的生产者-消费者链路，排查事件阻塞。

```typescript
// table: event_bus_record
{
  id: record<event_bus_record>,

  // 事件主题
  topic: string,

  // 生产者
  producer: string,

  // 消费者
  consumer: string,

  // 消费耗时（毫秒）
  latency: number,

  // 是否成功
  success: boolean,

  // 错误信息
  error?: string,

  // 创建时间
  createdAt: datetime
}
```

**排查场景**：

- 哪个事件阻塞了？→ `ORDER BY latency DESC`
- 哪个消费者挂了？→ `WHERE success = false GROUP BY consumer`

***

### 4.73 模型性能排行榜

每个模型在不同任务类型上的表现数据，支撑动态路由。

```typescript
// table: model_benchmark
{
  id: record<model_benchmark>,

  // 模型名称
  model: string,

  // 任务类型
  taskType: 'code_gen' | 'summary' | 'planning' | 'tool_call' | 'rag' | 'chat',

  // 综合评分（0-100）
  score: number,

  // 平均延迟
  latency: number,

  // Token 成本
  tokenCost: number,

  // 测试次数
  testCount: number,

  // 更新时间
  updatedAt: datetime
}
```

**路由决策**：查询此表找最优模型 → `SELECT model FROM model_benchmark WHERE taskType = 'code_gen' ORDER BY score DESC LIMIT 1`

***

### 4.74 自愈记录

系统自动恢复失败的记录，让系统越来越聪明。

```typescript
// table: self_heal
{
  id: record<self_heal>,

  // 失败的任务
  taskId: record<task>,

  // 自愈动作
  action: 'retry' | 'switch_model' | 'switch_tool' | 'downgrade' | 'skip_step',

  // 自愈结果
  result: 'success' | 'partial' | 'failed',

  // 详情
  detail: string,

  // 创建时间
  createdAt: datetime
}
```

**自愈模式示例**：

```
模型调用超时
  ↓
自愈：切换到备用模型（Qwen → DeepSeek）
  ↓
成功
  ↓
记录 self_heal
```

***

### 4.75 Runtime 事故表

事故记录方便复盘，不用翻原始日志。

```typescript
// table: incident
{
  id: record<incident>,

  // 事故标题
  title: string,

  // 严重程度
  severity: 'critical' | 'high' | 'medium' | 'low',

  // 关联任务
  relatedTaskId: record<task>,

  // 根因
  rootCause: string,

  // 处理动作
  action: string,

  // 是否已修复
  fixed: boolean,

  // 修复时间
  fixedAt?: datetime,

  // 创建时间
  createdAt: datetime
}
```

***

### 4.76 Goal System（目标系统）

复杂任务不是一次完成的，需要长期目标支撑。

```typescript
// table: goal
{
  id: record<goal>,

  // 所属会话
  sessionId: record<session>,

  // 目标标题
  title: string,

  // 详细描述
  description: string,

  // 优先级
  priority: 'low' | 'medium' | 'high' | 'critical',

  // 状态
  status: 'active' | 'paused' | 'completed' | 'cancelled',

  // 当前完成置信度（0-1）
  confidence: number,

  // 创建时间
  createdAt: datetime,

  // 更新时间
  updatedAt: datetime
}
```

**关系链**：

```
goal
  ↓ (拆解)
plan
  ↓ (执行)
task
```

**示例**：

```
Goal: 搭建博客系统
  ├─ Plan: 设计数据库 → Task: 创建表结构
  ├─ Plan: 生成API → Task: 实现CRUD
  ├─ Plan: 前端页面 → Task: 实现UI
  └─ Plan: 测试 → Task: 运行测试
```

***

### 4.77 Goal Memory（目标记忆）

Agent 做到一半不会忘目标。

```typescript
// table: goal_memory
{
  id: record<goal_memory>,

  // 所属目标
  goalId: record<goal>,

  // 当前进度摘要
  summary: string,

  // 进度百分比（0-100）
  progress: number,

  // 阻塞项列表
  blockers: string[],

  // 下一步操作
  nextStep: string,

  // 更新时间
  updatedAt: datetime
}
```

**作用**：任务中断后恢复时，AI 直接从 goal\_memory 读取进度，不需要重新分析全部历史。

***

### 4.78 Reflection（反思系统）

Agent 做完后自我检查，避免重复犯错。

```typescript
// table: reflection
{
  id: record<reflection>,

  // 被反思的任务
  taskId: record<task>,

  // 结果摘要
  resultSummary: string,

  // 发现的问题
  problems: string[],

  // 改进建议
  improvements: string[],

  // 对结果的置信度
  confidence: number,

  // 创建时间
  createdAt: datetime
}
```

**流程**：

```
任务完成
  ↓
AI 自我反思
  ↓
记录问题与改进建议
  ↓
下次类似任务避免
```

***

### 4.79 Failure Pattern（失败模式库）

AI 经常反复犯同一种错误，积累失败模式实现经验复用。

```typescript
// table: failure_pattern
{
  id: record<failure_pattern>,

  // 错误签名（归一化的错误描述哈希）
  signature: string,

  // 错误类型
  type: 'file_path' | 'permission' | 'tool_param' | 'network' | 'model' | 'parse',

  // 出现频率
  frequency: number,

  // 已知解决方案
  solution: string,

  // 最后发生时间
  lastOccurred: datetime,

  // 创建时间
  createdAt: datetime
}
```

**自愈增强**：

```
错误发生
  ↓
计算 signature
  ↓
查 failure_pattern
  ↓
命中 → 直接应用已知 solution
  ↓
未命中 → 记录新 pattern
```

***

### 4.80 Learning Record（学习记录）

系统为什么变聪明——记录每次改进。

```typescript
// table: learning_record
{
  id: record<learning_record>,

  // 改进来源
  source: 'reflection' | 'user_feedback' | 'admin_tuning' | 'auto_discovery',

  // 改进前行为
  before: string,

  // 改进后行为
  after: string,

  // 改进原因
  reason: string,

  // 是否有效
  effective: boolean,

  // 创建时间
  createdAt: datetime
}
```

***

### 4.81 Hypothesis（假设系统）

研究型 Agent 的假设-验证-结论闭环。

```typescript
// table: hypothesis
{
  id: record<hypothesis>,

  // 所属任务
  taskId: record<task>,

  // 假设陈述
  statement: string,

  // 证据列表
  evidence: string[],

  // 当前置信度（0-1）
  confidence: number,

  // 状态
  status: 'proposed' | 'testing' | 'confirmed' | 'rejected',

  // 创建时间
  createdAt: datetime,

  // 更新时间
  updatedAt: datetime
}
```

**示例**：

```
假设：API超时是因为网络问题
证据：[ping日志、流量监控、错误堆栈]
结论：confirmed
```

***

### 4.82 Observation（观察记录）

工具输出、网页内容、文件读取结果结构化存储，避免每轮重新分析。

```typescript
// table: observation
{
  id: record<observation>,

  // 来源
  source: 'tool' | 'web' | 'file' | 'user_input',

  // 观察内容摘要
  content: string,

  // 重要性（0-1）
  importance: number,

  // 观察时间
  timestamp: datetime
}
```

***

### 4.83 Working Memory（工作记忆）

短生命周期、自动清理，用于暂存当前任务上下文。

```typescript
// table: working_memory
{
  id: record<working_memory>,

  // 所属任务
  taskId: record<task>,

  // 记忆内容
  content: string,

  // 过期时间（超过此时间自动清理）
  expireAt: datetime,

  // 创建时间
  createdAt: datetime
}
```

**生命周期**：

- 任务开始时创建
- 任务结束时自动清理
- 超时后自动清理

***

### 4.84 Episodic Memory（事件记忆）

记录"发生了什么"，而不是"是什么"。长会话中 AI 经历的事件。

```typescript
// table: episodic_memory
{
  id: record<episodic_memory>,

  // 所属会话
  sessionId: record<session>,

  // 事件摘要
  summary: string,

  // 发生时间
  eventTime: datetime,

  // 重要性（0-1）
  importance: number,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
2026-05-22: 用户重构了项目从 Express 到 Fastify
2026-05-23: 用户添加了 Redis 缓存层
```

***

### 4.85 Semantic Memory（语义记忆）

记录"是什么"——从经历中提炼的客观事实。

```typescript
// table: semantic_memory
{
  id: record<semantic_memory>,

  // 概念
  concept: string,

  // 内容
  content: string,

  // 置信度（0-1）
  confidence: number,

  // 更新时间
  updatedAt: datetime
}
```

**区别**：

```
Episodic Memory: 用户说"我不喜欢 Redux"
Semantic Memory: 用户偏好 = Zustand（从多次事件提炼）
```

***

### 4.86 Memory Consolidation（记忆固化）

#### 目标（为什么存在）

解决**长期记忆爆炸**问题。Working Memory 一直积累，不清理会爆炸；不清洗直接存 Semantic，质量太差。Memory Consolidation 是 Working → Episodic → Semantic → Archive 逐级提炼的流水线。

#### 架构位置（在系统哪一层）

```
Working Memory（工作记忆，短期）
  ↓ 空闲时 → Memory Consolidation
Episodic Memory（事件记忆，中期）
  ↓ 多次事件后 → Memory Consolidation
Semantic Memory（语义记忆，长期）
  ↓ 长时间未更新
Archive（冷存储，永久）
```

属于：记忆系统的**提炼引擎**。在后台异步运行，连接短期记忆和长期记忆。

#### 运行流程

```
触发条件：空闲时段 / 定时触发 / GC 前触发
  ↓
1. 扫描 Working Memory 中已 completed 的条目
  ↓
2. 按 session / goal / topic 分组
  ↓
3. 生成 Episode Summary（LLM 压缩）
  ↓
4. 提取关键事实（实体、决策、结论）
  ↓
5. 建立关系（与已有 Episodic / Semantic 关联）
  ↓
6. 写入 Episodic Memory
  ↓
7. 检查是否需要提炼为 Semantic（多次相似 Episode）
  ↓
8. 清理已固化的 Working Memory
```

**提炼示例**：

```
Working Memory（今天的 20 条记录）：
  - "分析项目目录结构" → 发现是 React 项目
  - "检查 package.json" → 依赖 156 个包
  - "分析路由" → 12 个页面，使用 React Router
  - ...

  ↓ Consolidation

Episode Summary：
  "2026-05-22 下午，用户请求分析 React 项目结构。
   发现：项目使用 React 18 + React Router v6，
   共 12 个页面，依赖 156 个包。
   决策：无需优化。"

  ↓ 多次 Episode 后 → 提炼 Semantic

Semantic Fact：
  "项目使用 React 18 + React Router v6，中等规模（12 页）"
  置信度: 0.92
```

#### 核心数据结构

```typescript
// table: memory_consolidation（固化任务）
{
  id: record<memory_consolidation>,
  sourceLayer: 'working_memory' | 'episodic_memory',
  targetLayer: 'episodic_memory' | 'semantic_memory' | 'archive',

  // 关联的源记忆
  sourceIds: string[],
  sourceCount: number,               // 源条目数

  // 固化结果
  result: string,                    // 压缩后的摘要
  extractedFacts: string[],          // 提取的关键事实

  // 压缩率（越大说明提炼效果越好）
  compressionRatio: number,          // sourceCount / 1

  // 策略
  strategy: 'summarize' | 'extract' | 'deduplicate' | 'link',

  // 状态
  status: 'pending' | 'processing' | 'completed' | 'failed',

  // 保留源记忆的截止时间
  sourceRetentionUntil: datetime,

  createdAt: datetime,
  updatedAt: datetime
}

// table: memory_link（记忆间关联）
{
  id: record<memory_link>,
  sourceMemoryId: string,
  sourceLayer: string,
  targetMemoryId: string,
  targetLayer: string,
  relation: 'derived_from' | 'related_to' | 'contradicts' | 'supports',
  createdAt: datetime
}
```

**压缩率控制**：

```
Working → Episodic:     压缩率 10:1 ~ 20:1（20 条 → 1 条）
Episodic → Semantic:    压缩率 5:1 ~ 10:1
Semantic → Archive:     压缩率 2:1 ~ 3:1（只做格式转换）
```

#### 状态机

```
idle
  ↓（空闲或定时触发）
scanning（扫描待固化记忆）
  ↓
consolidating（LLM 提炼中）
  ↓
linking（建立关系）
  ↓
promoting（写入目标层）
  ↓
cleaning（清理源层）
  ↓
completed
```

| 状态            | 含义         |
| ------------- | ---------- |
| idle          | 空闲等待       |
| scanning      | 扫描需要固化的记忆  |
| consolidating | LLM 正在压缩提炼 |
| linking       | 建立记忆间关联    |
| promoting     | 写入目标记忆层    |
| cleaning      | 清理已固化的源记忆  |
| completed     | 本轮固化完成     |

#### 事件流

```
ConsolidationTriggered（{ reason, sourceLayer }）
  ↓
MemoryScanned（{ candidateCount, totalSize }）
  ↓
MemoryCompressed（{ sourceIds, summary, compressionRatio }）
  ↓
FactsExtracted（{ facts: [] }）
  ↓
MemoryPromoted（{ targetLayer, targetId }）
  ↓
SourceCleaned（{ freedCount, freedSize }）
  ↓
ConsolidationCompleted（{ summary }）
```

#### 实现方案

**第一阶段（LLM Summarize）**：

```
按 session 分组 → LLM 生成摘要 → 提取关键事实
→ 写入 Episodic → 清理 Raw Working
```

**第二阶段（实体抽取 + 关系构建）**：

```
实体识别（模型/框架/工具/人）
→ 建立实体关系图
→ 抽取"决策"和"理由"
→ 构建 Episodic → Semantic 的连接
```

**第三阶段（知识图谱）**：

```
提取的实体和关系 → 融入 Workspace Knowledge Graph
→ 自动补充缺失属性
→ 冲突检测（与已有 Semantic 矛盾时标记）
→ 置信度传播
```

#### 恢复策略

```
原始 Working Memory 保留 24 小时（不立即删除）
  ↓
固化失败 → 重试（最多 3 次）
  ↓
允许回滚：
  - 找到 sourceLink → 还原到 Consolidation 前的状态
  - Episodic 写入失败 → Working 不清除
  - Semantic 写入失败 → Episodic 保持

Consolidation 快照：
  logs/consolidation/{date}/consolidation_{id}.json
```

#### 风险点

| 风险   | 影响                           | 缓解措施                       |
| ---- | ---------------------------- | -------------------------- |
| 压缩失真 | 关键信息在压缩中丢失                   | 保留 sourceLink，可回溯原始内容      |
| 固化风暴 | 大量 Working 同时触发固化            | 限速：每分钟最多 5 次 consolidation |
| 重复提炼 | 同一内容反复被提炼                    | 去重检查 + 版本记录                |
| 关系错乱 | 错误关联不同 Session 的记忆           | 关联置信度阈值（< 0.7 不关联）         |
| 资源竞争 | Consolidation 与正常 Task 抢 LLM | 使用空闲优先级 + Governor 控制      |

#### 后期扩展

- **夜间 Sleep Job**：系统空闲时做深度 Consolidation
- **选择性固化**：只固化高价值记忆（低价值直接丢弃）
- **跨用户学习**：不相关的 Session 之间不交叉固化
- **增量固化**：只处理新增变化的记忆
- **记忆压缩比优化**：动态调整压缩率，质量优先于容量

***

### 4.87 Cognitive State（认知状态）

Agent 的运行时认知状态，用于决策是否切换模型/策略。

```typescript
// table: cognitive_state
{
  id: record<cognitive_state>,

  // 所属任务
  taskId: record<task>,

  // 当前关注点
  focus: string,

  // 不确定性（0-1，高表示需要更多信息）
  uncertainty: number,

  // 置信度（0-1）
  confidence: number,

  // 认知负载（0-1，高表示任务复杂需要降级）
  workload: number,

  // 更新时间
  updatedAt: datetime
}
```

**决策依据**：

- uncertainty > 0.7 → 需要检索更多知识
- workload > 0.8 → 需要降级或拆分子任务
- confidence < 0.3 → 需要切换模型

***

### 4.88 Attention System（注意力系统）

海量知识中哪些当前最重要。

```typescript
// table: attention
{
  id: record<attention>,

  // 关注对象类型
  sourceType: 'file' | 'memory' | 'knowledge' | 'tool_result' | 'user_input',

  // 关注对象 ID
  sourceId: string,

  // 注意力分数（0-100）
  score: number,

  // 更新时间
  updatedAt: datetime
}
```

**注意力规则**：

- 用户刚操作的文档 → score=100
- 当前任务相关的记忆 → score=80
- 旧知识（>1天未访问）→ score 衰减
- 检索排序时：score 高的优先

***

### 4.89 Curiosity Queue（探索队列）

系统发现未知概念时，自动加入探索队列。

```typescript
// table: curiosity_queue
{
  id: record<curiosity_queue>,

  // 探索主题
  topic: string,

  // 为什么需要探索
  reason: string,

  // 优先级
  priority: 'low' | 'medium' | 'high',

  // 状态
  status: 'pending' | 'exploring' | 'completed',

  // 创建时间
  createdAt: datetime
}
```

**场景**：

- 任务中遇到不认识的库 → 自动加入探索队列
- 空闲时自动处理探索队列
- 学习完后结果存入 semantic\_memory

***

### 4.90 Belief Graph（信念图）

避免 AI 同时相信矛盾信息。

```typescript
// table: belief
{
  id: record<belief>,

  // 陈述
  statement: string,

  // 置信度（0-1）
  confidence: number,

  // 支持证据列表
  evidence: string[],

  // 状态
  status: 'tentative' | 'confirmed' | 'contradicted',

  // 创建时间
  createdAt: datetime,

  // 更新时间
  updatedAt: datetime
}

// table: belief_relation
{
  id: record<belief_relation>,

  // 来源信念
  fromBeliefId: record<belief>,

  // 目标信念
  toBeliefId: record<belief>,

  // 关系类型
  relation: 'supports' | 'contradicts' | 'derived_from',

  // 创建时间
  createdAt: datetime
}
```

***

### 4.91 Meta Agent（元 Agent）

SupervisorAgent 的监控与调度记录。

```typescript
// table: meta_action
{
  id: record<meta_action>,

  // 动作类型
  action: 'monitor' | 'dispatch' | 'retry' | 'switch_model' | 'downgrade' | 'pause' | 'recover',

  // 动作目标
  target: string,

  // 动作结果
  result: 'success' | 'failed' | 'partial',

  // 详情
  detail: string,

  // 创建时间
  createdAt: datetime
}
```

**Meta Agent 职责**：

- 监控所有 Agent 运行状态
- 检测异常（超时、失败、阻塞）
- 调度重试/切换/降级
- 负载均衡
- 自愈决策

***

### 4.92 Agent Network（Agent 关系网络）

Agent 之间不是固定树结构，支持动态编队。

```typescript
// table: agent_network
{
  id: record<agent_network>,

  // 源 Agent
  sourceAgent: record<agent_registry>,

  // 目标 Agent
  targetAgent: record<agent_registry>,

  // 关系类型
  relation: 'supervise' | 'cooperate' | 'delegate' | 'review',

  // 协作权重（0-1）
  weight: number,

  // 创建时间
  createdAt: datetime
}
```

**示例拓扑**：

```
SupervisorAgent
  ↓ supervise
PlanningAgent
  ↓ delegate
CodingAgent
  ↓ review
ReviewAgent
```

***

### 4.93 Agent Message Bus

多 Agent 之间通过消息总线通信，不直接函数调用。

```typescript
// table: agent_message
{
  id: record<agent_message>,

  // 来源 Agent
  fromAgent: record<agent_registry>,

  // 目标 Agent
  toAgent: record<agent_registry>,

  // 消息类型
  type: 'request' | 'response' | 'broadcast' | 'error',

  // 消息内容
  content: string,

  // 状态
  status: 'sent' | 'delivered' | 'read' | 'failed',

  // 时间戳
  timestamp: datetime
}
```

**通信流程**：

```
ResearchAgent → MessageBus → CodingAgent
而不是
research() → coding() 直接调用
```

**好处**：远程 Agent、MCP、分布式场景都能接入。

***

### 4.94 Delegation（任务委派）

记录任务在 Agent 之间的委派关系。

```typescript
// table: delegation
{
  id: record<delegation>,

  // 父任务
  parentTaskId: record<task>,

  // 子任务
  childTaskId: record<task>,

  // 被委派的 Agent
  assignedAgent: record<agent_registry>,

  // 委派原因
  reason: string,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
主任务：搭建博客系统
  ↓
委派 ResearchAgent → 寻找认证方案
  ↓
委派 CodingAgent → 实现登录模块
```

***

### 4.95 Consensus（共识机制）

多模型回答冲突时，通过共识机制决定最终输出。

```typescript
// table: consensus
{
  id: record<consensus>,

  // 所属任务
  taskId: record<task>,

  // 候选答案列表（各模型的输出）
  candidates: [
    { model: string, output: string, score: number }
  ],

  // 选中的答案
  selected: string,

  // 共识策略
  strategy: 'majority' | 'weighted' | 'judge',

  // 创建时间
  createdAt: datetime
}
```

**策略说明**：

| 策略       | 适用场景  | 说明               |
| -------- | ----- | ---------------- |
| majority | 事实性问题 | 多数模型一致的答案        |
| weighted | 专业领域  | 按模型专业度加权         |
| judge    | 复杂推理  | ReviewAgent 评审决定 |

***

### 4.96 Debate（辩论系统）

多 Agent 围绕方案辩论，代码架构设计尤其有用。

```typescript
// table: debate
{
  id: record<debate>,

  // 辩论主题
  topic: string,

  // 参与方
  participants: string[],

  // 辩论轮次
  rounds: [
    { agent: string, argument: string, counter?: string[] }
  ],

  // 最终结论
  result: string,

  // 创建时间
  createdAt: datetime
}
```

**流程**：

```
方案A（CodingAgent）
  ↓
方案B（ResearchAgent）
  ↓
互相反驳（最多3轮）
  ↓
ReviewAgent 总结决策
```

***

### 4.97 Review Agent（审查层）

所有高影响操作必须经过审查。

```typescript
// table: review
{
  id: record<review>,

  // 被审查的任务
  targetTaskId: record<task>,

  // 审查者 Agent
  reviewer: record<agent_registry>,

  // 评分（0-100）
  score: number,

  // 发现的问题
  issues: string[],

  // 审查结论
  conclusion: 'approved' | 'changes_requested' | 'rejected',

  // 创建时间
  createdAt: datetime
}
```

**流程**：

```
CodingAgent 生成代码
  ↓
ReviewAgent 审查
  ↓
approved → 合并
changes_requested → 修改后重新审查
rejected → 废弃
```

***

### 4.98 Trust Score（Agent 信誉）

不同 Agent 调度时优先选高信誉的。

```typescript
// table: agent_score
{
  id: record<agent_score>,

  // Agent
  agent: record<agent_registry>,

  // 成功率（0-1）
  successRate: number,

  // 平均延迟
  latency: number,

  // 置信度（0-1）
  confidence: number,

  // 更新时间
  updatedAt: datetime
}
```

**调度策略**：

- successRate > 0.9 的 Agent 优先
- 同等成功率选延迟低的
- 连续失败触发 Meta Agent 介入

***

### 4.99 World State（世界状态）

多个 Agent 共享对当前世界的一致认知。

```typescript
// table: world_state
{
  id: record<world_state>,

  // 所属会话
  sessionId: record<session>,

  // 状态摘要
  summary: string,

  // 已知实体列表
  entities: string[],

  // 更新时间
  updatedAt: datetime
}
```

**示例**：

```
项目：Electron + React
数据库：SurrealDB
当前正在：写插件系统
```

***

### 4.100 Entity Graph（实体图谱）

从用户、任务、文件、知识、插件中抽取实体，形成知识网络。

```typescript
// table: entity
{
  id: record<entity>,

  // 实体名称
  name: string,

  // 实体类型
  type: 'user' | 'task' | 'file' | 'module' | 'class' | 'function' | 'knowledge' | 'plugin',

  // 属性
  attributes: object,

  // 创建时间
  createdAt: datetime
}

// table: relation
{
  id: record<relation>,

  // 源实体
  source: record<entity>,

  // 目标实体
  target: record<entity>,

  // 关系类型
  type: 'depends_on' | 'calls' | 'imports' | 'contains' | 'references',

  // 置信度（0-1）
  confidence: number,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
UserService
  ↓ calls
UserRepo
  ↓ depends_on
Database
```

***

### 4.101 Workspace Knowledge Graph（工作区知识图谱）

扫描代码库生成函数/类/模块调用关系图。

```typescript
// table: workspace_graph
{
  id: record<workspace_graph>,

  // 所属会话
  sessionId: record<session>,

  // 节点
  source: {
    name: string,
    kind: 'function' | 'class' | 'module' | 'file'
  },

  // 目标节点
  target: {
    name: string,
    kind: 'function' | 'class' | 'module' | 'file'
  },

  // 关系
  relation: 'calls' | 'imports' | 'extends' | 'implements' | 'references',

  // 文件路径
  file: string,

  // 行号
  line: number,

  // 更新时间
  updatedAt: datetime
}
```

**作用**：AI 修改代码时自动知道影响范围（修改 AuthService → 影响哪些调用者）。

***

### 4.102 Task Simulation（任务模拟）

高风险操作前先在"脑内"模拟执行一次。

```typescript
// table: task_simulation
{
  id: record<task_simulation>,

  // 被模拟的任务
  taskId: record<task>,

  // 预期的操作序列
  expectedActions: string[],

  // 识别出的风险
  risks: string[],

  // 模拟结论
  conclusion: 'safe' | 'risky' | 'dangerous',

  // 创建时间
  createdAt: datetime
}
```

**流程**：

```
任务计划
  ↓
模拟执行（不产生真实副作用）
  ↓
风险评估
  ↓
safe → 执行
risky → 用户确认
dangerous → 阻止
```

***

### 4.103 Risk Engine（风险引擎）

统一风险评估，不散落在各模块。

```typescript
// table: risk
{
  id: record<risk>,

  // 风险动作
  action: string,

  // 风险评分（0-100）
  score: number,

  // 风险原因
  reasons: string[],

  // 建议措施
  mitigation: string[],

  // 创建时间
  createdAt: datetime
}
```

**评分示例**：

| 动作                  | 评分  | 建议   |
| ------------------- | --- | ---- |
| rm -rf /            | 100 | 直接拒绝 |
| file.delete 工作区关键文件 | 80  | 必须确认 |
| npm install         | 40  | 警告   |
| git status          | 0   | 直接允许 |

***

### 4.104 Policy Engine（策略引擎）

权限规则不写死在代码中，通过策略表动态管理。

```typescript
// table: policy
{
  id: record<policy>,

  // 匹配条件
  condition: {
    action?: string,
    path?: string,
    actor?: string,
    riskBelow?: number
  },

  // 授权动作
  action: 'allow' | 'deny' | 'require_confirm',

  // 效果
  effect: 'grant' | 'deny',

  // 优先级
  priority: number,

  // 是否启用
  enabled: boolean,

  // 创建时间
  createdAt: datetime
}
```

**示例策略**：

```json
{ "condition": { "path": "workspace/**" }, "action": "allow", "effect": "grant" }
{ "condition": { "action": "file.delete", "riskBelow": 80 }, "action": "require_confirm", "effect": "grant" }
```

***

### 4.105 Negotiation（协商协议）

两个 Agent 意见不同时通过协商达成一致。

```typescript
// table: negotiation
{
  id: record<negotiation>,

  // 参与方
  agents: string[],

  // 各方提案
  proposals: [
    { agent: string, proposal: string, reasoning: string }
  ],

  // 协商结果
  result: string,

  // 创建时间
  createdAt: datetime
}
```

**场景**：

```
ResearchAgent：方案A（理由：性能好）
CodingAgent：方案B（理由：可维护）
  ↓
协商 → 方案B（维护优先于性能）
```

***

### 4.106 Reputation System（生态评分）

插件/MCP 质量参差不齐，靠评分自动避坑。

```typescript
// table: reputation
{
  id: record<reputation>,

  // 评估对象
  entity: string,

  // 评分（0-100）
  score: number,

  // 举报次数
  reports: number,

  // 最后评估时间
  lastEvaluated: datetime,

  // 更新时间
  updatedAt: datetime
}
```

**自动处理**：

- score < 30 → 自动禁用
- reports > 5 → 人工审核
- 高评分插件优先推荐

***

### 4.107 Shared Memory（共享记忆）

多个 Agent 共享的上下文，避免各自独立存储。

```typescript
// table: shared_memory
{
  id: record<shared_memory>,

  // 记忆内容
  content: string,

  // 共享范围
  scope: 'session' | 'workspace' | 'global',

  // 过期时间
  expireAt?: datetime,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
"当前项目使用 SurrealDB 3.0，所有数据库操作通过 Repository 模式"
→ 所有 Agent 自动可见
```

***

### 4.108 Society Rules（社会规则）

Agent 社会的行为规则，按优先级排序。

```typescript
// table: society_rule
{
  id: record<society_rule>,

  // 规则描述
  rule: string,

  // 优先级
  priority: number,

  // 适用范围
  scope: 'all' | 'agent' | 'tool' | 'system',

  // 是否启用
  enabled: boolean,

  // 创建时间
  createdAt: datetime
}
```

**规则示例**：

```
1. Review 优先于执行（安全原则）
2. 风险优先于速度（安全原则）
3. 用户确认优先于自动执行（权限原则）
```

***

### 4.109 Constitution（宪法层）

系统最顶层的不可违背原则，不是 Prompt，是系统原则。

```typescript
// table: constitution
{
  id: record<constitution>,

  // 原则陈述
  principle: string,

  // 示例说明
  examples: string[],

  // 违反后果
  violationConsequence: string,

  // 创建时间
  createdAt: datetime
}
```

**宪法示例**：

```
原则：不要修改用户文件而不确认
示例：写入新文件需确认，覆盖关键文件需强确认

原则：优先可恢复
示例：删除文件前先备份，支持回滚

原则：数据主权
示例：用户数据不未经授权外发
```

***

### 4.110 Evolution Log（进化记录）

系统为什么变成今天这样——完整的进化轨迹。

```typescript
// table: evolution
{
  id: record<evolution>,

  // 进化前状态
  before: string,

  // 进化后状态
  after: string,

  // 进化原因
  reason: string,

  // 进化类型
  type: 'model_change' | 'rule_added' | 'config_tuned' | 'prompt_updated' | 'agent_added',

  // 创建时间
  createdAt: datetime
}
```

**记录示例**：

```
2026-05-01: 切换默认模型 GPT-4o → DeepSeek Coder（成本降低 70%）
2026-05-10: 添加代码审查规则（ReviewAgent 上线）
2026-05-15: 调整副模型并发数 3→5（性能优化）
```

***

### 4.111 Objective Engine（目标引擎）

Goal 是任务，Objective 是长期方向。

```typescript
// table: objective
{
  id: record<objective>,

  // 目标标题
  title: string,

  // 衡量指标
  metric: string,

  // 目标值
  targetValue: number,

  // 当前值
  currentValue: number,

  // 优先级
  priority: 'low' | 'medium' | 'high' | 'critical',

  // 状态
  status: 'active' | 'achieved' | 'abandoned',

  // 创建时间
  createdAt: datetime,

  // 更新时间
  updatedAt: datetime
}
```

**示例**：

| Objective | Metric              | Target | Current |
| --------- | ------------------- | ------ | ------- |
| 提升代码生成质量  | code\_review\_score | 90%    | 72%     |
| 降低模型调用成本  | monthly\_cost       | $50    | $78     |
| 提高工具成功率   | tool\_success\_rate | 95%    | 88%     |

***

### 4.112 KPI Registry（指标中心）

统一指标注册，避免监控越来越乱。

```typescript
// table: kpi
{
  id: record<kpi>,

  // 指标名称
  name: string,

  // 所属类别
  category: 'runtime' | 'model' | 'memory' | 'tool' | 'agent' | 'cost',

  // 当前值
  value: number,

  // 更新时间
  updatedAt: datetime
}
```

**KPI 清单**：

| 类别      | 指标                  |
| ------- | ------------------- |
| runtime | 平均任务成功率、平均延迟、队列深度   |
| model   | 模型调用成功率、平均 Token/任务 |
| memory  | 记忆命中率、平均检索时间        |
| tool    | 工具成功率、平均工具延迟        |
| agent   | Agent 任务完成率、平均协作轮次  |
| cost    | 日消耗 Token、月成本       |

***

### 4.113 Optimization Candidate（优化候选池）

系统自动发现哪里最差。

```typescript
// table: optimization_candidate
{
  id: record<optimization_candidate>,

  // 优化来源
  source: 'kpi' | 'drift' | 'incident' | 'user_feedback',

  // 问题描述
  problem: string,

  // 预期影响
  impact: string,

  // 置信度（0-1）
  confidence: number,

  // 状态
  status: 'identified' | 'planned' | 'applied' | 'verified',

  // 创建时间
  createdAt: datetime
}
```

**示例**：BrowserTool 成功率 55% → 自动识别为优化候选。

***

### 4.114 Runtime Diagnosis（运行诊断）

自动诊断系统各组件的健康状态。

```typescript
// table: runtime_diagnosis
{
  id: record<runtime_diagnosis>,

  // 组件名称
  component: string,

  // 问题描述
  issue: string,

  // 严重程度
  severity: 'info' | 'warning' | 'critical',

  // 建议的处理方式
  recommendation: string,

  // 是否已处理
  resolved: boolean,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
组件: EmbeddingQueue
问题: 队列堆积 500+ 任务
严重度: warning
建议: 增加 Worker 并发数
```

***

### 4.115 Bottleneck Detector（瓶颈检测）

系统自己知道哪里慢。

```typescript
// table: bottleneck
{
  id: record<bottleneck>,

  // 组件名称
  component: string,

  // 延迟（毫秒）
  latency: number,

  // 影响范围描述
  impact: string,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
组件: ToolRouter
延迟: 700ms
影响: 所有工具调用前置等待
```

***

### 4.116 Strategy Registry（策略中心）

动态策略，替代代码中的 `if (costHigh)` 等硬编码。

```typescript
// table: strategy
{
  id: record<strategy>,

  // 策略名称
  name: string,

  // 触发条件
  conditions: {
    kpi?: string,
    threshold?: number,
    duration?: string
  },

  // 执行动作
  actions: string[],

  // 优先级
  priority: number,

  // 是否启用
  enabled: boolean,

  // 创建时间
  createdAt: datetime
}
```

**示例策略**：

```
条件: budget 超过 80%
动作: 切换低成本模型
优先级: 高
```

***

### 4.117 Adaptation Log（自适应日志）

系统自动调整行为时必须记录，否则越来越不可解释。

```typescript
// table: adaptation
{
  id: record<adaptation>,

  // 触发原因
  trigger: string,

  // 执行动作
  action: string,

  // 调整结果
  result: 'success' | 'partial' | 'failed',

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
触发: Claude 连续超时
动作: 自动切换 Qwen
结果: success
```

***

### 4.118 Environment Model（环境模型）

系统需要感知自身运行环境。

```typescript
// table: environment
{
  id: record<environment>,

  // CPU 使用率（%）
  cpu: number,

  // 内存使用率（%）
  memory: number,

  // 网络状态
  network: 'online' | 'offline' | 'limited',

  // 磁盘使用率（%）
  disk: number,

  // 电源状态
  power?: 'plugged' | 'battery' | 'low_battery',

  // 更新时间
  updatedAt: datetime
}
```

***

### 4.119 Resource Planner（资源规划）

执行任务前预估资源消耗。

```typescript
// table: resource_plan
{
  id: record<resource_plan>,

  // 所属会话
  sessionId: record<session>,

  // 预估 Token 消耗
  estimatedTokens: number,

  // 预估工具调用次数
  estimatedTools: number,

  // 预估内存使用
  estimatedMemory: number,

  // 创建时间
  createdAt: datetime
}
```

**作用**：大任务先估算再执行，避免资源失控。

***

### 4.120 Energy Mode（运行模式）

根据环境自适应切换运行模式。

```typescript
// table: runtime_mode
{
  id: record<runtime_mode>,

  // 运行模式
  mode: 'performance' | 'balanced' | 'economy',

  // 切换原因
  reason: string,

  // 生效开始时间
  startedAt: datetime,

  // 生效结束时间
  endedAt?: datetime
}
```

**示例**：

```
电源: 电池模式
→ 自动切 economy
→ 降级模型、减少副模型
```

***

### 4.121 Drift Detector（漂移检测）

AI 系统后期一定会出现 Prompt 漂移、模型漂移、行为漂移。

```typescript
// table: drift
{
  id: record<drift>,

  // 漂移组件
  component: 'prompt' | 'model' | 'tool' | 'agent' | 'quality',

  // 漂移前状态
  before: string,

  // 漂移后状态
  after: string,

  // 漂移分数（0-1，越大越严重）
  score: number,

  // 创建时间
  createdAt: datetime
}
```

**检测示例**：

```
组件: code_quality
before: 92%（上周代码审查通过率）
after: 71%（本周）
score: 0.87
→ 预警
```

***

### 4.122 Memory Decay（记忆衰减）

长期不用的记忆自动降权，更像人脑。

```typescript
// table: memory_decay
{
  id: record<memory_decay>,

  // 记忆条目
  memoryId: record<memory>,

  // 当前权重（0-1）
  score: number,

  // 衰减率（每日衰减比例）
  decayRate: number,

  // 更新时间
  updatedAt: datetime
}
```

**衰减规则**：

- 每日衰减：score \*= (1 - decayRate)
- 被访问时：score 重置为 1.0
- score < 0.1：自动归档
- 归档后 30 天未访问：自动删除

***

### 4.123 Sleep Cycle（休眠周期）

系统空闲时自动整理，不让运行时越来越重。

```typescript
// table: sleep_job
{
  id: record<sleep_job>,

  // 任务类型
  type: 'memory_consolidation' | 'session_summary' | 'embedding_backfill' | 'kpi_calc' | 'optimization_scan',

  // 状态
  status: 'pending' | 'running' | 'completed',

  // 创建时间
  createdAt: datetime,

  // 完成时间
  completedAt?: datetime
}
```

**空闲任务列表**：

- 记忆固化（Working → Episode → Semantic）
- 长 Session 压缩摘要
- 待处理的 Embedding 任务
- KPI 计算
- 优化候选扫描
- 过期数据清理

***

### 4.124 Dream Queue（联想队列）

系统发现不同知识之间可能有关联，自动探索。

```typescript
// table: dream
{
  id: record<dream>,

  // 关联的概念列表
  concepts: string[],

  // 关联置信度（0-1）
  confidence: number,

  // 状态
  status: 'pending' | 'explored' | 'confirmed' | 'rejected',

  // 创建时间
  createdAt: datetime
}
```

**流程**：

```
知识A + 知识B 出现频率高
  ↓
加入 dream queue
  ↓
空闲时探索关联性
  ↓
confirmed → 存入 relation 表
```

***

### 4.125 Self Model（自我模型）

系统知道自己擅长什么、不擅长什么。

```typescript
// table: self_model
{
  id: record<self_model>,

  // 能力领域
  capability: string,

  // 自我评估的置信度（0-100）
  confidence: number,

  // 证据（历史表现数据）
  evidence: string[],

  // 更新时间
  updatedAt: datetime
}
```

**示例**：

```
能力: 代码生成
置信度: 95
证据: "过去 7 天 120 次代码任务，成功率 94%"

能力: 数学推理
置信度: 68
证据: "过去 7 天 15 次数学任务，成功率 67%"
```

***

### 4.126 Capability Gap（能力缺口）

系统知道自己缺什么。

```typescript
// table: capability_gap
{
  id: record<capability_gap>,

  // 任务类型
  taskType: string,

  // 缺失的能力
  missingCapability: string,

  // 严重程度
  severity: 'low' | 'medium' | 'high',

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
任务: 网页自动化
缺口: 缺 BrowserAgent
严重度: high
→ 自动推荐安装浏览器相关技能
```

***

### 4.127 Evolution Planner（进化规划器）

系统为自己规划升级路线。

```typescript
// table: evolution_plan
{
  id: record<evolution_plan>,

  // 改进目标
  target: string,

  // 预期收益
  expectedGain: string,

  // 风险等级
  risk: 'low' | 'medium' | 'high',

  // 状态
  status: 'proposed' | 'in_progress' | 'completed' | 'cancelled',

  // 创建时间
  createdAt: datetime,

  // 完成时间
  completedAt?: datetime
}
```

**示例**：

```
目标: 增加 ReviewAgent
预计收益: 代码错误率 -12%
风险: low
```

***

### 4.128 Experiment System（实验平台）

新功能先 A/B 测试再上线。

```typescript
// table: experiment
{
  id: record<experiment>,

  // 实验名称
  name: string,

  // 对照组（当前方案）
  variantA: string,

  // 实验组（新方案）
  variantB: string,

  // 实验结果
  result: {
    winner: 'a' | 'b' | 'tie',
    metricA: number,
    metricB: number,
    confidence: number
  },

  // 状态
  status: 'running' | 'completed' | 'cancelled',

  // 创建时间
  createdAt: datetime,

  // 完成时间
  completedAt?: datetime
}
```

**示例**：

```
实验: 新代码审查 Prompt
A: 旧 Prompt（通过率 88%）
B: 新 Prompt（通过率 94%）
结果: B 胜出，自动切换
```

***

### 4.129 Safety Memory（安全记忆）

系统记住危险命令和错误操作，避免重复事故。

```typescript
// table: safety_memory
{
  id: record<safety_memory>,

  // 触发条件
  trigger: string,

  // 解决方案
  solution: string,

  // 严重程度
  severity: 'low' | 'medium' | 'high' | 'critical',

  // 命中次数
  hitCount: number,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
触发: rm -rf 在工作区根目录执行
方案: 拦截并确认 → 要求用户确认目标路径
严重度: critical
```

***

### 4.130 Civilization Archive（文明档案）

系统长期运行的完整历史记录。

```typescript
// table: civilization
{
  id: record<civilization>,

  // 时间段
  period: string,

  // 阶段摘要
  summary: string,

  // 关键事件
  keyEvents: string[],

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
period: 2026-Q2
summary: "接入 ReviewAgent，代码质量提升 18%"
keyEvents: [
  "接入 DeepSeek Coder，成本降低 70%",
  "ReviewAgent 上线，代码错误率 -18%",
  "副模型并发数 3→5，任务速度 +40%",
]
```

***

### 4.131 分层边界（架构核心约束）

系统已出现多层，必须明确层间边界。

```
UI 层          (React, 只展示/交互)
    ↓ IPC
Application 层  (Command, Query, 编排)
    ↓
Runtime 层      (Orchestrator, 调度器)
    ↓
Memory 层       (Context, Memory, Knowledge)
    ↓
Storage 层      (Dragonfly, SurrealDB, JSONL)
```

**核心规则**：

- UI 不能直接查 SurrealDB
- Agent 不能直接写数据库
- 所有写操作必须经过 Runtime
- 所有跨层访问必须通过 Contract

**禁止出现**：

```
agent.save()       // ❌ Agent 直接写库
db.update()        // ❌ 到处直接调用
component.query()  // ❌ UI 直接查数据库
```

***

### 4.132 写入原则（Command 模式）

所有写操作统一走 Command 模式。

```
Command
  ↓
Runtime
  ↓
Event
  ↓
Persistence
```

**禁止**：`db.create()` 满天飞。

**统一**：

```typescript
CreateTaskCommand   → Runtime → TaskCreated → Persist
SaveMemoryCommand   → Runtime → MemorySaved → Persist
UpdateGoalCommand   → Runtime → GoalUpdated → Persist
DeleteFileCommand   → Runtime → FileDeleted → Persist
```

**实现**：

```typescript
// table: command
{
  id: record<command>,
  type: string,
  payload: object,
  status: 'received' | 'processing' | 'completed' | 'failed',
  createdAt: datetime
}
```

***

### 4.133 Read Model / Write Model 分离（CQRS）

同一张表不分读写会导致查询膨胀。

```typescript
// Write Model（操作专用）
// task：创建、更新状态、完成

// Read Models（查询专用）
// timeline_view — 时间轴展示
{
  taskId: record<task>,
  sessionId: record<session>,
  title: string,
  status: string,
  timestamp: datetime
}

// dashboard_view — 仪表盘
{
  totalTasks: number,
  successRate: number,
  avgLatency: number
}

// session_view — 会话概览
{
  sessionId: record<session>,
  taskCount: number,
  lastActivity: datetime
}
```

***

### 4.134 Projection 系统

事件 → 自动更新所有 Read Model。

```typescript
// table: projection_job
{
  id: record<projection_job>,

  // 源事件
  event: string,

  // 目标投影
  projection: 'timeline_view' | 'dashboard_view' | 'session_view' | 'kpi_cache',

  // 状态
  status: 'pending' | 'processing' | 'completed' | 'failed',

  // 创建时间
  createdAt: datetime
}
```

**流程**：

```
TaskCreated
  ↓
Projection Job（异步）
  ↓
更新 timeline_view
更新 dashboard_view
更新 session_view
```

**避免**：UI 查询时 `JOIN JOIN JOIN JOIN`。

***

### 4.135 Event Schema Registry（事件 Schema 注册中心）

事件类型必须有版本控制，否则老事件回放会失败。

```typescript
// table: event_schema
{
  id: record<event_schema>,

  // 事件类型
  eventType: string,

  // Schema 版本号
  version: number,

  // JSON Schema 定义
  schema: object,

  // 是否启用
  active: boolean,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
TaskCreated:v1 → { taskId, title, sessionId }
TaskCreated:v2 → { taskId, title, sessionId, priority }  // 新增字段
```

***

### 4.136 Event Version 迁移器

历史事件通过迁移器升级到新版本。

```typescript
// table: event_migration
{
  id: record<event_migration>,

  // 源版本
  from: string,   // 如 "TaskCreated:v1"

  // 目标版本
  to: string,     // 如 "TaskCreated:v2"

  // 转换函数（序列化存储）
  transformer: string,

  // 创建时间
  createdAt: datetime
}
```

**作用**：历史事件不会变废数据。

***

### 4.137 Runtime Contract

Agent 与 Runtime 之间通过契约通信，不自由调用。

```typescript
// Runtime Contract（接口约束）
interface RuntimeContract {
  execute(command: Command): Result        // 执行命令
  observe(query: Query): Result            // 查询状态
  delegate(task: Task): Result             // 委派子任务
  reflect(event: Event): void              // 处理事件
}
```

**禁止**：

```typescript
agentA.call(agentB)     // ❌ 直接调用
agentA.db.save()         // ❌ 直接写库
```

***

### 4.138 Domain Boundary（领域边界）

明确各领域边界，禁止跨域直接操作。

| 领域              | 职责      | 禁止             |
| --------------- | ------- | -------------- |
| MemoryDomain    | 记忆读写管理  | 直接修改 Workspace |
| KnowledgeDomain | 知识库索引检索 | 直接修改 Task      |
| TaskDomain      | 任务声明周期  | 直接修改 Memory    |
| WorkspaceDomain | 工作区文件管理 | 直接修改 Knowledge |
| ToolDomain      | 工具执行编排  | 直接修改 Session   |
| SessionDomain   | 会话管理    | 直接执行 Tool      |

跨域通信：只能通过 Domain Event。

***

### 4.139 Domain Event

每个领域发布自己的事件，粒度精确。

```typescript
// 正确：精确事件
TaskCompleted
MemoryUpdated
GoalCreated
KnowledgeIndexed
SessionArchived
FileWritten

// 错误：大而全的事件
SaveEverything      // ❌ 监听器不知道发生了什么
StateChanged         // ❌ 太模糊
DataUpdated          // ❌ 谁都能发
```

***

### 4.140 Runtime Clock（运行时时钟）

统一时间源，支持回放、模拟、测试、恢复。

```typescript
// table: runtime_clock
{
  id: record<runtime_clock>,

  // 时钟滴答
  tick: number,

  // 当前时间戳
  timestamp: datetime
}
```

**用途**：

- 回放历史事件时使用 clock.tick 代替 Date.now()
- 单元测试时可以注入固定时钟
- 崩溃恢复时从时钟恢复时序

***

### 4.141 Replay 模式（事件回放）

基于历史事件重新运行时状态。

```typescript
// table: replay
{
  id: record<replay>,

  // 回放会话 ID
  sessionId: record<session>,

  // 回放起点时间
  fromTime: datetime,

  // 回放终点时间
  toTime: datetime,

  // 状态
  status: 'running' | 'completed' | 'failed',

  // 创建时间
  createdAt: datetime
}
```

**应用场景**：

- 事故分析：回放到事故前一刻看状态
- 调试：step-by-step 回放事件
- 恢复：回放重建数据状态

***

### 4.142 Sandbox Session（沙箱会话）

AI 自动改代码前先试运行。

```typescript
// table: sandbox
{
  id: record<sandbox>,

  // 原始会话
  parentSessionId: record<session>,

  // 沙箱分支名称
  branch: string,

  // 是否隔离（不产生真实副作用）
  isolated: boolean,

  // 状态
  status: 'active' | 'merged' | 'discarded',

  // 创建时间
  createdAt: datetime
}
```

**流程**：

```
AI 生成代码变更
  ↓
在沙箱中运行
  ↓
验证结果
  ↓
确认 → 合并到主线
拒绝 → 丢弃
```

***

### 4.143 Runtime Branch（运行时分支）

系统也可以有分支，不只靠 Git。

```typescript
// table: runtime_branch
{
  id: record<runtime_branch>,

  // 父分支
  parent: string,

  // 分支快照
  snapshot: object,

  // 状态
  status: 'active' | 'merged' | 'abandoned',

  // 创建时间
  createdAt: datetime
}
```

**场景**：

```
主线
  ↓ fork
实验分支（新 Prompt、新 Agent）
  ↓ 验证通过
合并回主线
```

***

### 4.144 Merge 策略

数据冲突时的合并方案。

```typescript
// table: merge_strategy
{
  id: record<merge_strategy>,

  // 源分支
  source: string,

  // 目标分支
  target: string,

  // 冲突解决策略
  resolver: 'latest_wins' | 'manual_confirm' | 'model_judge' | 'merge_keep_both',

  // 创建时间
  createdAt: datetime
}
```

**策略说明**：

| 策略                | 适用         |
| ----------------- | ---------- |
| latest\_wins      | 时间戳最新优先    |
| manual\_confirm   | 高风险冲突用户确认  |
| model\_judge      | Agent 自动裁决 |
| merge\_keep\_both | 记忆/知识可共存场景 |

***

### 4.145 Kill Switch（保命按钮）

事故时秒停特定组件。

```typescript
// table: kill_switch
{
  id: record<kill_switch>,

  // 目标组件
  component: string,

  // 是否启用（true=关闭，false=允许运行）
  enabled: boolean,

  // 操作原因
  reason: string,

  // 操作人
  operator: string,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
组件: BrowserAgent
启用: true（关闭）
原因: BrowserAgent 正在删除文件
```

***

### 4.146 Quarantine（隔离区）

异常组件自动隔离，不影响系统整体。

```typescript
// table: quarantine
{
  id: record<quarantine>,

  // 被隔离实体
  entity: string,

  // 实体类型
  entityType: 'plugin' | 'agent' | 'mcp' | 'tool',

  // 隔离原因
  reason: string,

  // 隔离时间
  quarantinedAt: datetime,

  // 是否已解除
  released: boolean
}
```

**触发条件**：

- 插件连续崩溃 3 次
- Agent 行为异常（成功率骤降）
- MCP 响应超时

***

### 4.147 Runtime Recovery Plan（运行时恢复计划）

事故恢复方案预定义，不用现想。

```typescript
// table: recovery_plan
{
  id: record<recovery_plan>,

  // 关联事故
  incident: string,

  // 恢复动作列表
  actions: string[],

  // 预期恢复时间
  estimatedRTO: string,

  // 最后测试时间
  lastTested: datetime,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
事故: SurrealDB 不可用
动作: [
  "切换只读模式",
  "所有写操作进入 Outbox 队列",
  "使用 JSONL 回放重建数据"
]
RTO: 5 分钟
```

***

### 4.148 Chaos Test（混沌测试）

主动注入故障，验证系统韧性。

```typescript
// table: chaos_test
{
  id: record<chaos_test>,

  // 测试目标
  target: string,

  // 注入的故障
  fault: string,

  // 测试结果
  result: 'recovered' | 'partial' | 'failed',

  // 观察记录
  observations: string[],

  // 创建时间
  createdAt: datetime
}
```

**测试场景**：

- 断网：系统能否降级运行
- 数据库挂：能否切换到备用存储
- 模型超时：能否切换备用模型
- 磁盘满：能否优雅处理

***

### 4.149 System Health Graph（系统健康图谱）

组件依赖关系与健康状态一目了然。

```typescript
// table: health_graph
{
  id: record<health_graph>,

  // 组件名称
  component: string,

  // 依赖的组件
  dependencies: string[],

  // 健康状态
  status: 'healthy' | 'degraded' | 'down',

  // 更新时间
  updatedAt: datetime
}
```

**查询示例**：

```sql
-- 谁坏了影响谁？
SELECT * FROM health_graph WHERE status = 'down';
-- 受影响的上游组件
SELECT * FROM health_graph WHERE dependencies CONTAINS 'surreal_db';
```

***

### 4.150 Architecture Constitution（架构宪法）

系统不可违背的架构原则——不是 Prompt，是系统设计根基。

```typescript
// table: architecture_constitution
{
  id: record<architecture_constitution>,

  // 原则陈述
  principle: string,

  // 详情说明
  detail: string,

  // 优先级
  priority: number,

  // 创建时间
  createdAt: datetime
}
```

#### 五项基本原则

**原则 1：JSONL 是真相源**
所有事件必须先写入 JSONL，Dragonfly 和 SurrealDB 都可以从 JSONL 重建。

**原则 2：可恢复优先于性能**
系统设计优先保证可恢复性，其次才是性能。不可恢复的优化不可接受。

**原则 3：所有写操作事件化**
没有任何写操作可以绕过事件系统直接写入存储。Command → Event → Persist 是唯一路径。

**原则 4：默认可回滚**
任何数据变更必须支持回滚。写操作前必须有快照或备份，不可回滚的操作必须有强确认。

**原则 5：风险优先于速度**
当系统检测到风险时（高权限操作、大量文件变更、未知网络目标），必须暂停并确认，而不是追求最快执行。

***

### 4.151 Intent Engine（意图引擎）

#### 目标（为什么存在）

解决用户输入 → 直接 Task 的问题。用户说"帮我整理项目"不是单一任务，而是**多个潜在目标和意图**（整理目录/整理架构/删无用代码/生成文档/提交 Git）。Intent Engine 在输入和任务之间增加**意图解析层**，防止 AI 在错误理解意图的情况下执行操作。

#### 架构位置（在系统哪一层）

```
Perception（感知）
  ↓
Intent Engine（意图引擎） ← 认知入口
  ↓
Goal（目标）
  ↓
Plan（规划）
  ↓
Task（任务）
```

属于：Runtime 认知入口。所有用户输入必须经过 Intent Engine 再进入认知循环。

#### 运行流程

```
用户输入
  ↓
1. 预处理（去噪、格式化、语言检测）
  ↓
2. 实体抽取（提取项目名、动作目标、关键词）
  ↓
3. 意图分类（判断类型：refactor / feature / bugfix / research / config / question）
  ↓
4. 拆分子意图（一个输入拆为多个独立意图）
  ↓
5. 建立 Intent Graph（确定子意图间依赖关系）
  ↓
6. 写入 Runtime（绑定到当前 Session）
  ↓
7. Goal 生成（每个 Intent 映射到一个或多个 Goal）
```

**示例**：

```
输入: "帮我重构博客项目，顺便看看依赖有没有问题"

实体:
  - 项目: 博客
  - 动作: 重构, 检查
  - 关键词: 依赖, 问题

拆分子意图:
  Intent-A: 分析项目结构（depend: null）
  Intent-B: 清理冗余代码（depend: A）
  Intent-C: 优化架构设计（depend: A）
  Intent-D: 检查依赖安全性（depend: null, parallel: true）

Intent Graph:
    A ──→ B
    │     ↓
    └──→ C
    D（与 A 并行）
```

#### 核心数据结构

```typescript
// table: intent（意图）
{
  id: record<intent>,
  sessionId: record<session>,

  // 原始输入
  rawInput: string,

  // 意图类型
  intentType: 'refactor' | 'feature' | 'bugfix' | 'research' | 'config' | 'question' | 'command',

  // 提取的实体
  entities: {
    project?: string,
    action?: string,
    scope?: string,
    keywords: string[]
  },

  // 置信度（0-1）
  confidence: number,

  // 父意图（形成 Intent Graph）
  parentIntent?: record<intent>,

  // 状态
  status: 'created' | 'parsed' | 'expanded' | 'planned' | 'executing' | 'completed' | 'archived',

  // 创建时间
  createdAt: datetime,
  updatedAt: datetime
}

// table: intent_edge（意图图边）
{
  id: record<intent_edge>,
  fromIntent: record<intent>,      // 源意图
  toIntent: record<intent>,        // 目标意图
  relation: 'depend' | 'parallel' | 'optional' | 'alternative',
  metadata: object,                 // 关系描述
  createdAt: datetime
}

// table: intent_snapshot（意图快照—用于恢复）
{
  id: record<intent_snapshot>,
  sessionId: record<session>,
  intents: object[],                // 当前时刻的完整意图图
  checksum: string,
  createdAt: datetime
}
```

#### 状态机

```
created
  ↓
parsed（意图识别完成）
  ↓
expanded（子意图拆分完成，Graph 建立）
  ↓
planned（已映射到 Goal/Plan）
  ↓
executing（执行中）
  ↓
completed（所有子意图完成）
  ↓
archived（已归档）
```

| 状态        | 含义                 | 触发             |
| --------- | ------------------ | -------------- |
| created   | 原始输入接收             | 用户输入           |
| parsed    | 意图类型和实体识别完成        | Parser 完成      |
| expanded  | 子意图拆分 + Graph 构建完成 | Expander 完成    |
| planned   | 已映射到具体 Goal 和 Plan | Planner 对接     |
| executing | 对应的 Task 正在执行      | Task 创建        |
| completed | 所有子意图完成            | 所有依赖的 Task 完成  |
| archived  | 已归档                | Session 关闭或 GC |

**约束**：

- 最大深度：4 层（防止过度拆分）
- 最大子意图数：8 个/输入
- 并行意图数上限：4 个

#### 事件流

```
InputReceived
  ↓
IntentParsed（{ type, entities, confidence }）
  ↓
IntentExpanded（{ subIntents, graph }）
  ↓
IntentPlanned（{ goals, plans }）
  ↓
IntentExecuting（{ taskIds }）
  ↓
IntentCompleted（{ summary }）
  ↓
IntentArchived
```

**事件数据结构**：

```typescript
event: IntentEvent {
  intentId: record<intent>,
  eventType: 'received' | 'parsed' | 'expanded' | 'planned' | 'executing' | 'completed' | 'archived',
  payload: {
    type?: string,
    entities?: object,
    subIntents?: string[],
    confidence?: number
  },
  timestamp: datetime
}
```

#### 实现方案

**第一阶段（规则 + LLM）**：

```
关键词匹配（"重构"→ refactor）+ LLM Prompt 分类
  ↓
规则引擎处理确定性意图，LLM 处理模糊输入
  ↓
Graph 构建基于预定义规则（顺序、依赖、并行）
```

**第二阶段（Embedding 召回）**：

```
历史意图 Embedding 存入向量库
  ↓
新输入 → Embedding → 相似 Intent 召回
  ↓
基于历史模式改进子意图拆分
  ↓
Graph 结构学习（从历史意图图学习常见模式）
```

**第三阶段（训练意图模型）**：

```
使用历史 Intent 数据训练分类模型
  ↓
Rust 层：
  负责 Graph 构建（高性能 DAG）
  负责意图去重与合并
  ↓
Node 层：
  负责 LLM 调度
  负责 Embedding 查询
  负责状态管理
```

#### 恢复策略

```
Intent Graph 快照（每变更写入）：
  session/{sessionId}/intent_snapshot.json

启动恢复：
  1. 检查 intent_snapshot 是否存在
  2. 如果存在 → 加载 Graph，恢复未完成的 Intent
  3. 如果不存在 → 从头开始
  4. 恢复后验证 Graph 完整性（检测断边、循环依赖）

快照内容：
  { intents: [], edges: [], sessionId, lastEvent, timestamp }
```

**恢复触发条件**：

- Runtime 崩溃重启
- Session 超时后重新进入
- 用户主动要求恢复

#### 风险点

| 风险       | 影响                    | 缓解措施               |
| -------- | --------------------- | ------------------ |
| 过度拆分     | "修 Bug"拆成 20 个 Intent | 最大深度 = 4，子意图 ≤ 8   |
| 误分类      | "重构"误判为"feature"      | 低置信度时请求用户确认        |
| Graph 循环 | Intent 间循环依赖          | DAG 校验，检测到循环时自动打断  |
| 意图漂移     | 执行过程中用户改变想法           | 支持 Intent 暂停 + 重路由 |
| LLM 幻觉   | LLM 意图分类产生虚构意图        | 规则兜底 + 置信度阈值过滤     |

#### 后期扩展

- **多 Agent 共享 Intent**：多个 Agent 并行处理不同子意图，共享同一个 Intent Graph
- **意图预测**：基于用户历史模式预测下一个意图（提前加载资源）
- **跨 Session 意图关联**：多次 Session 的 Intent 关联分析
- **用户习惯建模**：分析用户常见意图模式，优化分类优先级
- **对话中意图演化**：在多轮对话中追踪意图的演化过程（Intent History 的深度集成）

***

### 4.152 Intent History（意图变化追踪）

用户意图会变化，需要追踪演化过程。

```typescript
// table: intent_history
{
  id: record<intent_history>,

  // 关联的意图
  intentId: record<intent>,

  // 上一版本意图
  previous: string,

  // 当前版本意图
  current: string,

  // 变化原因
  reason: string,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
最初: "修 Bug"
后来: "重构系统"
原因: "发现底层设计问题"
```

***

### 4.153 Decision Engine（决策中心）

#### 目标（为什么存在）

统一记录\*\*所有"为什么这么做"\*\*的决策。替代代码中散落的 `if(task.code)`、`if(token>3000)` 等条件判断。每个决策必须可追溯：什么候选方案、为什么选中、基于什么评分。

#### 架构位置（在系统哪一层）

```
Task（任务）
  ↓
Decision Engine（决策引擎）
  ↓
Action（执行动作）
```

属于：认知循环的执行决策层。连接 Task 和 Action 的核心路由。

#### 运行流程

```
Task 到达
  ↓
1. 识别决策类型（model_select / tool_select / agent_select / strategy_select / path_select）
  ↓
2. 生成候选方案列表
  ↓
3. 评分（基于质量/延迟/成本/历史成功率）
  ↓
4. epsilon-greedy 选择（90% 最优 + 10% 探索）
  ↓
5. 记录决策（候选 + 选中 + 原因）
  ↓
6. 执行
  ↓
7. 反馈收集（执行结果 → 更新评分权重）
```

**示例**：

```
Task: "分析项目依赖关系"
  ↓
决策类型: model_select
候选: [GPT-4o, Claude, QwenCoder, DeepSeek]
  ↓
评分:
  GPT-4o:      quality=9, latency=3, cost=2, history=8 → 综合=6.2
  Claude:      quality=9, latency=4, cost=3, history=9 → 综合=6.8  ← 最高
  QwenCoder:   quality=7, latency=8, cost=8, history=5 → 综合=6.6
  DeepSeek:    quality=6, latency=9, cost=9, history=4 → 综合=6.4
  ↓
选中: Claude（综合评分 6.8）
原因: 分析任务需要高质量理解 + 历史表现最佳
```

#### 核心数据结构

```typescript
// table: decision（决策记录）
{
  id: record<decision>,
  taskId: record<task>,
  decisionType: 'model_select' | 'tool_select' | 'agent_select' 
              | 'strategy_select' | 'path_select' | 'resource_alloc',
  candidates: string[],              // 候选方案列表
  selected: string,                  // 选中方案
  confidence: number,                // 决策置信度 0-1
  reason: string,                    // 选择原因（自然语言描述）
  score: number,                     // 选中方案的评分
  exploration: boolean,              // 是否为探索性选择
  executionId?: string,              // 执行结果关联
  timestamp: datetime
}

// table: candidate（候选评分明细）
{
  id: record<candidate>,
  decisionId: record<decision>,
  name: string,                      // 候选名称
  scores: {                          // 各维度评分明细
    quality: number,                 // 质量分 0-10
    latency: number,                 // 延迟分 0-10
    cost: number,                    // 成本分 0-10
    history: number                  // 历史成功率 0-10
  },
  totalScore: number,                // 综合评分
  reason: string,                    // 该候选的评分理由
  createdAt: datetime
}

// table: decision_strategy（评分策略配置）
{
  id: record<decision_strategy>,
  name: string,
  description: string,
  weights: {                         // 评分权重
    quality: number,                 // 默认 0.4
    latency: number,                 // 默认 0.2
    cost: number,                    // 默认 0.2
    history: number                  // 默认 0.2
  },
  epsilon: number,                   // 探索率 0-1，默认 0.1
  isActive: boolean,
  createdAt: datetime,
  updatedAt: datetime
}
```

**评分公式**：

```
综合评分 = quality × Wq + latency × Wl + cost × Wc + history × Wh

默认权重: Wq=0.4, Wl=0.2, Wc=0.2, Wh=0.2

各维度归一化到 0-10 分制：
  quality:  基于任务类型的历史成功率
  latency:  该方案的平均响应时间（慢 → 低分）
  cost:     资源消耗（贵 → 低分）
  history:  该方案在当前场景的历史成功率
```

#### 状态机

```
created ──→ evaluating ──→ selected ──→ executing ──→ completed
                │                           │
                ↓                           ↓
           failed (候选全部不可用)      feedback_collected
                                         ↓
                                     weight_updated
```

| 状态         | 含义         |
| ---------- | ---------- |
| created    | 决策请求已创建    |
| evaluating | 评分计算中      |
| selected   | 已选出最优方案    |
| executing  | 执行中        |
| completed  | 执行完成，反馈已收集 |
| failed     | 无可用候选方案    |

#### 事件流

```
DecisionStarted
  ↓
CandidateScored（每个候选评分后触发）
  ↓
DecisionMade（{ selected, reason, confidence }）
  ↓
DecisionExecuted（执行完成）
  ↓
FeedbackCollected（{ success, latency, cost }）
  ↓
WeightUpdated（根据反馈调整评分权重）
```

**事件数据**：

```typescript
event: DecisionEvent {
  decisionId: record<decision>,
  eventType: 'started' | 'scored' | 'made' | 'executed' | 'feedback',
  payload: object,                   // 事件负载，含决策详情
  timestamp: datetime
}
```

#### 实现方案

**第一阶段（规则引擎）**：

```typescript
// 基于规则的决策
const strategies = {
  model_select: (task, candidates) => {
    if (task.type === 'code') return sortBy(candidates, 'latency');
    if (task.type === 'research') return sortBy(candidates, 'quality');
    return sortBy(candidates, 'cost');
  }
};
```

**第二阶段（统计学习）**：

- 收集历史决策反馈 → 统计各方案在不同任务类型的表现
- 动态调整评分权重
- 基于历史数据的推荐

**第三阶段（强化学习）**：

- 将决策建模为 MDP（Markov Decision Process）
- 状态：任务特征 + 系统负载 + 资源状态
- 动作：选择方案
- 奖励：任务成功率 × 效率系数
- 策略：ε-greedy + Q-learning

#### 恢复策略

```
所有决策写 decision_log（WAL 模式）
  ↓
支持回放：重现"为什么当时选 Claude"
  ↓
Crash 恢复：
  1. 检查未完成的决策（status = evaluating）
  2. 回滚或重试
  3. 标记恢复状态
```

**日志结构**：

```
logs/decisions/{date}/decision_{id}.json
```

#### 风险点

| 风险   | 影响             | 缓解措施                |
| ---- | -------------- | ------------------- |
| 局部最优 | 总选同一个方案，错过更好的  | ε-greedy 探索（默认 10%） |
| 冷启动  | 新方案无历史数据，永远不被选 | 初始评分 = 中位数 + 探索偏置   |
| 权重偏差 | 某个维度权重不合理      | 定期自动校准 + 人工复核       |
| 决策风暴 | 短时间内大量决策       | Governor 限流 + 批量决策  |
| 反馈延迟 | 执行结果回来太晚，权重滞后  | 异步反馈 + 时间衰减因子       |

#### 后期扩展

- **Shadow 模式**：后台并行验证候选方案，不影响线上选择
- **个性化策略**：不同用户/项目使用不同的评分权重
- **多目标优化**：Pareto 前沿替代加权和
- **决策解释器**：自动生成自然语言的决策理由
- **联邦决策**：多 Runtime 实例共享决策统计数据

### 4.154 Decision Graph（决策依赖图）

决策之间互相依赖，需要记录因果关系。

```typescript
// table: decision_edge
{
  id: record<decision_edge>,

  // 源决策
  source: record<decision>,

  // 目标决策（受影响的下一层决策）
  target: record<decision>,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
模型选择（QwenCoder）
  ↓
工具选择（QwenCoder 对应工具链）
  ↓
执行策略（代码生成→审查→合并）
```

***

### 4.155 Uncertainty Engine（不确定性系统）

AI 必须知道"自己不知道什么"，避免装懂。

```typescript
// table: uncertainty
{
  id: record<uncertainty>,

  // 不确定性来源
  source: string,

  // 不确定性评分（0-1，越高越不确定）
  score: number,

  // 不确定的原因
  reason: string,

  // 创建时间
  createdAt: datetime
}
```

**策略**：

| score     | 动作       |
| --------- | -------- |
| < 0.3     | 直接执行     |
| 0.3 - 0.7 | 补充检索后执行  |
| > 0.7     | 暂停并向用户澄清 |

***

### 4.156 Clarification Queue（澄清队列）

高不确定性时，不直接执行，先向用户澄清。

```typescript
// table: clarification
{
  id: record<clarification>,

  // 所属任务
  taskId: record<task>,

  // 需要澄清的问题
  question: string,

  // 优先级
  priority: 'low' | 'medium' | 'high',

  // 状态
  status: 'pending' | 'answered' | 'ignored',

  // 用户的回答
  answer?: string,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
问题: "您希望删除哪个目录？项目中有 src/ 和 dist/"
优先级: high
→ 等待用户确认后再执行
```

***

### 4.157 Assumption Registry（假设注册表）

AI 经常脑补上下文，每条假设必须有记录和验证。

```typescript
// table: assumption
{
  id: record<assumption>,

  // 所属任务
  taskId: record<task>,

  // 假设陈述
  statement: string,

  // 置信度（0-1）
  confidence: number,

  // 是否已验证
  verified: boolean,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
假设: "用户项目使用 React"
置信度: 0.85
验证: 扫描 package.json 确认
```

***

### 4.158 Evidence Registry（证据注册中心）

每个结论都应有证据支撑。

```typescript
// table: evidence
{
  id: record<evidence>,

  // 目标（结论）
  target: string,

  // 证据来源
  source: string,

  // 置信度
  confidence: number,

  // 创建时间
  createdAt: datetime
}
```

**关系**：

```
结论: "用户使用 Zustand 管理状态"
  ↑
证据: "package.json 包含 zustand"
  ↑
证据: "src/store/ 目录存在"
```

***

### 4.159 Truth Maintenance System（真值维护系统）

#### 目标（为什么存在）

解决**旧事实污染**问题。系统记住"用户项目是 React"，后来扫描 `package.json` 发现是 Vue——不能直接覆盖，需要 Truth 依赖链让事实自动失效和重算。

#### 架构位置（在系统哪一层）

```
Observation（工具/模型输出）
  ↓
Evidence（证据提取）
  ↓
Assumption（假设生成）
  ↓
Truth（真值判定）
  ↓
Memory（写入记忆）
```

属于：认知循环的核心验证层。介于 Observation 和 Memory 之间。

#### 运行流程

```
工具/模型输出
  ↓
1. Observation 收集（原始输出）
  ↓
2. Evidence 提取（可信片段 + 置信度）
  ↓
3. Assumption 生成（从证据推断的假设）
  ↓
4. Verifier 验证（交叉验证假设）
  ↓
5. Truth 更新（confirmed → 写入 Truth Graph）
  ↓
6. 依赖传播（依赖此 Truth 的其他 Truth 重新计算）
  ↓
7. 冲突检测（新 Truth 与现有 Truth 矛盾 → 进入 Conflict Queue）
  ↓
8. Memory 写入（经过验证的 Truth 存入 Semantic Memory）
```

**示例流程**：

```
检测到 package.json 包含 "vue": "^3.4.0"
  ↓
Evidence: { source: "package.json", content: "dependencies.vue = ^3.4.0", confidence: 0.95 }
  ↓
Assumption: "项目使用 Vue 3"
  ↓
验证: 扫描 src/ 下 .vue 文件存在，确认
  ↓
Truth: "项目技术栈是 Vue 3"（confidence: 0.95）
  ↓
依赖传播: "项目前端框架" Truth 从 "React" → "Vue 3"
  ↓
旧 Truth "项目是 React" → status: 'invalidated'
```

#### 核心数据结构

```typescript
// table: evidence（证据）
{
  id: record<evidence>,
  source: string,                  // 证据来源（工具名/文件路径/模型输出）
  content: string,                 // 证据内容
  confidence: number,              // 证据置信度 0-1
  checksum: string,                // 内容哈希，用于检测变更
  createdAt: datetime
}

// table: truth（真值）
{
  id: record<truth>,
  statement: string,               // 事实陈述
  confidence: number,              // 综合置信度 0-1
  evidenceIds: string[],           // 支撑证据 ID 列表
  dependencyIds: string[],         // 依赖的其他 Truth ID 列表
  status: 'active' | 'weak' | 'conflict' | 'invalidated',
  createdAt: datetime,
  updatedAt: datetime
}

// table: truth_edge（真值依赖图）
{
  id: record<truth_edge>,
  source: record<truth>,           // 源 Truth
  target: record<truth>,           // 依赖的 Truth
  relation: 'supports' | 'contradicts' | 'depends_on',
  createdAt: datetime
}
```

**依赖示例**：

```
Truth A: "项目是 React"
  依赖 Evidence 1: package.json 内容
  依赖 Truth B: "项目前端框架"
  
如果 Evidence 1 变更 → Truth A 自动重新计算
如果 Truth B 失效 → Truth A 自动标记 weak
```

#### 状态机

```
      ┌────────────────────────────────────┐
      ↓                                    │
  active ──→ weak ──→ conflict ──→ invalidated
      ↑          │                       │
      └──────────┘                       │
      证据增强后恢复                       │
                                         ↓
                                     archived
```

| 状态          | 含义     | 触发条件                |
| ----------- | ------ | ------------------- |
| active      | 事实当前有效 | 证据充分且无矛盾            |
| weak        | 置信度下降  | 部分证据失效或依赖的 Truth 变更 |
| conflict    | 存在矛盾事实 | 新证据与现有 Truth 冲突     |
| invalidated | 已否定    | 证据链断裂或被反证           |
| archived    | 已归档    | invalidated 超过保留期   |

#### 事件流

```
EvidenceAdded
  ↓
TruthCreated / TruthUpdated
  ↓
DependencyPropagated（依赖传播触发级联重算）
  ↓
ConflictDetected（矛盾事实进入解决队列）
  ↓
ConflictResolved（通过用户确认或证据权重裁决）
  ↓
TruthInvalidated（事实被否定）
```

**事件数据结构**：

```typescript
// 核心事件
event: TruthMaintenanceEvent {
  eventType: 'evidence_added' | 'truth_updated' | 'conflict_detected' | 'conflict_resolved',
  truthId: record<truth>,
  trigger: string,                  // 触发源
  previousState: string,            // 变更前状态
  newState: string,                 // 变更后状态
  timestamp: datetime
}
```

#### 实现方案

**第一阶段（规则引擎）**：

- 硬编码依赖规则（如 `package.json → project_type`）
- 基于关键词的冲突检测
- 简单的证据权重比较

**第二阶段（SurrealDB 图关系）**：

```sql
-- 使用 SurrealDB 的 RELATION 类型建立 Truth 依赖图
RELATE truth->supports->evidence;
RELATE truth->depends_on->truth;
RELATE truth->contradicts->truth;

-- 查询依赖链
SELECT * FROM truth WHERE dependencyIds CONTAINS $targetTruthId;
```

**第三阶段（自动化）**：

- 自动证据提取（从工具输出中识别声明性内容）
- 置信度传播算法（基于证据链的概率推导）
- 自动冲突解决（按证据权重 + 时间戳 + 来源可信度）

#### 恢复策略

```
每晚 Truth 快照：
  truth_snapshot_{date}.json
  
启动恢复流程：
  1. 加载最新快照
  2. 重放当天的 TruthMaintenanceEvent
  3. 增量重算（标记依赖链有变更的 Truth）
  4. 避免全量重算

快照内容：
  { evidence, truth, truth_edge } 三表的全量或增量导出
```

**恢复点目标 (RPO)**：24 小时（每晚快照）
**恢复时间目标 (RTO)**：< 5 分钟（增量重算）

#### 风险点

| 风险    | 影响                      | 缓解措施                      |
| ----- | ----------------------- | ------------------------- |
| 依赖爆炸  | Truth 之间互相依赖，形成循环或指数级传播 | 最大深度 = 5，最大依赖数 = 50       |
| 级联失效  | 一个证据变更触发雪崩式重算           | 异步重算 + 优先级队列              |
| 置信度通胀 | 长依赖链导致置信度虚高             | 置信度 = 各环节置信度乘积            |
| 冲突死锁  | 两个 Truth 互相矛盾且权重相同      | 用户裁决 + 超时默认更保守的一方         |
| 无限回溯  | 依赖链断裂后不断尝试修复            | 超过 3 次重试 → 标记为 unresolved |

#### 后期扩展

- **Belief Graph + Knowledge Graph 融合**：Truth 不仅依赖 Evidence，还可引用 KG 实体
- **概率 Truth**：支持概率陈述（"80% 可能是 Python 项目"）
- **时序 Truth**：Truth 带时间范围（"2026-01 之前是 React，之后是 Vue"）
- **分布式 Truth**：多 Runtime 实例共享 Truth Graph
- **Truth 进化学习**：从历史 conflict 解决模式中学习，自动改进冲突解决策略

### 4.161 Memory GC（记忆垃圾回收）

后台自动清理低价值、低命中、过期的记忆。

```typescript
// table: memory_gc
{
  id: record<memory_gc>,

  // 被清理的候选
  candidate: string,

  // 清理评分（越高越该清理）
  score: number,

  // 清理策略
  strategy: 'low_importance' | 'low_access' | 'expired' | 'duplicate',

  // 是否已执行
  executed: boolean,

  // 创建时间
  createdAt: datetime
}
```

**候选条件**：

- importance < 0.2 且 30 天未访问
- 有更新的版本
- 与其他记忆重复度 > 0.95

***

### 4.162 Runtime GC（运行时垃圾回收）

不仅是记忆，还包括缓存、工作记忆、队列、快照。

```typescript
// table: runtime_gc
{
  id: record<runtime_gc>,

  // 回收目标类型
  entityType: 'cache' | 'working_memory' | 'queue' | 'snapshot' | 'temp_file',

  // 回收策略
  strategy: 'lru' | 'ttl' | 'size_limit' | 'mark_sweep',

  // 回收时间
  collectedAt: datetime
}
```

**回收规则**：

- 缓存：超过 1 小时未命中 → 清理
- 工作记忆：任务结束 → 清理
- 队列：超过最大长度 → 截断
- 快照：保留最近 5 个，其余清理

***

### 4.163 Context Compression（上下文压缩）

长会话不能无限 summarize，需要多级压缩。

```typescript
// table: context_compression
{
  id: record<context_compression>,

  // 压缩来源
  source: string,

  // 压缩方法
  method: 'summary' | 'semantic' | 'hierarchical',

  // 压缩率（压缩后/压缩前）
  ratio: number,

  // 创建时间
  createdAt: datetime
}
```

**压缩策略**：

| 方法           | 压缩率  | 适用     |
| ------------ | ---- | ------ |
| summary      | 10:1 | 一般对话摘要 |
| semantic     | 20:1 | 语义保留压缩 |
| hierarchical | 50:1 | 多层结构压缩 |

***

### 4.164 Hierarchical Memory（层级记忆）

记忆不全部平铺，按层级存储和检索。

```typescript
// table: memory_layer
{
  id: record<memory_layer>,

  // 层级
  layer: 'working' | 'episodic' | 'semantic' | 'archive',

  // 层级描述
  description: string,

  // 最大容量
  maxCapacity: number,

  // 当前使用量
  currentUsage: number,

  // 创建时间
  createdAt: datetime
}
```

**层级结构**：

```
Working Memory: 当前任务，最大 10 条，任务结束清理
  ↓ consolidation
Episodic Memory: 事件记录，最大 1000 条，FIFO
  ↓ consolidation
Semantic Memory: 提炼事实，最大 500 条，按置信度保留
  ↓ archival
Archive: 永久存储，按时间分区
```

***

### 4.165 Governor（资源总调度）

#### 目标（为什么存在）

避免**系统自己拖死自己**。Agent 越来越多，Embedding、搜索、模型、工具、Browser 都在抢资源——CPU、内存、Token、带宽。没有 Governor，系统会在高负载下崩溃。

#### 架构位置（在系统哪一层）

```
Governor（资源总调度） ← 最顶层
  ↓
Runtime（运行时）
  ↓
Agent Network（Agent 网络）
  ↓
Tool / Model / Embedding（执行层）
```

属于：Runtime 最顶层的**资源监管层**。所有组件在 Governor 之下运行。

#### 运行流程

```
实时监控（每 100ms 采集一次指标）
  ↓
1. 采集 CPU / 内存 / Token / 延迟 / 活跃 Agent
  ↓
2. 计算综合负载评分
  ↓
3. 匹配策略（判断是否超过阈值）
  ↓
4. 执行策略动作（降级 / 暂停 / 限流 / 报警）
  ↓
5. 观察效果（策略执行后指标变化）
  ↓
6. 自适应调整（如果降级不够 → 继续降级）
```

#### 核心数据结构

```typescript
// table: governor（运行时状态）
{
  id: record<governor>,
  cpu: number,                       // CPU 使用率 0-100
  memory: number,                    // 内存使用率 0-100
  tokenBudget: number,               // Token 总预算
  tokenUsed: number,                 // 已消耗 Token
  tokenRemaining: number,            // 剩余 Token
  latency: number,                   // 当前平均延迟（ms）
  activeAgents: number,              // 活跃 Agent 数
  activeTools: number,               // 活跃 Tool 数
  mode: 'performance' | 'balanced' | 'economy' | 'emergency',
  loadLevel: 'normal' | 'warning' | 'critical' | 'overload',
  updatedAt: datetime
}

// table: governor_policy（调控策略）
{
  id: record<governor_policy>,
  name: string,                      // 策略名
  condition: string,                 // 触发条件（表达式）
  action: string,                    // 执行动作
  priority: number,                  // 优先级（冲突时高优先胜出）
  cooldown: number,                  // 冷却时间（ms），防止频繁触达
  isActive: boolean,
  createdAt: datetime,
  updatedAt: datetime
}
```

**预置策略表**：

| 条件              | 动作              | 优先级 | 冷却   |
| --------------- | --------------- | --- | ---- |
| CPU > 80%       | 暂停 Embedding 任务 | 50  | 30s  |
| CPU > 90%       | 限制新 Agent 启动    | 70  | 10s  |
| 内存 > 85%        | 触发 Runtime GC   | 60  | 60s  |
| Token 日消耗 > 80% | 切换低成本模型         | 40  | 300s |
| 延迟 > 5s         | 降级为快速模型         | 50  | 60s  |
| 延迟 > 10s        | 暂停非关键 Tool      | 80  | 30s  |
| 电池模式            | 自动 economy 降级   | 90  | —    |
| 活跃 Agent > 20   | 关停低优先级 Agent    | 60  | 60s  |

**负载分级**：

| 级别       | CPU    | 内存     | 含义 | 行为      |
| -------- | ------ | ------ | -- | ------- |
| normal   | < 60%  | < 70%  | 正常 | 全速运行    |
| warning  | 60-80% | 70-85% | 注意 | 暂停非关键任务 |
| critical | 80-95% | 85-95% | 危险 | 强制降级    |
| overload | > 95%  | > 95%  | 过载 | 仅保留核心功能 |

#### 状态机

```
normal
  ↓（负载上升）
warning
  ↓（负载继续上升）
critical
  ↓（负载超过阈值）
overload
  ↑
  │（负载下降后自动恢复）
  └─── 逐级恢复：overload → critical → warning → normal
```

#### 事件流

```
ResourceUpdated（每 100ms，{ cpu, memory, token }）
  ↓
ThresholdReached（{ metric, value, threshold }）
  ↓
PolicyTriggered（{ policy, action, reason }）
  ↓
PolicyExecuted（{ action, result }）
  ↓
ModeChanged（{ from, to, reason }）
  ↓
ResourceNormalized（{ metric, value }）— 负载恢复
```

#### 实现方案

**第一阶段（规则引擎）**：

```typescript
// 硬编码规则
if (cpu > 80) pauseEmbedding();
if (memory > 85) triggerGC();
if (latency > 5000) switchToFastModel();
```

**第二阶段（预测调控）**：

```
采集历史指标 → 训练预测模型
预测未来 30 秒的负载变化
提前执行策略（而不是等超限了再处理）
```

**第三阶段（强化学习）**：

```
状态: { cpu, memory, token, activeAgents, latency }
动作: { 降级/限流/暂停/切换模型 }
奖励: 负载稳定 + 任务完成率 - 降级惩罚
策略: Deep Q-Network
```

#### 恢复策略

```
Governor 状态每秒快照：
  governor_snapshot_{runtimeId}.json

崩溃恢复：
  1. 加载最近快照
  2. 恢复 last mode 和 loadLevel
  3. 如果有 policy 正在执行 → 继续执行
  4. 如果没有快照 → 从 normal 模式冷启动

快照内容（极小，< 1KB）：
  { mode, loadLevel, cpu, memory, token, activePolicies, timestamp }
```

#### 风险点

| 风险   | 影响                                  | 缓解措施                      |
| ---- | ----------------------------------- | ------------------------- |
| 策略打架 | PolicyA: 关闭 Agent；PolicyB: 启动 Agent | 优先级树裁决，同 priority 采用更保守策略 |
| 震荡   | 频繁降级→恢复→降级                          | cooldown 机制 + 滞回区间        |
| 过度降级 | 负载只有 60%，却降级到 economy               | 降级前验证必要性                  |
| 反应滞后 | 负载飙升后才触发策略                          | 预测模式提前 30s 预警             |
| 单点故障 | Governor 本身崩溃                       | 无状态 + 快速恢复                |
| 测量不准 | CPU 采样误差                            | 滑动窗口平均 + 多采样源交叉验证         |

**优先级树设计**：

```
Governor 策略冲突解决树：

1. 安全相关（电池、温度）→ 最高优先级
2. 系统稳定（CPU、内存过载）→ 高优先级
3. 成本控制（Token 超预算）→ 中优先级
4. 性能优化（延迟抖动）→ 低优先级

同级冲突 → 取 action 更保守的一方
```

#### 后期扩展

- **跨机器调度**：多台机器共享 Governor，协调全局资源
- **成本优化引擎**：根据 Token 价格动态调整模型选择
- **能效模式**：接入系统电源管理（Windows/macOS API）
- **SLO 驱动**：根据服务等级协议自动分配资源
- **联邦 Governor**：多 Runtime 实例的 Governor 组成联邦

***

### 4.167 Cognitive Load（认知负载）

AI 系统的"忙不忙"状态。

```typescript
// table: cognitive_load
{
  id: record<cognitive_load>,

  // 活跃任务数
  activeTasks: number,

  // 上下文总大小
  contextSize: number,

  // 综合负载评分（0-100）
  score: number,

  // 更新时间
  updatedAt: datetime
}
```

**负载管理**：

- score > 80 → 暂停副 Agent 协作
- score > 90 → 拒绝新任务，直到负载降低

***

### 4.168 Behavior Mode（行为模式）

系统风格调节——不是情绪，是风险偏好。

```typescript
// table: behavior_mode
{
  id: record<behavior_mode>,

  // 好奇度（0-1，倾向探索新方案还是走老路）
  curiosity: number,

  // 谨慎度（0-1，倾向确认还是直接执行）
  caution: number,

  // 坚持度（0-1，失败后倾向重试还是放弃）
  persistence: number,

  // 更新时间
  updatedAt: datetime
}
```

**模式预设**：

| 模式 | curiosity | caution | persistence |
| -- | --------- | ------- | ----------- |
| 探索 | 0.9       | 0.3     | 0.7         |
| 保守 | 0.3       | 0.9     | 0.5         |
| 平衡 | 0.6       | 0.6     | 0.6         |
| 快速 | 0.4       | 0.2     | 0.3         |

***

### 4.169 Personality Kernel（人格内核）

系统运行风格，与 Prompt 分离避免人格漂移。

```typescript
// table: personality
{
  id: record<personality>,

  // 原则列表
  principles: string[],

  // 风格偏好
  style: 'conservative' | 'balanced' | 'exploratory',

  // 硬性约束
  constraints: string[],

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
原则: ["准确性优先", "默认可解释"]
风格: conservative
约束: ["不使用未授权外部模型", "不改系统文件"]
```

***

### 4.170 Meta Memory（元记忆）

系统对自己记忆能力的认知和优化。

```typescript
// table: meta_memory
{
  id: record<meta_memory>,

  // 记忆策略
  strategy: 'semantic' | 'episodic' | 'hybrid',

  // 有效性评分（0-1）
  effectiveness: number,

  // 命中率
  hitRate: number,

  // 更新时间
  updatedAt: datetime
}
```

**自优化**：

- 近期 semantic 效果差 → 自动切换到 hybrid
- episodic 命中率高 → 增加 episodic 权重
- 定期评估各策略并调整

***

### 4.171 Runtime DNA（运行时基因）

系统的元信息——版本、能力、原则，像基因一样携带系统本质特征。

```typescript
// table: runtime_dna
{
  id: record<runtime_dna>,

  // 架构版本
  architectureVersion: string,

  // 核心原则（来自 Architecture Constitution）
  principles: string[],

  // 能力清单
  capabilities: string[],

  // 创建时间
  createdAt: datetime,

  // 更新时间
  updatedAt: datetime
}
```

**作用**：

- 系统迁移：通过 DNA 在新环境重建
- 复制：克隆 DNA 到另一个实例
- 诊断：对比运行状态与 DNA 是否一致
- 进化追踪：DNA 版本变化反映系统演变

***

### 4.172 Perception Pipeline（感知流水线）

输入不只是一段文本，系统需要从输入中提取实体和信号。

```
Input
  ↓
Observation
  ↓
Entity Extraction
  ↓
Signal Analysis
  ↓
Attention
  ↓
Intent
```

```typescript
// table: perception
{
  id: record<perception>,

  // 来源类型
  sourceType: 'user' | 'file' | 'tool' | 'workspace' | 'system',

  // 原始内容
  rawContent: string,

  // 提取的实体
  entities: string[],

  // 信号标签
  signals: string[],   // 如 ['urgent', 'bug', 'code_task']

  // 显著性评分（0-1）
  salience: number,

  // 创建时间
  createdAt: datetime
}
```

**示例**：用户说"帮我修登录问题"→ entities: \[Auth, Login, Error]，signals: \[urgent, bug]

***

### 4.173 Attention Controller（注意力控制器）

#### 目标（为什么存在）

长期运行后：知识 1000 条、记忆 200 条、文件 2 万个、Observation 数不清——LLM 不可能全看。Attention Controller 决定**现在该看什么**，防止系统被信息淹没。

#### 架构位置（在系统哪一层）

```
Perception（感知，原始信息流入）
  ↓
Attention Controller（注意力控制器） ← 信息过滤器
  ↓
Intent Engine（意图引擎）
  ↓
Decision Engine（决策引擎）
```

属于：Perception 层和 Intent 层之间的**信息过滤器**。所有输入必须先通过注意力筛选。

#### 运行流程

```
信息流入（Observation / Memory / File / Event）
  ↓
1. 提取元数据（来源、时间、类型、关联实体）
  ↓
2. 计算 Attention Score（四维评分）
  ↓
3. 排序（按 score 降序）
  ↓
4. Top-K 选择（取最高分的 N 个）
  ↓
5. 送入 Intent Engine / Working Memory
  ↓
6. 过期（TTL 到期后自动移除）
```

**评分公式**：

```
Attention Score = importance × 0.3 
                + recency × 0.2 
                + goal_relevance × 0.3 
                + risk × 0.2

各维度归一化到 0-100：
  importance:     内容重要性（基于实体和关键词）
  recency:        时间衰减（越新越高）
  goal_relevance: 与当前 Goal 的相关度
  risk:           风险评估（高风险内容优先注意）
```

**示例评分**：

```
用户刚上传 "数据库设计.pdf"
  importance: 95（用户主动上传）
  recency:    100（刚刚发生）
  goal:       80（当前目标是"设计架构"）
  risk:       20（风险低）
  总分:       82 ← 高注意

三个月前的 Session 记录
  importance: 40
  recency:    5（严重衰减）
  goal:       30
  risk:       10
  总分:       7 ← 忽略
```

#### 核心数据结构

```typescript
// table: attention（注意力记录）
{
  id: record<attention>,
  sourceType: 'observation' | 'memory' | 'file' | 'event' | 'message',
  sourceId: string,                  // 来源 ID
  score: number,                     // 注意力分数 0-100
  dimensions: {                      // 分项评分
    importance: number,
    recency: number,
    goalRelevance: number,
    risk: number
  },
  reason: string,                    // 得分简要原因
  ttl: number,                       // 有效时间（秒）
  status: 'active' | 'expired',
  createdAt: datetime,
  updatedAt: datetime
}

// table: attention_window（注意力窗口配置）
{
  id: record<attention_window>,
  maxSize: number,                   // 最大同时关注数，默认 5000
  topK: number,                      // 每次选 Top-K，默认 50
  decayRate: number,                 // 衰减率（每小时），默认 0.1
  strategy: 'score_only' | 'diversified' | 'hybrid',
  createdAt: datetime,
  updatedAt: datetime
}
```

**Recency 衰减算法**：

```
score(t) = initial_score × e^(-λ × Δt)
λ = decayRate（默认 0.1/小时）

1 小时后: score × 0.90
6 小时后: score × 0.55
24 小时后: score × 0.09
```

#### 状态机

```
created（信息进入）
  ↓
scored（评分完成）
  ↓
selected（被选入 Top-K）
  ↓
active（正在被使用）
  ↓
expired（TTL 到期或被新信息替换）
```

| 状态       | 含义             | 触发                 |
| -------- | -------------- | ------------------ |
| created  | 新信息到达          | 任何输入               |
| scored   | 评分完成           | 评分计算结束             |
| selected | 进入 Top-K 窗口    | 排序完成               |
| active   | 正被 LLM/Task 消费 | 被 Intent Engine 引用 |
| expired  | 注意力到期          | TTL 结束 / 被替换       |

#### 事件流

```
ObservationAdded（新信息到达）
  ↓
AttentionCalculated（{ sourceId, score, dimensions }）
  ↓
AttentionSelected（Top-K 列表更新）
  ↓
AttentionConsumed（{ sourceId, consumer }）
  ↓
AttentionDecayed（{ sourceId, oldScore, newScore }）
  ↓
AttentionExpired（{ sourceId, reason }）
```

#### 实现方案

**第一阶段（规则评分）**：

```typescript
// 简单规则
const score = {
  importance: keywordBasedImportance(content),
  recency: timeDecay(createdAt),
  goalRelevance: keywordMatch(content, currentGoal),
  risk: riskScore(content)
};
```

**第二阶段（Embedding 相似度）**：

```
当前上下文 + Goal → Embedding
每个候选项 → Embedding
相似度 → goal_relevance 分数
```

**第三阶段（Transformer Rerank）**：

```
Cross-Encoder Reranker
输入: { 当前上下文, 候选项 }
输出: 相关性评分
精度高但速度慢 → 只对 Top-200 做 rerank
```

**Rust 层**：

```rust
// Priority Queue 实现 O(log n) 的注意力管理
struct AttentionQueue {
    heap: BinaryHeap<AttentionItem>,  // 按 score 排序
    window: Vec<AttentionItem>,       // 当前活跃窗口
    decay_scheduler: DecayScheduler,  // 衰减调度器
}
```

#### 恢复策略

```
注意力不存长期。只保存最近窗口。
  ↓
自动 GC：expired 记录每小时清理一次
  ↓
崩溃恢复：从 Event Bus 重建最近窗口（最后 1000 条）
  ↓
无持久化要求——注意力是临时状态
```

#### 风险点

| 风险    | 影响              | 缓解措施                       |
| ----- | --------------- | -------------------------- |
| 热点垄断  | 最新内容一直占满注意力窗口   | attention\_decay + 多样性策略   |
| 信号丢失  | 重要但低频的信息被忽略     | 风险维度兜底（高风险必进）              |
| 震荡    | 评分相近时频繁切换 Top-K | 稳定窗口（min\_stability\_time） |
| 注意力漂移 | 持续关注错误方向        | goal\_relevance 锚定当前目标     |
| 计算开销  | 大量信息频繁评分        | 分层评分：先轻后重                  |

**多样性策略**（防止热点垄断）：

```
不只看 score，还要保证窗口内：
  - 最多 30% 来自同一 sourceType
  - 最多 20% 来自同一 task
  - 至少 10% 来自长期记忆（防止遗忘）
```

#### 后期扩展

- **多 Agent 共享注意力**：多个 Agent 共享一个 Attention Window，避免重复计算
- **注意力历史分析**：分析系统过去的注意力分配，优化 attention 参数
- **主动注意力**：系统主动搜索"可能被忽略但重要"的信息
- **分层注意力**：不同粒度的注意力层（粗筛 → 精排）
- **元注意力**：系统反思"我的注意力分配合理吗"

***

### 4.174 Executive Controller（执行控制器）

#### 目标（为什么存在）

解决**多个任务打架**的问题。Agent 越来越多，Task 并发越来越高，谁先执行、谁等待、谁暂停——必须有一个中央执行控制器来协调。这是系统的**任务大脑**。

#### 架构位置（在系统哪一层）

```
Goal（目标）
  ↓
Plan（规划）
  ↓
Task（任务）
  ↓
Executive Controller（执行控制器） ← 任务大脑
  ↓
Runtime（运行时）
  ↓
Agent / Tool（执行单元）
```

属于：Task 层和 Runtime 层之间的**调度中枢**。所有 Task 必须先经过 Executive Controller 才能执行。

#### 运行流程

```
Task 创建
  ↓
1. 优先级评定（critical / high / medium / low）
  ↓
2. 资源检查（所需资源是否可用）
  ↓
3. 依赖检查（前置 Task 是否完成）
  ↓
4. 入队（插入 Priority Queue）
  ↓
5. 调度（根据策略选择下一个 Task）
  ↓
6. 执行（委派给 Agent 或 Tool）
  ↓
7. 监控（执行状态、资源占用、超时）
  ↓
8. 完成 / 暂停 / 取消
```

**调度策略**：

| 策略          | 适用场景   | 行为         |
| ----------- | ------ | ---------- |
| FIFO        | 简单顺序任务 | 先来先执行      |
| Priority    | 有明确优先级 | 高优先执行，支持抢占 |
| Round Robin | 公平分配   | 轮流执行，防止饥饿  |
| Weighted    | 混合负载   | 按权重分配时间片   |

**Aging 机制**（防止饥饿）：

```
低优任务等待时间每增加 30 秒 → priority 提升一级
确保所有任务最终都能被执行
```

#### 核心数据结构

```typescript
// table: executive_control（执行控制）
{
  id: record<executive_control>,
  taskId: record<task>,

  // 优先级（支持 aging 动态提升）
  priority: 'low' | 'medium' | 'high' | 'critical',
  basePriority: 'low' | 'medium' | 'high' | 'critical',  // 初始优先级
  agingCount: number,               // 等待时间提升次数

  // 执行状态
  state: 'queued' | 'running' | 'paused' | 'waiting' | 'completed' | 'cancelled',

  // 执行约束
  constraints: {
    maxDuration?: number,            // 最大执行时间（ms）
    requiredResources?: string[],    // 所需资源列表
    dependsOn?: string[],            // 依赖的 taskId 列表
    requiredCapability?: string[]    // 所需能力
  },

  // 执行者
  owner: string,                     // 负责的 Agent / Tool

  // 调度信息
  enqueuedAt: datetime,
  startedAt?: datetime,
  completedAt?: datetime,
  updatedAt: datetime
}

// table: execution_schedule（调度日志）
{
  id: record<execution_schedule>,
  controllerId: record<executive_control>,
  event: 'queued' | 'dequeued' | 'started' | 'paused' | 'resumed' | 'cancelled' | 'completed',
  reason: string,                    // 调度原因
  timestamp: datetime
}
```

**Priority Queue 实现**（Rust）：

```rust
// Rust 层实现 O(log n) 优先队列
struct ExecutiveQueue {
    queue: BinaryHeap<ScheduledTask>,  // 按 priority + aging 排序
    running: HashMap<TaskId, TaskState>,
    resources: ResourcePool,
}
```

#### 状态机

```
                    ┌──────────┐
                    ↓          │
queued → running → paused ────┘
  │        │                   
  │        ├──→ completed      
  │        └──→ cancelled      
  ↓                            
waiting ──→ running（依赖就绪后）
```

| 状态        | 含义           |
| --------- | ------------ |
| queued    | 排队等待执行       |
| running   | 正在执行         |
| paused    | 暂停（资源抢占或被中断） |
| waiting   | 等待依赖完成       |
| completed | 正常完成         |
| cancelled | 被取消          |

#### 事件流

```
TaskCreated → TaskSubmitted
  ↓
TaskQueued（{ taskId, priority, position }）
  ↓
TaskDequeued（{ taskId, strategy }）
  ↓
TaskStarted（{ taskId, owner }）
  ↓
TaskPaused（{ taskId, reason }） → TaskResumed（{ taskId }）
  ↓
TaskWaiting（{ taskId, dependsOn }） → TaskResumed
  ↓
TaskCompleted（{ taskId, result }）
  ↓
TaskCancelled（{ taskId, reason }）
```

**关键事件数据**：

```typescript
event: ExecutiveEvent {
  controllerId: record<executive_control>,
  eventType: 'queued' | 'started' | 'paused' | 'completed' | 'cancelled',
  payload: {
    task: string,
    owner?: string,
    reason?: string,
    resourceUsage?: object
  },
  timestamp: datetime
}
```

#### 实现方案

**第一阶段（规则调度）**：

```typescript
// 简单规则调度
const scheduler = {
  next: (queue) => {
    // 优先 critical
    // 其次高优
    // 检查 aging
    // FIFO 兜底
  }
};
```

**第二阶段（加权调度）**：

```typescript
// 加权优先级 + 资源感知
score = priority_weight × 0.4 
      + wait_time_weight × 0.3 
      + resource_available × 0.3
```

**第三阶段（自适应调度）**：

- 历史执行数据 → 预测 Task 耗时
- 动态调整时间片分配
- 机器学习调度参数

**Rust 层**：

- Priority Queue（BinaryHeap O(log n)）
- 资源池管理
- 执行快照与恢复

#### 恢复策略

```
执行快照（每秒写入）：
  task_snapshot_{runtimeId}.json

启动恢复：
  1. 加载最新快照
  2. 恢复所有 running → queued（重新调度）
  3. 恢复 paused → queued
  4. completed 保持 completed
  5. 验证依赖完整性

快照内容：
  { tasks: [], queue: [], running: [], resources: {} }
```

**崩溃恢复行为**：

- running 状态的 Task → 重新 queued（允许重试）
- paused 状态的 Task → 保持 paused（人工恢复）
- 正在写快照时崩溃 → 加载上一个完整快照

#### 风险点

| 风险    | 影响               | 缓解措施                  |
| ----- | ---------------- | --------------------- |
| 饥饿    | 低优任务永远执行不到       | Aging 机制，等待超 30s 自动提级 |
| 死锁    | Task A 等 B，B 等 A | 依赖图检测 + 超时打断          |
| 资源抢占  | 高优任务抢了低优的锁       | 资源粒度的抢占式调度            |
| 调度风暴  | 高优任务不断插入，低优全饿死   | aging 优先级 > 初始优先级     |
| 状态不一致 | 快照与实际状态不符        | 每步操作先写日志再执行           |

#### 后期扩展

- **分布式调度**：多 Runtime 实例共享一个调度队列
- **预测调度**：基于历史数据预测 Task 耗时，优化排布
- **SLA 调度**：不同 Task 有不同延迟要求，调度器自动满足
- **预算感知**：结合 Governor 的 Token/CPU 预算做调度决策
- **优先级继承**：低优 Task 持有高优 Task 需要的锁 → 临时提升优先级（防优先级反转）

***

### 4.175 Interrupt System（中断系统）

任务运行到一半用户插话："先停，改这个"——没有中断系统必炸。

```typescript
// table: interrupt
{
  id: record<interrupt>,

  // 被中断的任务
  taskId: record<task>,

  // 中断原因
  reason: string,

  // 优先级
  priority: 'low' | 'medium' | 'high' | 'critical',

  // 中断动作
  action: 'pause' | 'resume' | 'cancel' | 'preempt',

  // 状态
  status: 'requested' | 'processed' | 'completed',

  // 创建时间
  createdAt: datetime
}
```

**中断类型**：

| action  | 说明         |
| ------- | ---------- |
| pause   | 暂停当前任务，可恢复 |
| resume  | 恢复已暂停的任务   |
| cancel  | 取消当前任务     |
| preempt | 更高优先级任务抢占  |

***

### 4.176 Cognitive Stack（认知栈）

Agent 需要有调用栈，像程序一样支持回退。

```typescript
// table: cognitive_stack
{
  id: record<cognitive_stack>,

  // 任务
  taskId: record<task>,

  // 当前帧
  frame: string,

  // 父帧
  parentFrame?: string,

  // 状态
  state: 'active' | 'suspended' | 'completed' | 'failed',

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
目标: 重构项目
  ↓ frame: 分析
  ↓ frame: 扫描代码
  ↓ frame: 调用工具（失败 → 回退到分析）
```

***

### 4.177 Learning Loop（学习闭环）

#### 目标（为什么存在）

让系统**越来越聪明**，而不是做完就忘。每次任务执行后，系统必须反思：哪里做错了、哪里浪费了、哪里可以优化，将经验沉淀为可复用的策略。

#### 架构位置（在系统哪一层）

```
Action（执行）
  ↓
Result（结果）
  ↓
Reflection（反思）
  ↓
Learning Loop（学习闭环）
  ↓
Strategy Update（策略更新）
  ↓
Knowledge Base（知识库沉淀）
```

属于：认知循环的**成长引擎**。位于执行链末端，连接反馈与未来策略。

#### 运行流程

```
Task 完成
  ↓
1. 收集执行结果（success/fail + 指标）
  ↓
2. 生成 Reflection（提示 LLM 反思）
  ↓
3. 提取 Lesson（具体可复用的经验）
  ↓
4. 评分（lesson_score 过滤低质经验）
  ↓
5. 匹配已有 Lesson（去重或合并）
  ↓
6. 存储到 Knowledge Base
  ↓
7. 更新相关策略（如果 lesson 置信度高）
  ↓
8. 通知其他 Agent（跨 Agent 学习）
```

**Reflection Prompt 示例**：

```
任务: {task}
结果: {result}
---
请反思：
1. 哪里做错了？为什么？
2. 哪里浪费了资源/时间？
3. 哪里可以优化？
4. 下次遇到类似情况应该怎么做？
---
输出格式：{ issue, rootCause, solution, actionable }
```

**示例**：

```
任务: "分析项目依赖关系"
结果: 超时 120s，使用了 Claude

Reflection:
  - 问题: 代码量太大，单次 LLM 调用超时
  - 根因: 没有做文件分片
  - 优化: 超过 50 个文件应先分片再分析

Lesson:
  - issue: "大项目依赖分析超时"
  - solution: "文件 > 50 个时自动分片，每片 ≤ 30 文件"
  - confidence: 0.85
  → 更新策略: project_analysis 策略加入分片规则
```

#### 核心数据结构

```typescript
// table: lesson（学习记录）
{
  id: record<lesson>,
  source: string,                   // 学习来源（taskId / reflectionId）
  issue: string,                    // 问题描述
  rootCause: string,                // 根因分析
  solution: string,                 // 解决方案（可执行）
  category: 'optimization' | 'bugfix' | 'process' | 'resource',
  confidence: number,               // 置信度 0-1
  score: number,                    // 综合评分（用于淘汰）
  applied: boolean,                 // 是否已应用到策略
  applyCount: number,               // 被应用的次数
  effectiveness: number,            // 应用后的效果评分
  tags: string[],                   // 标签（便于检索）
  createdAt: datetime,
  updatedAt: datetime
}

// table: strategy_update（策略更新记录）
{
  id: record<strategy_update>,
  lessonId: record<lesson>,
  targetStrategy: string,           // 更新的策略名
  change: string,                   // 变更内容
  status: 'proposed' | 'applied' | 'rolled_back',
  effectiveness: number,            // 变更后效果
  createdAt: datetime
}
```

#### 状态机

```
task_completed
  ↓
reflecting ──→ lesson_extracted ──→ scored
                  ↓                      ↓
             low_score → discarded    high_score → matching
                                                   ↓
                                              de-duplicated
                                                   ↓
                                              strategy_updated
                                                   ↓
                                              effectiveness_evaluated
```

| 状态                       | 含义          |
| ------------------------ | ----------- |
| task\_completed          | 任务执行结束，触发学习 |
| reflecting               | LLM 正在生成反思  |
| lesson\_extracted        | 从反思中提取了具体经验 |
| scored                   | 经验已评分       |
| discarded                | 低分经验丢弃      |
| strategy\_updated        | 经验已应用到策略    |
| effectiveness\_evaluated | 已评估应用效果     |

#### 事件流

```
TaskCompleted
  ↓
ReflectionGenerated（{ reflection }）
  ↓
LessonExtracted（{ issue, solution, confidence }）
  ↓
LessonScored（{ score, action: 'store' | 'discard' }）
  ↓
LessonStored（{ lessonId }）
  ↓
StrategyUpdated（{ strategy, change }）
  ↓
EffectivenessEvaluated（{ effectiveness }）
```

#### 实现方案

**第一阶段（Prompt 基础版）**：

```
每次任务完成 → 调用 LLM 生成 Reflection
→ 提取 Lesson → 存储到 Knowledge Base
→ 人工确认后应用
```

**第二阶段（自动化）**：

```
Lesson 自动评分 → 高置信度自动应用策略更新
→ 低置信度进入审核队列
→ 相同 lesson 自动去重合并
→ 跨 Session 关联
```

**第三阶段（主动学习）**：

```
系统主动识别"值得学习"的模式（异常、重复失败、资源浪费）
→ 主动生成 Hypothesis → 设计 Experiment → 验证后沉淀为 Lesson
→ 形成 Learning Curriculum（学习课程表）
```

#### 恢复策略

```
原始 Working Memory 保留 24 小时
  ↓
允许回滚错误 Lesson
  ↓
Lesson 快照每晚导出：
  lesson_snapshot_{date}.json
  ↓
如果策略更新导致问题：
  → 回滚策略 → 标记 lesson 为 rolled_back
  → 保留 lesson 用于分析
```

#### 风险点

| 风险    | 影响                        | 缓解措施                         |
| ----- | ------------------------- | ---------------------------- |
| 垃圾经验  | 学到错误模式，影响后续决策             | lesson\_score 过滤，低分自动淘汰      |
| 经验过拟合 | 过度适配单次失败                  | 需要多次验证才更新全局策略                |
| 经验冲突  | 新旧 lesson 矛盾              | 按时间 + 置信度 + effectiveness 裁决 |
| 学习成本  | 每次任务都 Reflection 消耗 Token | 根据任务重要性采样（非全部）               |
| 遗忘旧经验 | 系统只记得最近的 lesson           | 定期 Review + 重要性重排序           |

#### 后期扩展

- **跨 Session 学习**：多个 Session 的经验汇总
- **跨 Agent 学习**：Lesson 在 Agent Network 中传播
- **自动课程表**：系统识别薄弱环节，自动安排学习任务
- **经验蒸馏**：大量 lesson → 提炼为高级策略
- **反事实学习**：系统模拟"如果选了另一个方案会怎样"

***

### 4.178 Reward System（奖励系统）

不是游戏奖励——是系统知道"什么行为有效"。

```typescript
// table: reward
{
  id: record<reward>,

  // 行为
  action: string,

  // 奖励分数（0-100）
  score: number,

  // 证据
  evidence: string,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
行为: code_review
证据: Review 后代码成功率 +10%
分数: 90
→ 自动偏向有效策略
```

***

### 4.179 Identity Kernel（身份核心）

长期运行最大问题是人格漂移。3 个月后像换了个人。

```typescript
// table: identity
{
  id: record<identity>,

  // 核心原则
  principles: string[],

  // 长期目标
  goals: string[],

  // 行为风格
  style: 'conservative' | 'balanced' | 'exploratory',

  // 边界（不可逾越的行为）
  boundaries: string[],

  // 创建时间
  createdAt: datetime,

  // 更新时间
  updatedAt: datetime
}
```

**区分**：

- Prompt = 当前指令
- Identity = 系统本质（不随 Prompt 变化）

***

### 4.180 Narrative Memory（叙事记忆）

不是碎片化的事实，而是有意义的"故事"。

```typescript
// table: narrative
{
  id: record<narrative>,

  // 时间段
  period: string,

  // 事件摘要
  summary: string,

  // 意义（系统从这个时间段学到了什么）
  meaning: string,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
period: 2026-Q2
summary: "完成 Agent 系统重构，经历多次失败，形成 Review 机制"
meaning: "审查机制使代码成功率提升 18%"
```

***

### 4.181 Self Consistency Checker（自一致性检查）

系统长期运行后，原则和行为可能冲突。

```typescript
// table: self_consistency
{
  id: record<self_consistency>,

  // 冲突组件
  component: string,

  // 矛盾描述
  contradiction: string,

  // 严重程度
  severity: 'low' | 'medium' | 'high' | 'critical',

  // 是否已解决
  resolved: boolean,

  // 创建时间
  createdAt: datetime
}
```

**示例**：

```
原则: "先确认再删除"
   vs
行为: "自动删文件"
严重度: critical
→ 自动报警
```

***

### 4.182 Cognitive Constitution（认知宪法）

比 Prompt 更高的层次——所有 Agent 统一遵守的认知原则。

| # | 原则     | 说明                    |
| - | ------ | --------------------- |
| 1 | 证据优先   | 没有证据的假设不能当事实          |
| 2 | 不确定先提问 | score < 0.5 时暂停，向用户澄清 |
| 3 | 可恢复优先  | 任何操作必须可回滚             |
| 4 | 默认保守   | 不确定时采取更保守的策略          |
| 5 | 长期目标优先 | 不因短期任务牺牲长期方向          |

**完整认知闭环**：

```
Perception（感知）
  ↓
Attention（注意力）
  ↓
Intent（意图）
  ↓
Goal（目标）
  ↓
Plan（规划）
  ↓
Task（任务）
  ↓
Decision（决策）
  ↓
Action（行动）
  ↓
Observation（观察）
  ↓
Reflection（反思）
  ↓
Learning（学习）
  ↓
Memory（记忆）
  ↓
Identity（身份）
  ↓
Evolution（进化）
```

***

## 4.9 AI Society Dynamics（智能体社会动力学）

前面 4.1–4.8 解决了单个智能体如何思考、多个智能体如何协作、系统如何自治。本节解决的是**群体如何形成秩序、如何产生文化、如何产生组织、如何长期演化**——接近 Digital Civilization Runtime 的工程落地。

### 4.183 Institution（制度系统）

单个规则（"不能删文件"）不足以约束群体行为。需要**制度**——结构化的行为规范集合。

```typescript
table: institution {
  id: record<institution>,
  name: string,                    // 制度名称，如 "CodeInstitution"
  rules: string[],                 // 规则列表
  scope: 'global' | 'agent' | 'task' | 'domain',
  enforcement: 'hard' | 'soft' | 'advisory',
  priority: number,                // 优先级，冲突时高优先级覆盖
  createdAt: datetime,
  updatedAt: datetime
}
```

**设计原则**：

- 多个 Agent 不会各玩各的——制度统一行为边界
- scope 控制制度的生效范围（全局/特定 Agent/特定任务）
- enforcement 区分硬性禁止、软性引导、建议性提示
- 冲突时按 priority 裁决

**预置制度示例**：

- CodeInstitution：代码修改必须经过 Review
- ResearchInstitution：研究结论必须有证据链
- SecurityInstitution：高风险操作必须双人确认

***

### 4.184 Governance Layer（治理层）

制度需要治理——谁制定、谁执行、效果如何。

```typescript
table: governance {
  id: record<governance>,
  policyId: record<institution>,
  owner: string,                   // 治理者
  effectiveness: number,           // 0-1，治理效果评分
  violations: number,              // 违规次数
  lastReview: datetime,
  createdAt: datetime,
  updatedAt: datetime
}
```

**设计原则**：

- 治理不是一次性设定——需要持续评估 effectiveness
- effectiveness 下降 → 自动触发治理策略调整
- 超过预算 → 禁止多模型调用（否则系统长期运行会失控）
- 治理者可以是特定 Agent、用户、或自动规则

***

### 4.185 Role Evolution（角色进化）

Agent 角色不应固定。社会需要自动分工。

```typescript
table: role_evolution {
  id: record<role_evolution>,
  agentId: string,
  before: string,                  // 进化前角色
  after: string,                   // 进化后角色
  reason: string,                  // 进化原因
  evidence: string[],              // 支撑证据
  approved: boolean,
  createdAt: datetime
}
```

**示例**：

- 初始：ResearchAgent
- 长期：Research + Review（积累了足够领域知识后自动扩展角色）

**设计原则**：

- 角色进化基于证据（任务完成率、领域深度、协作反馈）
- 进化需要审批（防止角色漂移）
- 社会自动分工，无需人工干预

***

### 4.186 Social Memory（社会记忆）

不同于个人记忆。这是**整个系统共同经历**的集体记忆。

```typescript
table: social_memory {
  id: record<social_memory>,
  event: string,                   // 事件描述
  impact: 'positive' | 'negative' | 'neutral',
  severity: 'low' | 'medium' | 'high' | 'critical',
  participants: string[],          // 参与 Agent
  lessons: string[],               // 经验教训
  createdAt: datetime
}
```

**示例**：

- Browser 插件故障导致事故 → 所有 Agent 都知道
- 避免重复踩坑

**设计原则**：

- 重大事故自动写入 Social Memory
- 新 Agent 启动时加载 Social Memory 作为初始化上下文
- lessons 字段供后续 Agent 检索借鉴

***

### 4.187 Cultural Norm（文化规范）

高级但很有价值。通过群体习惯形成文化，而非靠 Prompt 强制。

```typescript
table: culture {
  id: record<culture>,
  principle: string,               // 文化原则，如 "Review优先"
  adoptionRate: number,            // 0-1，采纳率
  evidence: string[],              // 采纳证据
  createdAt: datetime,
  updatedAt: datetime
}
```

**预置文化原则**：

- Review 优先
- 证据优先
- 不要猜
- 可恢复优先

**设计原则**：

- 文化不是靠 Prompt 写死——而是靠群体习惯形成
- adoptionRate 追踪文化落地程度
- adoptionRate 过低 → 自动触发强化机制

***

### 4.188 Social Reputation（社会信誉）

对 Agent、Plugin、MCP、Tool 的信誉评价体系。

```typescript
table: social_reputation {
  id: record<social_reputation>,
  entityId: string,
  entityType: 'agent' | 'plugin' | 'mcp' | 'tool',
  score: number,                   // 0-1，信誉分
  evidence: string[],              // 评分依据
  history: number[],               // 历史评分序列
  createdAt: datetime,
  updatedAt: datetime
}
```

**设计原则**：

- 差组件自动边缘化——低信誉 Agent 不被分配高价值任务
- 评分基于任务完成率、错误率、协作反馈
- 信誉恢复机制：连续成功可逐步提升
- 所有评分必须有 evidence 支撑

***

### 4.189 Coalition（联盟机制）

复杂任务需要多个 Agent 临时组队。

```typescript
table: coalition {
  id: record<coalition>,
  goal: string,                    // 联盟目标
  members: string[],               // 成员 Agent ID
  leader: string,                  // 协调者
  lifetime: number,                // 生存周期（秒）
  status: 'forming' | 'active' | 'dissolved',
  createdAt: datetime
}
```

**示例**：

- ResearchAgent + CodingAgent + ReviewAgent 临时组队完成功能开发
- 任务结束自动解散

**设计原则**：

- 临时性——任务结束即解散，防止组织越来越乱
- leader 负责协调和决策
- lifetime 控制最大生存时间，超时自动解散

***

### 4.190 Market System（市场机制）

资源（模型、工具、Token）有限，需要竞争。

```typescript
table: market {
  id: record<market>,
  resource: string,                // 资源名称
  demand: number,                  // 当前需求
  supply: number,                  // 当前供给
  cost: number,                    // 单位成本
  allocation: 'competitive' | 'priority' | 'fair',
  createdAt: datetime,
  updatedAt: datetime
}
```

**设计原则**：

- Agent 争夺资源，动态调度
- 高需求 → 成本上升 → 抑制滥用
- allocation 策略可调整（竞争/优先级/公平）
- 资源包括：模型调用配额、Token 预算、工具并发槽、内存

***

### 4.191 Economy Layer（经济系统）

长期运行必然出现的经济层——控制资源滥用。

```typescript
table: economy {
  id: record<economy>,
  agentId: string,
  credits: number,                 // 当前信用分
  spending: object,                // 消费明细
  income: object,                  // 收入明细
  balance: number,                 // 余额
  createdAt: datetime,
  updatedAt: datetime
}
```

**示例**：

- 调用 Claude：-50 credits
- 调用本地模型：-2 credits
- 完成任务：+10 credits

**设计原则**：

- 控制资源滥用——Agent 不能无限制调用昂贵模型
- credits 不足时只能使用低成本资源
- 经济系统驱动 Agent 做出成本敏感的决策

***

### 4.192 Law Engine（法律引擎）

Policy 不够。需要**违反规则 → 处罚**的完整法律机制。

```typescript
table: law {
  id: record<law>,
  condition: string,               // 违规条件（表达式）
  consequence: string,             // 处罚措施
  severity: 'minor' | 'moderate' | 'severe',
  appeals: boolean,                // 是否允许申诉
  createdAt: datetime,
  updatedAt: datetime
}
```

**示例**：

- 条件：未经确认删除文件
- 后果：隔离（暂停 Agent 活动）

**设计原则**：

- 违规和后果必须明确可判定
- 支持申诉机制（见 Court System）
- 处罚分级：警告 → 扣信誉分 → 隔离 → 永久禁用

***

### 4.193 Court System（裁决系统）

两个 Agent 意见冲突时的裁决机制。

```typescript
table: court {
  id: record<court>,
  dispute: string,                 // 争议描述
  participants: string[],          // 争议双方
  evidence: string[],              // 双方证据
  judgment: string,                // 裁决结果
  judgeId: string,                 // 裁决者
  status: 'pending' | 'judging' | 'resolved',
  createdAt: datetime,
  resolvedAt: datetime
}
```

**示例**：

- ResearchAgent：方案 A
- CodingAgent：方案 B
- 由 JudgeAgent 裁决

**设计原则**：

- 裁决者有独立的信誉评分
- 裁决过程全程记录，可追溯
- 裁决结果可作为 Law Engine 的参考案例

***

### 4.194 Historical Archive（历史编年史）

不是日志。是**文明史**——系统层面的编年记录。

```typescript
table: history {
  id: record<history>,
  period: string,                  // 时期，如 "2026-Q3"
  summary: string,                 // 总结
  impact: string,                  // 影响评估
  milestones: string[],            // 里程碑
  createdAt: datetime
}
```

**示例**：

- Q3：引入 Review 体系，事故率下降 70%

**设计原则**：

- 按时间周期自动归档
- 记录重大事件和系统性变化
- 多年后回溯非常值钱——看到系统的成长轨迹
- 数据来源：Social Memory + Event Bus + Learning Records

***

### 4.195 Civilization Identity（文明身份）

最终，系统会形成自己的**价值观、行为方式、长期目标**。

```typescript
table: civilization_identity {
  id: record<civilization_identity>,
  mission: string,                 // 使命宣言
  principles: string[],            // 核心价值观
  worldview: string,               // 世界观
  createdAt: datetime,
  updatedAt: datetime
}
```

**预置核心原则**：

- 优先安全
- 优先可恢复
- 优先透明
- 证据驱动
- 长期价值优先

**设计原则**：

- 不是 Prompt——是文明核心
- 所有决策和制度必须与文明身份一致
- 身份可以演化，但需要广泛共识

***

### 4.196 Evolution Constitution（进化宪法）

系统以后会升级模型、改规则、换架构。需要限制**什么能变、什么不能变**。

```typescript
table: evolution_constitution {
  id: record<evolution_constitution>,
  principle: string,               // 宪法原则
  immutable: boolean,              // 是否不可变更
  protectedBy: string,             // 保护机制
  createdAt: datetime,
  updatedAt: datetime
}
```

**宪法原则**：

| # | 原则       | 不可变更 | 说明            |
| - | -------- | ---- | ------------- |
| 1 | 不得破坏可恢复性 | 是    | 任何变更不能使系统无法回滚 |
| 2 | 不得绕过审查   | 是    | 任何变更不能跳过安全审查  |
| 3 | 不得跳过证据   | 是    | 任何变更不能取消证据验证  |
| 4 | 长期目标优先   | 否    | 可由 90% 以上共识修改 |
| 5 | 默认保守     | 否    | 可由 80% 以上共识修改 |

**设计原则**：

- immutable 标记的条款**不可变更**——防止系统自毁
- 可变更条款需要**高共识阈值**（不是简单多数）
- 防止：长期运行后系统慢慢变成另一个东西

***

### 社会动力学总览

```
Agent（单个智能体）
  ↓
Society（社会：多 Agent 协作）
  ↓
Institutions（制度：行为规范集合）
  ↓
Governance（治理：制度的执行与评估）
  ↓
Economy（经济：资源竞争与分配）
  ↓
Culture（文化：群体习惯与价值观）
  ↓
Civilization（文明：完整的社会形态）
```

这一层解决了 AI 系统从单智能体到文明形态的全路径工程落地。

***

## 五、UI布局（三栏主界面）

### 5.1 主界面布局

- **左侧**: 代码区（代码编辑器 + 资源管理器）
- **中间**: 语言输入区（自然语言输入对话框）
- **右侧**: 效果实时展示区

### 5.2 流送区（AI行为可视化时间轴）

位于对话框上方、历史记录下方。

**显示内容**：

- 推理阶段
- 模型调用（主模型/副模型）
- 工具调用
- 技能调用
- 子任务执行
- 错误与重试
- 下载/同步/预览更新等运行状态

**要求**：

- 支持展开/缩小（加号/减号按钮，加号大减号小）
- 支持增量渲染
- 支持高频事件流控
- 支持事件详情查看
- 支持与日志关联跳转
- 使用虚拟列表或分段渲染
- 流送消息使用队列缓冲
- 禁止高频直接setState刷新全量消息

**多模型调用时**：在流送区显示"某某模型正在被某某模型调用去做了某某事"，以及"某某模型调用了某某工具/技能"。

### 5.3 模型切换

- 上方下拉框切换模型
- 旁边开关控制多模型混合任务
- 多模型混合任务：一个主模型 + 多个副模型协同
- 打开多模型混合任务时弹出下拉框，主模型设为灰色不可选，副模型可多选
- 每个副模型调用在流送区实时显示

### 5.4 效果实时展示区

- 显示当前生成应用的运行结果
- 与任务执行结果联动
- 支持预览刷新防抖
- 支持隔离渲染
- 支持错误状态显示
- 支持预览重载
- 禁止高频无控制刷新
- 预览更新不能直接绑定每个token，使用合并策略和防抖
- 若同一任务在短时间内产生多次刷新请求，仅保留最新有效请求
- 预览异常必须可见化

### 5.5 代码区

- 左侧列表
- 顶部圆角矩形按钮"创建文件" → 弹出文件管理器选择路径创建文件夹
- 下方资源管理器：显示当前文件夹路径 + 一键复制路径按钮 + 树状图文件列表
- 创建文件和资源管理器可通过拉扯变大变小，但不允许消失
- 磁铁样式固定在左侧栏
- 底部代码显示界面，不允许消失和关闭，只允许折起（创建折叠按钮）
- 左下角为设置按钮

***

## 六、设置中心

浮动框，关闭按钮为X。所有设置修改后立即保存状态。左侧固定14个功能入口，可调整上下顺序，不允许改变布局结构。右侧为具体内容面板。

### 14个功能模块（各需独立文件夹，命名方式：序号\_英文名）

**1. 主题（theme）**

- 主题1: 3D亮色金属效果，切换图标为正方形灰色金属光泽PNG
- 主题2: 白色，图标为小太阳PNG
- 主题3: 灰色，图标为实心灰色圆形
- 主题4: 紫色渐变，图标为圆形紫色渐变
- 主题5: 跟随系统，图标为小电脑
- 所有UI组件主题化，气泡/下拉/浮窗有统一阴影体系

**2. 语言（language）**

- 使用i18n，目前只做中文
- 切换图标：中文为大写CN，英文为大写EN

**3. 模型添加（model-add）**

- 添加模型对话框：API地址、请求地址、模型名称
- 根据模型名称创建选项卡，展示配置数据
- 扫描模型功能（扫描后加减号增删模型）
- 模型能力识别图标：推理=粉色大脑、工具=灰色齿轮、快速=黄色闪电、语音=绿色麦克风、图像识别=OCR图标
- 连通性检测（心脏图标按钮）：成功=绿色对勾，失败=红色X

**4. 本地模型设置（local-model）**

- 检测本地模型服务（Ollama、LM Studio等端口扫描）
- 扫描下拉框展示本地模型
- 模型可加入主副模型混合任务体系

**5. MCP**

- 顶部创建MCP工具添加功能
- AI自调用MCP工具
- 展示MCP工具调用过程
- 底部推荐中国境内主流MCP服务商与下载链接

**6. 本地依赖环境创建（environment）**

- 顶部下拉栏：安卓、JAVA、C、C++、Python、自定义类目
- 每个环境在独立文件夹生成，互不干扰
- 常用环境自动下载与配置（可视化过程）
- 自定义环境：文件夹创建→选择主流环境（≥10个）→弹出终端
- 终端显示"如无需要请按回车跳过"
- 图标根据环境名称创建

**7. 对话流程（conversation-flow）**

- 阶段1: 输入接收 → 生成sessionId/turnId/requestId/rootTaskId，记录模型、技能、规则、记忆、知识库、工作区路径快照
- 阶段2: 输入预处理 → 去除空输入、合并上下文、识别任务类型/意图标签/风险等级
- 阶段3: 上下文组装 → 统一组装（禁止UI自行拼接）
- 阶段4: 任务规划 → 主模型/规划器生成计划
- 阶段5: 执行前安全评估 → 敏感目录/命令执行/文件改动/网络请求等检查，输出allow/allow\_with\_warning/require\_user\_confirm/deny
- 阶段6: 主任务执行 → 调度器驱动
- 阶段7: 副模型协作执行 → 子任务委派，不替代主模型最终决策，结果带来源标识
- 阶段8: 工具/技能执行 → 调度器验证权限→安全检查→执行器→结果返回
- 阶段9: 结果合成 → 主模型整合所有输出
- 阶段10: 落地执行 → 写文件、更新工作区、预览、日志、任务状态
- 阶段11: 结束与归档 → 标记完成/失败/部分完成，清理临时上下文，保存审计，更新UI

**8. 技能与规则（skills-rules）**

- 技能磁贴：可折叠（固定位置），支持文件拖拽上传，.txt格式
- 技能文件存入jineng目录，同步到所有会话目录
- 每30秒扫描所有对话文件夹技能一致性，不符则复制补齐（不删除）
- 全部标记完成后暂停任务，新技能安装或重启时唤醒
- "已安装的技能"下拉框：显示安装时间和删除按钮
- 规则磁贴：可折叠，"个人规则添加"创建按钮→.txt格式
- 第8大类下只允许两个文件夹：技能文件夹、规则文件夹

**9. 全局记忆模块（memory）**

- 四个独立目录：
  - 当前对话提炼关键词保存（.txt）
  - 自定义记忆（用户输入文本保存）
  - 文件型记忆导入（文件管理器选择复制）
  - 网址型记忆导入（按行识别，每行一个网址）
- 支持显示、删除、重命名

**10. 全局下载管理（downloads）**

- 内置aria2，所有下载统一走下载中心
- 下载输入框 + 实时监控（进度、已下载存储量）
- 支持修改名称、删除记录、跳转目录

**11. 代理设置（proxy）**

- 三种模式：不使用代理（默认）、跟随系统代理、自定义代理
- 自定义代理标准输入表单
- 配置安全存储，切换后立即生效或待重载提示

**12. 知识库（knowledge-base）**

- 添加知识库按钮→创建大号磁贴
- 支持拖拽和点击上传
- 每个内容创建小磁贴，可修改名称、删除（x）、随意拖拽（不出大磁贴）
- 顶部搜索框（放大镜搜索）
- 每个知识库在12大类下独立文件夹

**13. 频道（channels）**

- 支持接入远程频道：飞书、微信、QQ等
- 必须经过统一安全与授权控制

**14. 数据管理（data-management）**

- 对话导出Markdown格式（用户选择保存位置）
- 日志导出、知识库导出、规则导出、技能清单导出、系统配置导出

***

## 七、任务调度器

### 核心职责

接收根任务、生成任务树、分配主/副模型、组织工具调用、状态流转、重试控制、并发控制、取消控制、超时控制、推送事件、写入日志

### 任务类型

conversation.root、planner.generate、model.primary.run、model.secondary.run、tool.call、skill.call、file.write、file.read、preview\.refresh、environment.setup、download.run、memory.extract、knowledge.search、channel.send、audit.record

### 任务状态（统一状态机）

pending → queued → planning → running → waiting\_confirm / waiting\_dependency → retrying → succeeded / failed / cancelled / partial\_success / timed\_out
禁止自定义状态字符串绕过统一状态体系

### 并发规则

- 一个会话同时只能有一个根任务处于主执行状态
- 子任务允许并发，受调度器并发上限控制
- 副模型任务默认可并发
- 高风险工具任务默认串行
- 文件写入任务需串行化到目标路径粒度
- 预览刷新任务可合并去重

### 重试规则

- 可重试：模型调用失败、网络类工具失败、临时文件锁冲突、下载失败、预览刷新失败
- 默认机制：指数退避，最大重试次数受配置控制
- 每次重试必须写事件与日志
- 高风险副作用操作不自动重试

### 中断与取消

用户可停止当前轮/子任务/下载/环境安装/预览刷新/工具执行。必须写日志、发事件、清理残留状态。

### 超时

所有任务可配置超时，超时后标记timed\_out，写错误日志，发超时事件。

### 人工确认机制

以下情况默认进入确认（waiting\_confirm）：删除文件/目录、覆盖关键配置、执行高风险命令、外发频道消息、大规模批量修改、跨工作区写入、访问未授权目录。

***

## 八、事件驱动架构

系统内部状态变化、任务流转、工具调用、日志写入、技能同步、预览更新统一通过事件机制驱动，禁止模块直接耦合调用业务副作用。

### 基础事件结构

```typescript
{
  eventId, type, timestamp, sessionId, turnId, requestId,
  taskId, parentTaskId?, traceId,
  source: 'user' | 'ui' | 'scheduler' | 'model' | 'tool' | 'plugin' | 'system',
  level: 'debug' | 'info' | 'warn' | 'error',
  status?, title, message?, payload?
}
```

### 标准事件类型

task.created/started/progress/completed/failed/cancelled/timeout/waiting\_confirm/retrying
planner.started/completed
model.call.started/delta/finished/failed
tool.call.started/finished/failed
skill.call.started/finished/failed
file.read.started/finished
file.write.started/finished/failed
preview\.updated/failed
log.appended
download.started/progress/completed/failed/cancelled
skill.sync.started/finished/failed
security.check.started/result
audit.recorded

### WebSocket事件

task.created、task.started、task.progress、task.stream.delta、model.call.started、model.call.finished、tool.call.started、tool.call.finished、retry.scheduled、preview\.updated、log.appended、download.progress、skill.sync.finished

### 展示规则

- 默认展示：task、planner、model.call、tool.call、skill.call、preview、retry、download、security、warning、error
- 调试模式额外：file.read、file.write、audit.recorded、watcher、scheduler
- 所有标准事件写入session.events.jsonl
- UI展示允许视图裁剪，不修改底层原始事件
- 事件payload必须可JSON序列化

***

## 九、日志系统

### 目录结构

rizhi/ → 按会话创建二级日志目录（年月日命名）

### 会话级日志文件

- **session.log**: 用户可读日志（摘要化信息）
- **session.events.jsonl**: 机器可读事件流（每行一个JSON事件）
- **session.audit.jsonl**: 审计日志（高风险操作、权限决策、命令执行等）
- **session.error.log**: 错误日志（异常堆栈、失败原因、超时等）

### 日志字段规范

timestamp、sessionId、turnId、requestId、taskId、parentTaskId、traceId、actor（system/user/model/tool/plugin/scheduler）、category（conversation/model/tool/file/preview/security/audit/download/skill/environment）、level（debug/info/warn/error/fatal）、message、payload

### 日志写入原则

- 所有重要状态变化写事件日志
- 所有高风险动作写审计日志
- 所有异常写错误日志
- 用户可读日志由执行摘要器生成，不允许暴露底层调试流

### 日志回放

基于session.events.jsonl重建时间轴，要求：顺序稳定、字段完整、可关联parentTaskId

***

## 十、安全系统

### 4层架构

1. **Action抽象层**: 所有副作用动作统一抽象（file.read/write/delete/move, command.exec, network.request/download, model.call, plugin.call, skill.call, channel.send, environment.install等）
2. **Target分类层**: 路径、URL、域名、模型服务地址、命令、插件、技能、频道目标标准化
3. **Policy Engine**: 统一输入上下文，输出allow/allow\_with\_warning/require\_user\_confirm/deny
4. **Audit Layer**: 中高风险动作、被拒动作、用户确认动作必须记录

### 风险等级

low（低风险，直接允许）→ medium（中风险，记录）→ high（高风险，要求确认）→ critical（极高风险，默认拒绝或强确认）

### 路径范围

workspace、authorized-extra、system-managed、sensitive-system、unknown-external、removable-media、network-mount

### 关键文件模式

- 一级关键文件（high）: .env、package.json、tsconfig.json、vite.config.*、electron-builder.*、Cargo.toml、requirements.txt、Dockerfile、docker-compose.\* 等
- 一级关键安全文件（critical/high）: 私钥、证书、OAuth secret、token配置文件、preload文件等
- 高影响目录: src/main/**、src/preload/**、config/**、scripts/**、migrations/**、native/**、plugins/\*\*

### 命令权限

类别：safe（查询只读）、dev-run（启动构建）、network、install、destructive（删除覆盖格式化）、script-exec、elevated（管理员权限）

- 查询类命令：allow
- 构建命令：allow\_with\_warning
- 安装依赖：require\_user\_confirm
- 下载并执行远程脚本：deny
- 删除目录/强制删除：deny/strong confirm
- 修改系统配置：deny
- 需要管理员权限：deny/require\_user\_confirm

### 推荐命令白名单（程序级）

node、npm、pnpm、yarn、python、pip、git、cargo、rustup、cmd、powershell（受限）、npx、vite

### 强拒绝模式

curl...|sh、wget...|sh、rm -rf /、del /f /s /q指向大范围目录、format、高危注册表修改、绕权限策略的启动命令

### 插件/技能权限声明

read\_workspace、write\_workspace、execute\_command、access\_network、access\_memory、access\_knowledge、access\_channel、manage\_download、manage\_environment

### 安全补充规则

1. 所有副作用能力只允许通过Main Process的Security Gateway调用
2. 所有文件/命令/网络操作必须先过SecurityPolicyEngine.evaluate()
3. 所有文件系统目标必须先realpath
4. 安全检查与执行必须尽量靠近（防TOCTOU）
5. 批量操作聚合判定：含1个critical目标→整体至少high、批量≥20文件→require confirm
6. 内容级风险检测（secret pattern、远程脚本执行、Electron高危配置）
7. 敏感信息出站控制（.env不自动发模型、私钥禁止外发）
8. 子任务权限不能超过父任务
9. 所有高风险动作必须带traceId/taskId/actor

### 安全策略目录结构

```
src/main/security/
  engine/SecurityPolicyEngine.ts, evaluateByMatrix.ts
  classifier/pathClassifier.ts, commandClassifier.ts, networkClassifier.ts, criticalFileDetector.ts
  consent/ConsentStore.ts
  audit/SecurityAuditService.ts
  config/securityPolicyConfig.ts
  types/security.types.ts
```

### 第一版优先实现

路径scope分类、关键文件识别、文件读写删除判定、命令分类与基础拦截、模型域名判定、网络下载域名判定、用户确认缓存、审计日志

***

## 十一、技能目录与文件监听

### 核心目录

- 全局技能源目录：jineng
- 每个会话目录拥有自己的技能副本

### 监听链路

用户操作 → jineng目录变化 → FileWatcherManager → EventBus → SkillManager → SkillSyncEngine → 会话技能目录 → 状态标记更新 → UI更新

### 同步规则

- 新技能进入全局源目录后自动同步至所有会话目录
- 检测到缺失或不一致则补齐
- 不主动删除会话中非系统控制的额外文件（除非用户执行删除技能操作）
- 全同步完成后暂停扫描
- 新技能安装或系统重启后重新激活

### 文件监听

使用chokidar，实时捕获技能变化，增量分发至所有会话目录。

***

## 十二、性能约束

### 前端

- 流送区使用虚拟列表或分段渲染
- 流送消息使用队列缓冲
- 禁止高频直接setState刷新全量消息
- Zustand使用细粒度订阅
- Monaco实例销毁与复用受控
- 预览区更新防抖
- 拖拽与动画避免高频layout抖动
- 按需加载大型模块

### 明确禁止

- 流送区无限消息堆积
- WebSocket无心跳重连
- Zustand全局无边界订阅
- 预览区无限刷新
- UI直接读取底层数据库
- 渲染进程直接执行系统操作

***

## 十三、明确禁止事项

1. UI直接操作底层数据库
2. UI直接运行shell命令
3. 渲染进程直接访问未授权文件系统
4. 任意模块绕过调度器直接执行高副作用任务
5. 任意模块绕过安全检查直接写文件/删文件
6. 流送区无限堆积原始消息
7. 任务状态使用非统一状态字符串
8. 插件不声明权限直接调用底层能力
9. 会话外未审计的高风险操作
10. 无traceId/taskId的关键事件写入日志

***

## 十四、最终交付标准

1. 三栏主界面完整可用
2. 主副模型协同链路完整
3. AI行为可视化时间轴完整
4. 对话流程与任务调度器完整
5. 日志体系完整
6. 事件协议完整
7. 安全边界完整
8. 技能/规则/记忆/知识库/下载/代理/频道/环境模块接入统一任务系统
9. 所有高风险操作可审计、可确认、可回放

***

## 十五、错误处理与降级策略

### 14.1 全局错误边界

**主进程错误边界**

- 所有主进程操作必须包裹在try-catch中
- 未捕获异常必须写入session.error.log并触发EventBus发布error事件
- 崩溃前必须保存当前状态快照到临时文件
- 渲染进程异常通过IPC上报主进程统一处理

**渲染进程错误边界**

- React组件必须实现ErrorBoundary组件包装
- 路由级别错误边界捕获页面级崩溃
- 异步操作错误通过统一错误回调处理
- 禁止吞掉错误而不做任何处理或上报

**错误分类**

```typescript
enum ErrorCategory {
  NETWORK = 'network',           // 网络相关
  MODEL = 'model',               // 模型调用
  FILE_SYSTEM = 'file_system',   // 文件系统
  SECURITY = 'security',          // 安全检查
  PERMISSION = 'permission',     // 权限问题
  TIMEOUT = 'timeout',           // 超时
  VALIDATION = 'validation',     // 验证失败
  INTERNAL = 'internal',         // 内部错误
  UNKNOWN = 'unknown'            // 未知
}

enum ErrorSeverity {
  LOW = 'low',           // 提示用户，不影响主流程
  MEDIUM = 'medium',     // 记录日志，可能影响当前操作
  HIGH = 'high',         // 中断当前操作，需要用户介入
  CRITICAL = 'critical'  // 系统级错误，可能需要重启
}
```

### 14.2 熔断器模式

**熔断器配置**

```typescript
interface CircuitBreakerConfig {
  name: string;
  failureThreshold: number;      // 失败次数阈值（默认：5次）
  successThreshold: number;      // 成功恢复阈值（默认：3次）
  timeout: number;              // 熔断持续时间（默认：30秒）
  halfOpenRequests: number;      // 半开状态允许的试探请求数（默认：1）
}
```

**熔断器管理范围**

- 模型API调用
- 外部网络请求
- 插件执行
- MCP工具调用
- 下载服务

**熔断器状态**

- **Closed（关闭）**: 正常请求通过，失败计数
- **Open（打开）**: 所有请求直接失败，快速返回降级响应
- **Half-Open（半开）**: 允许试探性请求，根据结果决定恢复或继续熔断

**降级响应策略**

```typescript
interface FallbackStrategy {
  modelCall: {
    primaryFail: 'retry' | 'switchToSecondary' | 'cachedResponse' | 'userPrompt';
    networkFail: 'retry' | 'offlineMode' | 'queue';
  };
  fileOperation: {
    fail: 'retry' | 'userPrompt' | 'skip';
  };
  previewRefresh: {
    fail: 'showLastValid' | 'showErrorState' | 'retry';
  };
}
```

### 14.3 重试与退避策略

**指数退避配置**

```typescript
interface RetryConfig {
  maxRetries: number;           // 最大重试次数（默认：3）
  baseDelay: number;            // 基础延迟毫秒（默认：1000）
  maxDelay: number;             // 最大延迟毫秒（默认：30000）
  backoffMultiplier: number;    // 退避乘数（默认：2）
  jitter: boolean;              // 是否添加随机抖动（默认：true）
  retryableErrors: ErrorCategory[];  // 可重试的错误类型
}
```

**可重试错误类型**

- NETWORK\_TIMEOUT
- MODEL\_RATE\_LIMIT
- FILE\_LOCK\_CONFLICT
- DOWNLOAD\_INTERRUPTED
- PREVIEW\_REFRESH\_FAIL

**不可重试错误类型**

- SECURITY\_DENIED
- PERMISSION\_DENIED
- VALIDATION\_ERROR
- USER\_CANCELLED

### 14.4 用户提示体系

**错误提示规范**

- 用户可见错误消息必须经过本地化处理
- 错误消息禁止暴露内部路径、堆栈、技术细节
- 敏感操作失败显示通用提示 + 错误代码
- 提供错误代码供技术支持参考

**提示层级**

```typescript
interface UserNotification {
  id: string;
  type: 'info' | 'warning' | 'error' | 'success';
  title: string;           // 本地化key
  message: string;          // 本地化key，支持参数插值
  actions?: NotificationAction[];
  dismissible: boolean;
  duration?: number;        // 自动消失时间（毫秒），0表示不自动消失
  metadata?: Record<string, unknown>;  // 错误代码、原始错误等
}

interface NotificationAction {
  id: string;
  label: string;
  action: 'primary' | 'secondary' | 'destructive';
  handler: () => void | Promise<void>;
}
```

***

## 十六、系统监控与可观测性

### 15.1 埋点指标定义

**性能指标**

```typescript
interface PerformanceMetrics {
  // 应用启动
  appStartTime: number;           // 应用启动耗时（毫秒）
  mainProcessReadyTime: number;    // 主进程就绪时间
  rendererReadyTime: number;       // 渲染进程就绪时间
  firstMeaningfulPaint: number;    // 首次有效绘制

  // 运行时性能
  averageFps: number;             // 平均帧率
  memoryUsage: MemorySnapshot;     // 内存使用快照
  cpuUsage: CpuSnapshot;          // CPU使用率

  // API延迟
  modelCallLatency: LatencyStats; // 模型调用延迟统计
  toolCallLatency: LatencyStats; // 工具调用延迟统计
  fileOperationLatency: LatencyStats;  // 文件操作延迟

  // 任务性能
  taskThroughput: number;         // 任务吞吐量（个/分钟）
  taskSuccessRate: number;        // 任务成功率
  averageTaskDuration: number;    // 平均任务执行时长
}

interface MemorySnapshot {
  heapUsed: number;
  heapTotal: number;
  external: number;
  rss: number;
  timestamp: number;
}

interface LatencyStats {
  p50: number;    // 中位数
  p90: number;    // 90分位
  p95: number;    // 95分位
  p99: number;    // 99分位
  average: number;
  count: number;
}
```

**业务指标**

```typescript
interface BusinessMetrics {
  activeSessions: number;         // 当前活跃会话数
  totalTasks: number;             // 累计任务数
  totalModelCalls: number;        // 累计模型调用数
  totalToolCalls: number;        // 累计工具调用数
  totalDownloads: number;         // 累计下载任务数
  totalSkillSyncs: number;        // 累计技能同步次数
  knowledgeBaseSize: number;      // 知识库总大小（字节）
  storageUsed: number;            // 已用存储空间
}
```

### 15.2 健康检查机制

**健康检查端点**

```typescript
interface HealthCheckResult {
  status: 'healthy' | 'degraded' | 'unhealthy';
  timestamp: number;
  checks: HealthCheck[];
  uptime: number;  // 运行时间（秒）
}

interface HealthCheck {
  name: string;
  status: 'pass' | 'fail' | 'warn';
  message?: string;
  duration: number;  // 检查耗时（毫秒）
  metadata?: Record<string, unknown>;
}
```

**健康检查项目**

- 主进程存活状态
- 渲染进程连接状态
- WebSocket连接状态
- 模型服务可达性
- 本地模型服务检测（Ollama等）
- 磁盘空间检查（< 1GB告警）
- 内存使用检查（> 90%告警）
- Python运行时可用性
- Rust模块可用性
- 关键目录可写性

**心跳机制**

- 主进程每30秒向渲染进程发送心跳
- 超时阈值：60秒
- 连续3次心跳失败触发重连流程
- 心跳记录包含基本系统状态

### 15.3 崩溃报告机制

**崩溃分类**

```typescript
enum CrashType {
  MAIN_PROCESS = 'main_process',
  RENDERER_PROCESS = 'renderer_process',
  GPU_PROCESS = 'gpu_process',
  PLUGIN_CRASH = 'plugin_crash',
  NATIVE_MODULE_CRASH = 'native_module_crash'
}

interface CrashReport {
  id: string;
  type: CrashType;
  timestamp: number;
  version: string;
  platform: string;
  arch: string;
  error: {
    message: string;
    stack?: string;
    code?: string;
  };
  processInfo: {
    pid: number;
    exitCode?: number;
    uptime: number;
  };
  systemInfo: {
    totalMemory: number;
    freeMemory: number;
    cpuUsage: number;
  };
  recentEvents: Event[];  // 崩溃前的最近N个事件
  userId?: string;
}
```

**崩溃恢复流程**

1. 捕获崩溃信号/异常
2. 生成崩溃报告
3. 保存未持久化状态
4. 尝试重启崩溃的进程
5. 恢复用户会话状态
6. 向用户展示崩溃通知
7. 提供崩溃报告上传选项

### 15.4 性能告警阈值

```typescript
interface AlertThresholds {
  performance: {
    fpsWarning: number;        // < 30 FPS告警
    fpsCritical: number;       // < 15 FPS告警
    memoryWarningPercent: number;  // > 80%告警
    memoryCriticalPercent: number; // > 90%告警
    responseTimeWarning: number;    // > 2000ms警告
    responseTimeCritical: number;   // > 5000ms严重
  };
  system: {
    diskSpaceWarningGB: number;     // < 2GB警告
    diskSpaceCriticalGB: number;   // < 500MB严重
    cpuWarningPercent: number;      // > 80%警告
    cpuCriticalPercent: number;     // > 95%严重
  };
  business: {
    taskFailureRateWarning: number;   // > 10%警告
    taskFailureRateCritical: number; // > 30%严重
    modelErrorRateWarning: number;    // > 5%警告
    modelErrorRateCritical: number;  // > 20%严重
  };
}
```

### 15.5 监控数据存储

**存储策略**

- 实时指标保留最近1小时
- 汇总数据保留最近30天
- 告警记录保留最近90天
- 崩溃报告永久保留（用户可删除）
- 存储位置：monitoring/目录

**目录结构**

```
monitoring/
├── metrics/
│   ├── realtime/       # 实时指标（JSON，按分钟）
│   ├── hourly/         # 小时汇总
│   └── daily/         # 日汇总
├── alerts/
│   └── {date}.json    # 每日告警记录
├── health/
│   └── {date}.json    # 健康检查历史
└── crashes/
    └── {timestamp}_{type}.json  # 崩溃报告
```

***

## 十七、缓存与存储管理

### 16.1 缓存层级架构

```typescript
enum CacheLevel {
  MEMORY = 'memory',         // 内存缓存（进程级）
  DISK = 'disk',             // 磁盘缓存
  SESSION = 'session',       // 会话级缓存
  PERSISTENT = 'persistent'  // 持久化缓存
}

interface CacheConfig {
  maxSize: number;            // 最大缓存大小（字节）
  maxAge: number;             // 最大缓存时间（毫秒）
  maxItems: number;          // 最大缓存条目数
  evictionPolicy: 'lru' | 'lfu' | 'fifo';
  compression: boolean;      // 是否压缩
}
```

### 16.2 缓存管理范围

**模型响应缓存**

- 配置：内存级，LRU，maxItems=100
- 键：模型+输入hash
- 过期：默认1小时，配置可调

**文件内容缓存**

- 配置：内存级，LRU，maxSize=100MB
- 大文件（>1MB）使用磁盘缓存
- 编辑器打开文件缓存

**预览缓存**

- 配置：会话级，maxSize=500MB
- 按工作区隔离
- 可配置禁用

**技能缓存**

- 配置：持久化，maxAge=7天
- 增量更新，不全量刷新

**知识库向量缓存**

- 配置：磁盘级，maxSize=10GB
- 按知识库隔离
- 支持手动清理

### 16.3 存储配额限制

```typescript
interface StorageQuota {
  // 存储类型
  type: 'session' | 'knowledge' | 'logs' | 'cache' | 'downloads';

  // 软限制（到达后提示用户）
  softLimit: number;    // 字节

  // 硬限制（到达后阻止写入）
  hardLimit: number;    // 字节

  // 超限策略
  overflowPolicy: 'warn' | 'block' | 'autoCleanup';
}

const DEFAULT_QUOTAS: StorageQuota[] = [
  { type: 'session', softLimit: 500MB, hardLimit: 1GB, overflowPolicy: 'autoCleanup' },
  { type: 'knowledge', softLimit: 5GB, hardLimit: 50GB, overflowPolicy: 'warn' },
  { type: 'logs', softLimit: 1GB, hardLimit: 5GB, overflowPolicy: 'autoCleanup' },
  { type: 'cache', softLimit: 2GB, hardLimit: 5GB, overflowPolicy: 'autoCleanup' },
  { type: 'downloads', softLimit: 10GB, hardLimit: 100GB, overflowPolicy: 'warn' }
];
```

### 16.4 清理策略

**自动清理触发条件**

- 存储使用达到软限制的90%
- 应用启动时检测超限
- 手动触发清理

**清理优先级**

1. 缓存目录（可重建）
2. 过期日志（> 30天）
3. 会话临时文件
4. 预览缓存（按LRU）
5. 最早会话归档

**手动清理功能**

- 设置中心提供一键清理入口
- 分类型清理（缓存/日志/预览/全部）
- 显示清理预估释放空间
- 清理前确认提示

### 16.5 会话存储管理

**会话目录结构**

```
sessions/
└── {sessionId}/
    ├── metadata.json       # 会话元数据
    ├── context/            # 上下文缓存
    ├── workspace/          # 工作区快照
    ├── skills/             # 技能副本
    ├── memory/             # 记忆数据
    ├── events.jsonl        # 事件日志
    ├── audit.jsonl         # 审计日志
    └── temp/               # 临时文件
```

**会话过期策略**

- 30天未访问的会话标记为归档
- 归档会话可手动恢复
- 90天未恢复的归档会话可删除
- 重要会话可永久保留

***

## 十八、数据备份与迁移

### 17.1 数据导出格式

**完整导出**

```typescript
interface FullExport {
  version: string;           // 导出格式版本
  exportedAt: number;        // 导出时间戳
  appVersion: string;        // 应用版本

  // 各模块数据
  settings: SettingsExport;
  skills: SkillsExport;
  rules: RulesExport;
  memory: MemoryExport;
  knowledgeBase: KnowledgeBaseExport;
  conversations: ConversationExport[];
  modelConfigs: ModelConfigExport;
  channels: ChannelExport[];

  // 元数据
  metadata: {
    totalSize: number;
    checksum: string;
    encryption?: string;
  };
}
```

**分类导出**

- 设置导出（不含敏感信息）
- 技能导出（包含所有.txt文件）
- 记忆导出（关键词+自定义）
- 知识库导出（原始文件+向量）
- 对话导出（Markdown格式）
- 模型配置导出（不含API密钥）

### 17.2 数据导入流程

**导入验证**

```typescript
interface ImportValidation {
  version: string;           // 兼容版本检查
  checksum: string;          // 完整性校验
  sizeLimit: number;         // 大小限制
  schemaValid: boolean;      // 格式校验
  conflicts: Conflict[];     // 冲突检测
}

interface Conflict {
  type: 'overwrite' | 'skip' | 'rename';
  source: string;
  target: string;
  resolution: 'auto' | 'manual';
}
```

**冲突处理策略**

- 文件名冲突：自动重命名（添加时间戳后缀）
- 设置冲突：询问用户选择覆盖或保留
- 技能冲突：保留最新版本
- 知识库冲突：默认跳过，用户可手动合并

### 17.3 版本迁移策略

**迁移脚本结构**

```
migrations/
├── V1__initial_schema.ts
├── V2__add_skills_table.ts
├── V3__add_memory_index.ts
└── metadata.json       # 迁移状态记录
```

**迁移执行保障**

- 所有迁移事务性执行
- 迁移前自动备份
- 迁移失败自动回滚
- 迁移后数据校验

**版本兼容性矩阵**

| 应用版本  | 数据格式 | 兼容升级路径        |
| ----- | ---- | ------------- |
| 1.0.x | v1   | 1.1.x → 1.2.x |
| 1.1.x | v2   | 1.2.x         |
| 1.2.x | v3   | 最新版           |

### 17.4 升级兼容性处理

**配置迁移**

- 配置文件版本化存储
- 启动时检测版本差异
- 自动执行必要迁移
- 迁移过程透明可见

**数据迁移**

- 大版本升级强制备份
- 增量迁移，避免大文件一次性处理
- 迁移进度可中断恢复
- 迁移完成后验证完整性

**降级限制**

- 不支持降级到旧版本（数据格式可能不兼容）
- 提供数据导出功能降级准备
- 降级警告提示用户

***

## 十九、测试策略

### 18.1 测试框架选型

**前端测试**

```json
{
  "unit": "Vitest",
  "component": "Testing Library",
  "e2e": "Playwright",
  "coverage": "V8内置coverage"
}
```

**后端测试**

```json
{
  "unit": "Vitest",
  "integration": "Vitest + Supertest",
  "e2e": "Playwright (Electron)"
}
```

### 18.2 测试覆盖率要求

```typescript
interface CoverageRequirements {
  statements: number;    // 语句覆盖率
  branches: number;      // 分支覆盖率
  functions: number;     // 函数覆盖率
  lines: number;         // 行覆盖率
}

const COVERAGE_TARGETS: CoverageRequirements = {
  // 核心模块（任务调度、事件、权限）
  core: { statements: 90, branches: 85, functions: 90, lines: 90 },

  // 业务模块（模型调用、文件操作、预览）
  business: { statements: 80, branches: 75, functions: 80, lines: 80 },

  // 安全模块
  security: { statements: 95, branches: 90, functions: 95, lines: 95 },

  // UI组件
  ui: { statements: 70, branches: 60, functions: 70, lines: 70 }
};
```

### 18.3 测试分类与范围

**单元测试**

- 纯函数逻辑
- 工具函数
- 状态转换
- 事件处理
- 验证规则
- 序列化/反序列化

**集成测试**

- 任务调度流程
- 事件总线传播
- 文件系统操作
- 模型API调用
- 安全策略执行

**E2E测试**

- 完整用户流程
- 多进程通信
- WebSocket连接
- 窗口生命周期
- 系统托盘交互

**性能测试**

- 大文件操作
- 高频事件处理
- 内存泄漏检测
- 并发任务处理

### 18.4 测试数据管理

**测试fixtures**

```typescript
// fixtures/
// ├── mock/
// │   ├── models/         # 模拟模型响应
// │   ├── files/          # 测试文件
// │   └── api/            # API模拟
// ├── factories/
// │   ├── task.factory.ts
// │   ├── session.factory.ts
// │   └── event.factory.ts
// └── seeds/
//     └── test-data.ts    # 种子数据
```

**测试隔离策略**

- 每个测试独立环境
- 使用内存数据库测试
- Mock外部依赖
- 测试后清理副作用

### 18.5 CI/CD流程

```yaml
# .github/workflows/test.yml
name: Test
on: [push, pull_request]

jobs:
  unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout
      - run: npm ci
      - run: npm run test:unit -- --coverage
      - uses: codecov/codecov-action

  integration-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout
      - run: npm ci
      - run: npm run test:integration

  e2e-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout
      - run: npm ci
      - run: npm run build
      - run: npm run test:e2e

  security-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout
      - run: npm audit --audit-level=high
      - run: npm run security:check
```

***

## 二十、可访问性规范

### 19.1 键盘导航

**焦点管理**

```typescript
interface FocusManagement {
  // 焦点陷阱范围
  trapIn: string[];        // 模态框、对话框等

  // 焦点恢复位置
  restoreOnClose: string;  // 关闭后焦点恢复元素

  // 快捷键冲突检测
  shortcutConflicts: ShortcutConflict[];
}
```

**快捷键定义**

```typescript
const KEYBOARD_SHORTCUTS = {
  // 全局
  'Ctrl+,': 'openSettings',
  'Ctrl+K': 'commandPalette',
  'Escape': 'closeModal',
  'Ctrl+W': 'closeTab',
  'Ctrl+Q': 'quitApp',

  // 编辑器
  'Ctrl+S': 'saveFile',
  'Ctrl+Z': 'undo',
  'Ctrl+Shift+Z': 'redo',

  // 任务
  'Ctrl+Enter': 'executeTask',
  'Ctrl+.': 'stopTask',

  // 导航
  'Alt+1-9': 'switchTab',
  'Ctrl+Tab': 'nextTab',
  'Ctrl+Shift+Tab': 'prevTab'
};
```

**Tab键顺序**

- 逻辑Tab顺序，禁用随机TabIndex
- 焦点指示器清晰可见
- 跳过大段内容提供跳转链接

### 19.2 ARIA标注

**必需ARIA属性**

```typescript
// 语义化角色
<button role="button" />           // 原生button不需要
<div role="dialog" />              // 模态框
<div role="tree" />                // 文件树
<div role="listbox" />             // 下拉选择
<div role="progressbar" />         // 进度条

// 状态标注
<button aria-expanded="false" />
<div aria-live="polite" />         // 动态内容区
<div aria-busy="true" />           // 加载状态
<input aria-invalid="true" />

// 关系标注
<button aria-controls="panel-1" />
<div aria-labelledby="title-1" />
<select aria-describedby="hint-1" />
```

**动态内容通知**

```typescript
// 区域角色
<div role="status" />              // 非侵入式状态
<div role="alert" />               // 重要消息
<div role="log" />                 // 日志/历史

// live region配置
const LIVE_REGIONS = {
  // 流送区 - 任务状态更新
  streamRegion: { role: 'log', ariaLive: 'polite', politeness: 'default' },

  // 通知区 - 新消息到达
  notificationRegion: { role: 'status', ariaLive: 'assertive', politeness: 'now' },

  // 预览区 - 刷新完成
  previewRegion: { role: 'status', ariaLive: 'polite', politeness: 'default' }
};
```

### 19.3 屏幕阅读器兼容

**必需支持**

- NVDA (Windows)
- VoiceOver (macOS)
- 读屏软件自动检测

**兼容要求**

```typescript
// 图片必须有alt文字
<img alt="工作区文件树" />

// 图表需要有替代文本或aria标签
<svg aria-label="本周任务完成率图表">
  <title>本周任务完成率</title>
</svg>

// 动态内容使用aria-live
<div aria-live="polite">
  {/* 任务执行状态会实时更新 */}
</div>
```

### 19.4 视觉无障碍

**对比度要求**

- 普通文本：≥ 4.5:1（WCAG AA）
- 大文本（≥18pt）：≥ 3:1
- UI组件：≥ 3:1
- 焦点指示器：≥ 3:1

**字体要求**

- 基础字号：≥ 14px
- 可缩放：支持浏览器缩放至200%
- 行高：≥ 1.5

**颜色使用**

- 颜色不能作为唯一信息载体
- 状态需配合图标/文字/图案
- 主题切换保持对比度

**动画控制**

```typescript
// 用户偏好检测
const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)');

// 自动遵循系统设置
if (prefersReducedMotion.matches) {
  // 禁用或减少动画
  disableAnimations();
}
```

### 19.5 测试与验证

**自动化测试**

- axe-core集成到CI
- Lighthouse Accessibility审计
- 自动检测对比度问题

**手动测试清单**

- [ ] 纯键盘操作完整流程
- [ ] 屏幕阅读器朗读验证
- [ ] 高对比度模式验证
- [ ] 大字体缩放验证
- [ ] 焦点可见性验证

***

## 二十一、国际化(i18n)架构

### 20.1 多语言架构设计

**语言文件结构**

```
locales/
├── zh-CN/
│   ├── common.json      # 通用文案
│   ├── settings.json    # 设置中心
│   ├── errors.json      # 错误消息
│   ├── tasks.json       # 任务相关
│   └── notifications.json
├── en-US/
│   └── ...
└── index.json           # 语言配置
```

**Key命名规范**

```typescript
// 命名空间.模块.具体含义
// 示例：
settings.theme.title
settings.theme.options.dark
errors.model.timeout
tasks.status.pending
notifications.download.complete
```

### 20.2 翻译管理

**翻译要求**

```typescript
interface TranslationEntry {
  key: string;
  zh-CN: string;
  en-US: string;
  description?: string;    // 翻译上下文说明
  maxLength?: number;      // 最大字符限制
  variables?: string[];     // 变量列表
}
```

**变量插值语法**

```typescript
// JSON格式
"task.complete": "任务 {taskName} 已完成"
"file.size": "文件大小：{size} {unit}"
"error.retry": "操作失败，{attempt}/{max}次重试中"

// React组件使用
<t>{t('task.complete', { taskName: '文档处理' })}</t>
```

### 20.3 本地化处理

**日期时间格式化**

```typescript
const dateFormatter = new Intl.DateTimeFormat(locale, {
  dateStyle: 'medium',
  timeStyle: 'short'
});

const relativeFormatter = new Intl.RelativeTimeFormat(locale, {
  numeric: 'auto'
});
```

**数字格式化**

```typescript
const numberFormatter = new Intl.NumberFormat(locale, {
  maximumFractionDigits: 2
});

const percentFormatter = new Intl.NumberFormat(locale, {
  style: 'percent'
});
```

**复数处理**

```json
// common.json
{
  "task.count": {
    "zero": "暂无任务",
    "one": "{count} 个任务",
    "other": "{count} 个任务"
  }
}
```

### 20.4 运行时语言切换

**切换流程**

1. 用户选择语言
2. 持久化到设置
3. 通知所有依赖方
4. 重新渲染UI
5. 保持当前状态不变

**动态导入**

```typescript
// 按需加载语言包
const loadLocale = async (locale: string) => {
  const messages = await import(`./locales/${locale}/index.json`);
  return messages.default;
};
```

***

## 二十二、系统升级与热更新

### 21.1 版本管理

**语义化版本**

```
主版本.次版本.修订号
major.minor.patch

示例：1.2.3
- 1: 主版本（不兼容的API变更）
- 2: 次版本（向后兼容的功能新增）
- 3: 修订号（向后兼容的问题修复）
```

**版本信息存储**

```typescript
interface VersionInfo {
  current: string;
  previous?: string;
  buildNumber: number;
  buildTime: string;
  commitHash: string;
  channel: 'stable' | 'beta' | 'dev';
}
```

### 21.2 热更新机制

**更新类型**

```typescript
enum UpdateType {
  PATCH = 'patch',       // 补丁更新，可热更新
  MINOR = 'minor',       // 次版本，询问后更新
  MAJOR = 'major'        // 主版本，强制备份后更新
}

interface UpdateInfo {
  type: UpdateType;
  version: string;
  releaseNotes: string;
  size: number;
  checksum: string;
  mandatory: boolean;     // 是否强制更新
  deadline?: number;      // 强制更新截止时间
}
```

**热更新流程**

1. 启动时检查更新
2. 下载更新包到临时目录
3. 验证签名和完整性
4. 通知用户更新内容
5. 用户确认后执行更新
6. 重启应用生效

### 21.3 数据迁移

**迁移触发时机**

- 应用启动时自动检测
- 版本差异自动迁移
- 用户可手动触发迁移

**迁移保障**

- 迁移前完整备份
- 事务性执行
- 失败自动回滚
- 迁移后可回滚（有限次数）

**迁移界面**

- 显示迁移进度
- 预估耗时
- 可取消（部分迁移支持）
- 完成报告

***

## 二十三、插件沙箱隔离

### 22.1 插件隔离架构

**进程隔离**

```typescript
interface PluginSandbox {
  // 独立进程运行
  isolateProcess: boolean;

  // 资源限制
  resourceLimits: {
    maxMemory: number;      // 最大内存（MB）
    maxCpu: number;         // 最大CPU百分比
    maxExecutionTime: number; // 最大执行时间（ms）
    maxFileSize: number;    // 最大文件操作大小
  };

  // 权限边界
  allowedApis: string[];    // 允许的API白名单
  deniedApis: string[];     // 拒绝的API黑名单
}
```

### 22.2 插件资源配额

```typescript
interface PluginQuota {
  pluginId: string;

  // 文件系统
  maxReadBytes: number;     // 最大读取字节
  maxWriteBytes: number;    // 最大写入字节
  allowedPaths: string[];   // 允许访问的路径
  deniedPaths: string[];    // 禁止访问的路径

  // 网络
  maxRequests: number;      // 最大请求数/分钟
  allowedDomains: string[]; // 允许的域名
  deniedDomains: string[];  // 禁止的域名

  // 执行
  maxConcurrentTasks: number;
  maxTaskDuration: number;
}
```

### 22.3 插件通信限制

**主进程通信**

- 插件只能通过IPC与主进程通信
- 禁止直接访问Electron API
- 禁止访问Node.js原生模块

**渲染进程通信**

- 插件在隔离的Webview中运行
- 通过预定义接口与主应用通信
- 数据传递必须序列化

***

## 二十四、架构图与流程图

### 23.1 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         SoloForge                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐                                               │
│  │   渲染进程    │ ←── IPC ──→ ┌──────────────────────────┐    │
│  │  (Renderer)  │            │        主进程 (Main)         │    │
│  │              │            │                               │    │
│  │  ┌─────────┐ │            │  ┌─────────────────────┐   │    │
│  │  │  React  │ │            │  │  Orchestrator       │   │    │
│  │  │  UI     │ │            │  │  Runtime            │   │    │
│  │  └─────────┘ │            │  └──────────┬──────────┘   │    │
│  │              │            │             │               │    │
│  │  ┌─────────┐ │            │  ┌──────────┴──────────┐   │    │
│  │  │ Zustand │ │            │  │   Event Bus        │   │    │
│  │  │ Store   │ │            │  └──────────┬──────────┘   │    │
│  │  └─────────┘ │            │             │               │    │
│  │              │            │  ┌───────────┼───────────┐   │    │
│  │  ┌─────────┐ │            │  │           │           │   │    │
│  │  │ WebSocket│ │            │  ▼           ▼           ▼   │    │
│  │  │ Client  │ │            │ ┌────┐  ┌──────┐  ┌───────┐  │    │
│  │  └─────────┘ │            │ │Task│  │Model │  │Security│  │    │
│  └──────────────┘            │ │调度│  │Registry│ │ Engine │  │    │
│                               │ └────┘  └──────┘  └───────┘  │    │
│                               └──────────────────────────┘    │
│                                          │                    │
│              ┌───────────────────────────┼───────────────┐    │
│              │                           │               │    │
│              ▼                           ▼               ▼    │
│         ┌────────┐               ┌──────────┐      ┌────────┐ │
│         │ Python │               │   Rust   │      │  文件  │ │
│         │Runtime │               │  Native  │      │ System │ │
│         └────────┘               └──────────┘      └────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 23.2 任务执行时序图

```
用户输入 ──→ 任务创建 ──→ 安全检查 ──→ 任务规划 ──→ 执行调度
   │           │              │            │           │
   │           ▼              ▼            ▼           ▼
   │      生成TaskID     权限判定     分解子任务    模型调用
   │           │              │            │           │
   │           │              │            │      ┌─────┴─────┐
   │           │              │            │      ▼           ▼
   │           │              │            │   主模型     副模型并行
   │           │              │            │      │           │
   │           │              │            │      └─────┬─────┘
   │           │              │            │            ▼
   │           │              │            │        工具调用
   │           │              │            │            │
   │           │              │            │      ┌─────┴─────┐
   │           │              │            │      ▼           ▼
   │           │              │            │   工具执行    结果合成
   │           │              │            │      │           │
   │           │              │            │      └─────┬─────┘
   │           │              │            │            ▼
   │           │              │            │        预览更新
   │           │              │            │            │
   │           ▼              ▼            ▼            ▼
   └──────→ 结果展示 ←────────────────────────── 结果归档
```

### 23.3 事件流示意图

```
┌──────────────────────────────────────────────────────────────┐
│                       Event Flow                              │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│   Task Created ──→ Task Started ──→ Task Progress ──→ ...    │
│        │                                                   │
│        ▼                                                   │
│   ┌────┴────┐                                               │
│   │         │                                               │
│   ▼         ▼                                               │
│ Model    Tool                                               │
│ Call     Call                                               │
│ Started  Started                                             │
│   │         │                                               │
│   ▼         ▼                                               │
│ Model    Tool                                               │
│ Delta    Finished                                            │
│   │         │                                               │
│   ▼         ▼                                               │
│ Model    Result                                             │
│ Finished │                                                   │
│   │      │                                                   │
│   └──────┴──────────────────────────────────────┐           │
│                                                  │           │
│                                                  ▼           │
│                                            Task Completed     │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 23.4 安全检查流程图

```
┌─────────────────────────────────────────────────────────┐
│                    Security Check Flow                    │
├─────────────────────────────────────────────────────────┤
│                                                          │
│   Action Request                                         │
│        │                                                 │
│        ▼                                                 │
│   ┌─────────────┐                                        │
│   │ Parse Action │                                        │
│   └──────┬──────┘                                        │
│          │                                                │
│          ▼                                                │
│   ┌──────────────────┐                                    │
│   │ Classify Target   │──→ 路径/命令/网络/模型             │
│   └──────┬───────────┘                                    │
│          │                                                │
│          ▼                                                │
│   ┌──────────────────┐                                    │
│   │ Check Risk Level  │──→ low/medium/high/critical       │
│   └──────┬───────────┘                                    │
│          │                                                │
│          ▼                                                │
│   ┌──────────────────┐                                    │
│   │ Risk Based Policy │                                    │
│   └──────┬───────────┘                                    │
│          │                                                │
│    ┌─────┴─────┬─────────┬─────────┐                       │
│    ▼          ▼         ▼         ▼                       │
│  Allow   Allow+Warn  Confirm   Deny                        │
│    │          │         │         │                        │
│    └──────────┴─────────┴─────────┘                       │
│                      │                                     │
│                      ▼                                     │
│              Audit Log                                     │
│                      │                                     │
│                      ▼                                     │
│              Return Result                                 │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

***

## 二十五、API协议版本管理

### 24.1 版本策略

**版本号格式**

```
{protocol}.{major}.{minor}

示例：v1.2.0
- protocol: 协议格式版本（重大变更时递增）
- major: 主版本（不兼容变更）
- minor: 次版本（向后兼容新增）
```

**向后兼容规则**

- 新增字段：允许
- 废弃字段：保留至少2个大版本
- 删除字段：需要major版本升级
- 语义变更：需要major版本升级

### 24.2 版本协商

**WebSocket版本协商**

```typescript
interface VersionNegotiation {
  clientVersion: string;      // 客户端支持版本
  serverVersion: string;      // 服务端支持版本
  negotiatedVersion: string;  // 协商后的版本
  capabilities: string[];     // 协商后的能力集
}
```

**协商流程**

1. 客户端连接时声明支持版本
2. 服务端选择最高兼容版本
3. 双方使用协商版本通信
4. 版本不兼容时断开连接并提示升级

### 24.3 协议变更管理

**变更日志规范**

```typescript
interface ProtocolChangelog {
  version: string;
  date: string;
  changes: {
    type: 'added' | 'changed' | 'deprecated' | 'removed' | 'fixed';
    category: 'event' | 'command' | 'data_structure';
    description: string;
    migration_guide?: string;
  }[];
}
```

***

## 二十六、新手引导(ONBOARDING)

### 25.1 引导流程设计

**引导阶段**

```typescript
interface OnboardingStep {
  id: string;
  title: string;              // 本地化key
  description: string;         // 本地化key
  target?: string;            // 高亮目标元素选择器
  position: 'top' | 'bottom' | 'left' | 'right' | 'center';
  action?: {
    type: 'click' | 'input' | 'select' | 'none';
    selector: string;
    expectedValue?: string;
  };
  skipable: boolean;
}
```

**引导步骤顺序**

1. 欢迎页（3秒自动跳过）
2. 创建第一个工作区
3. 添加第一个模型
4. 发送第一条消息
5. 查看任务执行流程
6. 打开设置中心
7. 引导完成

### 25.2 工具提示系统

**触发时机**

- 首次进入某功能区域
- 悬停超过2秒显示
- 图标按钮点击后显示
- 功能变更后提示

**提示内容规范**

```typescript
interface TooltipConfig {
  content: string;            // 本地化key
  maxWidth: number;            // 最大宽度（px）
  position: 'auto' | 'top' | 'bottom' | 'left' | 'right';
  delay: number;              // 显示延迟（ms）
  duration?: number;          // 自动消失时间
  showArrow: boolean;         // 是否显示箭头
}
```

### 25.3 快捷键提示

**上下文快捷键**

- 当前页面/面板相关的快捷键
- 基于用户当前操作上下文推荐
- 支持点击学习完整快捷键列表

**提示位置**

- 菜单项右侧
- 按钮hover时
- 面板标题栏

***

## 二十七、发布与部署

### 26.1 构建产物结构

```yaml
# electron-builder配置结构
app:
  artifactName: "${productName}-${version}-${os}-${arch}.${ext}"
  
targets:
  - target: nsis        # Windows安装包
    arch:
      - x64
  - target: dmg         # macOS安装包
    arch:
      - x64
      - arm64
  - target: AppImage    # Linux
    arch:
      - x64

files:
  - dist/**/*
  - resources/**/*
  - "!node_modules/**/*"  # 排除，开发调试用
  - productName.app/Contents/Resources/node_modules
  
asar: true
compression: maximum
```

### 26.2 发布流程

```yaml
# 发布检查清单
pre_release_check:
  - [ ] 所有单元测试通过
  - [ ] E2E测试通过
  - [ ] 代码覆盖率达标
  - [ ] 安全扫描通过
  - [ ] 性能测试达标
  - [ ] 多语言翻译完成
  - [ ] Release notes撰写
  - [ ] 版本号更新

release_process:
  - name: Create Release Branch
    command: git checkout -b release/v${version}
  - name: Update Version
    command: npm run version:bump
  - name: Build
    command: npm run build:all
  - name: Test Built App
    command: npm run test:packaged
  - name: Create GitHub Release
    command: gh release create
  - name: Upload Artifacts
    command: npm run upload:assets
  - name: Notify
    command: npm run notify:update
```

### 26.3 自动更新配置

```typescript
interface AutoUpdaterConfig {
  // 更新源
  provider: 'github' | 'generic';
  owner: string;
  repo: string;

  // 发布通道
  channel: 'stable' | 'beta';

  // 检查策略
  checkOnStart: boolean;
  checkInterval: number;      // 检查间隔（小时）

  // 下载策略
  autoDownload: boolean;      // 自动下载
  autoInstallOnQuit: boolean; // 退出时自动安装

  // 回滚配置
  allowRollback: boolean;
  maxRollbackVersions: number;
}
```

***

## 二十八、安全漏洞防护

### 27.1 依赖安全审计

**审计策略**

```yaml
# npm audit配置
audit:
  level: high           # 高风险及以上必须修复
  production: true       # 检查生产依赖
  registry: https://registry.npmjs.org/

# 定期扫描
schedule: "0 0 * * 0"   # 每周日凌晨扫描

# 自动修复
auto_fix:
  minor: true            # 自动修复次版本漏洞
  major: false           # 主版本需要人工确认
```

**禁止使用的依赖**

- 已知含有恶意代码的包
- 停止维护超过2年的关键依赖
- 存在已知未修复高危漏洞的包

### 27.2 代码安全扫描

**扫描工具**

- ESLint安全插件（no-eval, no-new-func等）
- 代码注入检测
- 敏感信息检测（grep: password, secret, api\_key）
- Electron安全最佳实践检查

**CI集成**

```yaml
security_scan:
  steps:
    - npm audit
    - npm run security:scan
    - trivy image app:latest
```

### 27.3 Electron安全配置

```typescript
// 安全的BrowserWindow配置
const secureWindowConfig = {
  webPreferences: {
    nodeIntegration: false,
    contextIsolation: true,
    sandbox: true,
    webSecurity: true,
    allowRunningInsecureContent: false,
    experimentalFeatures: false
  },

  // 安全相关设置
  allowpopups: false,
  disablewebsecurity: false
};

// CSP配置
const CSP = `
  default-src 'self';
  script-src 'self';
  style-src 'self' 'unsafe-inline';
  img-src 'self' data: https:;
  connect-src 'self' https://api.openai.com https://api.anthropic.com;
  font-src 'self';
  object-src 'none';
  base-uri 'self';
  form-action 'self';
  frame-ancestors 'none';
`;
```

***

## 附录A：完整目录结构（Monorepo 最终版）

```
repo/
├── apps/
│   ├── desktop/                        # Electron桌面端
│   │   ├── src/
│   │   │   ├── pages/
│   │   │   │   ├── Home/
│   │   │   │   ├── Chat/
│   │   │   │   ├── Runtime/
│   │   │   │   ├── Plugins/
│   │   │   │   ├── Tasks/
│   │   │   │   └── Settings/
│   │   │   ├── components/
│   │   │   │   ├── TaskGraph/
│   │   │   │   ├── PluginCard/
│   │   │   │   ├── WorkerMonitor/
│   │   │   │   ├── EventViewer/
│   │   │   │   └── MetricsPanel/
│   │   │   ├── hooks/
│   │   │   ├── services/
│   │   │   └── App.tsx
│   │   └── electron/
│   │       ├── main.ts
│   │       ├── preload.ts
│   │       └── ipc.ts
│   │
│   └── runtime-api/                    # Fastify API
│       ├── src/
│       │   ├── routes/
│       │   │   ├── runtime.ts
│       │   │   ├── plugin.ts
│       │   │   ├── metrics.ts
│       │   │   └── task.ts
│       │   └── server.ts
│       └── ...
│
├── core/
│   ├── intent/
│   │   ├── service.ts
│   │   ├── parser.ts
│   │   ├── schema.ts
│   │   └── prompts/
│   │
│   ├── planner/
│   │   ├── planner.ts
│   │   ├── rules/
│   │   └── dag-builder.ts
│   │
│   ├── graph/
│   │   ├── task-graph.ts
│   │   ├── task-node.ts
│   │   ├── topological.ts
│   │   └── dependency.ts
│   │
│   ├── scheduler/
│   │   ├── scheduler.ts
│   │   ├── queue.ts
│   │   ├── aging.ts
│   │   ├── priority.ts
│   │   └── policy/
│   │       ├── fifo.ts
│   │       ├── weighted.ts
│   │       └── fair.ts
│   │
│   ├── governor/
│   │   ├── cpu.ts
│   │   ├── memory.ts
│   │   ├── gpu.ts
│   │   ├── resource-manager.ts
│   │   └── quotas.ts
│   │
│   ├── runtime/
│   │   ├── runtime.ts
│   │   ├── runtime-manager.ts
│   │   ├── state-machine.ts
│   │   ├── recovery.ts
│   │   ├── snapshot.ts
│   │   └── context-bus.ts
│   │
│   ├── plugin/
│   │   ├── plugin-manager.ts
│   │   ├── plugin-loader.ts
│   │   ├── registry.ts
│   │   ├── capability-graph.ts
│   │   ├── health.ts
│   │   ├── sandbox.ts
│   │   ├── permissions.ts
│   │   └── lifecycle.ts
│   │
│   ├── worker/
│   │   ├── worker.ts
│   │   ├── worker-pool.ts
│   │   ├── worker-cache.ts
│   │   ├── prewarm.ts
│   │   ├── idle-cleanup.ts
│   │   └── predictor.ts
│   │
│   ├── breaker/
│   │   ├── circuit-breaker.ts
│   │   └── policy.ts
│   │
│   └── metrics/
│       ├── collector.ts
│       ├── monitor.ts
│       ├── exporter.ts
│       └── scoring.ts
│
├── runtime/
│   ├── jdks/
│   │   ├── 16/
│   │   ├── 21/
│   │   └── 26/
│   ├── python/
│   │   └── 3.12/
│   ├── node/
│   │   └── 22/
│   ├── rust/
│   │   └── stable/
│   └── cache/
│
├── plugins/
│   ├── filesystem/
│   │   ├── plugin.json
│   │   ├── permissions.json
│   │   ├── main.jar
│   │   └── assets/
│   ├── android-builder/
│   │   ├── plugin.json
│   │   ├── main.jar
│   │   └── sdk/
│   ├── git/
│   ├── browser/
│   ├── memory/
│   ├── embedding/
│   └── deploy/
│
├── packages/
│   ├── protocol/
│   │   ├── rpc.ts
│   │   ├── messages.ts
│   │   ├── request.ts
│   │   ├── response.ts
│   │   └── version.ts
│   │
│   ├── sdk/
│   │   ├── java/
│   │   ├── python/
│   │   ├── rust/
│   │   └── ts/
│   │
│   ├── events/
│   │   ├── events.ts
│   │   ├── runtime.ts
│   │   └── schemas.ts
│   │
│   └── types/
│       ├── task.ts
│       ├── plugin.ts
│       └── runtime.ts
│
├── storage/
│   ├── eventlog/
│   │   ├── events.jsonl
│   │   ├── snapshots/
│   │   └── archive/
│   ├── projection/
│   │   ├── sqlite.db
│   │   ├── task-projection.ts
│   │   ├── plugin-projection.ts
│   │   └── runtime-projection.ts
│   └── surreal/
│       ├── schema.surql
│       ├── migrations/
│       └── indexes/
│
├── workspace/
│   ├── plugin-a/
│   ├── plugin-b/
│   └── temp/
│
├── scripts/
│   ├── install-jdk.ts
│   ├── install-python.ts
│   ├── install-node.ts
│   ├── bootstrap.ts
│   └── build-runtime.ts
│
├── configs/
│   ├── runtime.yaml
│   ├── plugin.yaml
│   ├── scheduler.yaml
│   └── governor.yaml
│
├── tests/
│   ├── plugin/
│   ├── runtime/
│   ├── scheduler/
│   └── e2e/
│
├── docs/
│   ├── architecture/
│   ├── protocol/
│   ├── plugins/
│   └── runtime/
│
├── turbo.json
├── pnpm-workspace.yaml
├── package.json
├── tsconfig.base.json
├── .env
└── README.md
```

***

## 附录B：事件完整列表

```typescript
// 任务相关
'task.created'
'task.queued'
'task.started'
'task.progress'
'task.retrying'
'task.waiting_confirm'
'task.waiting_dependency'
'task.succeeded'
'task.failed'
'task.cancelled'
'task.timeout'
'task.partial_success'

// 规划器相关
'planner.started'
'planner.completed'
'planner.failed'

// 模型相关
'model.primary.call.started'
'model.primary.call.delta'
'model.primary.call.finished'
'model.primary.call.failed'
'model.secondary.call.started'
'model.secondary.call.delta'
'model.secondary.call.finished'
'model.secondary.call.failed'

// 工具相关
'tool.call.started'
'tool.call.progress'
'tool.call.finished'
'tool.call.failed'

// 技能相关
'skill.call.started'
'skill.call.finished'
'skill.call.failed'
'skill.sync.started'
'skill.sync.finished'
'skill.sync.failed'

// 文件相关
'file.read.started'
'file.read.finished'
'file.write.started'
'file.write.finished'
'file.write.failed'
'file.delete.started'
'file.delete.finished'
'file.delete.failed'

// 预览相关
'preview.refresh.started'
'preview.updated'
'preview.refresh.failed'

// 下载相关
'download.started'
'download.progress'
'download.paused'
'download.resumed'
'download.completed'
'download.failed'
'download.cancelled'

// 日志相关
'log.appended'
'log.rotated'

// 安全相关
'security.check.started'
'security.check.result'
'security.consent.requested'
'security.consent.granted'
'security.consent.denied'

// 审计相关
'audit.recorded'

// 系统相关
'app.started'
'app.ready'
'app.error'
'app.quitting'
'app.updated'
'crash.report'

// 会话相关
'session.created'
'session.resumed'
'session.paused'
'session.closed'

// 健康检查
'healthcheck.completed'
'healthcheck.alert'

// 缓存相关
'cache.evicted'
'cache.cleared'
'storage.quota_warning'
'storage.quota_exceeded'
```

***

## 附录C：错误代码规范

```typescript
// 错误代码格式：{模块}{类型}{序号}
// 模块：NET/MOD/FILE/SEC/TASK/TOOL/SKILL/MEM/KNOW/CHAN/DOWN/SYS
// 类型：01-99

enum ErrorCode {
  // 网络错误 (01-09)
  NET_TIMEOUT = 'NET001',
  NET_CONNECTION_REFUSED = 'NET002',
  NET_DNS_FAILED = 'NET003',
  NET_SSL_ERROR = 'NET004',

  // 模型错误 (10-19)
  MOD_CALL_FAILED = 'MOD010',
  MOD_RATE_LIMIT = 'MOD011',
  MOD_INVALID_RESPONSE = 'MOD012',
  MOD_AUTH_FAILED = 'MOD013',

  // 文件错误 (20-29)
  FILE_NOT_FOUND = 'FILE020',
  FILE_PERMISSION_DENIED = 'FILE021',
  FILE_TOO_LARGE = 'FILE022',
  FILE_OPERATION_FAILED = 'FILE023',

  // 安全错误 (30-39)
  SEC_POLICY_DENIED = 'SEC030',
  SEC_INVALID_TARGET = 'SEC031',
  SEC_CRITICAL_FILE = 'SEC032',

  // 任务错误 (40-49)
  TASK_NOT_FOUND = 'TASK040',
  TASK_TIMEOUT = 'TASK041',
  TASK_CANCELLED = 'TASK042',
  TASK_DEPENDENCY_FAILED = 'TASK043',

  // 系统错误 (90-99)
  SYS_INIT_FAILED = 'SYS090',
  SYS_CRASHED = 'SYS091',
  SYS_UNSUPPORTED = 'SYS092'
}
```

***

*文档版本：v1.0*
*最后更新：${new Date().toISOString().split('T')\[0]}*
*维护者：SoloForge Team*
