# SoloForge AI Society 服务依赖
# Python: 3.13.14(3.12 兼容至 2026-Q4)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 职责：AI 社会数据存储与 MARL 强化学习
# ⚠️  AI 社会专用数据库 ⚠️  与主项目完全隔离
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Deep Learning - PyTorch MAPPO (强化学习资源调度)
# CPU 版本安装: pip install torch --index-url https://download.pytorch.org/whl/cpu
# 3.13 兼容: 2.6.0+ 官方 cp313 wheel
torch>=2.6.0,<3.0.0

# Numerical Computing
# 3.13 兼容: numpy 2.0+
numpy>=2.0.0,<3.0.0

# IPC Serialization - MessagePack (IPC 通信协议)
msgpack>=1.0.0

# High-Performance Computing - Numba (可选，JIT 加速)
# 自动降级：无 numba 时使用纯 NumPy 计算
# 3.13 兼容: 0.61.0+ (llvmlite 0.44+)
numba>=0.61.0

# Local LLM Inference - llama-cpp-python (Phase 5 实时训练)
# 用于本地小模型推理 (Qwen2.5-0.5B-Instruct GGUF), 驱动 PromptOptimizer
# Windows 预编译 wheel: pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
# 模型下载: python tools/download_small_llm.py
llama-cpp-python>=0.3.0

# Development Dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.3.0
