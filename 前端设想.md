# SoloForge 前端设想

> 来源：基于 SoloForge 项目 `UI/`、`src/` 实际源码 + 8 份文档（README / 系统规格说明 / 治理白皮书 / 融合文档 v3.0 / 数据库架构 / 任务进度等）
> 生成日期：2026-06-03
> 修正版：与 `UI/src/` 实际目录、组件、API 严格对齐

---

## 一、整体定位

SoloForge 是一个 **Node.js 后端 + Vite/React 18 浏览器前端** 的本地应用，后端把内核状态通过 HTTP + SSE 暴露给前端。前端定位是"AI 多智能体协作控制台"：

- **任务执行流** 而非"聊天消息流"
- **流送区** 实时展示 AI 思考过程
- **统一通过 REST API + SSE** 读取后端状态
- **多模型混合** 协作（主模型 + 副模型）

---

## 二、技术栈（实测）

### 2.1 前端（`UI/` 目录）

| 类别 | 选型 | 备注 |
|------|------|------|
| 构建工具 | **Vite 5.4.2** | `vite.config.ts` |
| UI 框架 | **React 18.3.1** + `react-dom` | |
| 语言 | **TypeScript 5.5.3** | `UI/tsconfig.json` |
| 入口 | `UI/index.html` + `UI/src/main.tsx` + `UI/src/App.tsx` | |
| 样式 | **Tailwind CSS（CDN）** | `https://cdn.tailwindcss.com`（**非构建式**，运行时注入） |
| 字体 | Inter / JetBrains Mono / Hanken Grotesk | Google Fonts |
| 图标 | **Material Symbols Outlined** | Google Fonts（变体 FILL/wght/GRAD/opsz） |
| 状态管理 | **React Context + useState**（无 Zustand） | ThemeProvider 提供主题 |
| 路由 | **无 React Router** | 用 `activity` state 切换面板 |
| 虚拟列表 | **无 React Virtual** | 实际未使用 |
| 终端 | **xterm.js**（待确认） | `UI/src/components/terminal/Terminal.tsx` |
| 代码编辑器 | **自研 CodeEditor**（非 Monaco） | `UI/src/components/editor/CodeEditor.tsx`（22.3K） |
| 动画 | **motion v12.40.0**（后端依赖） | 用于后端管理 UI |

### 2.2 后端（`src/` 目录）

| 类别 | 选型 | 备注 |
|------|------|------|
| 运行时 | **Node.js 22 LTS**(24 LTS 可选) | `package.json` `@types/node: ^22.0.0` |
| 语言 | **TypeScript** | `strict: false` |
| 入口 | `src/index.ts`（10.8K） | `npm start` = `tsx src/index.ts` |
| API 服务 | `src/api-server.ts`（37.9K） | HTTP + SSE，端口 **3001** |
| 总装 | `src/bootstrap.ts`（17.5K） | Runtime 装配 |
| 模块 | `src/core/` + `src/kernel/` + `src/data/` + `src/runtime/` + `src/observability/` | |
| 进程管理 | **tsx**（`^4.22.4`） | 直接 ts 执行 |
| 后端管理 UI | `src/ui/index.html`（57.4K 单页） | 独立大 HTML，`localhost:3001/ui/` 访问 |

### 2.3 数据库与基础设施

| 类别 | 选型 | 备注 |
|------|------|------|
| 热数据 | **Garnet v1.1.10** | 端口 6379，Redis 协议 |
| 温数据 | **SurrealDB 3.0.x** | 嵌入式 `rocksdb://` 协议 |
| 冷数据 | **JSONL** | `data/jsonl/` |
| Python | **3.13.14** | `bin/python-3.13/` |
| Rust | **stable** | `rust_core/` |
| 客户端 | `ioredis: ^5.11.0` / `surrealdb: ^2.0.3` / `@surrealdb/node: ^3.0.3` / `ulid: ^3.0.2` | |
| 序列化 | `@msgpack/msgpack: ^3.1.3` | |
| 测试 | **Vitest 4.1.7** | |

### 2.4 强制版本约束

- **禁止 Rust nightly**(Node 24 LTS 与 Python 3.13.14 已于 2026-06 启用,3.12 兼容至 2026-Q4)
- **稳定性 > 性能 > 顶级性能**
- ⚠️ `electron: ^42.3.0` 在 devDependencies 中，但**实际未启用**（无 electron 主进程代码）

---

## 三、目录结构（实际）

```
SoloForge/
├── UI/                          # Vite + React 前端
│   ├── index.html               # 入口 HTML（Tailwind CDN + 字体）
│   ├── package.json             # 前端独立 package
│   ├── tsconfig.json
│   ├── vite.config.ts           # 端口 5173，反代到 :3001
│   └── src/
│       ├── main.tsx             # React 挂载入口
│       ├── App.tsx              # 主布局 + 全局状态
│       ├── api/
│       │   ├── client.ts        # fetch 封装 + SSE 订阅
│       │   └── index.ts         # 导出
│       ├── hooks/
│       │   ├── useBackend.ts    # 拉取 kernel/system/db/agents/events
│       │   ├── useChat.ts       # 对话/流送（localStorage 持久化）
│       │   ├── useResources.ts   # 文件树（前端内置示例）
│       │   └── useKeyboard.ts   # 全局快捷键
│       ├── themes/
│       │   ├── types.ts         # Theme / ThemeTokens 类型
│       │   ├── themes.ts        # 4 个主题定义
│       │   ├── ThemeProvider.tsx # CSS 变量注入 <html>
│       │   ├── global.css       # 全局样式
│       │   └── index.ts
│       ├── types/
│       │   ├── types.ts         # 后端返回类型
│       │   └── index.ts
│       └── components/
│           ├── layout/          # TopBar / ActivityBar / LeftSidebar
│           ├── panels/          # CenterPanels / SettingsModal / SidePanels
│           ├── chat/            # ChatPanel
│           ├── editor/          # CodeEditor（自研）
│           ├── preview/         # PreviewPane
│           ├── resources/       # FileTree
│           ├── statusbar/       # StatusBar
│           ├── terminal/        # Terminal
│           ├── overlays/        # CommandPalette / GlobalSearch / SkillsMarket / RecentActivity / FeatureTour / Notifications / Splash / RoleSelector
│           └── ui/              # Button
│
├── src/                         # Node.js 后端
│   ├── index.ts                 # 入口：启动 SurrealDB + 看门狗
│   ├── bootstrap.ts             # 总装
│   ├── api-server.ts            # HTTP + SSE
│   ├── core/                    # 业务领域
│   │   ├── agent/               # autonomous_agent
│   │   ├── court/               # consensagent / llm_escalation
│   │   ├── decision/            # rtr-racer-engine
│   │   ├── economy/
│   │   ├── events/
│   │   ├── governor/            # mappo-client / ipc
│   │   ├── law/
│   │   ├── logger/
│   │   └── society/             # institution/governance/culture/reputation/role-evolution/coalition/social-memory
│   ├── data/
│   │   ├── surreal_driver_live.ts
│   │   ├── surreal_persistence.ts
│   │   ├── delete_protection.ts
│   │   ├── transaction_kernel.ts
│   │   ├── data-archiver.ts
│   │   ├── garnet/              # client/cache/queue/lock/compensation/index
│   │   ├── jsonl/
│   │   ├── consumers/           # governor-shadow-consumer
│   │   └── repositories/        # decision/court/event/trace/surreal/factory
│   ├── kernel/                  # 微内核
│   │   ├── runtime-kernel.ts
│   │   ├── command-bus.ts
│   │   ├── transaction-manager.ts
│   │   ├── state-ownership.ts
│   │   ├── scheduler-client.ts
│   │   ├── governor-shadow-orchestrator.ts
│   │   ├── shadow-governor-client.ts
│   │   ├── backpressure.ts
│   │   ├── watchdog.ts
│   │   ├── tracing.ts
│   │   ├── orchestration.ts
│   │   ├── registry.ts
│   │   ├── observability/       # telemetry-exporter
│   │   ├── runtime-component.ts
│   │   └── domains/             # ai-runtime
│   ├── runtime/                 # 运行时
│   │   ├── kernel.ts
│   │   ├── supervisor.ts
│   │   ├── registry.ts
│   │   ├── health.ts
│   │   ├── lifecycle.ts
│   │   ├── orchestration.ts
│   │   ├── tracing.ts
│   │   └── state/state.ts
│   └── ui/
│       └── index.html           # 后端管理 UI（独立大 HTML，57.4K）
│
├── python/                      # Python 3.12 MARL 训练
│   ├── mappo_server.py
│   ├── configs/
│   ├── data/ai_society/         # SQLite + LanceDB
│   ├── datasets/
│   ├── experiments/
│   ├── checkpoints/
│   ├── artifacts/
│   └── soloforge_ai_society/
│
├── rust_core/                   # Rust 调度器
│
├── migrations/                  # SurrealDB 迁移（v1_base ~ v5_events）
│   ├── down/
│   ├── 20240101000000__v1_base_schema_migrations.surql
│   ├── 20240101010000__v2_decision_chain.surql
│   ├── 20240101020000__v3_court_governance.surql
│   ├── 20240101030000__v4_governor_marl.surql
│   ├── 20240101040000__v5_event_audit.surql
│   └── 202605281200__init_baseline.surql
│
├── infra/                       # 旧 Schema（向后兼容）
│
├── scripts/                     # 启动/迁移/归档/观测脚本
│   ├── db-migrate.ts
│   ├── db-rollback.ts
│   ├── db-status.ts
│   ├── db-common.ts
│   ├── start-archiver.ts
│   ├── test-archiver.ts
│   ├── long-term-observation.ts
│   └── start-admin-ui.bat
│
├── tests/
│
├── docs/                        # 文档
│   ├── SOLOFORGE-GOVERNANCE-WHITEPAPER.md
│   └── db-migration-baseline.md
│
├── data/                        # 运行时数据（不入版本控制）
│   ├── soloforge_db/            # SurrealDB RocksDB
│   └── jsonl/                   # 冷数据
│
├── bin/                         # 二进制
│   ├── garnet/portable/net10.0/
│   └── python-3.13/
│
├── patches/                     # npm patch
│
├── reports/                     # 报告
│
└── 文档/                        # 项目自带 8 份文档
    ├── README.md
    ├── 系统规格说明.md
    ├── 任务进度.md
    ├── SoloForge-database-arch.md
    ├── 数据库实施蓝图-团队分工版.md
    ├── AI社会体系与数据存储方案-完整解析.md
    └── SoloForge_技术规范融合文档_v3.0.docx
```

---

## 四、端口与代理

| 端口 | 服务 | 入口 |
|------|------|------|
| **3001** | 后端 API + SSE + 后端管理 UI | `npm start` |
| **5173** | 前端开发服务器 | `cd UI && npm run dev` |
| **6379** | Garnet（独立进程） | `bin/garnet/portable/net10.0/GarnetServer.exe --lua --lua-script-timeout 5000 --port 6379` |

### Vite 代理（`UI/vite.config.ts`）

```typescript
proxy: {
  '/api':     { target: 'http://localhost:3001', changeOrigin: true },
  '/metrics': { target: 'http://localhost:3001', changeOrigin: true },
  '/ui':      { target: 'http://localhost:3001', changeOrigin: true },
}
```

### API Base 解析（`UI/src/api/client.ts`）

```typescript
function resolveApiBase(): string {
  // 1) VITE_API_BASE 环境变量
  // 2) 浏览器同源（3001 / 80 / 空）→ ''（相对路径）
  // 3) dev: 5173 → ''（走 Vite 代理）
  // 4) 默认: 'http://localhost:3001'
}
```

---

## 五、主界面布局（实测）

App.tsx 注释：**「完全按线框图：左 资源+代码 | 中 流送+历史+对话 | 右 预览 | 顶 栏 | 底 状态栏」**

```
┌──────────────────────────────────────────────────────────┐
│  TopBar（顶栏）                                            │
│  [项目名] [模型设置] [连接状态] [最后更新] [刷新] [设置] [命令面板] [活动] [搜索]│
├──────┬──────────┬──────────────────────┬──────────────────┤
│      │          │                      │                  │
│ 活   │  Left    │   CenterPanels       │   PreviewPane    │
│ 动   │  Sidebar │   ─ StreamPanel      │   ─ 预览生成结果  │
│ 栏   │          │   ─ HistoryPanel     │   ─ 系统/内核信息 │
│      │  资源/   │   ─ ChatPanel        │                  │
│      │  搜索/   │                      │                  │
│      │  法庭/   │                      │                  │
│      │  源码/   │                      │                  │
│      │  Git/    │                      │                  │
│      │  终端    │                      │                  │
│      │          │                      │                  │
├──────┴──────────┴──────────────────────┴──────────────────┤
│  StatusBar（状态栏）                                        │
│  [项目] [内核] [系统] [调度器] [设置入口]                      │
└──────────────────────────────────────────────────────────┘
```

**附加层**（覆盖在上方）：
- 顶部错误条：后端不可达时显示
- **Splash**（启动屏）
- **WelcomeNotifications**（欢迎通知）
- **CommandPalette**（Ctrl+K / Ctrl+Shift+P）
- **GlobalSearch**（Ctrl+Shift+F）
- **SkillsMarket**（技能市场）
- **RecentActivity**（最近活动）
- **FeatureTour**（功能巡览，未完成时首次启动展示）
- **SettingsModal**（Ctrl+,）
- **NotificationCenter**（右下角通知）

---

## 六、Activity 栏（左侧活动栏 6 个）

通过 `activity` state 切换 `LeftSidebar` 内容：

| 活动 ID | 中文 | 触发快捷键 |
|--------|------|-----------|
| `explorer` | 资源管理 | Ctrl+B |
| `court` | 法庭 | Ctrl+P |
| `git` | 源码管理 | Ctrl+G |
| `search` | 搜索 | Ctrl+S |
| `terminal` | 终端 | ` `（反引号） |

`SettingsModal` 不属于 activity 切换，是模态弹窗。

---

## 七、TopBar（顶栏）

显示内容：
- 项目名（可编辑，默认 `SoloForge`，存 `localStorage.soloforge.project.name`）
- 模型设置（来自 `chat.settings`）
- 后端连接状态（`connected: bool`）
- 最后更新时间（`new Date().toLocaleTimeString('zh-CN')`）
- 刷新按钮
- 设置入口（Ctrl+,）
- 命令面板（Ctrl+K / Ctrl+Shift+P）
- 全局搜索（Ctrl+Shift+F）
- 最近活动

---

## 八、StatusBar（状态栏）

底部状态栏：
- 项目名
- 内核状态
- 系统状态
- 调度器统计
- 设置入口

---

## 九、主题系统（实测 4 主题）

### 9.1 4 个主题（`UI/src/themes/themes.ts`）

| ID | 中文名 | 风格 | 主色 |
|----|--------|------|------|
| `default-dark` | **暗金**（默认） | 深色暖金 | `#e7c35a` |
| `ocean-dark` | **深海** | 深色蓝调 | `#58a6ff` |
| `morning-light` | **晨光** | 浅色暖橙 | `#d97706` |
| `amethyst` | **紫晶** | 深色紫调 | `#c084fc` |

**注**：文档里描述 5 个主题（含「跟随系统」），实际只实现 4 个。

### 9.2 主题切换原理

主题 = 一组 CSS 变量值（`ThemeTokens`），运行时注入 `<html>` 根节点。Tailwind 颜色映射 `var(--color-xxx)`，变量一换全局跟着变。**零页面刷新、零重新渲染，纯 DOM 操作 < 1ms**。

**实现位置**：`UI/src/themes/ThemeProvider.tsx:37-46` `applyThemeTokens()`，循环 `TOKEN_TO_CSS_VAR`（同文件 16-34 行）逐个 `root.style.setProperty(cssVar, tokens[key])`；同时根据亮度判断 `dark`/`light` 状态并 toggle `<html>` class。

### 9.3 主题深浅自动判定（`isDarkTheme`）

源码：`UI/src/themes/ThemeProvider.tsx:49-55`

```typescript
function isDarkTheme(tokens: ThemeTokens): boolean {
  const hex = tokens.bg.replace('#', '');
  const r = parseInt(hex.substring(0, 2), 16);
  const g = parseInt(hex.substring(2, 4), 16);
  const b = parseInt(hex.substring(4, 6), 16);
  return (r * 299 + g * 587 + b * 114) / 1000 < 128;
}
```

**注**：系数 299/587/114 是 ITU-R BT.601 亮度权重（人眼对绿最敏感，对蓝最钝），阈值 128 是 0-255 的中点。**优点**：纯函数、无外部依赖；**局限**：只看 `bg` 不看 `surface`，所以"晨光"主题（`bg=#ffffff` 偏亮）也能正确进入 light 分支，但若未来出现"暗紫 + 亮文字"这种对比反转主题仍会归为 light。

### 9.4 17 个 Token → CSS 变量表

源码：`UI/src/themes/ThemeProvider.tsx:16-34`

| Token（ts 字段） | CSS 变量 | 用途 |
|---|---|---|
| `bg` | `--color-bg` | 应用最底层背景 |
| `bgDim` | `--color-bg-dim` | 比 bg 更暗/更亮的一层（用于 Modal 背板） |
| `surface` | `--color-surface` | 卡片/面板底色 |
| `surfaceLow` | `--color-surface-low` | 凹陷区（输入框背景） |
| `surfaceHigh` | `--color-surface-high` | 抬升区（hover、激活态） |
| `primary` | `--color-primary` | 主题主色（按钮、链接、强调） |
| `onPrimary` | `--color-on-primary` | 主色之上文字 |
| `primaryContainer` | `--color-primary-container` | 主色弱化容器背景 |
| `onPrimaryContainer` | `--color-on-primary-container` | 主色容器文字 |
| `text` | `--color-text` | 正文 |
| `textSecondary` | `--color-text-secondary` | 次级文字（提示、placeholder） |
| `border` | `--color-border` | 标准边框 |
| `borderLight` | `--color-border-light` | 弱化边框（分隔线） |
| `accent` | `--color-accent` | 辅助强调（信息色，不属主色） |
| `success` | `--color-success` | 成功态 |
| `warning` | `--color-warning` | 警告态 |
| `danger` | `--color-danger` | 错误态 |

### 9.5 4 主题对比

数据来源：`UI/src/themes/themes.ts:9-106`

| 主题 | id | bg | surface | primary | accent | 风格 |
|---|---|---|---|---|---|---|
| 暗金 | `default-dark` | `#121414` | `#1e2020` | `#e7c35a` | `#58a6ff` | 深色暖金（默认） |
| 深海 | `ocean-dark` | `#0d1117` | `#161b22` | `#58a6ff` | `#bc8cff` | 深色蓝调 |
| 晨光 | `morning-light` | `#ffffff` | `#ffffff` | `#d97706` | `#2563eb` | 浅色暖橙 |
| 紫晶 | `amethyst` | `#13111c` | `#1d1a2e` | `#c084fc` | `#f472b6` | 深色紫调 |

**关键观察**：
- 3 深 1 浅；`morning-light` 是唯一浅色，其余 bg 亮度均 < 0x80。
- `accent` 与 `primary` 多数互补（暗金 primary 暖金 / accent 冷蓝；深海 primary 冷蓝 / accent 紫），形成"主色 + 信息色"对照。
- `success` / `warning` / `danger` 在 3 个深色主题中保持一致（`#3fb950` / `#d29922` / `#f85149`），仅在晨光中调整为更深的版本（`#059669` / `#d97706` / `#dc2626`）以保证浅底对比度。
- `amethyst` 的 success/warning/danger 用了 300/400 系（`#34d399` / `#fbbf24` / `#fb7185`），整体调性偏粉紫。

### 9.6 应用范围（`bg` vs `surface` vs `surfaceLow/High`）

| 层级 | 出现在 |
|---|---|
| `bg` | `body` / App 最外层（`App.tsx:95` `bg-bg`） |
| `bgDim` | 启动屏背板、Modal 背板 |
| `surface` | TopBar / StatusBar / ActivityBar / Card |
| `surfaceLow` | 输入框、搜索框（凹陷） |
| `surfaceHigh` | 按钮 hover、下拉菜单激活项、代码编辑器选中 |

**注**：以上划分是阅读实际 className 推断的，非来自 `ThemeProvider` 显式约束，添加新组件时无需严格遵守，但跨组件保持一致能避免"整页雪花"。

### 9.7 实现权衡注记

- ✅ **零运行时依赖**：仅靠原生 CSS 变量 + Tailwind 颜色映射（CDN 配置 `var(--color-xxx)`），无 styled-components / emotion。
- ✅ **纯数据驱动**：`themes.ts` 加新主题零组件改动，Token 缺失会在编译期报 TS 错。
- ⚠️ **不持久化"主题偏好以外"的 UI 状态**：仅 `soloforge-theme` 一个键，浅色/深色无单独键（由 `isDarkTheme` 实时推导）。
- ⚠️ **暗色 vs 浅色由 bg 亮度推导**，未提供"强制浅色/强制深色"开关；晨光主题下用户无法"再深一点"。
- ❌ **未实现"跟随系统"**：文档中规划的 5 主题（含 `system`）未落地。
- ❌ **过渡动画全局生效**：`UI/index.html:68` 的 `* { transition: ... }` 会让任何属性变化都产生 0.2s 动画，大列表/打字时可能引发可感知的"颜色拖尾"。

### 9.8 持久化

`localStorage.soloforge-theme` 保存当前主题 ID。

---

## 十、对话与流送（`useChat` Hook）

### 10.1 9 项设置（`ChatSettings`）

```typescript
{
  hybridEnabled:   true,                            // 多模型混合
  primaryModel:    'MiniMax-M3',                    // 主模型
  secondaryModel:  'claude-haiku-4.5',              // 副模型
  systemPrompt:    '你是 SoloForge 的 AI 助手。',
  temperature:     0.7,
  enableTools:     true,
  enableMemory:    true,
  enableRag:       false,
  streamLocally:   true,                            // 客户端模拟流式
}
```

### 10.2 本地持久化

- 对话历史：`localStorage.soloforge.chat.history.v1`
- 设置：`localStorage.soloforge.chat.settings.v1`

### 10.3 流式协议

- 后端：非流式 `/api/chat/send`（占位）
- 客户端：把一次返回**拆成多块**模拟流式（注释：「客户端模拟流式」）

---

## 十一、ChatPanel / StreamPanel / HistoryPanel

- **ChatPanel**（16.6K）— 对话输入与消息展示
- **StreamPanel**（12.2K）— 流送区
- **HistoryPanel**（18.0K）— 历史对话列表

---

## 十二、文件树与代码编辑器

### 12.1 FileTree（9.0K）
- `UI/src/components/resources/FileTree.tsx`
- 数据来源：`useResources` Hook
- 持久化：`localStorage.soloforge.files.tree.v1`
- **默认树（前端内置示例，便于开箱即用）**：
  ```
  soloforge-workspace/
  ├── src/
  │   ├── components/
  │   │   ├── Layout.tsx / ChatInput.tsx / FileTree.tsx / Modal.tsx
  │   │   ├── Button.tsx / CodeEditor.tsx / SettingsPanel.tsx
  │   ├── index.ts
  │   ├── api-server.ts (9872B)
  │   ├── kernel/runtime-kernel.ts (5402B)
  │   ├── core/court/consensagent.ts (3300B)
  │   ├── data/repositories/surreal-repositories.ts (4120B)
  ├── rust_core/
  │   ├── main.rs (880B)
  │   ├── scheduler.rs (4220B)
  ├── python/
  │   ├── mappo_server.py (6720B)
  ```

### 12.2 CodeEditor（22.3K，自研）
**非 Monaco**，自实现，注释中可见。

---

## 十三、后端 API 端点（实测）

`UI/src/api/client.ts` 中封装的全部接口：

### 13.1 基础

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/health` | GET | 健康检查 `{ status, uptime }` |

### 13.2 内核

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/kernel/status` | GET | 内核状态 |
| `/api/kernel/health` | GET | 健康详情 |
| `/api/kernel/events?limit=50` | GET | 事件流 |

### 13.3 系统

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/status` | GET | 系统状态 |
| `/api/database/stats` | GET | 数据库统计 |
| `/api/agents` | GET | 智能体列表 |

### 13.4 观测

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/observation/data` | GET | 观测数据 |
| `/api/observation/start` | POST | 开始观测 |
| `/api/observation/stop` | POST | 停止观测 |
| `/api/observation/clear` | POST | 清空观测 |

### 13.5 调度

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/scheduler/stats` | GET | 调度器统计 |

### 13.6 事件

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/events/list?limit=100` | GET | 事件列表 `{ events, total, connected }` |
| `/api/events/stream` | GET (SSE) | 实时事件流 |

---

## 十四、轮询与 SSE

### 14.1 轮询

| Hook | 间隔 | 内容 |
|------|------|------|
| `useBackend` | **3s** | kernel + system + health + db + agents + events（并行 6 个请求） |
| `useObservation` | **2s** | observation 数据 |
| `useScheduler` | **3s** | scheduler 统计 |

`Promise.allSettled` 容错：任何一个失败不影响其他。

### 14.2 SSE 重连

```typescript
es.onerror = () => {
  es?.close();
  setTimeout(connect, 3000);  // 3s 后重连
};
```

---

## 十五、快捷键（`useKeyboard` Hook）

| 组合 | 动作 |
|------|------|
| `Ctrl+K` / `Ctrl+Shift+P` | 打开命令面板 |
| `Ctrl+Shift+F` | 打开全局搜索 |
| `Ctrl+,` | 打开设置 |
| `Ctrl+N` | 新建对话 |
| `` ` ``（反引号） | 打开终端 |
| `Ctrl+B` | 切到资源管理 |
| `Ctrl+P` | 切到法庭 |
| `Ctrl+G` | 切到源码管理 |
| `Ctrl+S` | 切到搜索 |
| `Ctrl+L` | 清空流送区 |
| `Ctrl+R` | 刷新后端 |

注：`Ctrl+S` 实际切到搜索活动（不是浏览器保存），可能与浏览器习惯冲突。

---

## 十六、Overlays（覆盖层组件）

| 组件 | 大小 | 说明 |
|------|------|------|
| `CommandPalette` | 10.1K | 命令面板（Ctrl+K） |
| `GlobalSearch` | 15.5K | 全局搜索（Ctrl+Shift+F） |
| `SkillsMarket` | 10.7K | 技能市场 |
| `RecentActivity` | 7.6K | 最近活动 |
| `FeatureTour` | 4.9K | 功能巡览（首次启动） |
| `Notifications` | 7.3K | 通知中心（含 WelcomeNotifications） |
| `Splash` | 2.8K | 启动屏 |
| `RoleSelector` | 5.3K | 角色选择器 |

`FeatureTour` 持久化：`localStorage.soloforge.tour.completed`

---

## 十七、PreviewPane（49.5K，最大组件）

右侧预览区，接收 `system` / `kernel` / `observation` / `db` / `agents` / `events` props，展示：

- 系统状态
- 内核状态
- 观测数据
- 数据库统计
- 智能体列表
- 事件列表

---

## 十八、核心安全设计

### 18.1 前后端边界

```
UI (5173)  ───Vite Proxy───>  后端 API (3001)
                                 │
                                 ├─ RuntimeKernel
                                 ├─ TelemetryMetricExporter
                                 ├─ SurrealPersistence
                                 └─ DataArchiverService
```

### 18.2 SSE vs WebSocket

**实际只用了 SSE**（`EventSource`），文档里描述的 WebSocket 没在 UI 代码中体现。

### 18.3 后端不可达降级

```typescript
useEffect(() => {
  if (connected) {
    pushNotification({ level: 'success', title: '后端已连接', message: API_BASE });
  } else {
    pushNotification({ level: 'warning', title: '后端离线', message: '已切换到本地模式' });
  }
}, [connected]);
```

顶部错误条：`后端不可达：${API_BASE} — 已切换到本地模式，部分功能不可用`

---

## 十九、性能约束

### 19.1 前端规则（实测）

- ✅ **轮询节流**：3s / 2s（避免高频 setState）
- ✅ **SSE 自动重连**：3s 间隔
- ✅ **Promise.allSettled**：部分失败不影响整体
- ✅ **localStorage 持久化**：避免每次重新拉取
- ✅ **CSS 变量主题切换**：< 1ms DOM 操作
- ✅ **scrollbar-hide**：隐藏滚动条
- ✅ **主题过渡动画**：`transition: background-color 0.2s ease, color 0.15s ease, border-color 0.2s ease;`

### 19.2 文档要求但未实现的

- ❌ **流送区无限消息堆积防护**：未用虚拟列表
- ❌ **WebSocket 心跳**：用 SSE 替代
- ❌ **React Router 路由**：用 `activity` state 替代

---

## 二十、新手引导

### 20.1 FeatureTour（4.9K）

**首次启动**：`localStorage` 中无 `soloforge.tour.completed` → 弹出引导
**关闭**：写入 `soloforge.tour.completed = '1'`
**重新打开**：从 CommandPalette 触发，清除 localStorage 后开启

### 20.2 Splash（2.8K）

启动屏，覆盖在 App 之上

### 20.3 WelcomeNotifications（Notifications.tsx 内）

欢迎通知，启动后推送

---

## 二十一、设置面板（13.0K）

`SettingsModal` 通过 Ctrl+, 打开，Props：
- `open` / `onClose`
- `apiBase` / `connected` / `lastUpdate`
- `projectName` / `onProjectNameChange`
- `onClearHistory`（chat.clearAll）

---

## 二十二、Types（类型定义）

`UI/src/types/types.ts` 定义的后端返回类型：

```typescript
KernelStatus
SystemStatus
DbStats
Agent
KernelEvent
HealthStatus
ObservationData    // { cycleId, timestamp, entropy, interventions, courtCases, coalitions }
SchedulerStats
ChatMessage
StreamChunk
FileNode           // { id, name, type, path, children?, language?, size? }
```

---

## 二十三、对比文档设想 vs 实际实现

| 项 | 文档设想 | 实际实现 |
|----|---------|----------|
| 主题数 | 5 个（含跟随系统） | **4 个**（暗金/深海/晨光/紫晶） |
| 状态管理 | Zustand | **React Context + useState** |
| 路由 | React Router | **activity state** |
| 编辑器 | Monaco | **自研 CodeEditor** |
| 虚拟列表 | React Virtual | **未用** |
| 通信 | WebSocket | **SSE**（3s 重连） |
| 启动命令 | `npm start`（Electron） | **`tsx src/index.ts`**（Node 22） |
| 前端入口 | `apps/desktop/` | **`UI/`** |
| 后端框架 | Fastify | **原生 `http` 模块 + 自实现路由** |
| 状态库版本 | Electron 37.x | **electron 42.3.0**（devDep，但未启用） |
| 后端管理 UI | `/admin` | **`/ui/index.html`** |
| 设置入口数 | 14 个 | **单一 SettingsModal**（具体 14 个模块未在 UI 中实现） |
| 主题图标 | 各种 PNG | **无图标**（用主题名 + CSS 变量） |

---

## 二十四、对话流程 11 阶段（文档设想）

> 注：实际 UI 中**未明确实现 11 阶段**，"对话流程"作为文档中的规划存在。

| # | 阶段 | 关键动作 |
|---|------|---------|
| 1 | **输入接收** | 生成 sessionId / turnId / requestId / rootTaskId |
| 2 | **输入预处理** | 去空输入、合并上下文、识别任务类型/意图标签/风险等级 |
| 3 | **上下文组装** | 统一组装（禁止 UI 自行拼接） |
| 4 | **任务规划** | 主模型/规划器生成计划 |
| 5 | **执行前安全评估** | 输出 allow / allow_with_warning / require_user_confirm / deny |
| 6 | **主任务执行** | 调度器驱动 |
| 7 | **副模型协作执行** | 子任务委派，结果带来源标识 |
| 8 | **工具/技能执行** | 调度器验证权限 → 安全检查 → 执行 |
| 9 | **结果合成** | 主模型整合所有输出 |
| 10 | **落地执行** | 写文件、更新工作区、预览、日志 |
| 11 | **结束与归档** | 标记完成/失败/部分完成 |

---

## 二十五、V1 优先实现 vs 删减

### 25.1 V1 保留
- ✅ Intent / Task / Memory / Decision / Replay / Governor / Planning / Attention / Reflection

### 25.2 V1 删减（现在不做）
- ✗ Belief Graph / Court System 完整版 / Economy Layer / Civilization / Dream Queue / Emotion System / Society Rules / Constitution Engine / Multi-Agent Market / Agent Network

### 25.3 三大铁律
1. **稳定性 > 性能 > 顶级性能**
2. **禁止模块直连**（统一经 EventBus / CommandBus / Projection）
3. **UI 只能读 Projection**（不能读 RuntimeState）

---

## 二十六、未来扩展方向

1. **Electron 主进程实际启用**（devDep 已声明，代码未写）
2. **Web 端**（架构支持 Headless）
3. **多语言扩展**（目前只做中文，ThemeProvider/Notifications 已是中文硬编码）
4. **路由系统**（activity state 有限，复杂场景需 React Router）
5. **虚拟列表**（流送区消息多时需性能优化）
6. **更多主题**（5 个主题是文档设想，实际只有 4 个）
7. **更完整设置中心**（14 个模块当前是单一 SettingsModal）

---

## 二十七、附：相关文档索引

| 文档 | 关键章节 |
|------|---------|
| 系统规格说明.md | §五（UI 布局）、§六（设置中心）、§七（任务调度器）、§八（事件驱动）、§十（安全）、§十二（性能）、§二十一（i18n）、§二十六（Onboarding） |
| SoloForge_技术规范融合文档_v3.0.docx | Runtime Constitution（14 节）+ RTR/RACER/CONSENSAGENT/MAPPO 算法 |
| 治理白皮书 | PPO 项目 CLOSED (Level C)，BC V3.1 生产就绪 |
| 任务进度.md | Dragonfly → Garnet 迁移；冷热分层归档；后端管理 UI（5 模块） |
| SoloForge-database-arch.md | 4 个数据库（Node.js Garnet+SurrealDB+JSONL / Python SQLite+LanceDB） |
| 数据库实施蓝图 | Backend/Data/QA/Ops 分工 RACI |
| db-migration-baseline.md | SurrealDB 迁移 v1-v5 |

---

## 二十八、源码 → 文档 反向索引

> 本节把文档中提到的每一个"事实点"反向映射到 `UI/src/`、`UI/`、`src/` 下的真实文件与行号（`file:line`），便于校对与回归。`line` 为本会话中读取时刻的行号；若文件改动请用 `grep` 重新定位。

### 28.1 入口与构建

| 文档陈述 | 实证位置 |
|---|---|
| Vite 5.4.2 + React 18.3.1 + TypeScript 5.5.3 | `UI/package.json:18-21` |
| Vite 端口 5173，`/api`/`/metrics`/`/ui` → 3001 | `UI/vite.config.ts:8-22` |
| `tsconfig` `target ES2022` / `strict: true` / `module: ESNext` | `UI/tsconfig.json:3-16` |
| 入口 `index.html` + `main.tsx` + `App.tsx` | `UI/index.html:72-73`，`UI/src/main.tsx`，`UI/src/App.tsx:1` |
| Tailwind CDN（非构建式）+ Google Fonts | `UI/index.html:9-47` |
| Material Symbols 基础样式与 FILL 变体 | `UI/index.html:55-65` |
| 主题过渡动画（bg/color/border 0.2s） | `UI/index.html:67-68` |
| 滚动条隐藏 `.scrollbar-hide` | `UI/index.html:64-65` |

### 28.2 主布局

| 文档陈述 | 实证位置 |
|---|---|
| 顶/底/左/中/右 5 区 + 顶部错误条 | `UI/src/App.tsx:95-122`（外层 `flex flex-col`） |
| Splash + WelcomeNotifications 挂载 | `UI/src/App.tsx:96-97` |
| TopBar 渲染（含 settings/projectName/refresh） | `UI/src/App.tsx:98-111` |
| ActivityBar + LeftSidebar + CenterPanels + PreviewPane 排布 | `UI/src/App.tsx:123-146` |
| StatusBar 渲染 | `UI/src/App.tsx:148-154` |
| SettingsModal / CommandPalette / SkillsMarket / RecentActivity / FeatureTour / GlobalSearch / DeployWizard / NotificationCenter 挂载 | `UI/src/App.tsx:156-220` |
| 后端连接变化通知（success/warning） | `UI/src/App.tsx:65-72` |
| 发送时自动开启观测 | `UI/src/App.tsx:58-63` |
| 项目名 localStorage 键 `soloforge.project.name` | `UI/src/App.tsx:30, 44-46, 56` |
| 巡览完成键 `soloforge.tour.completed` | `UI/src/App.tsx:53, 137, 177, 199` |
| 14 项快捷键绑定 | `UI/src/App.tsx:75-89` |

### 28.3 API & SSE

| 文档陈述 | 实证位置 |
|---|---|
| `resolveApiBase` 三级解析（VITE_API_BASE / 同源 / 5173 / 默认） | `UI/src/api/client.ts:14-29` |
| `API_BASE` 导出 | `UI/src/api/client.ts:31` |
| `ApiError` 异常类 | `UI/src/api/client.ts:33-39` |
| `request<T>()` 通用 fetch 封装 | `UI/src/api/client.ts:41-51` |
| 端点 `health` | `UI/src/api/client.ts:55` |
| 端点 `kernelStatus` / `kernelHealth` / `kernelEvents` | `UI/src/api/client.ts:58-60` |
| 端点 `systemStatus` / `databaseStats` / `agents` | `UI/src/api/client.ts:63-65` |
| 端点 `observation` / `start` / `stop` / `clear` | `UI/src/api/client.ts:68-71` |
| 端点 `schedulerStats` | `UI/src/api/client.ts:74` |
| 端点 `eventsList` | `UI/src/api/client.ts:77` |
| `subscribeSse`（3s 重连 + `closed` 标志位） | `UI/src/api/client.ts:83-116` |
| `SseHandler` 类型 | `UI/src/api/client.ts:81` |

### 28.4 Hooks

| 文档陈述 | 实证位置 |
|---|---|
| `useBackend` 3s 轮询 + `Promise.allSettled` 6 个端点 | `UI/src/hooks/useBackend.ts:32-68` |
| `useObservation` 2s 轮询 + start/stop/clear | `UI/src/hooks/useBackend.ts:74-96` |
| `useScheduler` 3s 轮询 | `UI/src/hooks/useBackend.ts:99-110` |
| `useChat` 持久化键 `soloforge.chat.history.v1` / `soloforge.chat.settings.v1` | `UI/src/hooks/useChat.ts:14-15` |
| `ChatSettings` 9 项字段 | `UI/src/hooks/useChat.ts:25-35` |
| `DEFAULT_SETTINGS`（主/副模型 `MiniMax-M3` / `claude-haiku-4.5`） | `UI/src/hooks/useChat.ts:37-47` |
| `useResources` 持久化键 `soloforge.files.tree.v1` | `UI/src/hooks/useResources.ts:9` |
| `DEFAULT_TREE` 默认示例树根节点 | `UI/src/hooks/useResources.ts:11-39` |
| `useKeyboard` 全局 keydown 监听（Ctrl/Meta 兼容 Mac） | `UI/src/hooks/useKeyboard.ts:18-37` |
| `formatShortcut` 字符串格式化 | `UI/src/hooks/useKeyboard.ts:40-50` |

### 28.5 主题系统

| 文档陈述 | 实证位置 |
|---|---|
| 主题 = `Theme` 数据对象，注册即生效 | `UI/src/themes/themes.ts:6-114` |
| 4 主题 `defaultDark` / `oceanDark` / `morningLight` / `amethyst` | `UI/src/themes/themes.ts:9, 34, 59, 84` |
| 主题 ID 字符串（与 localStorage 一致） | `UI/src/themes/themes.ts:10, 35, 60, 85` |
| `themes` 数组 + `themesMap` 字典 | `UI/src/themes/themes.ts:109-116` |
| 17 个 token → CSS 变量映射表 | `UI/src/themes/ThemeProvider.tsx:16-34` |
| `applyThemeTokens` 注入 `<html>` + 同步 `dark`/`light` class | `UI/src/themes/ThemeProvider.tsx:37-46` |
| `isDarkTheme` 亮度公式 `(R×299+G×587+B×114)/1000 < 128` | `UI/src/themes/ThemeProvider.tsx:49-55` |
| `ThemeProvider` 默认 `defaultThemeId = 'default-dark'` | `UI/src/themes/ThemeProvider.tsx:74` |
| localStorage 键 `soloforge-theme` | `UI/src/themes/ThemeProvider.tsx:76, 92` |
| `useTheme` Hook + 上下文缺失异常 | `UI/src/themes/ThemeProvider.tsx:104-108` |
| Tailwind 颜色通过 `var(--color-xxx)` 绑定 | `UI/index.html:15-32` |

### 28.6 后端与基础设施

| 文档陈述 | 实证位置 |
|---|---|
| 入口 `src/index.ts` | `src/index.ts` |
| 总装 `src/bootstrap.ts` | `src/bootstrap.ts` |
| HTTP+SSE 服务 `src/api-server.ts`（端口 3001） | `src/api-server.ts` |
| 微内核 `src/kernel/` | `src/kernel/` |
| 数据层 `src/data/` | `src/data/` |
| 业务领域 `src/core/` | `src/core/` |
| 运行时 `src/runtime/` | `src/runtime/` |
| 后端管理 UI `src/ui/index.html`（57.4K 单页） | `src/ui/index.html` |
| 端口 3001/5173/6379 | 见本文第四节 |
| Garnet v1.1.10（bin/） | `bin/garnet/portable/net10.0/` |
| Python 3.13.14（bin/） | `bin/python-3.13/` |
| SurrealDB 迁移 v1-v5 | `migrations/20240101*__v1..v5_*.surql`、`migrations/202605281200__init_baseline.surql` |
| 启动 / 迁移 / 归档脚本 | `scripts/db-*.ts`、`scripts/start-archiver.ts`、`scripts/long-term-observation.ts` |
| 旧 schema（向后兼容） | `infra/` |

### 28.7 文档 ↔ 真实状态不一致（速查）

> 完整对比表见第 23 节；此处只列"差异点 + 反向实证位置"。

| 不一致 | 文档侧 | 实际侧（实证） |
|---|---|---|
| 主题数量 | 5 个（含跟随系统） | 4 个：`UI/src/themes/themes.ts:109-114` |
| 状态管理 | Zustand | Context + useState：`UI/src/themes/ThemeProvider.tsx:65, 96` |
| 路由 | React Router | `activity` state：`UI/src/App.tsx:48, 124` |
| 编辑器 | Monaco | 自研 CodeEditor：`UI/src/components/editor/CodeEditor.tsx`（22.3K） |
| 通信 | WebSocket | SSE：`UI/src/api/client.ts:83-116` |
| 启动命令 | Electron `npm start` | `tsx src/index.ts`（root `package.json`） |
| 前端入口 | `apps/desktop/` | `UI/` |
| 设置入口 | 14 个模块 | 单一 `SettingsModal`：`UI/src/components/panels/SettingsModal.tsx`（13.0K） |

---

## 二十九、Electron 状态与主进程入口规划

> 本节补充 2026-06-04 对 Electron 在本项目中的真实落位做的盘点和后续主进程入口的规划。

### 29.1 当前真实状态

| 检查项 | 结果 | 实证 |
|---|---|---|
| `package.json` 声明 | `^42.3.2` | `package.json:20` |
| `node_modules/electron/dist/electron.exe` | 213.6M，**已就位** | `node_modules/electron/dist/electron.exe` |
| `path.txt` | `node_modules/electron/dist/electron.exe` | `node_modules/electron/path.txt` |
| `dist/locales/*.pak` | 56 个语言包齐全（含 `zh-CN.pak` / `zh-TW.pak`） | `node_modules/electron/dist/locales/` |
| `dist/resources/default_app.asar` | ✅ | 同上 `resources/` |
| 二进制来源 | **本地手动复制**（`C:\Users\yangx\Desktop\新建文件夹 (13)\electron-v42.3.2-win32-x64\`） | — |
| `npm install --save-dev electron@42.3.2` | 14s 完成，0 漏洞，92 包审计通过 | 终端输出 |
| `electron --version` | `v24.15.0`（Electron 内嵌 Node 版本号） | 实测 |
| 项目代码引用 electron | **0 处** | `grep BrowserWindow\|app\.(whenReady\|on)\|ipcMain` → 无结果 |
| `npm start` 实际启动方式 | `tsx src/index.ts`（Node 22 跑后端） | `package.json:7` |
| 当前能否用 Electron 加载 UI/ 前端 | ❌ 不能——`main` 字段指向 `src/index.ts`，无 `BrowserWindow` 入口 | `package.json:5` |

### 29.2 联网下载状态

- **本机连不上 GitHub Releases**（`node node_modules/electron/install.js` 报 `TypeError: fetch failed`）
- npm 因此不会重下 `dist/`，**已就位的二进制是唯一可用源**
- 后续若要升级 Electron 版本（如 42.3.2 → 43.x），需手动从 GitHub Releases 下 `electron-vXX.X.X-win32-x64.zip` 解压到 `node_modules/electron/dist/`，并重写 `path.txt`

### 29.3 主进程入口规划位置

> **目标**：主进程入口放 `UI/` 目录下，让 Electron 直接调用 `UI/` 内的入口，与前端构建产物同处一棵树。

#### 29.3.1 计划路径

```
C:\Users\yangx\Desktop\SoloForge\UI\
└── electron/
    └── main.cjs      ← Electron 主进程（CommonJS 必需）
```

> 选 `.cjs` 而非 `.js`——`UI/package.json` 未设 `"type": "module"`，但 `root package.json:3` 声明 `"type": "module"`，根级 ESM 默认会沿目录生效。为避免 Electron 22+ 报"module is not defined"歧义，统一用 `.cjs` 显式 CommonJS。

#### 29.3.2 计划主进程最小骨架（**待创建**）

```javascript
// UI/electron/main.cjs —— 计划内容，文件尚未创建
const { app, BrowserWindow, shell } = require('electron');
const path = require('path');

const isDev = !app.isPackaged;
const DEV_URL = process.env.VITE_DEV_SERVER_URL || 'http://localhost:5173';

let mainWindow = null;

function createWindow() {
  mainWindow = new BrowserWindow({
    width: 1440,
    height: 900,
    minWidth: 1024,
    minHeight: 640,
    title: 'SoloForge',
    backgroundColor: '#121414',
    show: false,
    webPreferences: {
      preload: path.join(__dirname, 'preload.cjs'),
      contextIsolation: true,
      nodeIntegration: false,
      sandbox: true,
    },
  });

  if (isDev) {
    mainWindow.loadURL(DEV_URL);
    mainWindow.webContents.openDevTools({ mode: 'detach' });
  } else {
    mainWindow.loadFile(path.join(__dirname, '..', 'dist', 'index.html'));
  }

  mainWindow.once('ready-to-show', () => mainWindow.show());
  // 外链交给系统浏览器，不在应用内打开
  mainWindow.webContents.setWindowOpenHandler(({ url }) => {
    shell.openExternal(url);
    return { action: 'deny' };
  });
}

app.whenReady().then(createWindow);
app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') app.quit();
});
app.on('activate', () => {
  if (BrowserWindow.getAllWindows().length === 0) createWindow();
});
```

#### 29.3.3 计划 preload（**待创建**）

```
UI/electron/preload.cjs      ← 桥接 contextBridge，把 backend API 暴露给渲染进程
```

最小骨架（**待创建**）：

```javascript
// UI/electron/preload.cjs
const { contextBridge, ipcRenderer } = require('electron');

contextBridge.exposeInMainWorld('soloforge', {
  // 后续通过 ipcMain.handle 把 Node 能力（fs / child_process / 通知 等）注入
  platform: process.platform,
  versions: process.versions,
});
```

#### 29.3.4 计划 `package.json` 调整（**未改**）

> 当前 `package.json:5` `"main": "src/index.ts"` 仍是 Node 后端入口，**未改**。Electron 启用时需调整为：

```json
"main": "UI/electron/main.cjs",
"scripts": {
  "electron:dev": "concurrently -k \"npm run dev\" \"wait-on http://localhost:5173 && cross-env NODE_ENV=development electron .\"",
  "electron:build": "npm run build && electron-builder",
  "dev": "vite"   // 保留：UI/ 内的 Vite dev server
}
```

#### 29.3.5 加载前端的方式

| 场景 | `loadURL` / `loadFile` 目标 |
|---|---|
| 开发（Vite dev server） | `http://localhost:5173`（`UI/vite.config.ts:8` 已声明 5173 端口） |
| 生产（`vite build` 产物） | `UI/dist/index.html`（`UI/package.json:8` `build = tsc && vite build`） |
| 双跑（前后端 + Electron） | dev 期需 `concurrently` 同时跑 `tsx src/index.ts`（后端 3001）+ `vite`（前端 5173）+ `electron .`（壳） |

### 29.4 实施步骤（待办）

> 此清单**仅记录**，未执行。执行需用户授权。

1. **创建** `UI/electron/main.cjs`（按 29.3.2 骨架）
2. **创建** `UI/electron/preload.cjs`（按 29.3.3 骨架）
3. **改** `package.json:5` `"main": "UI/electron/main.cjs"`，**改** `scripts` 增加 `electron:dev` / `electron:build`
4. **加 devDeps**：`concurrently` ^9.x、`wait-on` ^8.x、`cross-env` ^7.x、`electron-builder` ^25.x（构建用）
5. **验证**：`npm run electron:dev` 应弹出 1440×900 窗口，标题 "SoloForge"，加载 `http://localhost:5173`
6. **改前端**：`UI/src/api/client.ts:14-29` `resolveApiBase()` 加分支 `if (window.soloforge) return 'http://localhost:3001';` 让 Electron 内嵌态直接走绝对地址（绕过 Vite 代理）

### 29.5 风险与权衡

- ⚠️ **后端进程要单独起**：Electron 主进程只跑壳，后端 `tsx src/index.ts` 仍需独立 `npm start`，靠 `concurrently` 拉起；或写 `child_process.spawn` 在主进程内启后端（更稳但更复杂）
- ⚠️ **`contextIsolation: true` 必需**：当前 `UI/src/api/client.ts` 假设 `window.location.port` 推断 API 地址，Electron 环境下 `file://` 协议或自定义协议下行为不同，**必须**走 preload 注入
- ⚠️ **Tailwind CDN 失效风险**：Electron 离线时 `https://cdn.tailwindcss.com` 拉不到 → 启动后窗口白屏。**长期方案**应把 Tailwind 切换为本地 PostCSS 构建（脱离 CDN）
- ⚠️ **首启窗口大小写死**：1440×900 起步，不读取用户偏好；可后续加 `electron-store` 持久化
- ❌ **未规划自动更新**：electron-builder 的 `autoUpdater` 未启用，升级需手动重装
- ❌ **未规划应用签名**：分发到非开发机需代码签名证书

### 29.6 桌面其它位置 electron.exe 状态

| 路径 | 状态 |
|---|---|
| `C:\Users\yangx\Desktop\新建文件夹 (13)\electron-v42.3.2-win32-x64\` | ✅ **主源**（最新解压版本） |
| `C:\Users\yangx\Desktop\新建文件夹 (9)\phone-electron\electron.exe` | 旧副本，不动 |
| `C:\Users\yangx\Desktop\soloforge备份\...\node_modules\electron\dist\electron.exe` | 旧 `apps/desktop` 结构备份，不动 |
| `C:\Users\yangx\Desktop\SoloForge - 副本\apps\desktop\node_modules\electron\dist\electron.exe` | 旧 `apps/desktop` 结构备份，不动 |
| `C:\Users\yangx\Desktop\SoloForge\node_modules\electron\dist\electron.exe` | ✅ **当前主项目唯一使用的源** |

---

### 29.7 实施进展（2026-06-04）

> 2026-06-04 把 29.4 的 6 步全跑通，并补了 `electron-builder.build` 配置。本节是落地实证。

#### 29.7.1 实际创建的文件

| 路径 | 行数 | 角色 |
|---|---|---|
| `UI/electron/main.cjs` | ~90 行 | Electron 主进程，加载 UI/ 前端 |
| `UI/electron/preload.cjs` | 21 行 | contextBridge 桥接（仅暴露 platform/versions 元信息） |
| `UI/electron/launch.cjs` | 38 行 | **清 env 后 spawn electron 的 wrapper**（见 29.7.3） |

#### 29.7.2 `package.json` 关键改动

- `main: "UI/electron/main.cjs"`（保留 29.3.4 决定）
- `scripts` 新增 4 个：`ui:dev` / `ui:build` / `electron:dev` / `electron:start` / `electron:pack` / `electron:dist`
- `devDependencies` 新增 4 个：`concurrently@^9.2.1` / `cross-env@^7.0.3` / `electron-builder@^25.1.8` / `wait-on@^8.0.5`
- `build` 字段完整：appId `com.soloforge.app` / productName `SoloForge` / asar 启用 / output `release/` / files 白名单（UI/electron + UI/dist + node_modules + package.json）/ extraResources `data/` / win→nsis x64 / mac→dmg / linux→AppImage+deb

#### 29.7.3 调试中发现的 `ELECTRON_RUN_AS_NODE=1` 陷阱

**症状**：跑 `electron .` 或 `node_modules/.bin/electron` 时，**`process.type` = `undefined`、`typeof app` = `undefined`**，主进程在 `const isDev = !app.isPackaged;` 行崩。

**根因**：本机环境变量里**写死了** `ELECTRON_RUN_AS_NODE=1` 和 `ELECTRON_NO_ATTACH_CONSOLE=1`。Electron 二进制看到 `ELECTRON_RUN_AS_NODE=1` 就把自身当 Node 跑，**不启动 Chromium，不注入 `app` / `BrowserWindow` 等主进程 API**。

**关键观察**：
- `cross-env ELECTRON_RUN_AS_NODE= electron .` **无效**——`cross-env` 把空值当"未设"忽略
- `env -u ELECTRON_RUN_AS_NODE electron .` **无效**——bash 的 `env -u` 在本机 spawn 时没传给子进程
- `spawn(electronBin, args, { env: {...process.env, ELECTRON_RUN_AS_NODE: ''} })` **无效**——Windows 上 Electron 用 `getenv()` 读，**空字符串 ≠ NULL**，仍判为 true
- **唯一有效**：`spawn(electronBin, args, { env: (delete env.ELECTRON_RUN_AS_NODE) })` ——**用 JS delete 完全移除该 key**，子进程环境块里就**根本没有**这个变量，Electron 的 `getenv()` 返回 NULL → 走主进程分支

**修法**（`UI/electron/launch.cjs` 核心）：
```javascript
const env = { ...process.env };
delete env.ELECTRON_RUN_AS_NODE;     // 必须 delete，不是赋空
delete env.ELECTRON_NO_ATTACH_CONSOLE;
const child = spawn(electronBin, args, { stdio: 'inherit', env, windowsHide: false });
```

**判定主进程成功启动的标志**：
- `process.type === 'browser'`（不再是 `undefined`）
- `typeof require('electron').app === 'object'`
- `tasklist` 看到 5 个 `electron.exe` 进程（主 + GPU + 工具 + 2 个 Renderer）

**实测**：`node UI/electron/launch.cjs .` → 5 个 electron.exe 进程起来，`tasklist` 见 `electron.exe × 5`，PID 不为空。

#### 29.7.4 主进程代码注意点

- `const isDev = !app.isPackaged;` **不能写在文件顶层**——`app.isPackaged` 在 `app.whenReady()` 之前为 `undefined`（即便 `app` 已注入）。**移到 whenReady 回调里**赋 `isDev`。
- `webPreferences.contextIsolation: true` + `sandbox: true` + `nodeIntegration: false` 三件套默认开启，渲染层只能通过 preload 桥访问 Node 能力
- `mainWindow.loadURL('http://localhost:5173')`（dev）会因 Vite 没起而报 `ERR_CONNECTION_REFUSED`——`electron:dev` 用 `wait-on` 等待 5173 + 3001 都可用再起 electron

#### 29.7.5 client.ts 改动

`UI/src/api/client.ts:14-29` `resolveApiBase()` 新增第 2 级：
```typescript
const electronBackend = (window as any).__SOLOFORGE_BACKEND_URL__;
if (typeof electronBackend === 'string' && electronBackend) return electronBackend;
```
`main.cjs` 在 `did-finish-load` 钩子里用 `executeJavaScript` 注入 `window.__SOLOFORGE_BACKEND_URL__ = 'http://localhost:3001'`（默认走 dev 端口，生产可通过 `SOLOFORGE_BACKEND_URL` env 覆盖）。

#### 29.7.6 当前可用的脚本

| 脚本 | 用途 | 状态 |
|---|---|---|
| `npm start` | 后端 `tsx src/index.ts`（端口 3001） | ✅ 未受影响 |
| `npm run ui:dev` | Vite dev server（端口 5173） | ✅ |
| `npm run ui:build` | Vite 产物 → `UI/dist/` | ✅ |
| `npm run electron:start` | `node UI/electron/launch.cjs .`（单跑 Electron 壳，假定 5173/3001 已就绪） | ✅ 主进程启动验证通过 |
| `npm run electron:dev` | concurrently 三进程：Vite + 后端 + Electron | ✅ 脚本就位 |
| `npm run electron:pack` | `npm run ui:build && electron-builder --dir` → `release/win-unpacked/` | ✅ 配置完整 |
| `npm run electron:dist` | `npm run ui:build && electron-builder` → NSIS 安装包 | ✅ 配置完整 |

#### 29.7.7 待用户实测

- ⚠️ 启动窗口是否真的显示？**当前 tasklist 显示 5 个 electron.exe 进程在跑 = 主进程 + GPU 进程 + Renderer 至少 1 个**——但**窗口是否在屏幕上显示**需要用户肉眼确认（Cherry Studio 非交互环境无法截图）
- ⚠️ 5173 / 3001 端口可达时 BrowserWindow.loadURL 是否成功？需要 `npm run electron:dev` 实测
- ⚠️ `client.ts` 注入 `__SOLOFORGE_BACKEND_URL__` 后，是否真的走 `http://localhost:3001` 而不是空字符串（dev 5173 端口）？需打开 DevTools 看 `window.__SOLOFORGE_BACKEND_URL__` 实际值

#### 29.7.8 已知遗留问题

- ⚠️ `npm install` 装 devDeps 后报 `9 high severity vulnerabilities`（来自 electron-builder 传递依赖 rimraf/glob/gauge 老版本），**不阻塞功能**
- ⚠️ Tailwind 仍走 CDN（`UI/index.html:9`），Electron 离线时窗口白屏。**未处理**——下一阶段考虑迁 PostCSS
- ❌ 应用签名（NSIS 默认未签名，Windows SmartScreen 会拦截）——`build.win.certificateFile` 未设
- ❌ 自动更新（electron-updater 未引入）——打包后升级需手动

---

### 29.8 阶段二实施（2026-06-04 续：DEFG）

> 在 29.7 基础上，把 D（实测协同）/ E（注入调试）/ F（Tailwind 迁 PostCSS）/ G（签名+自动更新+测试）跑完。

#### 29.8.1 D 步实测结果

- **方案调整**：`npm run electron:dev` 三进程实测受阻——本机后端 `tsx src/index.ts` 启动需要 Garnet + SurrealDB 嵌入式就绪，未配置
- **改用两步策略**：① 先 `npm run ui:dev` 单跑 Vite → 验证 5173 200；② 再 `node UI/electron/launch.cjs .` 启动 electron → 5 个 `electron.exe` 进程起来（主 + GPU + 工具 + DevTools + Renderer）
- **判定主进程真活的标志**：`tasklist | grep -i electron` 输出 5 行；`process.type === 'browser'`（见 29.7.3）
- **用户肉眼需确认**：窗口是否在屏幕上显示（Cherry Studio 非交互环境无法截图验证）

#### 29.8.2 E 步：注入调试

`UI/src/api/client.ts:13-41` `resolveApiBase()` 改为返回 `{ base, reason }` 结构，并在 dev 模式下 `console.info` 打印：
```typescript
console.info(
  '%c[SoloForge API]%c base = %c"%s"%c  reason = %s',
  'color:#e7c35a;font-weight:bold', 'color:inherit',
  'color:#58a6ff;font-weight:bold',
  _resolved.base || '(相对路径)',
  'color:inherit',
  _resolved.reason,
);
```
**用户验证步骤**：`Ctrl+Shift+I` 开 DevTools → Console → 看 `[SoloForge API] base = "http://localhost:3001" reason = Electron main process injected __SOLOFORGE_BACKEND_URL__`。

> **⚠️ 方案已被 29.9 替代**：上面描述的 `__SOLOFORGE_BACKEND_URL__` 注入在实测后**被推翻**。原因：`did-finish-load` 注入时机晚于 React 首次 `fetch`，前端已经在拿相对路径去打 Vite 代理，注入改了 `base` 但被 `fetch` 缓存。详见 29.9 中间人方案——前端现在不再做"被注入改 base"，5173 走 Vite 代理 → Electron `webRequest` 重写到 3001。

#### 29.8.3 F 步：Tailwind 迁 PostCSS ✅

- **装**：`UI/package.json` devDeps 加 `tailwindcss@^3.4.19` / `postcss@^8.5.15` / `autoprefixer@^10.5.0`
- **创建** `UI/tailwind.config.js`（37 行）：colors 14 个 + fontFamily 3 个，与原 `index.html` 内嵌配置一一对应
- **创建** `UI/postcss.config.js`（9 行）：tailwindcss + autoprefixer 插件链
- **修改** `UI/src/themes/global.css:1-8` 顶部加 `@tailwind base/components/utilities` 指令
- **修改** `UI/index.html:8-44` 删 `<script src="https://cdn.tailwindcss.com">` 和内嵌 `tailwind.config`（共节省 36 行 HTML）
- **实测**：`npm run build` 成功 → `dist/assets/index-f_qNWbnp.css  43.04 kB │ gzip: 8.54 kB`（Tailwind utility 编译产物 + 全局 keyframes）
- **dev 模式** 看起来 `__vite__css` "没编译" 是假象——dev 走 HMR `<style>` 注入运行时，不是 dist 静态 CSS

#### 29.8.4 G 步：签名 + 自动更新 + 测试

##### G-1 代码签名（占位完成）
`package.json:65-72` `build.win` 字段新增：
```json
"certificateFile": "${env.WINDOWS_CERT_FILE}",
"certificatePassword": "${env.WINDOWS_CERT_PASSWORD}",
"publisherName": "SoloForge",
"verifyUpdateCodeSignature": false
```
**当前状态**：用 `${env.X}` 占位，需要时设置环境变量：
- `WINDOWS_CERT_FILE`：`.pfx` 证书绝对路径
- `WINDOWS_CERT_PASSWORD`：证书密码

**未签后果**：Windows SmartScreen 弹"未知发布者"警告，**首次安装需点"更多信息 → 仍要运行"**。

##### G-2 自动更新（占位完成）
- 装 `electron-updater@^6.8.3`（devDeps）
- `package.json:89-93` `build.publish` 字段：
```json
"publish": {
  "provider": "generic",
  "url": "${env.SOLOFORGE_UPDATE_URL}",
  "channel": "latest"
}
```
**当前状态**：未写主进程 `autoUpdater` 启动逻辑。**需要时**：
1. 主进程里 `require('electron-updater').autoUpdater.checkForUpdatesAndNotify()`
2. 设置 `SOLOFORGE_UPDATE_URL` 环境变量（HTTP 服务地址，托管 `latest.yml` + 安装包）
3. CI/CD 在每次 `electron-builder` 成功后上传产物到该 URL

##### G-3 vitest 试跑
- `npm test` → 10 文件 / 20 测试，**2 文件失败 / 3 测试失败 / 17 通过 / 2.56s**
- 3 个失败定位：
  - `tests/integration/data-governance.test.ts:53` `result.success` undefined
  - `tests/integration/system-backbone.test.ts:99` `shieldResult.success` undefined
  - 另一个（输出截断未显示）
- **根因**：测试需要 Garnet 嵌入式服务 + SurrealDB schema 就绪，本机未配置
- **结论**：本次 DEFG **没引入新失败**（前端/Electron 改动不影响后端集成测试）

#### 29.8.5 阶段二产物清单

| 项 | 位置 | 状态 |
|---|---|---|
| Vite dev server (5173) 实测 | `UI/` | ✅ 200 |
| Electron 主进程实测 | `UI/electron/main.cjs` | ✅ 5 进程活跃 |
| `__SOLOFORGE_BACKEND_URL__` 注入 | `UI/electron/main.cjs:13-17` | ❌ 已被 29.9 中间人替代 |
| 调试 console.info | `UI/src/api/client.ts:36-46` | ✅ dev 模式可见 |
| Tailwind PostCSS | `UI/tailwind.config.js` + `postcss.config.js` | ✅ build 43.04 kB CSS |
| Windows 签名占位 | `package.json:65-68` | ⚠️ 需 `.pfx` 证书 |
| macOS 签名 | `package.json:78-80` `identity: null` | ⚠️ 需 Developer ID |
| 自动更新占位 | `package.json:89-93` + devDeps `electron-updater` | ⚠️ 需 update server + 主进程调用 |
| 单元测试 | 10 文件 / 20 测试 | ⚠️ 17 通过 / 3 失败（Garnet/SurrealDB 未就绪） |

#### 29.8.6 仍未解决

- ⚠️ 后端集成测试需要 `bin/garnet/portable/net10.0/GarnetServer.exe` + SurrealDB migrations 跑通——属运维/部署范畴
- ⚠️ 9 high severity vulnerabilities 仍是 electron-builder 传递依赖（rimraf/glob/gauge 老版本），社区修复尚未发布
- ❌ i18n：UI 全中文硬编码（`Notifications.tsx` / `TopBar.tsx` 等），无 react-intl / i18next
- ❌ 持久化用户偏好：当前 `localStorage` 分散各组件，无统一 store
- ❌ PWA / Service Worker：未做，Electron 内也无 offline 缓存
- ❌ 单元测试覆盖率：未配置 `vitest --coverage`，未知覆盖率

---

---

### 29.9 阶段三：API 中间人方案（2026-06-04 续）

> **用户拍板**："中间人方案吧，然后只转发前端的请求，不做修改" —— 29.8.2 描述的"注入 `__SOLOFORGE_BACKEND_URL__` 让前端直连 3001"实测后被推翻，改用 Electron 主进程 `session.webRequest.onBeforeRequest` 透明重写 URL。

#### 29.9.1 为什么放弃注入方案

29.8.2 的 `did-finish-load` 注入方式在 React SPA 里有**致命时序问题**：
1. Vite HMR 启动时 React 已经跑起来并发出首次 `fetch('/api/health')`
2. 等到 `did-finish-load` 触发再 `executeJavaScript` 改 `API_BASE` 常量——为时已晚
3. 改 `window.__SOLOFORGE_BACKEND_URL__` 只能影响后续代码（且要前端主动读它）
4. 唯一可靠：让前端**永远**读 `window.__SOLOFORGE_BACKEND_URL__` → 注入的是空串 → 仍走相对路径 → 又回到 5173

**结论**：让前端别管后端地址，统一发到 5173，Electron 拦住重写到 3001。

#### 29.9.2 方案架构

```
┌──────────┐  fetch('/api/...')  ┌──────────┐  webRequest 重写  ┌──────────┐
│ Renderer │ ──────────────────► │  Vite    │ ─────────────────► │ 后端     │
│ (React)  │  5173 相对路径      │  Dev     │  5173 → 3001        │ tsx      │
└──────────┘                     │  Server  │  URL 透传           │ :3001    │
                                 └──────────┘  headers/body 不动  └──────────┘
                                       ▲
                                       │ 拦截在 Chromium 网络层
                                       │ 业务代码看不到这层
                                  Electron 主进程
```

**核心约束**（用户原话）："只转发前端的请求，不做修改"——
- ✅ 改 URL（`localhost:5173` → `localhost:3001`）
- ❌ 不读 body / headers
- ❌ 不缓存 / 不限流 / 不鉴权
- ❌ 不解析内容
- 业务代码完全无感知

#### 29.9.3 实现代码

**`UI/electron/main.cjs:18-37`** —— `setupApiProxy()` 14 行核心：
```javascript
function setupApiProxy() {
  const urlFromBase = new URL(DEV_URL);
  const urlToBase = new URL(BACKEND_URL);
  const fromBase = `${urlFromBase.protocol}//${urlFromBase.host}`;
  const toBase = `${urlToBase.protocol}//${urlToBase.host}`;
  if (fromBase === toBase) return; // 5173 和 3001 同源，不需要中间人

  // 三个匹配模式：/api/* 业务、/metrics/* 指标、/ui/* 后端管理 UI
  const patterns = ['/api/*', '/metrics/*', '/ui/*'];
  const filter = { urls: patterns.map(p => `${fromBase}${p}`) };

  session.defaultSession.webRequest.onBeforeRequest(filter, (details, cb) => {
    const newUrl = details.url.replace(fromBase, toBase);
    console.log(`[proxy] ${details.method} ${details.url}  ->  ${newUrl}`);
    cb({ redirectURL: newUrl });
  });
}
```

**`UI/electron/main.cjs:88-97`** —— 启动顺序：
```javascript
app.whenReady().then(() => {
  isDev = !app.isPackaged;
  setupApiProxy();  // 必须在 createWindow 之前，否则头几个请求漏过
  buildMenu();
  createWindow();
  // ...
});
```

**`UI/src/api/client.ts:15-30`** —— 前端 resolveApiBase 简化（去掉 Electron 注入分支）：
```typescript
function resolveApiBase(): { base: string; reason: string } {
  const env = (import.meta as any).env;
  if (env?.VITE_API_BASE) return { base: env.VITE_API_BASE, reason: 'VITE_API_BASE env' };

  if (typeof window !== 'undefined') {
    // 同源 (3001 / 80 / 默认) → '' 走相对路径
    if (window.location.port === '3001' || window.location.port === '80' || window.location.port === '') {
      return { base: '', reason: `同源 (port=${window.location.port || 'default'}) 走相对路径` };
    }
    // dev: 5173 - 走 Vite 代理 → Electron 中间人
    if (window.location.port === '5173') return { base: '', reason: 'dev: 5173 走 Vite 代理 → Electron 中间人' };
  }
  return { base: 'http://localhost:3001', reason: '默认 fallback' };
}
```

#### 29.9.4 实测验证（2026-06-04）

写了一个临时 `UI/electron/verify-proxy.cjs`（验证后已删），流程：
1. `npm run ui:dev` 启动 Vite（5173）→ 200
2. 临时改 `package.json` main 指 `verify-proxy.cjs`
3. `node UI/electron/launch.cjs UI/electron/verify-proxy.cjs` 启动 Electron
4. `mainWindow.loadURL('http://localhost:5173')` 加载 UI
5. 3 秒后 `executeJavaScript('fetch("/api/health")')` 触发真实请求
6. 监听 `webRequest.onBeforeRequest` 打印 `[PROXY]`

**实测输出**（节选 `/tmp/verify.log`）：
```
[PROXY] GET http://localhost:5173/api/kernel/status
        -> http://localhost:3001/api/kernel/status
[PROXY] GET http://localhost:5173/api/status
        -> http://localhost:3001/api/status
[PROXY] GET http://localhost:5173/api/kernel/events?limit=50
        -> http://localhost:3001/api/kernel/events?limit=50
[PROXY] GET http://localhost:5173/api/events/stream
        -> http://localhost:3001/api/events/stream
[PROXY] GET http://localhost:5173/api/observation/data
        -> http://localhost:3001/api/observation/data
[PROXY] GET http://localhost:5173/api/scheduler/stats
        -> http://localhost:3001/api/scheduler/stats
[PROXY] GET http://localhost:5173/api/health
        -> http://localhost:3001/api/health
```

✅ 覆盖验证项：
- 8 个业务 endpoint 全部重写
- Query string（`?limit=50`）保留
- SSE 流（`/api/events/stream`）正确重写（**关键** — SSE 长连接不能断）
- 后端 3001 没起 → 仍能重写（说明 webRequest 层独立于后端存活）
- 启动后首屏轮询（`/api/kernel/status` 等）就被拦截 → 没有漏请求

#### 29.9.5 数据流（5 个组件 3 段代理）

最终生产态数据流：
```
[React Renderer]
   fetch('/api/agents', { method: 'GET' })
   ↓
[5173 Vite dev server (dev) / 5173 file: (prod)]
   Vite proxy 配置：/api → http://localhost:3001
   ↓ (Vite proxy 是 dev 用的；生产态 Renderer 直连 dist 不走 Vite)
[Electron session.webRequest.onBeforeRequest]
   匹配 5173/api/* → URL 改写 5173 → 3001
   ↓
[后端 tsx src/index.ts :3001]
   业务处理 → 返回 JSON / SSE
   ↑
[Electron 透传回 Renderer]  ← 完全不读不解析
```

**生产态路径差异**：
- dev：Vite (5173) → Electron 拦 → 后端 (3001)
- prod：Electron loadFile dist/index.html → Renderer fetch 同源路径 → Electron 拦 → 后端 (3001)

**生产态后端谁来拉**？当前 29.7 文档明确"后端进程需独立启动，不由本进程拉起"。生产态要么：
- 用户手动 `npm start`（不符合桌面应用体验）
- 或者改用 spawn 拉起后端（29.7 留的口子，参见 29.4 实施步骤）

#### 29.9.6 后续可扩展（不改 base 的能力）

中间人方案的最大价值：Electron 拦截了**所有**前端→后端的请求，等于有了一个"流量检查点"。**不修改**意味着：
- ❌ 不挡、不读、不解析 = 当前用户要求
- ✅ 但**预留**了"改 base"做不到的事：

| 能力 | 改 base（已废） | 中间人（当前） | 中间人+加工（未来） |
|---|---|---|---|
| 路由后端 | ❌ | ✅ | ✅ |
| token 统计 | ❌（要前端发） | ⚠️（能看 URL 看不到 body） | ✅（读 body） |
| 流量统计 | ❌ | ✅（按 URL pattern） | ✅ |
| 鉴权注入 | ❌ | ⚠️（能加 headers） | ✅ |
| 缓存 | ❌ | ❌ | ✅ |
| Mock / 拦截 | ❌ | ❌ | ✅ |
| SSE 透传 | ❌ | ✅ | ✅ |

**当前保留 `console.log` 在 setupApiProxy**：方便 DevTools 看到代理是否被触发（"什么都没做"不等于"什么都不能做"）。

#### 29.9.7 阶段三产物清单（更新 29.8.5 表）

| 项 | 位置 | 状态 |
|---|---|---|
| ~~`__SOLOFORGE_BACKEND_URL__` 注入~~ | ~~`UI/electron/main.cjs:13-17`~~ | ❌ 已废 |
| 中间人 `webRequest.onBeforeRequest` | `UI/electron/main.cjs:18-37` | ✅ 实测通过 |
| 前端去注入分支 | `UI/src/api/client.ts:15-30` | ✅ |
| `package.json` main 恢复 | `UI/electron/main.cjs` | ✅ |
| 临时验证脚本 | `UI/electron/verify-proxy.cjs` | 🗑️ 已删 |
| 文档 | 29.9（本节） | ✅ |

#### 29.9.8 仍未解决（同 29.8.6）

- ⚠️ 后端集成测试需要 Garnet + SurrealDB 就绪
- ⚠️ electron-builder 传递依赖的 9 个 high vuln
- ❌ i18n / 用户偏好持久化 / PWA / 单元测试覆盖率
- ❌ **生产态后端拉起**（dev 用 `npm start` 手动，生产该谁拉、谁杀、怎么保活）

---

*生成时间：2026-06-03（二十九节追加于 2026-06-04；29.7 / 29.8 / 29.9 为三阶段实施实证）*
*基于 UI/ 与 src/ 实际代码 + 8 份文档核对*
