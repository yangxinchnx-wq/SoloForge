# SoloForge 项目安装清单

> **生成日期**: 2026-07-16  
> **项目**: SoloForge — AI 多智能体自治系统核心框架  
> **操作系统**: Windows 10/11 (x64)  
> **工作目录**: `C:\Users\yangx\Desktop\SoloForge`

---

## 目录

1. [运行环境](#1-运行环境)
2. [主后端核心 (Root RACER)](#2-主后端核心-root-racer)
3. [前端 UI (Electron 桌面应用)](#3-前端-ui-electron-桌面应用)
4. [AI Society Python 服务](#4-ai-society-python-服务)
5. [Rust 调度器](#5-rust-调度器)
6. [Java Agent Advisor 服务](#6-java-agent-advisor-服务)
7. [数据库与存储服务](#7-数据库与存储服务)
8. [向量数据库与嵌入模型](#8-向量数据库与嵌入模型)
9. [本地大模型推理](#9-本地大模型推理)
10. [DevOps / CI / 构建工具](#10-devops--ci--构建工具)
11. [端口分配总览](#11-端口分配总览)
12. [目录结构总览](#12-目录结构总览)

---

## 1. 运行环境

| 软件 | 版本 | 作用 |
|------|------|------|
| **Windows 10/11** | — | 运行操作系统（x64） |
| **Node.js** | v24.15.0 | 主后端运行时，驱动 RACER 调度器 |
| **npm** | 11.12.1 | Node.js 包管理器 |
| **Python** | 3.13.14 (3.12 兼容至 2026-Q4) | AI Society 服务运行时，MAPPO 强化学习引擎 |
| **Rust / rustc** | 1.95.0 | Rust 高性能任务调度器编译环境 |
| **Java (JDK)** | 26 (build 26+35-2893) | Java Agent Advisor 链（Spring AI 2.0） |
| **Java (bin/jdk-23-mini)** | 23 (mini 版) | 项目内打包的 Java 精简运行时 |
| **TypeScript** | ~5.8.2 | 前端/后端共用类型系统 |

---

## 2. 主后端核心 (Root RACER)

> 入口: `src/index.ts`  
> 启动方式: `tsx src/index.ts` (即 `npm start`)

### 2.1 运行时配置

| 项目 | 版本/值 | 作用 |
|------|---------|------|
| **TypeScript target** | ES2022 | 编译目标 |
| **模块系统** | ESNext / ESM | ES Modules 原生支持 |
| **运行时** | tsx ^4.22.4 | TypeScript 直运行器（无需编译） |

### 2.2 生产依赖

| 依赖 | 版本 | 作用 |
|------|------|------|
| `express` | ^5.2.1 | HTTP API 服务器（3001 端口） |
| `surrealdb` | ^2.0.3 | SurrealDB 官方客户端（嵌入式连接） |
| `@surrealdb/node` | ^3.0.3 | SurrealDB Node.js 原生引擎（rocksdb:// 协议） |
| `ioredis` | ^5.11.0 | Garnet/Redis 兼容客户端（热数据缓存） |
| `ws` | ^8.21.0 | WebSocket 服务器（前端实时通信） |
| `kafkajs` | ^2.2.4 | Apache Kafka 客户端（事件流/审计） |
| `node-llama-cpp` | ^3.19.0 | 本地 LLM 推理（llama.cpp Node.js binding） |
| `keytar` | ^7.9.0 | 系统安全存储（密钥/Token 存储） |
| `motion` | ^12.40.0 | 动画库（后端 SSE 动画相关） |
| `ulid` | ^3.0.2 | 有序唯一 ID 生成器 |
| `@msgpack/msgpack` | ^3.1.3 | MessagePack 序列化（IPC 通信） |

### 2.3 OpenTelemetry 可观测性栈

| 依赖 | 版本 | 作用 |
|------|------|------|
| `@opentelemetry/api` | ^1.9.1 | OpenTelemetry 标准 API |
| `@opentelemetry/api-logs` | ^0.57.2 | 日志 API |
| `@opentelemetry/sdk-node` | ^0.57.2 | 完整 SDK（追踪+指标+日志） |
| `@opentelemetry/sdk-trace-base` | ^1.30.1 | 追踪基础 SDK |
| `@opentelemetry/sdk-trace-node` | ^1.30.1 | Node.js 追踪 SDK |
| `@opentelemetry/sdk-logs` | ^0.57.2 | 日志 SDK |
| `@opentelemetry/resources` | ^1.30.1 | 资源属性管理 |
| `@opentelemetry/semantic-conventions` | ^1.43.0 | 语义化约定 |

### 2.4 开发依赖

| 依赖 | 版本 | 作用 |
|------|------|------|
| `vitest` | ^4.1.7 | 测试框架 |
| `@types/node` | ^22.0.0 | Node.js 类型声明 |
| `@types/ws` | ^8.18.1 | WebSocket 类型声明 |
| `@types/express` | ^5.0.6 | Express 类型声明 |
| `tsx` | ^4.22.4 | TypeScript 运行器 |

### 2.5 后端核心模块（src/）

| 模块 | 文件数 | 职责 |
|------|--------|------|
| `src/kernel/` | 20+ | 微内核：运行时、命令总线、事务管理、调度器客户端、看门狗 |
| `src/core/agent/` | 15+ | 自主智能体：注册、工具循环、通信总线、进化、信誉 |
| `src/core/court/` | 5 | 司法仲裁：盲审、LLM 终审升级 |
| `src/core/decision/` | 1 | RACER 决策引擎（流控竞价） |
| `src/core/governor/` | 1 | MAPPO 强化学习客户端 |
| `src/core/society/` | 10+ | AI 社会：联盟、文化、治理、制度、角色演化、社交记忆 |
| `src/core/llm/` | 5 | LLM 代理、熔断器、模型能力配置 |
| `src/core/browser-use/` | 7 | 浏览器自动化（MCP 客户端、编排器） |
| `src/data/` | 20+ | 数据持久层：Repository、Garnet 缓存、JSONL 归档、Outbox |
| `src/server/` | 8 | REST API 路由：Agent、Canvas、系统、工具执行、Vault |
| `src/security/` | 15+ | 安全层：Token 管理、审计流、多租户、密钥库 |
| `src/observability/` | 8 | 可观测性：Sentry 适配、Metrics、OTEL 初始化 |
| `src/runtime/` | 8 | 运行时：健康检查、生命周期、TCP 客户端 |

---

## 3. 前端 UI (Electron 桌面应用)

> 目录: `UI/`  
> 打包输出: `UI/release/`

### 3.1 框架与运行时

| 软件 | 版本 | 作用 |
|------|------|------|
| **Electron** | ^42.4.1 | 桌面应用容器（Chromium + Node.js 混合） |
| **React** | ^19.0.1 | 前端 UI 框架 |
| **React DOM** | ^19.0.1 | React 的 DOM 渲染器 |
| **Vite** | ^8.1.0 | 前端构建工具 + Dev Server（Rolldown 打包引擎） |
| **TypeScript** | ~5.8.2 | 类型系统 |
| **Tailwind CSS** | ^4.1.14 | 原子化 CSS 框架 |
| **@tailwindcss/vite** | ^4.1.14 | Tailwind Vite 插件 |

### 3.2 前端 UI 库与组件

| 依赖 | 版本 | 作用 |
|------|------|------|
| `antd` | ^6.4.4 | Ant Design UI 组件库（通过 stubs 模拟） |
| `@lobehub/ui` | ^5.15.17 | LobeHub UI 组件库（通过 stubs 模拟） |
| `@lobehub/icons` | ^5.10.0 | 图标库 |
| `@lobehub/fluent-emoji` | ^4.1.0 | Fluent 表情符号（通过 stubs 模拟） |
| `@heroicons/react` | ^2.2.0 | Heroicons React 图标组件 |
| `recharts` | ^3.8.1 | 数据可视化图表库（StatsModal 统计面板） |

### 3.3 前端状态管理与动画

| 依赖 | 版本 | 作用 |
|------|------|------|
| `zustand` | ^5.0.14 | 轻量级状态管理（appStore） |
| `xstate` | ^5.32.4 | 状态机（复杂状态逻辑） |
| `framer-motion` | ^12.42.2 | 动画库（页面转场、组件动画） |
| `animejs` | ^4.5.0 | JavaScript 动画引擎 |

### 3.4 拖拽与交互

| 依赖 | 版本 | 作用 |
|------|------|------|
| `@dnd-kit/core` | ^6.3.1 | 拖拽核心库 |
| `@dnd-kit/sortable` | ^10.0.0 | 可排序列表拖拽 |
| `@dnd-kit/modifiers` | ^9.0.0 | 拖拽修饰器 |
| `@dnd-kit/utilities` | ^3.2.2 | 拖拽工具函数 |

### 3.5 3D 渲染

| 依赖 | 版本 | 作用 |
|------|------|------|
| `three` | ^0.172.0 | WebGL 3D 渲染引擎 |
| `@react-three/fiber` | ^9.6.1 | React 的 Three.js 渲染器 |
| `@react-three/drei` | ^10.7.7 | Three.js 实用组件库 |

### 3.6 工具与辅助

| 依赖 | 版本 | 作用 |
|------|------|------|
| `cheerio` | ^1.2.0 | 服务端 HTML 解析（爬虫/抓取） |
| `detect-port` | ^2.1.0 | 端口检测工具 |
| `dotenv` | ^17.2.3 | 环境变量加载 |
| `http-proxy-middleware` | ^4.1.1 | 开发环境 API 代理 |
| `keytar` | ^7.9.0 | 系统安全存储（UI 端密钥管理） |
| `node-llama-cpp` | ^3.19.0 | 本地 LLM 推理（Electron 主进程直接加载） |
| `@babel/parser` | ^8.0.4 | JavaScript/TypeScript 解析器 |
| `png-to-ico` | ^3.0.2 | PNG 转 ICO 图标工具 |
| `ioredis` | ^5.11.1 | Garnet/Redis 客户端（UI Server 端） |
| `express` | ^4.21.2 | UI Server（3000 端口，Electron 内嵌） |
| `@surrealdb/node` | ^3.0.3 | SurrealDB Node.js 客户端 |
| `surrealdb` | 2.0.3 | SurrealDB 客户端（固定版本） |

### 3.7 前端开发依赖

| 依赖 | 版本 | 作用 |
|------|------|------|
| `electron-builder` | ^26.15.3 | Electron 打包构建工具（NSIS 安装包） |
| `esbuild` | ^0.28.0 | 极速打包器（核心后端模块） |
| `typescript` | ~5.8.2 | TypeScript 编译器 |
| `vite` | ^8.1.0 | 构建工具 |
| `vitest` | ^4.1.9 | 测试框架 |
| `@vitest/coverage-v8` | ^4.1.9 | Vitest 覆盖率（V8 引擎） |
| `tsx` | ^4.21.0 | TypeScript 运行器 |
| `tailwindcss` | ^4.1.14 | CSS 框架 |
| `autoprefixer` | ^10.4.21 | CSS 自动前缀器 |
| `sharp` | ^0.35.3 | 图像处理（图标生成） |
| `puppeteer-core` | ^25.3.0 | 浏览器自动化核心（无头 Chrome） |
| `chrome-remote-interface` | ^0.33.3 | Chrome DevTools 协议客户端 |
| `react-is` | ^18.3.1 | React 开发工具依赖 |
| `@types/node` | ^22.14.0 | Node.js 类型 |
| `@types/express` | ^4.17.21 | Express 类型（UI Server） |
| `@vitejs/plugin-react` | ^6.0.3 | Vite React 插件 |
| `@rollup/rollup-win32-x64-msvc` | ^4.62.2 | Rollup 原生绑定（Windows） |
| `@vue/compiler-sfc` | ^3.5.39 | Vue 单文件组件编译器（React + Vue 混合编译） |

### 3.8 UI 架构

| 组件 | 说明 |
|------|------|
| `UI/src/main.tsx` | React 应用入口 |
| `UI/src/App.tsx` | 主应用组件 |
| `UI/src/context/` | Context 上下文（主题、布局） |
| `UI/src/components/` | 核心 UI 组件（ChatPanel、FileExplorer、PreviewPanel 等） |
| `UI/src/state/` | Zustand 状态管理 |
| `UI/electron/main.cjs` | Electron 主进程 |
| `UI/electron/local-llm-manager.cjs` | 本地 LLM 管理（node-llama-cpp） |
| `UI/electron/proxy-service.cjs` | 网络代理管理（4 模式） |
| `UI/vite.config.ts` | Vite 8 + Rolldown 构建配置 |

---

## 4. AI Society Python 服务

> 目录: `python/`  
> 运行方式: `run_marl.bat` / `run_service.bat` / `run_trainer.bat`  
> Python: 3.13.14（3.12 兼容）

### 4.1 核心依赖

| 依赖 | 版本 | 作用 |
|------|------|------|
| `qdrant-client` | >=1.12.0 | Qdrant 向量数据库客户端（社会记忆语义检索） |
| `sentence-transformers` | >=3.0.0 | MiniLM 384-dim 多语言嵌入模型 |
| `torch` | >=2.6.0, <3.0.0 | PyTorch MAPPO 强化学习框架（CPU 版本） |
| `numpy` | >=2.0.0, <3.0.0 | 数值计算 |
| `msgpack` | >=1.0.0 | MessagePack 序列化（IPC 通信） |
| `numba` | >=0.61.0 | JIT 加速（可选，无则降级纯 NumPy） |
| `llama-cpp-python` | >=0.3.0 | 本地小模型推理（Qwen2.5-0.5B-Instruct GGUF） |

### 4.2 开发依赖

| 依赖 | 版本 | 作用 |
|------|------|------|
| `pytest` | >=8.0.0 | 测试框架 |
| `pytest-asyncio` | >=0.23.0 | 异步测试支持 |
| `ruff` | >=0.3.0 | 代码格式化和 lint |

### 4.3 Python 核心模块

| 模块 | 职责 |
|------|------|
| `governor_rl/` | MAPPO 强化学习（资源调度策略） |
| `marl_service/` | MARL 服务（8765/8766 端口） |
| `browser_use_service/` | 浏览器自动化服务 |
| `soloforge_ai_society/` | AI 社会核心（社交记忆、信誉、治理） |
| `training/` | 训练相关 |
| `scripts/` | 脚本集合 |
| `mappo_governor.py` | infra/ 下 MAPPO 治理器配置 |

---

## 5. Rust 调度器

| 项目 | 说明 |
|------|------|
| **可执行文件** | `bin/scheduler.exe` |
| **Rust 版本** | rustc 1.95.0 |
| **协议** | newline-delimited JSON（与 Java Agent 一致） |
| **连接** | TCP Socket，被 Node.js `scheduler-client.ts` 调用 |
| **职责** | 高性能任务调度（微秒级精度） |

---

## 6. Java Agent Advisor 服务

| 项目 | 说明 |
|------|------|
| **Java 版本** | JDK 26 (build 26+35-2893) |
| **框架** | Spring AI 2.0 + Spring Boot |
| **端口** | 8770 (HTTP REST) / 8771 (Raw TCP Socket) |
| **绑定** | 127.0.0.1（仅本地） |
| **Advisor 链** | SystemPrompt → PoolInject → RAG → ToolCalling → PoolWrite → RateLimit → Audit |
| **职责** | Agent 执行引擎、多 worker 协调、RAG 检索、工具调用循环 |
| **状态** | 待实施（当前 training-only，需恢复运行时模块） |

---

## 7. 数据库与存储服务

| 数据库 | 版本 | 端口 | 存储类型 | 用途 |
|--------|------|------|----------|------|
| **Garnet** | v1.1.10 | 6379 | 内存数据库（Redis 兼容） | 热数据：缓存、队列、Session Context、Task State |
| **SurrealDB** | 2.0.3 | 8400 | 文档/图数据库（RocksDB 后端） | 温数据：决策记录、仲裁卷宗、MARL 遥测、审计日志 |
| **JSONL** | — | — | 文件存储 | 冷数据：事件归档、审计日志 |

### 7.1 数据库文件

| 路径 | 说明 |
|------|------|
| `bin/garnet/portable/net10.0/GarnetServer.exe` | Garnet 可执行文件 |
| `bin/surreal.exe` | SurrealDB 可执行文件 |
| `bin/duckdb/` | DuckDB（嵌入式分析数据库） |
| `infra/schema.surql` | SurrealDB Schema 定义 |
| `migrations/` | 数据库迁移脚本 |
| `data/soloforge_db/` | SurrealDB RocksDB 数据目录 |

### 7.2 SurrealDB Schema 表

| 表名 | 用途 |
|------|------|
| `migration_history` | 迁移历史 |
| `decision` | RACER 引擎流控决策记录 |
| `courtSubmission` | 多智能体仲裁决议卷宗 |
| `courtVerdict` | 法庭裁决记录 |
| `marlEpisode` | MAPPO 强化学习遥测特征 |
| `eventLog` | 内核事件审计日志 |
| `traceLinkage` | 全链路追踪记录 |

---

## 8. 向量数据库与嵌入模型

| 组件 | 版本/路径 | 端口 | 作用 |
|------|-----------|------|------|
| **Qdrant** | v1.18.2 | 6333/6334 | 向量数据库（社会记忆语义检索） |
| **Qdrant 可执行文件** | `bin/qdrant/qdrant.exe` | — | Qdrant 服务端 |
| **嵌入模型** | paraphrase-multilingual-MiniLM-L12-v2 | — | 384-dim 多语言嵌入 |
| **模型路径** | `bin/models/paraphrase-multilingual-MiniLM-L12-v2` | — | 本地预下载模型 |

---

## 9. 本地大模型推理

| 组件 | 版本/说明 | 作用 |
|------|-----------|------|
| **node-llama-cpp** | ^3.19.0 | Node.js / Electron 直接加载 llama.cpp |
| **llama-cpp-python** | >=0.3.0 | Python 端本地推理 |
| **模型格式** | GGUF | 量化模型格式 |
| **示例模型** | Qwen2.5-0.5B-Instruct | 本地小模型（Phase 5 实时训练） |
| **构建模式** | `build: "never"` | 仅使用预编译二进制，不尝试源码编译 |

---

## 10. DevOps / CI / 构建工具

### 10.1 根目录

| 工具 | 版本/路径 | 作用 |
|------|-----------|------|
| **tsx** | ^4.22.4 | TypeScript 运行器 |
| **vitest** | ^4.1.7 | 测试框架 |
| **.eslintrc.cjs** | — | ESLint 代码检查 |
| **.prettierrc** | — | Prettier 代码格式化 |
| **start-all.bat** | — | 一键启动全部服务 |
| **start-garnet.bat** | — | 启动 Garnet |

### 10.2 UI 目录

| 工具 | 版本 | 作用 |
|------|------|------|
| **electron-builder** | ^26.15.3 | Electron 打包（NSIS 安装包） |
| **esbuild** | ^0.28.0 | 核心后端模块打包 |
| **vitest** | ^4.1.9 | 前端测试 |
| **@vitest/coverage-v8** | ^4.1.9 | 覆盖率报告 |

### 10.3 Python 目录

| 工具 | 版本 | 作用 |
|------|------|------|
| **pytest** | >=8.0.0 | 测试框架 |
| **ruff** | >=0.3.0 | 代码格式化/lint |
| **hatchling** | — | Python 构建后端 |

### 10.4 打包分发

| 项目 | 说明 |
|------|------|
| **Electron 打包目标** | Windows NSIS 安装包（x64） |
| **输出目录** | `UI/release/` |
| **打包内容** | dist (前端)、electron、resources/core、garnet、git-service、scheduler.exe、surreal.exe |

---

## 11. 端口分配总览

| 端口 | 服务 | 协议 | 说明 |
|------|------|------|------|
| **3000** | UI Server | HTTP + WebSocket | Express 前端代理（Electron 内） |
| **3001** | RACER Core | HTTP | Node.js 主后端 |
| **3002** | git-service | HTTP | Git 服务（Go） |
| **6379** | Garnet | Redis 协议 | 内存缓存/队列 |
| **8400** | SurrealDB | HTTP + WebSocket | 持久化数据库 |
| **8765** | MARL Service | TCP | MAPPO 推理服务 |
| **8766** | MARL Reputation | HTTP | 信誉 HTTP 接口 |
| **8770** | Java Agent | HTTP REST | Spring AI 训练/CRUD/健康检查 |
| **8771** | Java Agent | Raw TCP | RACER ↔ Java 实时通信 |
| **6333** | Qdrant | gRPC | 向量数据库 |
| **6334** | Qdrant | HTTP | 向量数据库 API |

---

## 12. 目录结构总览

```
SoloForge/
├── src/                    # 主后端核心 (TypeScript, Node.js)
│   ├── index.ts            #   入口：启动 SurrealDB + 看门狗
│   ├── bootstrap.ts        #   总装工厂：纯连线装配
│   ├── kernel/             #   微内核层
│   ├── core/               #   业务领域层 (agent/court/decision/governor/society)
│   ├── data/               #   数据持久层
│   ├── server/             #   REST API 路由
│   ├── security/           #   安全层
│   ├── llm/                #   LLM 代理
│   ├── runtime/            #   运行时
│   └── observability/      #   可观测性
│
├── UI/                     # 前端 (Electron + React + Vite)
│   ├── src/                #   React 应用源码
│   ├── electron/           #   Electron 主进程
│   ├── vite.config.ts      #   Vite 8 构建配置
│   └── release/            #   打包输出
│
├── python/                 # AI Society (Python 3.13)
│   ├── governor_rl/        #   MAPPO 强化学习
│   ├── marl_service/       #   MARL 服务
│   ├── browser_use_service/#   浏览器自动化
│   └── soloforge_ai_society/#  AI 社会核心
│
├── bin/                    # 可执行文件与二进制
│   ├── garnet/             #   Garnet v1.1.10
│   ├── qdrant/             #   Qdrant v1.18.2
│   ├── surreal.exe         #   SurrealDB
│   ├── scheduler.exe       #   Rust 调度器
│   ├── models/             #   嵌入模型
│   ├── nodejs/             #   Node.js 运行时
│   ├── python-3.13/        #   Python 运行时
│   ├── jdk-23-mini/        #   Java 精简运行时
│   └── duckdb/             #   DuckDB
│
├── infra/                  # 基础设施配置
│   ├── schema.surql        #   SurrealDB Schema
│   └── mappo_governor.py   #   MAPPO 治理器
│
├── migrations/             # 数据库迁移脚本
├── scripts/                # 工具脚本
├── .git/                   # Git 版本控制
├── .agents/                # Agent 配置
├── patches/                # 依赖补丁
├── data/                   # 运行时数据
└── tests/ / test/          # 测试文件
```

---

> **注意**: 本清单基于项目配置文件和源码分析生成，实际运行时的精确版本可能与声明略有差异（如 semver `^` 前缀表示允许小版本升级）。如需精确版本，请在安装后运行 `npm ls` / `pip list` 获取 lock 文件中的实际版本。
